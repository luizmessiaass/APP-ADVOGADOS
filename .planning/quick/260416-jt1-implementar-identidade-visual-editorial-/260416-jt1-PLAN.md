---
phase: quick
plan: 260416-jt1
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - app-cliente/build.gradle.kts
  - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Color.kt
  - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Type.kt
  - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt
  - app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/navigation/Routes.kt
  - app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/navigation/ClienteNavGraph.kt
  - app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/auth/LoginScreen.kt
  - app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/welcome/WelcomeScreen.kt
  - app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/processo/ProcessoListScreen.kt
  - app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/processo/ProcessoDetailScreen.kt
autonomous: true
requirements: []

must_haves:
  truths:
    - "O tema Editorial Juris (cores navy/gold/off-white) é aplicado globalmente via PortalJuridicoTheme"
    - "Google Fonts Manrope e Inter são carregadas em runtime via ui-text-google-fonts"
    - "WelcomeScreen é a primeira tela do app-cliente e exibe o gradiente naval com CTA"
    - "LoginScreen redesenhada com campos CPF/Senha estilo Editorial Juris"
    - "ProcessoListScreen lista processos com badges, featured card e bottom nav"
    - "ProcessoDetailScreen exibe detalhes com card gradiente CTA para chatbot"
  artifacts:
    - path: "core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Color.kt"
      provides: "Tokens de cor Editorial Juris completos (navy, gold, off-white)"
    - path: "core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Type.kt"
      provides: "Typography com Manrope e Inter via GoogleFont provider"
    - path: "core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt"
      provides: "PortalJuridicoTheme com novo ColorScheme Editorial Juris"
    - path: "app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/welcome/WelcomeScreen.kt"
      provides: "WelcomeScreen composable"
    - path: "app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/auth/LoginScreen.kt"
      provides: "LoginScreen redesenhada"
    - path: "app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/processo/ProcessoListScreen.kt"
      provides: "ProcessoListScreen composable"
    - path: "app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/processo/ProcessoDetailScreen.kt"
      provides: "ProcessoDetailScreen composable"
  key_links:
    - from: "Type.kt"
      to: "ui-text-google-fonts"
      via: "GoogleFont.Provider + FontFamily"
      pattern: "GoogleFont\\.Provider"
    - from: "ClienteNavGraph.kt"
      to: "WelcomeScreen/ProcessoListScreen/ProcessoDetailScreen"
      via: "composable(Routes.*)"
      pattern: "composable\\(Routes\\."
    - from: "LoginScreen.kt"
      to: "Routes.WELCOME"
      via: "navController.navigate on success"
      pattern: "Routes\\.WELCOME"
---

<objective>
Implementar a identidade visual Editorial Juris no app-cliente Android.

Purpose: Elevar a percepção de qualidade e confiança do app-cliente com uma UI editorial — navy escuro, dourado, tipografia Manrope/Inter — diferenciando o produto de apps jurídicos genéricos.
Output: Tema completo em core-ui, cinco telas redesenhadas/novas no app-cliente, dependência Google Fonts configurada.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/PROJECT.md

<!-- Key existing interfaces -->
<interfaces>
From app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/auth/LoginScreen.kt:
```kotlin
// Current signature — keep the same ViewModel wiring:
@Composable
fun LoginScreen(navController: NavController, viewModel: LoginViewModel = hiltViewModel())
// uiState: LoginUiState (Success | Loading | Error)
// On Success → navigates to Routes.ONBOARDING (change to Routes.WELCOME after this task)
```

From app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/navigation/Routes.kt:
```kotlin
object Routes {
    const val LOGIN = "login"
    const val ONBOARDING = "onboarding"
    const val LGPD_CONSENT = "lgpd_consent"
    const val PROCESSO_LIST = "processo_list"
    const val PROCESSO_DETAIL = "processo_detail"
    const val PROCESSO_DETAIL_WITH_ID = "processo_detail/{processoId}"
    // WELCOME not yet defined — add it
}
```

From core-ui Theme.kt:
```kotlin
@Composable
fun PortalJuridicoTheme(darkTheme: Boolean = isSystemInDarkTheme(), dynamicColor: Boolean = false, content: @Composable () -> Unit)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependência Google Fonts + tokens de cor e tipografia Editorial Juris</name>
  <files>
    gradle/libs.versions.toml,
    app-cliente/build.gradle.kts,
    core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Color.kt,
    core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Type.kt,
    core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt
  </files>
  <action>
**1. libs.versions.toml** — adicionar entrada na seção [libraries] (versão BOM-managed, sem version.ref):
```toml
androidx-compose-ui-text-google-fonts = { group = "androidx.compose.ui", name = "ui-text-google-fonts" }
```

**2. app-cliente/build.gradle.kts** — adicionar logo após `implementation(libs.androidx.compose.foundation)`:
```kotlin
implementation(libs.androidx.compose.ui.text.google.fonts)
```
(Gradle alias: hifens viram pontos — `androidx-compose-ui-text-google-fonts` → `libs.androidx.compose.ui.text.google.fonts`)

**3. core-ui/Color.kt** — substituir todo o conteúdo pelo conjunto completo de tokens Editorial Juris:
```kotlin
package com.aethixdigital.portaljuridico.ui.theme

import androidx.compose.ui.graphics.Color

// Editorial Juris — Navy / Gold / Off-white
val EjPrimary            = Color(0xFF041631)
val EjPrimaryContainer   = Color(0xFF1B2B47)
val EjOnPrimary          = Color(0xFFFFFFFF)
val EjOnPrimaryContainer = Color(0xFF8393B4)
val EjInversePrimary     = Color(0xFFB7C7EA)

val EjSecondary                = Color(0xFF735C00)
val EjSecondaryContainer       = Color(0xFFFED65B)
val EjSecondaryFixed           = Color(0xFFFFE088)
val EjOnSecondaryFixed         = Color(0xFF241A00)
val EjSecondaryFixedDim        = Color(0xFFE9C349)
val EjOnSecondaryFixedVariant  = Color(0xFF574500)
val EjOnSecondaryContainer     = Color(0xFF745C00)

val EjBackground              = Color(0xFFFAF9F5)
val EjSurface                 = Color(0xFFFAF9F5)
val EjSurfaceContainerLowest  = Color(0xFFFFFFFF)
val EjSurfaceContainerLow     = Color(0xFFF5F4F0)
val EjSurfaceContainer        = Color(0xFFEFEEEA)
val EjSurfaceContainerHigh    = Color(0xFFE9E8E4)
val EjSurfaceContainerHighest = Color(0xFFE3E2DF)
val EjSurfaceVariant          = Color(0xFFE3E2DF)
val EjSurfaceDim              = Color(0xFFDBDAD6)

val EjOnSurface        = Color(0xFF1B1C1A)
val EjOnSurfaceVariant = Color(0xFF44474D)
val EjOutline          = Color(0xFF75777E)
val EjOutlineVariant   = Color(0xFFC5C6CE)

val EjError            = Color(0xFFBA1A1A)
val EjErrorContainer   = Color(0xFFFFDAD6)
val EjOnError          = Color(0xFFFFFFFF)
val EjOnErrorContainer = Color(0xFF93000A)

val EjInverseSurface   = Color(0xFF30312E)
val EjInverseOnSurface = Color(0xFFF2F1ED)
```

**4. core-ui/Type.kt** — substituir todo o conteúdo para carregar Manrope e Inter via GoogleFont:
```kotlin
package com.aethixdigital.portaljuridico.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.text.googlefonts.Font
import androidx.compose.ui.text.googlefonts.GoogleFont
import androidx.compose.ui.unit.sp
import com.aethixdigital.portaljuridico.ui.R

private val provider = GoogleFont.Provider(
    providerAuthority = "com.google.android.gms.fonts",
    providerPackage  = "com.google.android.gms",
    certificates     = R.array.com_google_android_gms_fonts_certs
)

private val ManropeFontFamily = FontFamily(
    Font(googleFont = GoogleFont("Manrope"), fontProvider = provider, weight = FontWeight.Normal),
    Font(googleFont = GoogleFont("Manrope"), fontProvider = provider, weight = FontWeight.Medium),
    Font(googleFont = GoogleFont("Manrope"), fontProvider = provider, weight = FontWeight.SemiBold),
    Font(googleFont = GoogleFont("Manrope"), fontProvider = provider, weight = FontWeight.Bold),
    Font(googleFont = GoogleFont("Manrope"), fontProvider = provider, weight = FontWeight.ExtraBold),
)

private val InterFontFamily = FontFamily(
    Font(googleFont = GoogleFont("Inter"), fontProvider = provider, weight = FontWeight.Normal),
    Font(googleFont = GoogleFont("Inter"), fontProvider = provider, weight = FontWeight.Medium),
    Font(googleFont = GoogleFont("Inter"), fontProvider = provider, weight = FontWeight.SemiBold),
    Font(googleFont = GoogleFont("Inter"), fontProvider = provider, weight = FontWeight.Bold),
)

val Typography = Typography(
    displayLarge = TextStyle(fontFamily = ManropeFontFamily, fontWeight = FontWeight.ExtraBold, fontSize = 57.sp, lineHeight = 64.sp),
    headlineLarge = TextStyle(fontFamily = ManropeFontFamily, fontWeight = FontWeight.ExtraBold, fontSize = 32.sp, lineHeight = 40.sp),
    headlineMedium = TextStyle(fontFamily = ManropeFontFamily, fontWeight = FontWeight.Bold, fontSize = 28.sp, lineHeight = 36.sp),
    titleLarge = TextStyle(fontFamily = ManropeFontFamily, fontWeight = FontWeight.Bold, fontSize = 22.sp, lineHeight = 28.sp),
    bodyLarge = TextStyle(fontFamily = InterFontFamily, fontWeight = FontWeight.Normal, fontSize = 16.sp, lineHeight = 24.sp, letterSpacing = 0.5.sp),
    bodyMedium = TextStyle(fontFamily = InterFontFamily, fontWeight = FontWeight.Normal, fontSize = 14.sp, lineHeight = 20.sp, letterSpacing = 0.25.sp),
    labelLarge = TextStyle(fontFamily = InterFontFamily, fontWeight = FontWeight.Medium, fontSize = 14.sp, lineHeight = 20.sp, letterSpacing = 0.1.sp),
    labelMedium = TextStyle(fontFamily = InterFontFamily, fontWeight = FontWeight.Medium, fontSize = 12.sp, lineHeight = 16.sp, letterSpacing = 0.5.sp),
    labelSmall = TextStyle(fontFamily = InterFontFamily, fontWeight = FontWeight.Medium, fontSize = 10.sp, lineHeight = 16.sp, letterSpacing = 0.5.sp),
)
```

IMPORTANTE: O `R.array.com_google_android_gms_fonts_certs` referencia o resource array que o plugin de Google Fonts adiciona automaticamente ao módulo core-ui quando a dependência é incluída. Se o módulo core-ui não tiver a dependência diretamente no build.gradle.kts, adicione lá também:
- Verificar se `core-ui/build.gradle.kts` já tem `implementation(platform(libs.androidx.compose.bom))` e `implementation(libs.androidx.compose.ui.text.google.fonts)` — se não tiver, adicionar ambas.

**5. core-ui/Theme.kt** — substituir o ColorScheme (light only — dark não usado no app-cliente) usando os novos tokens. Remover o DarkColorScheme para simplificar (app-cliente é light-only por decisão editorial):
```kotlin
package com.aethixdigital.portaljuridico.ui.theme

import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.lightColorScheme
import androidx.compose.runtime.Composable

private val EditorialJurisColorScheme = lightColorScheme(
    primary                = EjPrimary,
    onPrimary              = EjOnPrimary,
    primaryContainer       = EjPrimaryContainer,
    onPrimaryContainer     = EjOnPrimaryContainer,
    inversePrimary         = EjInversePrimary,
    secondary              = EjSecondary,
    onSecondary            = EjOnSecondaryFixed,
    secondaryContainer     = EjSecondaryContainer,
    onSecondaryContainer   = EjOnSecondaryContainer,
    background             = EjBackground,
    onBackground           = EjOnSurface,
    surface                = EjSurface,
    onSurface              = EjOnSurface,
    surfaceVariant         = EjSurfaceVariant,
    onSurfaceVariant       = EjOnSurfaceVariant,
    surfaceContainerLowest = EjSurfaceContainerLowest,
    surfaceContainerLow    = EjSurfaceContainerLow,
    surfaceContainer       = EjSurfaceContainer,
    surfaceContainerHigh   = EjSurfaceContainerHigh,
    surfaceContainerHighest= EjSurfaceContainerHighest,
    outline                = EjOutline,
    outlineVariant         = EjOutlineVariant,
    error                  = EjError,
    onError                = EjOnError,
    errorContainer         = EjErrorContainer,
    onErrorContainer       = EjOnErrorContainer,
    inverseSurface         = EjInverseSurface,
    inverseOnSurface       = EjInverseOnSurface,
)

@Composable
fun PortalJuridicoTheme(
    darkTheme: Boolean = false,   // app-cliente é light-only; parâmetro mantido para compatibilidade
    dynamicColor: Boolean = false,
    content: @Composable () -> Unit
) {
    MaterialTheme(
        colorScheme = EditorialJurisColorScheme,
        typography  = Typography,
        content     = content
    )
}
```
  </action>
  <verify>O projeto compila sem erros após `./gradlew :core-ui:assembleDebug :app-cliente:assembleDemoDebug`. Verificar no Build Output que não há "Unresolved reference: EjPrimary" ou "Unresolved reference: ManropeFontFamily".</verify>
  <done>Color.kt, Type.kt, Theme.kt em core-ui contêm tokens Editorial Juris. libs.versions.toml e app-cliente/build.gradle.kts incluem ui-text-google-fonts. O projeto compila.</done>
</task>

<task type="auto">
  <name>Task 2: Routes.kt + WelcomeScreen</name>
  <files>
    app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/navigation/Routes.kt,
    app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/welcome/WelcomeScreen.kt
  </files>
  <action>
**1. Routes.kt** — adicionar constante WELCOME:
```kotlin
package com.aethixdigital.portaljuridico.cliente.navigation

object Routes {
    const val WELCOME   = "welcome"           // NOVO
    const val LOGIN     = "login"
    const val ONBOARDING = "onboarding"
    const val LGPD_CONSENT = "lgpd_consent"
    const val PROCESSO_LIST = "processo_list"
    const val PROCESSO_DETAIL = "processo_detail"
    const val PROCESSO_DETAIL_WITH_ID = "processo_detail/{processoId}"
}
```

**2. Criar WelcomeScreen.kt** em `app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/welcome/`:

Layout (rolável, fundo EjBackground #FAF9F5):
- Status bar cor transparente (não mexer — já gerenciado pela Activity)
- Box centralizado: Card branco (96x96dp, shadow elevation 2dp, cornerRadius 12dp) com ícone `Icons.Outlined.AccountBalance` tamanho 48dp, cor EjSecondary (dourado)
- Spacer 24dp
- Text "Meu Processo" — style headlineLarge (Manrope ExtraBold 32sp via Typography), color EjPrimary
- Box 48x4dp, fundo EjSecondary (linha dourada abaixo do título)
- Spacer 8dp
- Text "Seu processo jurídico em português claro." — style headlineMedium Bold Manrope, color EjPrimary
- Spacer 12dp
- Text "Acompanhe cada etapa do seu processo sem precisar ligar para o advogado." — style bodyLarge Inter, color EjOnSurfaceVariant
- Spacer 32dp
- Row 2 colunas com weight(1f) each — tiles de feature:
  - Tile "Tradução Jurídica": Card(SurfaceContainerLow, cornerRadius 12dp, shadow 2dp), ícone Gavel dourado 28dp em Box 40x40 (fundo EjSurfaceContainerHigh, cornerRadius 8dp), Text título labelLarge bold, Text descrição labelSmall OnSurfaceVariant
  - Tile "Alertas em Tempo Real": mesmo padrão, ícone Notifications dourado
- Spacer 32dp
- Botão primário full-width (height 56dp, cornerRadius 12dp):
  ```kotlin
  Box(
      modifier = Modifier
          .fillMaxWidth()
          .height(56.dp)
          .clip(RoundedCornerShape(12.dp))
          .background(
              Brush.linearGradient(
                  colors = listOf(Color(0xFF041631), Color(0xFF1B2B47)),
                  start = Offset(0f, 0f),
                  end = Offset(Float.POSITIVE_INFINITY, Float.POSITIVE_INFINITY)
              )
          )
          .clickable { onComecarClick() },
      contentAlignment = Alignment.Center
  ) {
      Row(verticalAlignment = Alignment.CenterVertically, horizontalArrangement = Arrangement.Center) {
          Text("Começar agora", color = Color.White, style = MaterialTheme.typography.labelLarge.copy(fontWeight = FontWeight.Bold, fontSize = 16.sp))
          Spacer(Modifier.width(8.dp))
          Icon(Icons.AutoMirrored.Outlined.ArrowForward, contentDescription = null, tint = Color.White, modifier = Modifier.size(20.dp))
      }
  }
  ```
- Spacer 12dp
- TextButton "Já sou cliente" — texto cor EjPrimary, sem fundo, centralizado, onClick → onJaSouClienteClick()
- Spacer 24dp
- Text "Ao continuar, você concorda com os Termos de Uso e Política de Privacidade." — labelSmall, EjOnSurfaceVariant, textAlign Center

Assinatura do composable:
```kotlin
@Composable
fun WelcomeScreen(
    onComecarClick: () -> Unit,
    onJaSouClienteClick: () -> Unit
)
```

Usar `LazyColumn` ou `Column` dentro de `Scaffold(containerColor = EjBackground)` com padding horizontal 24dp.
Imports necessários: `androidx.compose.foundation.background`, `androidx.compose.foundation.clickable`, `androidx.compose.foundation.layout.*`, `androidx.compose.foundation.shape.RoundedCornerShape`, `androidx.compose.material.icons.Icons`, `androidx.compose.material.icons.automirrored.outlined.ArrowForward`, `androidx.compose.material.icons.outlined.AccountBalance`, `androidx.compose.material.icons.outlined.Gavel`, `androidx.compose.material.icons.outlined.Notifications`, `androidx.compose.material3.*`, `androidx.compose.ui.Alignment`, `androidx.compose.ui.Modifier`, `androidx.compose.ui.draw.clip`, `androidx.compose.ui.geometry.Offset`, `androidx.compose.ui.graphics.Brush`, `androidx.compose.ui.graphics.Color`, `androidx.compose.ui.text.font.FontWeight`, `androidx.compose.ui.text.style.TextAlign`, `androidx.compose.ui.unit.dp`, `androidx.compose.ui.unit.sp`.
  </action>
  <verify>Arquivo WelcomeScreen.kt existe em features/welcome/. Routes.kt contém `const val WELCOME = "welcome"`. `./gradlew :app-cliente:compileDemoDebugKotlin` sem erros.</verify>
  <done>WelcomeScreen implementada com gradiente naval, CTA buttons e feature tiles. Routes.kt atualizado.</done>
</task>

<task type="auto">
  <name>Task 3: LoginScreen redesenhada (Editorial Juris)</name>
  <files>
    app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/auth/LoginScreen.kt
  </files>
  <action>
Substituir completamente o conteúdo de LoginScreen.kt mantendo a mesma assinatura pública e lógica de ViewModel. Apenas a UI muda.

Layout dentro de `Scaffold(containerColor = EjBackground)` com `Column` scrollable (verticalScroll + rememberScrollState()), padding horizontal 24dp:

**Header (Logo):**
- Spacer 48dp do topo
- Box centralizado (align Modifier.align(Alignment.CenterHorizontally)):
  - Card 64x64dp, cornerRadius 16dp, background gradiente naval (mesmo Brush do WelcomeScreen):
    - Icon Gavel 32dp, tint White, Alignment.Center

**Título:**
- Spacer 20dp
- Text "Meu Processo" — headlineLarge (Manrope ExtraBold), color EjPrimary, align Center
- Spacer 4dp
- Text "Justiça na palma da sua mão." — bodyMedium Inter, color EjOnSurfaceVariant, align Center

**Subtítulo seção:**
- Spacer 36dp
- Text "Bem-vindo de volta" — style titleLarge (Manrope Bold 22sp), color EjPrimary

**Campo CPF:**
- Spacer 20dp
- Text "CPF" — labelLarge Medium Inter, color EjOnSurface
- Spacer 6dp
- TextField customizado (não OutlinedTextField — usar TextField sem outline):
  ```kotlin
  TextField(
      value = cpf,
      onValueChange = { cpf = it },
      modifier = Modifier.fillMaxWidth().clip(RoundedCornerShape(12.dp)),
      colors = TextFieldDefaults.colors(
          focusedContainerColor   = EjSurfaceContainerHigh,
          unfocusedContainerColor = EjSurfaceContainerHigh,
          focusedIndicatorColor   = Color.Transparent,
          unfocusedIndicatorColor = Color.Transparent,
          focusedTextColor        = EjOnSurface,
          unfocusedTextColor      = EjOnSurface,
      ),
      placeholder = { Text("000.000.000-00", color = EjOnSurfaceVariant) },
      trailingIcon = { Icon(Icons.Outlined.Person, contentDescription = null, tint = EjOnSurfaceVariant) },
      keyboardOptions = KeyboardOptions(keyboardType = KeyboardType.Number),
      singleLine = true
  )
  ```
  Nota: o campo existente usa `email` — mantenha a variável como `email` (o backend aceita CPF no campo email); apenas mude o label para "CPF" e o placeholder.

**Campo Senha:**
- Spacer 12dp
- Text "Senha" — labelLarge Medium Inter, color EjOnSurface
- Spacer 6dp
- TextField similar ao CPF com:
  - trailingIcon = IconButton para toggle visibilidade (ícone Visibility/VisibilityOff)
  - visualTransformation igual ao existente
  - keyboardOptions KeyboardType.Password

**Esqueci senha:**
- Spacer 8dp
- Row com `Arrangement.End`:
  - TextButton "Esqueci minha senha" — cor EjSecondary (dourado)

**Card de erro** (mostrar apenas quando `uiState is LoginUiState.Error`):
- Spacer 12dp
- Box com:
  - fundo EjErrorContainer com alpha 0.4f
  - borda esquerda 4dp cor EjError usando `Modifier.drawBehind { drawRect(color = EjError, size = Size(4.dp.toPx(), size.height)) }`
  - cornerRadius 8dp
  - padding 12dp
  - Row: Icon(Icons.Outlined.Info, EjError, 20dp) + Spacer 8dp + Text(mensagem, EjOnErrorContainer, bodyMedium)

**Botão Entrar:**
- Spacer 24dp
- Box gradiente naval full-width 56dp altura 12dp radius (mesmo padrão WelcomeScreen) com clickable que chama `viewModel.login(email, password)` quando `email.isNotBlank() && password.isNotBlank() && uiState !is LoginUiState.Loading`:
  - Se Loading: CircularProgressIndicator 20dp branco
  - Senão: Row com Text "Entrar" + ArrowForward icon (branco)
- Se disabled (campos vazios ou loading), aplicar alpha 0.5f no Box

**Separador "OU ACESSE COM":**
- Spacer 20dp
- Row: HorizontalDivider weight(1f) + Spacer 8dp + Text "OU ACESSE COM" labelSmall EjOnSurfaceVariant + Spacer 8dp + HorizontalDivider weight(1f)

**Botão Biometria:**
- Spacer 12dp
- OutlinedButton full-width height 48dp cornerRadius 12dp:
  - border = BorderStroke(1.dp, EjOutlineVariant)
  - colors = ButtonDefaults.outlinedButtonColors(containerColor = EjSurfaceContainerLow)
  - Row: Icon Fingerprint 20dp EjPrimary + Spacer 8dp + Text "Entrar com Biometria" labelLarge EjPrimary

**Footer conta:**
- Spacer 20dp
- Text centralizado: "Não possui uma conta? " + "Solicite acesso" (cor EjSecondary, underline via TextDecoration.Underline)
- Usar AnnotatedString com SpanStyle para o link

**Card LGPD:**
- Spacer 20dp
- Card(cornerRadius 8dp, containerColor EjSurfaceContainerLow, border nulo):
  - Modifier.drawBehind para borda esquerda 4dp EjSecondary
  - padding 12dp
  - Row: Box 32x32dp (fundo EjSecondaryFixed, cornerRadius 6dp) com Icon VerifiedUser 20dp EjSecondary + Spacer 12dp + Column: Text "Dados protegidos pela LGPD" labelLarge Bold EjOnSurface + Text "Suas informações são criptografadas." bodyMedium EjOnSurfaceVariant

- Spacer 32dp (espaço do teclado)

**Manter lógica existente:**
```kotlin
LaunchedEffect(uiState) {
    if (uiState is LoginUiState.Success) {
        navController.navigate(Routes.PROCESSO_LIST) {
            popUpTo(Routes.LOGIN) { inclusive = true }
        }
    }
}
```
(Mudar destino de ONBOARDING para PROCESSO_LIST — o fluxo Welcome → Login → ProcessoList está definido.)

Imports adicionais necessários: `androidx.compose.foundation.BorderStroke`, `androidx.compose.foundation.rememberScrollState`, `androidx.compose.foundation.verticalScroll`, `androidx.compose.material.icons.outlined.Fingerprint`, `androidx.compose.material.icons.outlined.Info`, `androidx.compose.material.icons.outlined.Lock`, `androidx.compose.material.icons.outlined.Person`, `androidx.compose.material.icons.outlined.VerifiedUser`, `androidx.compose.material3.HorizontalDivider`, `androidx.compose.material3.OutlinedButton`, `androidx.compose.material3.TextFieldDefaults`, `androidx.compose.ui.draw.drawBehind`, `androidx.compose.ui.geometry.Size`, `androidx.compose.ui.text.SpanStyle`, `androidx.compose.ui.text.TextDecoration`, `androidx.compose.ui.text.buildAnnotatedString`, `androidx.compose.ui.text.withStyle`, `com.aethixdigital.portaljuridico.ui.theme.*`.
  </action>
  <verify>`./gradlew :app-cliente:compileDemoDebugKotlin` sem erros. LoginScreen.kt contém `EjSurfaceContainerHigh`, `Icons.Outlined.Fingerprint` e `Icons.Outlined.VerifiedUser` confirmando o redesenho.</verify>
  <done>LoginScreen redesenhada com campos CPF/Senha estilo Editorial Juris, card de erro, botão biometria e card LGPD. Lógica de ViewModel preservada.</done>
</task>

<task type="auto">
  <name>Task 4: ProcessoListScreen</name>
  <files>
    app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/processo/ProcessoListScreen.kt
  </files>
  <action>
Criar arquivo em `app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/processo/`.

ProcessoListScreen é uma tela **de apresentação / preview** com dados mockados — sem ViewModel por enquanto, para não criar dependências ainda não implementadas. Quando o ViewModel de ProcessoList for integrado em fase posterior, os parâmetros serão adicionados.

```kotlin
@Composable
fun ProcessoListScreen(
    onProcessoClick: (String) -> Unit,
    onNotificacoesClick: () -> Unit = {},
    onPerfilClick: () -> Unit = {}
)
```

**Estrutura geral:** Scaffold com:
- topBar: TopAppBar customizada
- bottomBar: BottomNavBar customizada
- containerColor: EjBackground
- content: LazyColumn

**TopAppBar:**
```kotlin
CenterAlignedTopAppBar(
    title = {
        Row(verticalAlignment = Alignment.CenterVertically) {
            Icon(Icons.Outlined.AccountBalance, contentDescription = null,
                 tint = EjSecondary, modifier = Modifier.size(20.dp))
            Spacer(Modifier.width(6.dp))
            Text("Meu Processo",
                 style = MaterialTheme.typography.titleLarge,
                 color = EjPrimary)
        }
    },
    actions = {
        IconButton(onClick = onNotificacoesClick) {
            Icon(Icons.Outlined.Notifications, contentDescription = "Notificações",
                 tint = EjOnSurfaceVariant)
        }
        // Avatar initials circle
        Box(
            modifier = Modifier.size(32.dp).clip(CircleShape)
                .background(EjPrimaryContainer).clickable(onClick = onPerfilClick),
            contentAlignment = Alignment.Center
        ) {
            Text("JC", style = MaterialTheme.typography.labelSmall,
                 color = EjOnPrimaryContainer, fontWeight = FontWeight.Bold)
        }
        Spacer(Modifier.width(8.dp))
    },
    colors = TopAppBarDefaults.centerAlignedTopAppBarColors(
        containerColor = EjSurfaceContainerLowest
    )
)
```

**BottomNavBar** (NavigationBar):
```kotlin
NavigationBar(containerColor = EjSurfaceContainerLowest, tonalElevation = 0.dp) {
    listOf(
        Triple(Icons.Outlined.AccountBalance, "Início", true),
        Triple(Icons.Outlined.Schedule, "Andamentos", false),
        Triple(Icons.Outlined.Payments, "Documentos", false),
        Triple(Icons.Outlined.Chat, "Pagamentos", false),
        Triple(Icons.Outlined.Person, "Mais", false),
    ).forEach { (icon, label, selected) ->
        NavigationBarItem(
            selected = selected,
            onClick = {},
            icon = { Icon(icon, contentDescription = label) },
            label = { Text(label, style = MaterialTheme.typography.labelSmall) },
            colors = NavigationBarItemDefaults.colors(
                selectedIconColor = EjSecondary,
                selectedTextColor = EjSecondary,
                indicatorColor    = EjSecondaryFixed.copy(alpha = 0.3f),
                unselectedIconColor = EjOnSurfaceVariant,
                unselectedTextColor = EjOnSurfaceVariant,
            )
        )
    }
}
```

**LazyColumn items:**

Item 1 — Saudação:
```kotlin
item {
    Column(modifier = Modifier.padding(horizontal = 24.dp).padding(top = 20.dp)) {
        Text("Bem-vindo de volta", style = MaterialTheme.typography.labelMedium,
             color = EjOnSurfaceVariant)
        Text("Olá, João!", style = MaterialTheme.typography.displayLarge.copy(fontSize = 30.sp),
             color = EjPrimary)
    }
}
```

Item 2 — Featured card (urgente):
```kotlin
item {
    Spacer(Modifier.height(20.dp))
    Card(
        modifier = Modifier.padding(horizontal = 24.dp).fillMaxWidth()
            .drawBehind {
                drawRect(EjSecondary, size = Size(4.dp.toPx(), size.height))
            },
        shape = RoundedCornerShape(12.dp),
        colors = CardDefaults.cardColors(containerColor = EjSurfaceContainerLowest),
        elevation = CardDefaults.cardElevation(defaultElevation = 2.dp)
    ) {
        Column(modifier = Modifier.padding(16.dp)) {
            Row(verticalAlignment = Alignment.CenterVertically,
                horizontalArrangement = Arrangement.SpaceBetween,
                modifier = Modifier.fillMaxWidth()) {
                // Badge URGENTE
                Box(
                    modifier = Modifier
                        .clip(RoundedCornerShape(50))
                        .background(EjSecondaryFixed)
                        .padding(horizontal = 10.dp, vertical = 4.dp)
                ) {
                    Text("URGENTE", style = MaterialTheme.typography.labelSmall.copy(fontWeight = FontWeight.Bold),
                         color = EjOnSecondaryFixed)
                }
                Text("há 2 horas", style = MaterialTheme.typography.labelSmall,
                     color = EjOnSurfaceVariant)
            }
            Spacer(Modifier.height(10.dp))
            Text("Novidades no seu processo",
                 style = MaterialTheme.typography.titleLarge, color = EjOnSurface)
            Spacer(Modifier.height(4.dp))
            Text("Juiz designou data de audiência",
                 style = MaterialTheme.typography.bodyMedium, color = EjOnSurfaceVariant)
            Spacer(Modifier.height(12.dp))
            TextButton(onClick = { onProcessoClick("1") }, contentPadding = PaddingValues(0.dp)) {
                Text("Ver Detalhes", color = EjSecondary,
                     style = MaterialTheme.typography.labelLarge.copy(fontWeight = FontWeight.Bold))
                Spacer(Modifier.width(4.dp))
                Icon(Icons.AutoMirrored.Outlined.ArrowForward, contentDescription = null,
                     tint = EjSecondary, modifier = Modifier.size(16.dp))
            }
        }
    }
}
```

Item 3 — Dica do dia card:
```kotlin
item {
    Spacer(Modifier.height(12.dp))
    Card(
        modifier = Modifier.padding(horizontal = 24.dp).fillMaxWidth()
            .drawBehind {
                drawRect(EjSecondary.copy(alpha = 0.2f), size = Size(4.dp.toPx(), size.height))
            },
        shape = RoundedCornerShape(12.dp),
        colors = CardDefaults.cardColors(containerColor = EjSurfaceContainerLow),
        elevation = CardDefaults.cardElevation(defaultElevation = 0.dp)
    ) {
        Row(modifier = Modifier.padding(12.dp), verticalAlignment = Alignment.Top) {
            Box(
                modifier = Modifier.size(40.dp).clip(RoundedCornerShape(8.dp))
                    .background(EjSecondaryFixed),
                contentAlignment = Alignment.Center
            ) {
                Icon(Icons.Outlined.Lightbulb, contentDescription = null,
                     tint = EjSecondary, modifier = Modifier.size(22.dp))
            }
            Spacer(Modifier.width(12.dp))
            Column {
                Text("Dica do Dia", style = MaterialTheme.typography.labelLarge.copy(fontWeight = FontWeight.Bold),
                     color = EjOnSurface)
                Spacer(Modifier.height(2.dp))
                Text("Sentença não é o fim — saiba o que fazer após a decisão judicial.",
                     style = MaterialTheme.typography.bodyMedium, color = EjOnSurfaceVariant)
                Spacer(Modifier.height(6.dp))
                Text("Saiba mais", style = MaterialTheme.typography.labelMedium.copy(
                    textDecoration = TextDecoration.Underline), color = EjSecondary)
            }
        }
    }
}
```

Item 4 — Atalhos 2 colunas:
```kotlin
item {
    Spacer(Modifier.height(20.dp))
    Row(modifier = Modifier.padding(horizontal = 24.dp).fillMaxWidth(),
        horizontalArrangement = Arrangement.spacedBy(12.dp)) {
        // Faturas (dark primary)
        Card(
            modifier = Modifier.weight(1f).height(90.dp),
            shape = RoundedCornerShape(12.dp),
            colors = CardDefaults.cardColors(containerColor = EjPrimary),
            elevation = CardDefaults.cardElevation(2.dp)
        ) {
            Column(modifier = Modifier.padding(14.dp)) {
                Icon(Icons.Outlined.Payments, contentDescription = null,
                     tint = EjSecondaryFixed, modifier = Modifier.size(24.dp))
                Spacer(Modifier.height(6.dp))
                Text("Faturas e\nPagamentos",
                     style = MaterialTheme.typography.labelMedium.copy(fontWeight = FontWeight.Bold),
                     color = EjOnPrimary)
            }
        }
        // Chat com advogado (light)
        Card(
            modifier = Modifier.weight(1f).height(90.dp),
            shape = RoundedCornerShape(12.dp),
            colors = CardDefaults.cardColors(containerColor = EjSurfaceContainerLowest),
            elevation = CardDefaults.cardElevation(2.dp)
        ) {
            Column(modifier = Modifier.padding(14.dp)) {
                Icon(Icons.Outlined.Chat, contentDescription = null,
                     tint = EjPrimary, modifier = Modifier.size(24.dp))
                Spacer(Modifier.height(6.dp))
                Text("Falar com\nAdvogado",
                     style = MaterialTheme.typography.labelMedium.copy(fontWeight = FontWeight.Bold),
                     color = EjOnSurface)
            }
        }
    }
}
```

Item 5 — Seção "Processos Ativos":
```kotlin
item {
    Spacer(Modifier.height(24.dp))
    Row(
        modifier = Modifier.padding(horizontal = 24.dp).fillMaxWidth(),
        horizontalArrangement = Arrangement.SpaceBetween,
        verticalAlignment = Alignment.CenterVertically
    ) {
        Text("Processos Ativos", style = MaterialTheme.typography.titleLarge, color = EjOnSurface)
        TextButton(onClick = {}) {
            Text("Ver todos", color = EjSecondary, style = MaterialTheme.typography.labelLarge)
        }
    }
}
```

Item 6 — Lista de processo cards (usar `items` com lista mock de 2):

Mock data class local (private data class dentro do arquivo ou top-level private):
```kotlin
private data class ProcessoMock(val id: String, val nome: String, val numeroCnj: String)
private val processosMock = listOf(
    ProcessoMock("1", "Ação de Cobrança", "0001234-56.2023.8.26.0100"),
    ProcessoMock("2", "Inventário Extrajudicial", "0007890-12.2022.8.26.0050"),
)
```

Card de processo:
```kotlin
items(processosMock) { processo ->
    Spacer(Modifier.height(8.dp))
    Card(
        modifier = Modifier.padding(horizontal = 24.dp).fillMaxWidth()
            .clickable { onProcessoClick(processo.id) },
        shape = RoundedCornerShape(12.dp),
        colors = CardDefaults.cardColors(containerColor = EjSurfaceContainerLowest),
        elevation = CardDefaults.cardElevation(2.dp)
    ) {
        Row(modifier = Modifier.padding(16.dp), verticalAlignment = Alignment.CenterVertically) {
            Box(
                modifier = Modifier.size(44.dp).clip(CircleShape).background(EjSurfaceContainer),
                contentAlignment = Alignment.Center
            ) {
                Icon(Icons.Outlined.Gavel, contentDescription = null,
                     tint = EjPrimary, modifier = Modifier.size(24.dp))
            }
            Spacer(Modifier.width(14.dp))
            Column(modifier = Modifier.weight(1f)) {
                Text(processo.nome,
                     style = MaterialTheme.typography.labelLarge.copy(fontWeight = FontWeight.Bold),
                     color = EjOnSurface, maxLines = 1)
                Spacer(Modifier.height(2.dp))
                Text(processo.numeroCnj,
                     style = MaterialTheme.typography.labelSmall, color = EjOnSurfaceVariant,
                     maxLines = 1)
                Spacer(Modifier.height(6.dp))
                Box(
                    modifier = Modifier.clip(RoundedCornerShape(50))
                        .background(EjSecondaryFixed).padding(horizontal = 8.dp, vertical = 3.dp)
                ) {
                    Text("EM ABERTO",
                         style = MaterialTheme.typography.labelSmall.copy(fontWeight = FontWeight.Bold),
                         color = EjOnSecondaryFixed)
                }
            }
            Icon(Icons.AutoMirrored.Outlined.KeyboardArrowRight, contentDescription = null,
                 tint = EjOnSurfaceVariant)
        }
    }
}
```

Item final — bottom padding:
```kotlin
item { Spacer(Modifier.height(24.dp)) }
```

Imports: `androidx.compose.foundation.lazy.LazyColumn`, `androidx.compose.foundation.lazy.items`, `androidx.compose.material.icons.outlined.Gavel`, `androidx.compose.material.icons.outlined.Lightbulb`, `androidx.compose.material.icons.outlined.Notifications`, `androidx.compose.material.icons.outlined.Payments`, `androidx.compose.material.icons.outlined.Person`, `androidx.compose.material.icons.outlined.Schedule`, `androidx.compose.material3.CenterAlignedTopAppBar`, `androidx.compose.material3.NavigationBar`, `androidx.compose.material3.NavigationBarItem`, `androidx.compose.material3.NavigationBarItemDefaults`, `androidx.compose.material3.TopAppBarDefaults`, `androidx.compose.ui.geometry.Size`, `com.aethixdigital.portaljuridico.ui.theme.*`.
  </action>
  <verify>`./gradlew :app-cliente:compileDemoDebugKotlin` sem erros. ProcessoListScreen.kt existe em features/processo/.</verify>
  <done>ProcessoListScreen com TopAppBar, featured card urgente, dica do dia, atalhos 2 colunas, lista de processos com badges e BottomNavBar.</done>
</task>

<task type="auto">
  <name>Task 5: ProcessoDetailScreen + ClienteNavGraph wiring</name>
  <files>
    app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/features/processo/ProcessoDetailScreen.kt,
    app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/navigation/ClienteNavGraph.kt
  </files>
  <action>
**1. ProcessoDetailScreen.kt** — criar em `features/processo/`:

```kotlin
@Composable
fun ProcessoDetailScreen(
    processoId: String,
    onBackClick: () -> Unit,
    onConsultorIaClick: () -> Unit = {}
)
```

Scaffold: `containerColor = EjBackground`, `topBar` customizada.

**TopAppBar:**
```kotlin
TopAppBar(
    title = { Text("Sentença Proferida", style = MaterialTheme.typography.titleLarge, color = EjOnSurface) },
    navigationIcon = {
        IconButton(onClick = onBackClick) {
            Icon(Icons.AutoMirrored.Outlined.ArrowForward, // usar ArrowBack
                 contentDescription = "Voltar", tint = EjOnSurface)
        }
    },
    actions = {
        IconButton(onClick = {}) {
            Icon(Icons.Outlined.Share, contentDescription = "Compartilhar", tint = EjOnSurfaceVariant)
        }
    },
    colors = TopAppBarDefaults.topAppBarColors(containerColor = EjSurfaceContainerLowest)
)
```
NOTA: Para ícone back usar `Icons.AutoMirrored.Outlined.ArrowForward` com `modifier = Modifier.scale(scaleX = -1f, scaleY = 1f)` ou preferir `Icons.Outlined.ArrowBackIosNew` (import `androidx.compose.material.icons.outlined.ArrowBackIosNew`).

**Body — LazyColumn padding 24dp:**

Item 1 — label DECISÃO JUDICIAL:
```kotlin
item {
    Spacer(Modifier.height(16.dp))
    Row(verticalAlignment = Alignment.CenterVertically) {
        Box(modifier = Modifier.size(8.dp).clip(CircleShape).background(EjSecondary))
        Spacer(Modifier.width(8.dp))
        Text("DECISÃO JUDICIAL",
             style = MaterialTheme.typography.labelMedium.copy(
                 fontWeight = FontWeight.Bold, letterSpacing = 1.5.sp),
             color = EjSecondary)
    }
}
```

Item 2 — Headline:
```kotlin
item {
    Spacer(Modifier.height(12.dp))
    Text(
        "O juiz tomou uma decisão final sobre o seu caso.",
        style = MaterialTheme.typography.headlineLarge,
        color = EjPrimary
    )
}
```

Item 3 — Card processo info:
```kotlin
item {
    Spacer(Modifier.height(20.dp))
    Card(
        modifier = Modifier.fillMaxWidth(),
        shape = RoundedCornerShape(12.dp),
        colors = CardDefaults.cardColors(containerColor = EjSurfaceContainerLowest),
        elevation = CardDefaults.cardElevation(2.dp)
    ) {
        Row(modifier = Modifier.padding(16.dp), verticalAlignment = Alignment.CenterVertically) {
            Box(
                modifier = Modifier.size(44.dp).clip(CircleShape).background(EjPrimary),
                contentAlignment = Alignment.Center
            ) {
                Icon(Icons.Outlined.Gavel, contentDescription = null,
                     tint = EjOnPrimary, modifier = Modifier.size(24.dp))
            }
            Spacer(Modifier.width(14.dp))
            Column {
                Text("Processo nº", style = MaterialTheme.typography.labelSmall, color = EjOnSurfaceVariant)
                Text("0001234-56.2023.8.26.0100",
                     style = MaterialTheme.typography.labelLarge.copy(fontWeight = FontWeight.Bold),
                     color = EjOnSurface)
            }
        }
    }
}
```

Item 4 — Seção "O que aconteceu?":
```kotlin
item {
    Spacer(Modifier.height(24.dp))
    Row(verticalAlignment = Alignment.CenterVertically) {
        Icon(Icons.Outlined.Info, contentDescription = null,
             tint = EjPrimary, modifier = Modifier.size(20.dp))
        Spacer(Modifier.width(8.dp))
        Text("O que aconteceu?", style = MaterialTheme.typography.titleLarge, color = EjOnSurface)
    }
    Spacer(Modifier.height(10.dp))
    Text(
        buildAnnotatedString {
            append("O juiz analisou todas as provas do processo e proferiu a ")
            withStyle(SpanStyle(fontWeight = FontWeight.Bold)) { append("sentença") }
            append(". Isso significa que ele tomou uma decisão definitiva sobre o caso em primeira instância. A decisão determina que a parte ré deve ")
            withStyle(SpanStyle(fontWeight = FontWeight.Bold)) { append("pagar o valor cobrado") }
            append(" mais correção monetária e honorários advocatícios.")
        },
        style = MaterialTheme.typography.bodyLarge,
        color = EjOnSurface
    )
}
```

Item 5 — Card "O que isso significa?":
```kotlin
item {
    Spacer(Modifier.height(20.dp))
    Card(
        modifier = Modifier.fillMaxWidth().drawBehind {
            drawRect(EjSecondary, size = Size(4.dp.toPx(), size.height))
        },
        shape = RoundedCornerShape(12.dp),
        colors = CardDefaults.cardColors(containerColor = EjSurfaceContainerLowest),
        elevation = CardDefaults.cardElevation(2.dp)
    ) {
        Column(modifier = Modifier.padding(16.dp)) {
            Row(verticalAlignment = Alignment.CenterVertically) {
                Icon(Icons.Outlined.Lightbulb, contentDescription = null,
                     tint = EjSecondary, modifier = Modifier.size(20.dp))
                Spacer(Modifier.width(8.dp))
                Text("O que isso significa?",
                     style = MaterialTheme.typography.titleLarge, color = EjOnSurface)
            }
            Spacer(Modifier.height(12.dp))
            listOf(
                "A decisão é favorável a você",
                "A parte contrária tem 15 dias para recorrer",
                "Você pode solicitar o cumprimento da sentença"
            ).forEach { item ->
                Row(verticalAlignment = Alignment.Top, modifier = Modifier.padding(vertical = 4.dp)) {
                    Icon(Icons.Outlined.VerifiedUser, contentDescription = null,
                         tint = Color(0xFF2E7D32), modifier = Modifier.size(18.dp))
                    Spacer(Modifier.width(8.dp))
                    Text(item, style = MaterialTheme.typography.bodyMedium, color = EjOnSurface)
                }
            }
        }
    }
}
```

Item 6 — Próximos Passos:
```kotlin
item {
    Spacer(Modifier.height(24.dp))
    Row(verticalAlignment = Alignment.CenterVertically) {
        Icon(Icons.Outlined.Schedule, contentDescription = null,
             tint = EjPrimary, modifier = Modifier.size(20.dp))
        Spacer(Modifier.width(8.dp))
        Text("Próximos passos", style = MaterialTheme.typography.titleLarge, color = EjOnSurface)
    }
    Spacer(Modifier.height(10.dp))
    Text(
        "Aguarde o prazo de recurso da parte contrária. Caso não haja recurso, seu advogado poderá iniciar a execução da sentença para garantir o pagamento.",
        style = MaterialTheme.typography.bodyLarge, color = EjOnSurface
    )
    Spacer(Modifier.height(12.dp))
    // Pill de prazo
    Box(
        modifier = Modifier.clip(RoundedCornerShape(50))
            .background(EjSurfaceContainerHigh)
            .padding(horizontal = 14.dp, vertical = 6.dp)
    ) {
        Row(verticalAlignment = Alignment.CenterVertically) {
            Icon(Icons.Outlined.Schedule, contentDescription = null,
                 tint = EjOnSurfaceVariant, modifier = Modifier.size(14.dp))
            Spacer(Modifier.width(6.dp))
            Text("Prazo final: 24 Out", style = MaterialTheme.typography.labelMedium,
                 color = EjOnSurfaceVariant)
        }
    }
}
```

Item 7 — CTA card gradiente:
```kotlin
item {
    Spacer(Modifier.height(24.dp))
    Box(
        modifier = Modifier.fillMaxWidth().clip(RoundedCornerShape(16.dp))
            .background(
                Brush.linearGradient(
                    colors = listOf(Color(0xFF041631), Color(0xFF1B2B47)),
                    start = Offset(0f, 0f),
                    end = Offset(Float.POSITIVE_INFINITY, Float.POSITIVE_INFINITY)
                )
            )
            .padding(20.dp)
    ) {
        Column {
            Text("Restou alguma dúvida sobre o texto jurídico?",
                 style = MaterialTheme.typography.titleLarge, color = Color.White)
            Spacer(Modifier.height(6.dp))
            Text("Nosso Consultor IA explica em linguagem simples.",
                 style = MaterialTheme.typography.bodyMedium, color = Color.White.copy(alpha = 0.7f))
            Spacer(Modifier.height(16.dp))
            Button(
                onClick = onConsultorIaClick,
                colors = ButtonDefaults.buttonColors(containerColor = EjSecondaryContainer),
                shape = RoundedCornerShape(12.dp),
                modifier = Modifier.fillMaxWidth().height(48.dp)
            ) {
                Text("Perguntar ao Consultor IA",
                     color = EjOnSecondaryFixed,
                     style = MaterialTheme.typography.labelLarge.copy(fontWeight = FontWeight.Bold))
            }
        }
    }
    Spacer(Modifier.height(32.dp))
}
```

**2. ClienteNavGraph.kt** — substituir os TODO() dos composables existentes e adicionar WelcomeScreen:

```kotlin
package com.aethixdigital.portaljuridico.cliente.navigation

import androidx.compose.runtime.Composable
import androidx.navigation.NavHostController
import androidx.navigation.compose.NavHost
import androidx.navigation.compose.composable
import com.aethixdigital.portaljuridico.cliente.features.auth.LoginScreen
import com.aethixdigital.portaljuridico.cliente.features.processo.ProcessoDetailScreen
import com.aethixdigital.portaljuridico.cliente.features.processo.ProcessoListScreen
import com.aethixdigital.portaljuridico.cliente.features.welcome.WelcomeScreen

@Composable
fun ClienteNavGraph(navController: NavHostController, startDestination: String) {
    NavHost(navController = navController, startDestination = startDestination) {
        composable(Routes.WELCOME) {
            WelcomeScreen(
                onComecarClick = {
                    navController.navigate(Routes.LOGIN) {
                        popUpTo(Routes.WELCOME) { inclusive = true }
                    }
                },
                onJaSouClienteClick = {
                    navController.navigate(Routes.LOGIN)
                }
            )
        }
        composable(Routes.LOGIN) {
            LoginScreen(navController = navController)
        }
        composable(Routes.ONBOARDING) {
            // Onboarding ainda não implementado — redirecionar para ProcessoList
            ProcessoListScreen(
                onProcessoClick = { id ->
                    navController.navigate("${Routes.PROCESSO_DETAIL}/$id")
                }
            )
        }
        composable(Routes.LGPD_CONSENT) {
            // LGPD consent ainda não implementado — placeholder navegável
            ProcessoListScreen(
                onProcessoClick = { id ->
                    navController.navigate("${Routes.PROCESSO_DETAIL}/$id")
                }
            )
        }
        composable(Routes.PROCESSO_LIST) {
            ProcessoListScreen(
                onProcessoClick = { id ->
                    navController.navigate("${Routes.PROCESSO_DETAIL}/$id")
                }
            )
        }
        composable(Routes.PROCESSO_DETAIL_WITH_ID) { backStackEntry ->
            val processoId = backStackEntry.arguments?.getString("processoId") ?: return@composable
            ProcessoDetailScreen(
                processoId = processoId,
                onBackClick = { navController.popBackStack() },
                onConsultorIaClick = { /* chatbot — fase futura */ }
            )
        }
    }
}
```
  </action>
  <verify>`./gradlew :app-cliente:assembleDemoDebug` sem erros. ProcessoDetailScreen.kt e ClienteNavGraph.kt atualizados. Grep confirma: `grep -r "WelcomeScreen\|ProcessoListScreen\|ProcessoDetailScreen" app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/navigation/ClienteNavGraph.kt`.</verify>
  <done>ProcessoDetailScreen com CTA gradiente para chatbot IA. ClienteNavGraph conecta Welcome → Login → ProcessoList → ProcessoDetail sem TODO() restantes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| UI → ViewModel | Inputs de CPF/senha passam para LoginViewModel |
| App → Google Fonts CDN | Fontes baixadas em runtime do GMS provider |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-jt1-01 | Spoofing | GoogleFont.Provider certificates | mitigate | R.array.com_google_android_gms_fonts_certs valida o pacote GMS — não alterar o certificado array |
| T-jt1-02 | Information Disclosure | LoginScreen CPF field | accept | Campo mascarado com PasswordVisualTransformation se necessário; CPF é semi-público no contexto jurídico BR |
| T-jt1-03 | Denial of Service | Google Fonts fallback | accept | Compose FontFamily fallback automático para sistema font se GMS offline; UX degradada mas funcional |
</threat_model>

<verification>
Após executar todas as tasks em sequência:

```bash
# 1. Compilação completa
./gradlew :app-cliente:assembleDemoDebug

# 2. Verificar tokens no Color.kt
grep -c "EjPrimary\|EjSecondary\|EjSurface" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Color.kt

# 3. Verificar Google Fonts na dependência
grep "ui-text-google-fonts" gradle/libs.versions.toml app-cliente/build.gradle.kts

# 4. Verificar telas wired no NavGraph
grep -c "WelcomeScreen\|ProcessoListScreen\|ProcessoDetailScreen" app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/navigation/ClienteNavGraph.kt

# 5. Verificar ausência de TODO() no NavGraph
grep "TODO" app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/navigation/ClienteNavGraph.kt
```

Resultado esperado: build bem-sucedido, nenhum TODO() no NavGraph, 3 telas referenciadas.
</verification>

<success_criteria>
- `./gradlew :app-cliente:assembleDemoDebug` completa sem erros de compilação
- Color.kt contém os 30+ tokens Editorial Juris (navy, gold, off-white)
- Type.kt usa GoogleFont.Provider para Manrope e Inter
- WelcomeScreen exibe gradiente naval, feature tiles e dois CTAs
- LoginScreen exibe campos CPF/Senha com fundo SurfaceContainerHigh, botão gradiente, card LGPD
- ProcessoListScreen exibe featured card urgente com badge dourado, atalhos 2 colunas, lista de processos
- ProcessoDetailScreen exibe headline Manrope, card info processo, card "O que isso significa?", CTA gradiente para chatbot
- ClienteNavGraph conecta todas as telas sem TODO() restantes
</success_criteria>

<output>
Após conclusão, criar `.planning/quick/260416-jt1-implementar-identidade-visual-editorial-/260416-jt1-SUMMARY.md` com:
- Telas implementadas
- Tokens de cor adicionados
- Dependência Google Fonts configurada
- Decisões técnicas (dark mode desabilitado para app-cliente, dados mock em ProcessoList/Detail)
</output>
