---
phase: 06-push-notifications-in-app-center
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/0007_device_tokens_notifications.sql
  - gradle/libs.versions.toml
autonomous: true
requirements:
  - NOTIFY-01
  - NOTIFY-02
  - NOTIFY-03
  - NOTIFY-04
  - NOTIFY-05
  - NOTIFY-07

must_haves:
  truths:
    - "Tabela device_tokens existe no Supabase com RLS: usuário só vê seus próprios tokens"
    - "Tabela notifications existe com índices para query de não-lidas e histórico"
    - "Version catalog tem firebase-bom 34.12.0, work-runtime 2.11.2, hilt-work 1.2.0, google-services 4.4.2"
    - "Nenhuma lib duplicada ou com sufixo -ktx proibido adicionada"
  artifacts:
    - path: "supabase/migrations/0007_device_tokens_notifications.sql"
      provides: "Schema das tabelas device_tokens e notifications + RLS policies"
      contains: "CREATE TABLE device_tokens"
    - path: "gradle/libs.versions.toml"
      provides: "Version catalog atualizado com deps Firebase, WorkManager, hilt-work"
      contains: "firebaseBom"
  key_links:
    - from: "supabase/migrations/0007_device_tokens_notifications.sql"
      to: "supabase/migrations/0006_datajud_schema.sql"
      via: "REFERENCES processos(id) ON DELETE SET NULL"
      pattern: "processos"
    - from: "gradle/libs.versions.toml"
      to: "app-cliente/build.gradle.kts"
      via: "libs.firebase.bom, libs.firebase.messaging, libs.work.runtime, libs.hilt.work"
      pattern: "firebaseBom"
---

<objective>
Criar os fundamentos de infraestrutura da Phase 6: (1) migration SQL com as tabelas `device_tokens` e `notifications` no Supabase com RLS multi-tenant; (2) atualizar o version catalog Android com todas as dependências novas necessárias — Firebase BOM, WorkManager, hilt-work, google-services plugin.

Purpose: Estabelecer o schema de banco de dados e as entradas do version catalog que todas as tasks posteriores da fase dependem. Nenhum código Android ou backend pode ser implementado sem esses dois artefatos.
Output: `supabase/migrations/0007_device_tokens_notifications.sql` + entradas novas em `gradle/libs.versions.toml`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/06-push-notifications-in-app-center/06-CONTEXT.md
@.planning/phases/06-push-notifications-in-app-center/06-RESEARCH.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Migration SQL — device_tokens e notifications com RLS</name>
  <read_first>
    - supabase/migrations/0006_datajud_schema.sql (verificar nomes de tabelas referenciadas: processos, tenants)
    - supabase/migrations/0004_rls_policies.sql (padrão de RLS existente no projeto)
    - .planning/phases/06-push-notifications-in-app-center/06-RESEARCH.md §"Schema Supabase"
  </read_first>
  <files>supabase/migrations/0007_device_tokens_notifications.sql</files>
  <action>
Criar o arquivo `supabase/migrations/0007_device_tokens_notifications.sql` com o seguinte conteúdo EXATO:

**Tabela device_tokens:**
```sql
CREATE TABLE device_tokens (
    id            UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id       UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    tenant_id     UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
    token         TEXT NOT NULL,
    device_id     TEXT NOT NULL,
    platform      TEXT NOT NULL DEFAULT 'android',
    created_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    updated_at    TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    UNIQUE (user_id, device_id)
);

ALTER TABLE device_tokens ENABLE ROW LEVEL SECURITY;

CREATE POLICY "device_tokens: owner access"
    ON device_tokens
    FOR ALL
    USING (user_id = auth.uid())
    WITH CHECK (
        user_id = auth.uid()
        AND tenant_id = (SELECT (auth.jwt()->'app_metadata'->>'tenant_id')::uuid)
    );
```

**Tabela notifications:**
```sql
CREATE TABLE notifications (
    id              UUID PRIMARY KEY DEFAULT gen_random_uuid(),
    user_id         UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE,
    tenant_id       UUID NOT NULL REFERENCES tenants(id) ON DELETE CASCADE,
    processo_id     UUID REFERENCES processos(id) ON DELETE SET NULL,
    titulo          TEXT NOT NULL DEFAULT 'Nova movimentação',
    corpo           TEXT NOT NULL,
    deep_link       TEXT,
    is_read         BOOLEAN NOT NULL DEFAULT FALSE,
    created_at      TIMESTAMPTZ NOT NULL DEFAULT NOW(),
    read_at         TIMESTAMPTZ
);

CREATE INDEX idx_notifications_user_unread
    ON notifications (user_id, is_read, created_at DESC)
    WHERE is_read = FALSE;

CREATE INDEX idx_notifications_user_created
    ON notifications (user_id, created_at DESC);

ALTER TABLE notifications ENABLE ROW LEVEL SECURITY;

CREATE POLICY "notifications: owner read"
    ON notifications
    FOR SELECT
    USING (user_id = auth.uid());

CREATE POLICY "notifications: owner update (mark read)"
    ON notifications
    FOR UPDATE
    USING (user_id = auth.uid())
    WITH CHECK (user_id = auth.uid());
```

**Notas críticas:**
- NÃO adicionar policy de INSERT na tabela `notifications` para usuário autenticado — INSERT é feito exclusivamente pelo backend via service role key (que bypassa RLS).
- Usar `ON DELETE SET NULL` em `notifications.processo_id` — processo pode ser deletado sem perder histórico de notificações.
- Verificar antes: confirmar que `processos` e `tenants` existem nas migrations anteriores (0006 e 0001 respectivamente) antes de criar a FOREIGN KEY.
  </action>
  <verify>
    <automated>grep -n "CREATE TABLE device_tokens" supabase/migrations/0007_device_tokens_notifications.sql && grep -n "CREATE TABLE notifications" supabase/migrations/0007_device_tokens_notifications.sql && grep -n "ENABLE ROW LEVEL SECURITY" supabase/migrations/0007_device_tokens_notifications.sql | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CREATE TABLE device_tokens" supabase/migrations/0007_device_tokens_notifications.sql` retorna exatamente 1 match
    - `grep "CREATE TABLE notifications" supabase/migrations/0007_device_tokens_notifications.sql` retorna exatamente 1 match
    - `grep "ENABLE ROW LEVEL SECURITY" supabase/migrations/0007_device_tokens_notifications.sql` retorna 2 linhas (uma por tabela)
    - `grep "UNIQUE (user_id, device_id)" supabase/migrations/0007_device_tokens_notifications.sql` retorna 1 match
    - `grep "idx_notifications_user_unread" supabase/migrations/0007_device_tokens_notifications.sql` retorna 1 match
    - `grep "ON DELETE SET NULL" supabase/migrations/0007_device_tokens_notifications.sql` retorna 1 match (processo_id)
    - `grep "ON DELETE CASCADE" supabase/migrations/0007_device_tokens_notifications.sql` retorna pelo menos 2 matches (user_id nas duas tabelas)
  </acceptance_criteria>
  <done>Arquivo de migration criado com as duas tabelas, índices e RLS policies corretas. Schema alinhado com o padrão multi-tenant do projeto (tenant_id em todas as tabelas, RLS por user_id = auth.uid()).</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Version catalog — Firebase BOM, WorkManager, hilt-work, google-services</name>
  <read_first>
    - gradle/libs.versions.toml (estado atual COMPLETO — não assumir o que está lá)
    - .planning/phases/06-push-notifications-in-app-center/06-RESEARCH.md §"Adições ao libs.versions.toml"
  </read_first>
  <files>gradle/libs.versions.toml</files>
  <action>
Adicionar ao `gradle/libs.versions.toml` as seguintes entradas (sem duplicar entradas existentes — verificar antes):

**Na seção `[versions]`** — adicionar após as entradas existentes:
```toml
firebaseBom = "34.12.0"
workManager = "2.11.2"
hiltWork = "1.2.0"
googleServices = "4.4.2"
```

**Na seção `[libraries]`** — adicionar após as entradas existentes:
```toml
firebase-bom = { group = "com.google.firebase", name = "firebase-bom", version.ref = "firebaseBom" }
firebase-messaging = { group = "com.google.firebase", name = "firebase-messaging" }
work-runtime = { group = "androidx.work", name = "work-runtime", version.ref = "workManager" }
work-testing = { group = "androidx.work", name = "work-testing", version.ref = "workManager" }
hilt-work = { group = "androidx.hilt", name = "hilt-work", version.ref = "hiltWork" }
hilt-work-compiler = { group = "androidx.hilt", name = "hilt-compiler", version.ref = "hiltWork" }
```

**Na seção `[plugins]`** — adicionar após as entradas existentes:
```toml
google-services = { id = "com.google.gms.google-services", version.ref = "googleServices" }
```

**Regras críticas:**
- NÃO adicionar `firebase-messaging-ktx` — foi integrado ao artifact principal no BOM 34.0.0.
- NÃO adicionar `work-runtime-ktx` — é alias vazio desde WorkManager 2.9.0; usar apenas `work-runtime`.
- `firebase-messaging` NÃO tem `version.ref` porque a versão é gerenciada pelo BOM (platform BoM).
- Se `hilt-compiler` já existir em `[libraries]` com outro nome, NÃO criar duplicata — verificar antes e usar o nome existente.
  </action>
  <verify>
    <automated>grep -n "firebaseBom\|workManager\|hiltWork\|googleServices\|firebase-bom\|firebase-messaging\|work-runtime\|hilt-work\|google-services" gradle/libs.versions.toml</automated>
  </verify>
  <acceptance_criteria>
    - `grep "firebaseBom = \"34.12.0\"" gradle/libs.versions.toml` retorna 1 match
    - `grep "workManager = \"2.11.2\"" gradle/libs.versions.toml` retorna 1 match
    - `grep "hiltWork = \"1.2.0\"" gradle/libs.versions.toml` retorna 1 match
    - `grep "firebase-bom" gradle/libs.versions.toml` retorna exatamente 1 match (sem duplicata)
    - `grep "firebase-messaging" gradle/libs.versions.toml` retorna exatamente 1 match e NÃO contém `version.ref` (versão via BOM)
    - `grep "work-runtime-ktx" gradle/libs.versions.toml` retorna 0 matches (proibido)
    - `grep "firebase-messaging-ktx" gradle/libs.versions.toml` retorna 0 matches (proibido)
    - `grep "google-services" gradle/libs.versions.toml` retorna pelo menos 1 match na seção `[plugins]`
    - `grep "work-testing" gradle/libs.versions.toml` retorna 1 match
  </acceptance_criteria>
  <done>Version catalog atualizado com todas as dependências da Phase 6. O executor do Plan 02 pode referenciar libs.firebase.bom, libs.firebase.messaging, libs.work.runtime, libs.hilt.work e libs.hilt.work.compiler em qualquer build.gradle.kts.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Migration SQL → Supabase DB | SQL executado via CLI com credenciais de service account — superfície de ataque da migration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Elevation of Privilege | `device_tokens` RLS policy | mitigate | Policy usa `auth.uid()` para USING e WITH CHECK; `tenant_id` verificado contra JWT `app_metadata` — impede inserção de token em tenant alheio |
| T-06-02 | Information Disclosure | `notifications` RLS — ausência de INSERT policy para user | accept | INSERT só via service role (backend) que bypassa RLS por design Supabase; usuário autenticado nunca pode inserir notificação própria |
| T-06-03 | Tampering | `google-services.json` e `firebase-admin` service account JSON | mitigate | Esses arquivos devem estar em `.gitignore`; service account via env var `FIREBASE_SERVICE_ACCOUNT_JSON`; task de setup documenta isso explicitamente |
</threat_model>

<verification>
Após execução deste plano:
1. `grep "CREATE TABLE device_tokens" supabase/migrations/0007_device_tokens_notifications.sql` — deve retornar 1 match
2. `grep "CREATE TABLE notifications" supabase/migrations/0007_device_tokens_notifications.sql` — deve retornar 1 match
3. `grep "firebaseBom = \"34.12.0\"" gradle/libs.versions.toml` — deve retornar 1 match
4. `grep "work-runtime-ktx" gradle/libs.versions.toml` — deve retornar 0 matches
</verification>

<success_criteria>
- Migration SQL criada com tabelas device_tokens e notifications, 2 políticas RLS, 2 índices de query
- Version catalog com 4 versões novas, 6 libraries novas, 1 plugin novo
- Zero libs proibidas adicionadas (work-runtime-ktx, firebase-messaging-ktx)
- Arquivo pronto para `supabase db push` no Plan 03
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/06-push-notifications-in-app-center/06-01-SUMMARY.md`
</output>
