---
phase: 07-stripe-billing-grace-period
plan: 09
type: execute
wave: 7
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
gap_closure: true
requirements: [BILLING-01, BILLING-02]

must_haves:
  truths:
    - "REQUIREMENTS.md contains an explicit note on BILLING-01 and BILLING-02 recording that they are intentionally deferred to a future phase per CONTEXT.md D-01 decision"
    - "ROADMAP.md Phase 7 success criteria 1 and 5 are annotated with a deferral marker referencing D-01"
    - "No Stripe Checkout or Customer Portal code is introduced by this plan"
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "BILLING-01/02 deferral override documented"
      contains: "DEFERRED"
    - path: ".planning/ROADMAP.md"
      provides: "Phase 7 SC-1 and SC-5 annotated with deferral"
      contains: "provider-agnostic"
  key_links:
    - from: ".planning/REQUIREMENTS.md BILLING-01/02"
      to: ".planning/phases/07-stripe-billing-grace-period/07-CONTEXT.md D-01"
      via: "cross-reference in requirement text"
      pattern: "D-01|07-CONTEXT"
---

<objective>
Record the intentional architectural deferral of BILLING-01 (Stripe Checkout) and BILLING-02 (Stripe Customer Portal) in both REQUIREMENTS.md and ROADMAP.md, with an explicit reference to CONTEXT.md D-01 as the decision authority. This closes Gap 6 without introducing Stripe code.

Purpose: The VERIFICATION.md flags BILLING-01/02 as failed because the ROADMAP success criteria reference Stripe explicitly, but CONTEXT.md D-01 deliberately chose provider-agnostic infrastructure. Without an explicit override, future verifiers will keep re-flagging this as a gap. This plan makes the deferral explicit and traceable.
Output: Updated REQUIREMENTS.md and ROADMAP.md with deferral annotations. No code changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/07-stripe-billing-grace-period/07-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add BILLING-01/02 deferral override note to REQUIREMENTS.md</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (full file — locate the BILLING section and BILLING-01/02 entries)
    - .planning/phases/07-stripe-billing-grace-period/07-CONTEXT.md (confirm D-01 exact wording)
  </read_first>
  <action>
    In .planning/REQUIREMENTS.md, locate the Stripe Billing (BILLING) section containing BILLING-01 through BILLING-07.

    Update BILLING-01 and BILLING-02 entries to add explicit deferral notation. The update must:

    1. Keep the original requirement text intact for historical record
    2. Add `[DEFERRED — see Phase 7 override]` marker after the checkbox
    3. Add a deferral block directly under BILLING-02

    Change:
    ```
    - [ ] **BILLING-01**: Escritório consegue assinar um plano via Stripe Checkout hosted
    - [ ] **BILLING-02**: Escritório consegue gerenciar assinatura (cancelar, mudar plano) via Stripe Customer Portal
    ```

    To:
    ```
    - [ ] **BILLING-01** `[DEFERRED]`: Escritório consegue assinar um plano via Stripe Checkout hosted
    - [ ] **BILLING-02** `[DEFERRED]`: Escritório consegue gerenciar assinatura (cancelar, mudar plano) via Stripe Customer Portal

    > **Override — Phase 7 Architectural Decision (D-01):**
    > BILLING-01 and BILLING-02 are intentionally deferred. Phase 7 CONTEXT.md D-01 established that the
    > payment provider is not yet chosen (Stripe, Asaas, Pix, or other). Phase 7 implements
    > provider-agnostic infrastructure: entitlement middleware, grace-period state machine, generic webhook
    > receiver, and admin panel. Stripe Checkout and Customer Portal integration will be planned in a
    > dedicated future phase once the commercial model is defined. This is an accepted gap — not a
    > defect. Reference: `.planning/phases/07-stripe-billing-grace-period/07-CONTEXT.md` §D-01.
    ```

    Do not modify any other part of REQUIREMENTS.md.
  </action>
  <verify>
    <automated>grep -n "DEFERRED\|Override.*Phase 7\|D-01" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DEFERRED" .planning/REQUIREMENTS.md` returns at least 2 matches (one for BILLING-01, one for BILLING-02)
    - `grep "Override.*Phase 7\|D-01" .planning/REQUIREMENTS.md` returns at least 1 match
    - `grep "provider-agnostic" .planning/REQUIREMENTS.md` returns at least 1 match
    - BILLING-03 through BILLING-07 entries are unchanged: `grep "BILLING-0[3-7]" .planning/REQUIREMENTS.md` returns 5 matches with no DEFERRED marker
  </acceptance_criteria>
  <done>REQUIREMENTS.md BILLING-01 and BILLING-02 entries are annotated with [DEFERRED] and a reference block explaining the D-01 decision. Future verifiers will recognize this as an intentional architectural choice, not a missing implementation.</done>
</task>

<task type="auto">
  <name>Task 2: Annotate ROADMAP.md Phase 7 success criteria with deferral</name>
  <files>.planning/ROADMAP.md</files>
  <read_first>
    - .planning/ROADMAP.md (locate Phase 7 section — "### Phase 7: Stripe Billing & Grace Period" — and its success criteria 1 and 5)
  </read_first>
  <action>
    In .planning/ROADMAP.md, locate the Phase 7 section. Find Success Criteria 1 and 5 which reference Stripe explicitly.

    Current SC-1:
    ```
      1. An escritório can complete a Stripe Checkout flow (hosted), and the backend reflects the resulting `subscription_status` on the tenant record
    ```

    Current SC-5:
    ```
      5. From app_escritorio, "Gerenciar assinatura" opens the Stripe Customer Portal via Custom Tabs and plan changes round-trip back to the tenant record
    ```

    Update them to:
    ```
      1. ~~An escritório can complete a Stripe Checkout flow (hosted), and the backend reflects the resulting `subscription_status` on the tenant record~~ **[DEFERRED — D-01: payment provider not yet chosen; Stripe integration planned for future phase. Provider-agnostic webhook infrastructure is implemented.]**
    ```
    ```
      5. ~~From app_escritorio, "Gerenciar assinatura" opens the Stripe Customer Portal via Custom Tabs and plan changes round-trip back to the tenant record~~ **[DEFERRED — D-01: Stripe Customer Portal deferred. Contact link (WhatsApp/email) used instead until provider is chosen.]**
    ```

    Also update the Plans line from `**Plans**: TBD` to `**Plans**: 9 plans` and add the new gap closure plans to the plan list:

    Append to the existing Plans list in Phase 7:
    ```
    - [ ] 07-07-PLAN.md — Gap closure: fix admin/tenants.ts audit columns + implement green billing tests (BILLING-03/04/06/07)
    - [ ] 07-08-PLAN.md — Gap closure: wire BillingStatusViewModel + billing UI into ClienteListScreen, MensagemBottomSheet, ProcessoDetailScreen (D-10)
    - [ ] 07-09-PLAN.md — Gap closure: document BILLING-01/02 deferral override (D-01 architectural decision)
    ```

    Do not change any other phase entries or the overview section.
  </action>
  <verify>
    <automated>grep -n "DEFERRED.*D-01\|provider-agnostic\|07-07-PLAN\|07-08-PLAN\|07-09-PLAN" .planning/ROADMAP.md</automated>
  </verify>
  <acceptance_criteria>
    - `grep "DEFERRED.*D-01" .planning/ROADMAP.md` returns at least 2 matches (SC-1 and SC-5)
    - `grep "07-07-PLAN\|07-08-PLAN\|07-09-PLAN" .planning/ROADMAP.md` returns 3 matches
    - `grep "provider-agnostic" .planning/ROADMAP.md` returns at least 1 match
    - Phase 7 Plans count updated from TBD to `9 plans`
    - ROADMAP.md still parses as valid Markdown (no broken list structures)
  </acceptance_criteria>
  <done>ROADMAP.md Phase 7 success criteria 1 and 5 are annotated with strikethrough + DEFERRED markers referencing D-01. Three new gap closure plans are listed. Future verification tools will recognize the deferral override and not flag BILLING-01/02 as defects.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| planning docs → verifier | Verifier reads REQUIREMENTS.md and ROADMAP.md to assess coverage; explicit override annotations prevent false-positive gap flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-7-09-01 | Repudiation | Deferral of payment integration without audit trail | mitigate | Mitigation: explicit written record in REQUIREMENTS.md + ROADMAP.md with D-01 reference makes the decision traceable and attributable |
| T-7-09-02 | Tampering | Future developer removes deferral markers | accept | Rationale: planning docs are in git — any removal is visible in git history. Low risk for single-developer B2B project. |
</threat_model>

<verification>
- `grep "DEFERRED" .planning/REQUIREMENTS.md` → 2+ matches
- `grep "DEFERRED.*D-01" .planning/ROADMAP.md` → 2+ matches
- `grep "07-07-PLAN\|07-08-PLAN\|07-09-PLAN" .planning/ROADMAP.md` → 3 matches
- No Stripe Checkout or Customer Portal files were created: `find apps/ -name "*.ts" | xargs grep -l "checkout-session\|customer-portal" 2>/dev/null` → empty
</verification>

<success_criteria>
1. REQUIREMENTS.md BILLING-01 and BILLING-02 have [DEFERRED] markers and a reference block explaining the D-01 decision authority.
2. ROADMAP.md Phase 7 SC-1 and SC-5 have strikethrough + DEFERRED annotations with D-01 reference.
3. ROADMAP.md Phase 7 plan list includes the three gap closure plans (07-07, 07-08, 07-09).
4. No new Stripe code exists anywhere in the codebase.
5. Future verifiers will not flag BILLING-01/02 as defects — the deferral is now an explicit, documented, traceable architectural decision.
</success_criteria>

<output>
After completion, create `.planning/phases/07-stripe-billing-grace-period/07-09-SUMMARY.md`
</output>
