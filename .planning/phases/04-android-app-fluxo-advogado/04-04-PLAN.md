---
phase: 04-android-app-fluxo-advogado
plan: 04
type: execute
wave: 2
depends_on:
  - "04-01"
  - "04-02"
  - "04-03"
files_modified:
  - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteScreen.kt
  - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteViewModel.kt
  - core-data/src/main/java/com/aethixdigital/portaljuridico/data/repository/ClienteRepository.kt
  - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/navigation/EscritorioNavGraph.kt
  - app-escritorio/src/test/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteViewModelTest.kt
autonomous: true
requirements:
  - ESCR-02
  - ESCR-03
  - ESCR-10

must_haves:
  truths:
    - "Campo CPF exibe erro em tempo real após primeiro blur quando CPF é inválido (incluindo 111.111.111-11)"
    - "Campo CNJ exibe erro em tempo real após primeiro blur ou ao atingir 20 dígitos quando formato é inválido"
    - "CNJ aceita entrada com ou sem formatação e normaliza antes de validar"
    - "Botão Cadastrar fica desabilitado enquanto há erros de validação local"
    - "Ao submeter, app faz POST /api/v1/clientes e exibe loading; em sucesso navega de volta à lista"
    - "Em erro de email/CPF duplicado, exibe mensagem de erro específica do backend"
  artifacts:
    - path: "app-escritorio/.../feature/clientes/cadastro/CadastroClienteViewModel.kt"
      provides: "MVVM cadastro com validação em tempo real + submit"
      exports: ["CadastroClienteViewModel", "CadastroUiState", "CadastroFormState"]
    - path: "app-escritorio/.../feature/clientes/cadastro/CadastroClienteScreen.kt"
      provides: "Tela com 4 campos (nome, CPF, email, CNJ) + validação inline"
      exports: ["CadastroClienteScreen"]
  key_links:
    - from: "CadastroClienteScreen"
      to: "CpfValidator.isValidCpf (core-common)"
      via: "viewModel.onCpfBlur() → validação local imediata"
      pattern: "isValidCpf"
    - from: "CadastroClienteViewModel"
      to: "ClienteRepository.cadastrarCliente"
      via: "viewModelScope.launch { clienteRepository.cadastrarCliente(...) }"
      pattern: "cadastrarCliente"
---

<objective>
Implementar a tela de cadastro de novo cliente com validação em tempo real de CPF (mod11) e CNJ (regex + normalização) usando as funções de :core-common criadas em 04-02, mais a submissão via Retrofit ao backend.

Purpose: ESCR-02 e ESCR-03 são os únicos pontos de entrada de dados do advogado para o sistema. Validação no formulário (D-11) evita chamadas desnecessárias ao backend e oferece feedback instantâneo.
Output: Tela funcional de cadastro com validação inline, loading, e navegação pós-sucesso.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md
@.planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md
@.planning/phases/04-android-app-fluxo-advogado/04-03-SUMMARY.md

<interfaces>
<!-- Contratos dos planos anteriores. Usar diretamente. -->

De core-common (04-02):
```kotlin
// CpfValidator.kt (funções top-level)
fun isValidCpf(cpf: String): Boolean
fun formatCpf(raw: String): String

// CnjValidator.kt (funções top-level)
fun isValidCnjFormat(input: String): Boolean
fun normalizeCnj(input: String): String
```

De core-network (04-01):
```kotlin
// ClienteApi (já definido)
suspend fun cadastrarCliente(request: CadastroClienteRequest): CadastroClienteResponse
// CadastroClienteRequest: nome, cpf, email, numeroCnj (JSON: numero_cnj)
// CadastroClienteResponse: id, nome, cpf

// Adicionar a ClienteRepository:
suspend fun cadastrarCliente(nome: String, cpf: String, email: String, numeroCnj: String): Result<String>  // retorna id
```

De EscritorioNavGraph (04-03):
```kotlin
// O stub CadastroCliente deve ser substituído pela chamada real:
composable<EscritorioRoute.CadastroCliente> {
    CadastroClienteScreen(onSuccess = { navController.popBackStack() })
}
```

Decisão D-11: validação em tempo real após primeiro onFocusChanged (blur) OU quando campo atinge comprimento esperado:
- CPF: 11 dígitos → validar ao digitar o 11º dígito
- CNJ: 20 dígitos → validar ao digitar o 20º dígito; com formatação → 25 chars → validar no blur
Decisão D-12: unicidade de CPF verificada apenas ao submeter (via backend).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: CadastroClienteViewModel + CadastroClienteScreen com validação inline</name>
  <files>
    core-data/src/main/java/com/aethixdigital/portaljuridico/data/repository/ClienteRepository.kt,
    app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteViewModel.kt,
    app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteScreen.kt,
    app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/navigation/EscritorioNavGraph.kt,
    app-escritorio/src/test/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteViewModelTest.kt
  </files>
  <read_first>
    - core-data/src/main/java/com/aethixdigital/portaljuridico/data/repository/ClienteRepository.kt (adicionar cadastrarCliente)
    - core-common/src/main/java/com/aethixdigital/portaljuridico/common/validation/CpfValidator.kt (confirmar função isValidCpf existe)
    - core-common/src/main/java/com/aethixdigital/portaljuridico/common/validation/CnjValidator.kt (confirmar isValidCnjFormat e normalizeCnj)
    - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/navigation/EscritorioNavGraph.kt (substituir stub CadastroCliente)
    - .planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md §D-11, D-12, D-14 (validação real-time, unicidade no submit, UiState sealed)
  </read_first>
  <behavior>
    CadastroClienteViewModel tests (RED first):
    - cpfError nulo quando CPF ainda não foi digitado (campo intocado)
    - cpfError = "CPF inválido" quando onCpfBlur() chamado com CPF inválido "111.111.111-11"
    - cpfError = null quando onCpfBlur() com CPF válido "123.456.789-09"
    - cnjError = "Formato de CNJ inválido" quando onCnjBlur() com CNJ inválido
    - cnjError = null quando onCnjBlur() com CNJ válido formatado
    - isSubmitEnabled = false quando há erros de validação
    - isSubmitEnabled = true quando todos campos preenchidos e sem erros
    - cadastrar() com sucesso → UiState.Success(id)
    - cadastrar() com falha → UiState.Error("mensagem do backend")
  </behavior>
  <action>
    **Atualizar ClienteRepository.kt** — adicionar método cadastrarCliente:
    ```kotlin
    // Adicionar à interface ClienteRepository:
    suspend fun cadastrarCliente(nome: String, cpf: String, email: String, numeroCnj: String): Result<String>

    // Adicionar à ClienteRepositoryImpl:
    override suspend fun cadastrarCliente(nome: String, cpf: String, email: String, numeroCnj: String): Result<String> =
        runCatching {
            val response = clienteApi.cadastrarCliente(
                CadastroClienteRequest(nome = nome, cpf = cpf, email = email, numeroCnj = numeroCnj)
            )
            response.id
        }
    ```

    **CadastroClienteViewModel.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.clientes.cadastro
    import androidx.lifecycle.ViewModel; import androidx.lifecycle.viewModelScope
    import com.aethixdigital.portaljuridico.common.validation.isValidCpf
    import com.aethixdigital.portaljuridico.common.validation.isValidCnjFormat
    import com.aethixdigital.portaljuridico.common.validation.normalizeCnj
    import com.aethixdigital.portaljuridico.data.repository.ClienteRepository
    import dagger.hilt.android.lifecycle.HiltViewModel
    import kotlinx.coroutines.flow.*; import kotlinx.coroutines.launch
    import javax.inject.Inject

    data class CadastroFormState(
        val nome: String = "",
        val cpf: String = "",
        val email: String = "",
        val cnj: String = "",
        val cpfError: String? = null,
        val cnjError: String? = null,
        val emailError: String? = null,
        val cpfTouched: Boolean = false,
        val cnjTouched: Boolean = false,
        val emailTouched: Boolean = false
    ) {
        val isSubmitEnabled: Boolean
            get() = nome.isNotBlank() && cpf.isNotBlank() && email.isNotBlank() && cnj.isNotBlank()
                && cpfError == null && cnjError == null && emailError == null
                && cpfTouched && cnjTouched  // só habilita após campos terem sido validados
    }

    sealed class CadastroUiState {
        object Idle : CadastroUiState()
        object Loading : CadastroUiState()
        data class Success(val clienteId: String) : CadastroUiState()
        data class Error(val message: String) : CadastroUiState()
    }

    @HiltViewModel
    class CadastroClienteViewModel @Inject constructor(
        private val clienteRepository: ClienteRepository
    ) : ViewModel() {
        private val _formState = MutableStateFlow(CadastroFormState())
        val formState: StateFlow<CadastroFormState> = _formState.asStateFlow()

        private val _uiState = MutableStateFlow<CadastroUiState>(CadastroUiState.Idle)
        val uiState: StateFlow<CadastroUiState> = _uiState.asStateFlow()

        fun onNomeChange(value: String) { _formState.update { it.copy(nome = value) } }
        fun onEmailChange(value: String) {
            val err = if (value.isNotBlank() && !android.util.Patterns.EMAIL_ADDRESS.matcher(value).matches())
                "E-mail inválido" else null
            _formState.update { it.copy(email = value, emailError = err, emailTouched = true) }
        }
        fun onCpfChange(value: String) {
            val digits = value.filter { it.isDigit() }
            val formatted = com.aethixdigital.portaljuridico.common.validation.formatCpf(digits)
            // Validar ao atingir 11 dígitos (D-11)
            val error = if (digits.length == 11 && !isValidCpf(digits)) "CPF inválido" else null
            _formState.update { it.copy(cpf = if (digits.length <= 11) formatted else it.cpf, cpfError = error) }
        }
        fun onCpfBlur() {
            val digits = _formState.value.cpf.filter { it.isDigit() }
            val error = when {
                digits.isEmpty() -> null
                !isValidCpf(digits) -> "CPF inválido"
                else -> null
            }
            _formState.update { it.copy(cpfError = error, cpfTouched = true) }
        }
        fun onCnjChange(value: String) {
            val digits = value.filter { it.isDigit() }
            // Validar ao atingir 20 dígitos (D-11)
            val error = if (digits.length == 20 && !isValidCnjFormat(value)) "Formato de CNJ inválido" else null
            _formState.update { it.copy(cnj = value, cnjError = error) }
        }
        fun onCnjBlur() {
            val value = _formState.value.cnj
            val error = when {
                value.isBlank() -> null
                !isValidCnjFormat(value) -> "Formato de CNJ inválido"
                else -> null
            }
            _formState.update { it.copy(cnjError = error, cnjTouched = true) }
        }

        fun cadastrar() {
            val form = _formState.value
            if (!form.isSubmitEnabled) return
            viewModelScope.launch {
                _uiState.value = CadastroUiState.Loading
                // D-12: unicidade de CPF verificada pelo backend
                clienteRepository.cadastrarCliente(
                    nome = form.nome.trim(),
                    cpf = form.cpf.filter { it.isDigit() },  // enviar apenas dígitos
                    email = form.email.trim(),
                    numeroCnj = normalizeCnj(form.cnj)       // normalizar CNJ antes de enviar
                ).fold(
                    onSuccess = { id -> _uiState.value = CadastroUiState.Success(id) },
                    onFailure = { _uiState.value = CadastroUiState.Error(it.message ?: "Erro ao cadastrar cliente") }
                )
            }
        }
    }
    ```

    **CadastroClienteScreen.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.clientes.cadastro
    import androidx.compose.foundation.layout.*
    import androidx.compose.foundation.rememberScrollState; import androidx.compose.foundation.verticalScroll
    import androidx.compose.foundation.text.KeyboardOptions
    import androidx.compose.material.icons.Icons; import androidx.compose.material.icons.automirrored.filled.ArrowBack
    import androidx.compose.material3.*
    import androidx.compose.runtime.*
    import androidx.compose.ui.Modifier; import androidx.compose.ui.text.input.KeyboardType; import androidx.compose.ui.unit.dp
    import androidx.hilt.navigation.compose.hiltViewModel

    @OptIn(ExperimentalMaterial3Api::class)
    @Composable
    fun CadastroClienteScreen(
        onSuccess: () -> Unit,
        onBack: () -> Unit,
        viewModel: CadastroClienteViewModel = hiltViewModel()
    ) {
        val formState by viewModel.formState.collectAsState()
        val uiState by viewModel.uiState.collectAsState()

        LaunchedEffect(uiState) {
            if (uiState is CadastroUiState.Success) onSuccess()
        }

        Scaffold(
            topBar = {
                TopAppBar(
                    title = { Text("Cadastrar Cliente") },
                    navigationIcon = {
                        IconButton(onClick = onBack) {
                            Icon(Icons.AutoMirrored.Filled.ArrowBack, contentDescription = "Voltar")
                        }
                    }
                )
            }
        ) { innerPadding ->
            Column(
                modifier = Modifier
                    .fillMaxSize()
                    .padding(innerPadding)
                    .verticalScroll(rememberScrollState())
                    .padding(16.dp),
                verticalArrangement = Arrangement.spacedBy(12.dp)
            ) {
                // Nome
                OutlinedTextField(
                    value = formState.nome,
                    onValueChange = viewModel::onNomeChange,
                    label = { Text("Nome completo *") },
                    modifier = Modifier.fillMaxWidth(),
                    singleLine = true
                )
                // CPF — validação em tempo real (D-11)
                OutlinedTextField(
                    value = formState.cpf,
                    onValueChange = viewModel::onCpfChange,
                    label = { Text("CPF *") },
                    modifier = Modifier.fillMaxWidth(),
                    singleLine = true,
                    isError = formState.cpfError != null,
                    supportingText = formState.cpfError?.let { { Text(it, color = MaterialTheme.colorScheme.error) } },
                    keyboardOptions = KeyboardOptions(keyboardType = KeyboardType.Number),
                    placeholder = { Text("000.000.000-00") }
                )
                // E-mail
                OutlinedTextField(
                    value = formState.email,
                    onValueChange = viewModel::onEmailChange,
                    label = { Text("E-mail *") },
                    modifier = Modifier.fillMaxWidth(),
                    singleLine = true,
                    isError = formState.emailError != null,
                    supportingText = formState.emailError?.let { { Text(it, color = MaterialTheme.colorScheme.error) } },
                    keyboardOptions = KeyboardOptions(keyboardType = KeyboardType.Email)
                )
                // CNJ — validação em tempo real (D-11), aceita com ou sem formatação
                OutlinedTextField(
                    value = formState.cnj,
                    onValueChange = viewModel::onCnjChange,
                    label = { Text("Número do processo (CNJ) *") },
                    modifier = Modifier.fillMaxWidth(),
                    singleLine = true,
                    isError = formState.cnjError != null,
                    supportingText = formState.cnjError?.let { { Text(it, color = MaterialTheme.colorScheme.error) } },
                    placeholder = { Text("0001234-55.2023.8.26.0100") }
                )
                Spacer(modifier = Modifier.height(8.dp))
                if (uiState is CadastroUiState.Error) {
                    Text(
                        text = (uiState as CadastroUiState.Error).message,
                        color = MaterialTheme.colorScheme.error,
                        style = MaterialTheme.typography.bodySmall
                    )
                }
                Button(
                    onClick = viewModel::cadastrar,
                    modifier = Modifier.fillMaxWidth(),
                    enabled = formState.isSubmitEnabled && uiState !is CadastroUiState.Loading
                ) {
                    if (uiState is CadastroUiState.Loading) {
                        CircularProgressIndicator(modifier = Modifier.size(20.dp), strokeWidth = 2.dp)
                    } else {
                        Text("Cadastrar")
                    }
                }
                Text(
                    "* Campos obrigatórios",
                    style = MaterialTheme.typography.labelSmall,
                    color = MaterialTheme.colorScheme.onSurfaceVariant
                )
            }
        }
    }
    ```

    **Atualizar EscritorioNavGraph.kt** — substituir stub CadastroCliente:
    No bloco `composable<EscritorioRoute.CadastroCliente>`, substituir o stub `Text(...)` por:
    ```kotlin
    CadastroClienteScreen(
        onSuccess = { navController.popBackStack() },
        onBack = { navController.popBackStack() }
    )
    ```
    Adicionar import: `import com.aethixdigital.portaljuridico.escritorio.feature.clientes.cadastro.CadastroClienteScreen`

    **CadastroClienteViewModelTest.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.clientes.cadastro
    import com.aethixdigital.portaljuridico.data.repository.ClienteRepository
    import com.aethixdigital.portaljuridico.network.model.dto.ClienteItem
    import kotlinx.coroutines.Dispatchers; import kotlinx.coroutines.ExperimentalCoroutinesApi
    import kotlinx.coroutines.test.*; import org.junit.*; import org.junit.Assert.*

    @OptIn(ExperimentalCoroutinesApi::class)
    class CadastroClienteViewModelTest {
        private val testDispatcher = UnconfinedTestDispatcher()
        private lateinit var fakeRepo: FakeCadastroClienteRepository
        private lateinit var viewModel: CadastroClienteViewModel

        @Before fun setup() {
            Dispatchers.setMain(testDispatcher)
            fakeRepo = FakeCadastroClienteRepository()
            viewModel = CadastroClienteViewModel(fakeRepo)
        }
        @After fun tearDown() { Dispatchers.resetMain() }

        @Test fun initialState_noErrors() {
            val form = viewModel.formState.value
            assertNull(form.cpfError); assertNull(form.cnjError)
            assertFalse(form.isSubmitEnabled)
        }
        @Test fun onCpfBlur_invalidCpf_setsError() {
            viewModel.onCpfChange("11111111111")
            viewModel.onCpfBlur()
            assertNotNull(viewModel.formState.value.cpfError)
            assertEquals("CPF inválido", viewModel.formState.value.cpfError)
        }
        @Test fun onCpfBlur_validCpf_noError() {
            viewModel.onCpfChange("12345678909")
            viewModel.onCpfBlur()
            assertNull(viewModel.formState.value.cpfError)
        }
        @Test fun onCnjBlur_invalidCnj_setsError() {
            viewModel.onCnjChange("invalido")
            viewModel.onCnjBlur()
            assertNotNull(viewModel.formState.value.cnjError)
        }
        @Test fun onCnjBlur_validCnj_noError() {
            viewModel.onCnjChange("0001234-55.2023.8.26.0100")
            viewModel.onCnjBlur()
            assertNull(viewModel.formState.value.cnjError)
        }
        @Test fun cadastrar_success_emitsSuccess() = runTest {
            fakeRepo.cadastrarResult = Result.success("cliente-uuid-123")
            viewModel.onNomeChange("João Silva")
            viewModel.onEmailChange("joao@test.com")
            viewModel.onCpfChange("12345678909"); viewModel.onCpfBlur()
            viewModel.onCnjChange("0001234-55.2023.8.26.0100"); viewModel.onCnjBlur()
            viewModel.cadastrar()
            val state = viewModel.uiState.value
            assertTrue(state is CadastroUiState.Success)
            assertEquals("cliente-uuid-123", (state as CadastroUiState.Success).clienteId)
        }
        @Test fun cadastrar_failure_emitsError() = runTest {
            fakeRepo.cadastrarResult = Result.failure(Exception("CPF já cadastrado"))
            viewModel.onNomeChange("João Silva")
            viewModel.onEmailChange("joao@test.com")
            viewModel.onCpfChange("12345678909"); viewModel.onCpfBlur()
            viewModel.onCnjChange("0001234-55.2023.8.26.0100"); viewModel.onCnjBlur()
            viewModel.cadastrar()
            assertTrue(viewModel.uiState.value is CadastroUiState.Error)
        }
    }

    class FakeCadastroClienteRepository : ClienteRepository {
        var cadastrarResult: Result<String> = Result.success("fake-id")
        override suspend fun listarClientes(search: String?) = Result.success(emptyList<ClienteItem>())
        override suspend fun cadastrarCliente(nome: String, cpf: String, email: String, numeroCnj: String) = cadastrarResult
    }
    ```
  </action>
  <verify>
    <automated>./gradlew :app-escritorio:test --tests "*.CadastroClienteViewModelTest" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "fun onCpfBlur" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteViewModel.kt retorna match
    - grep "fun onCnjBlur" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteViewModel.kt retorna match
    - grep "isValidCpf" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteViewModel.kt retorna match
    - grep "normalizeCnj" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteViewModel.kt retorna match
    - grep "isError = formState.cpfError != null" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteScreen.kt retorna match
    - grep "CadastroClienteScreen" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/navigation/EscritorioNavGraph.kt retorna match (stub substituído)
    - ./gradlew :app-escritorio:test --tests "*.CadastroClienteViewModelTest" retorna "7 tests, 0 failures"
  </acceptance_criteria>
  <done>CadastroClienteViewModel com validação real-time CPF/CNJ. CadastroClienteScreen com 4 campos + isError inline. 7 testes passando. NavGraph atualizado com rota real. ESCR-02 e ESCR-03 cobertos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CadastroClienteScreen → CadastroClienteViewModel | Dados brutos do formulário (CPF, CNJ, nome, email) entram como strings sem sanitização prévia |
| ViewModel → ClienteRepository → Backend | Apenas CPF normalizado (só dígitos) e CNJ normalizado (formato padrão) enviados ao backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-04-01 | Tampering | CPF bypass — 111.111.111-11 aceito | mitigate | CpfValidator.isValidCpf tem guard all-same-digits; coberto por testes em 04-02. onCpfBlur() chama isValidCpf que rejeita. |
| T-04-04-02 | Information Disclosure | CPF em logs de rede (OkHttp DEBUG) | mitigate | HttpLoggingInterceptor ativo APENAS em BuildConfig.DEBUG. Release logs não incluem body com CPF. |
| T-04-04-03 | Tampering | CNJ forjado passando validação de formato | accept | App valida FORMATO apenas (D-11). Backend valida check-digit mod-97 (DATAJUD-01). Divisão correta de responsabilidades. |
| T-04-04-04 | Spoofing | Email de cliente duplicado | mitigate | Backend retorna erro 409 com mensagem "CPF já cadastrado" — ViewModel exibe em UiState.Error. |
</threat_model>

<verification>
Verificação manual pós-execução:
1. `./gradlew :app-escritorio:test --tests "*.CadastroClienteViewModelTest"` — 7 tests, 0 failures
2. `./gradlew :app-escritorio:assembleDebug` — BUILD SUCCESSFUL
3. Tela de cadastro abre ao tocar no FAB da lista
4. Digitar "111.111.111-11" no campo CPF e sair do campo: exibe "CPF inválido"
5. Digitar CPF válido "123.456.789-09": erro some
6. Botão Cadastrar desabilitado quando há erro
</verification>

<success_criteria>
- 7 testes unitários passando em CadastroClienteViewModelTest
- CadastroClienteViewModel usa isValidCpf e normalizeCnj de :core-common
- CadastroClienteScreen tem isError nos campos CPF e CNJ
- EscritorioNavGraph.CadastroCliente aponta para tela real (não stub)
- assembleDebug BUILD SUCCESSFUL
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-android-app-fluxo-advogado/04-04-SUMMARY.md`
</output>
