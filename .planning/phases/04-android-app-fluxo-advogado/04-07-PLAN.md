---
phase: 04-android-app-fluxo-advogado
plan: 07
type: execute
wave: 2
depends_on:
  - "04-06"
files_modified:
  - app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/HiltTestRunner.kt
  - app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/login/LoginScreenTest.kt
  - app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/list/ClienteListScreenTest.kt
  - app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteScreenTest.kt
  - app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/preview/PreviewScreenTest.kt
  - app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/MensagemBottomSheetTest.kt
  - app-escritorio/build.gradle.kts
  - .github/workflows/android.yml
autonomous: true
requirements:
  - ESCR-11

must_haves:
  truths:
    - "HiltTestRunner está registrado como testInstrumentationRunner em app-escritorio/build.gradle.kts"
    - "LoginScreenTest verifica que os campos email e senha e o botão Entrar estão presentes"
    - "ClienteListScreenTest verifica que o campo de busca e um card de cliente são exibidos"
    - "CadastroClienteScreenTest verifica que erros de CPF inválido são exibidos inline"
    - "PreviewScreenTest verifica que o disclaimer de IA é exibido em cada MovimentacaoCard"
    - "MensagemBottomSheetTest verifica que o botão Enviar fica desabilitado com campo vazio"
    - "CI workflow executa lint + unit tests + assembleDebug no app-escritorio em cada push"
  artifacts:
    - path: "app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/HiltTestRunner.kt"
      provides: "Custom test runner necessário para @HiltAndroidTest"
      contains: "class HiltTestRunner"
    - path: "app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/login/LoginScreenTest.kt"
      provides: "UI tests ESCR-01"
    - path: "app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/list/ClienteListScreenTest.kt"
      provides: "UI tests ESCR-04/05"
    - path: ".github/workflows/android.yml"
      provides: "CI: lint + test + assembleDebug para módulo app-escritorio"
      contains: "app-escritorio"
  key_links:
    - from: "HiltTestRunner"
      to: "testInstrumentationRunner em build.gradle.kts"
      via: "testInstrumentationRunner = \"...HiltTestRunner\""
      pattern: "HiltTestRunner"
    - from: "CI workflow"
      to: ":app-escritorio:lint + :app-escritorio:test"
      via: "./gradlew :app-escritorio:lint :app-escritorio:test :app-escritorio:assembleDebug"
      pattern: "app-escritorio"
---

<objective>
Adicionar a suíte de UI tests instrumentados para o app-escritorio (com HiltTestRunner), atualizar o build.gradle.kts com hilt-android-testing e ui-test-junit4, e atualizar o CI workflow para executar lint + unit tests + assembleDebug no módulo app-escritorio em cada push — entregando ESCR-11 completo.

Purpose: ESCR-11 fecha o ciclo de qualidade: sem CI verde não há release. Os UI tests garantem que as telas críticas (login, lista, cadastro, preview, mensagem) renderizam corretamente com dados fake antes de qualquer integração real com o backend.
Output: 5 UI tests instrumentados + HiltTestRunner + CI atualizado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md
@.planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md
@.planning/phases/04-android-app-fluxo-advogado/04-VALIDATION.md
@.planning/phases/04-android-app-fluxo-advogado/04-06-SUMMARY.md

<interfaces>
<!-- Contratos de planos anteriores. Usar diretamente. -->

De 04-RESEARCH.md §Hilt Test Setup (padrão exato de HiltTestRunner e @HiltAndroidTest):
```kotlin
class HiltTestRunner : AndroidJUnitRunner() {
    override fun newApplication(cl: ClassLoader?, name: String?, context: Context?): Application =
        super.newApplication(cl, HiltTestApplication::class.java.name, context)
}
// Registrar em build.gradle.kts:
testInstrumentationRunner = "com.aethixdigital.portaljuridico.escritorio.HiltTestRunner"

// Padrão de test com @BindValue:
@HiltAndroidTest
class ExampleScreenTest {
    @get:Rule(order = 0) val hiltRule = HiltAndroidRule(this)
    @get:Rule(order = 1) val composeTestRule = createAndroidComposeRule<MainActivity>()
    @BindValue @JvmField val fakeRepo: SomeRepository = FakeSomeRepository()
}
```

De gradle/libs.versions.toml (adicionadas em 04-01):
- hilt-android-testing — para @HiltAndroidTest
- turbine — para Flow testing (unit tests)
- androidx-compose-ui-test-junit4 — para ComposeTestRule
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: HiltTestRunner + androidTest build config + 5 UI tests</name>
  <files>
    app-escritorio/build.gradle.kts,
    app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/HiltTestRunner.kt,
    app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/login/LoginScreenTest.kt,
    app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/list/ClienteListScreenTest.kt,
    app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/cadastro/CadastroClienteScreenTest.kt,
    app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/preview/PreviewScreenTest.kt,
    app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/MensagemBottomSheetTest.kt
  </files>
  <read_first>
    - app-escritorio/build.gradle.kts (adicionar testInstrumentationRunner, androidTestImplementation deps)
    - gradle/libs.versions.toml (confirmar hilt-android-testing e turbine presentes — adicionados em 04-01)
    - .planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md §Hilt Test Setup (padrão HiltTestRunner + @BindValue)
    - .planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md §Anti-Patterns (pitfall 6: hilt-navigation-compose artifact)
    - .planning/phases/04-android-app-fluxo-advogado/04-VALIDATION.md §Per-Task Verification Map (mapa de req → UI test)
  </read_first>
  <action>
    **Atualizar app-escritorio/build.gradle.kts**:
    No bloco defaultConfig, alterar testInstrumentationRunner:
    ```kotlin
    testInstrumentationRunner = "com.aethixdigital.portaljuridico.escritorio.HiltTestRunner"
    ```
    No bloco dependencies, ADICIONAR:
    ```kotlin
    // Testing
    testImplementation(libs.turbine)
    androidTestImplementation(libs.hilt.android.testing)
    kspAndroidTest(libs.hilt.compiler)
    androidTestImplementation(platform(libs.androidx.compose.bom))
    androidTestImplementation(libs.androidx.compose.ui.test.junit4)
    debugImplementation(libs.androidx.compose.ui.test.manifest)
    ```

    **HiltTestRunner.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio
    import android.app.Application; import android.content.Context
    import androidx.test.runner.AndroidJUnitRunner
    import dagger.hilt.android.testing.HiltTestApplication
    class HiltTestRunner : AndroidJUnitRunner() {
        override fun newApplication(cl: ClassLoader?, name: String?, context: Context?): Application =
            super.newApplication(cl, HiltTestApplication::class.java.name, context)
    }
    ```

    **LoginScreenTest.kt** — verifica campos da tela de login:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.login
    import androidx.compose.ui.test.*
    import androidx.compose.ui.test.junit4.createAndroidComposeRule
    import androidx.test.ext.junit.runners.AndroidJUnit4
    import com.aethixdigital.portaljuridico.data.repository.AuthRepository
    import com.aethixdigital.portaljuridico.escritorio.MainActivity
    import dagger.hilt.android.testing.BindValue; import dagger.hilt.android.testing.HiltAndroidRule
    import dagger.hilt.android.testing.HiltAndroidTest
    import org.junit.*; import org.junit.runner.RunWith

    @HiltAndroidTest
    @RunWith(AndroidJUnit4::class)
    class LoginScreenTest {
        @get:Rule(order = 0) val hiltRule = HiltAndroidRule(this)
        @get:Rule(order = 1) val composeTestRule = createAndroidComposeRule<MainActivity>()

        @BindValue @JvmField
        val fakeAuthRepository: AuthRepository = FakeAuthRepositoryForUI()

        @Test
        fun loginScreen_showsEmailPasswordAndButton() {
            composeTestRule.onNodeWithText("E-mail").assertExists()
            composeTestRule.onNodeWithText("Senha").assertExists()
            composeTestRule.onNodeWithText("Entrar").assertExists()
        }
        @Test
        fun loginButton_disabledWhenFieldsEmpty() {
            composeTestRule.onNodeWithText("Entrar").assertIsNotEnabled()
        }
    }

    class FakeAuthRepositoryForUI : AuthRepository {
        override suspend fun login(email: String, password: String) = Result.success("advogado")
        override suspend fun logout() {}
        override suspend fun getSavedToken(): String? = null  // sempre mostra login
        override fun isValidAdvogadoToken(token: String) = false
    }
    ```

    **ClienteListScreenTest.kt** — verifica busca e card de cliente:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.clientes.list
    import androidx.compose.ui.test.*
    import androidx.compose.ui.test.junit4.createAndroidComposeRule
    import androidx.test.ext.junit.runners.AndroidJUnit4
    import com.aethixdigital.portaljuridico.data.repository.AuthRepository
    import com.aethixdigital.portaljuridico.data.repository.ClienteRepository
    import com.aethixdigital.portaljuridico.escritorio.MainActivity
    import com.aethixdigital.portaljuridico.network.model.dto.ClienteItem
    import dagger.hilt.android.testing.*
    import org.junit.*; import org.junit.runner.RunWith

    @HiltAndroidTest
    @RunWith(AndroidJUnit4::class)
    class ClienteListScreenTest {
        @get:Rule(order = 0) val hiltRule = HiltAndroidRule(this)
        @get:Rule(order = 1) val composeTestRule = createAndroidComposeRule<MainActivity>()

        @BindValue @JvmField
        val fakeAuthRepository: AuthRepository = FakeAuthRepositoryLoggedIn()

        @BindValue @JvmField
        val fakeClienteRepository: ClienteRepository = FakeClienteRepositoryWithData()

        @Test
        fun clienteList_showsSearchField() {
            composeTestRule.onNodeWithText("Buscar por nome, CPF ou processo...").assertExists()
        }
        @Test
        fun clienteList_showsClienteCard() {
            composeTestRule.onNodeWithText("João Silva").assertExists()
        }
    }

    class FakeAuthRepositoryLoggedIn : AuthRepository {
        override suspend fun login(email: String, password: String) = Result.success("advogado")
        override suspend fun logout() {}
        override suspend fun getSavedToken(): String? = "fake.valid.token"
        override fun isValidAdvogadoToken(token: String) = true
    }

    class FakeClienteRepositoryWithData : ClienteRepository {
        override suspend fun listarClientes(search: String?) = Result.success(listOf(
            ClienteItem("1", "João Silva", "123.456.789-09", "2h atrás", "Em andamento")
        ))
        override suspend fun cadastrarCliente(nome: String, cpf: String, email: String, numeroCnj: String) = Result.success("id")
        override suspend fun getClienteById(id: String) = Result.success(ClienteItem("1", "João Silva", "123.456.789-09", "2h atrás", "Em andamento"))
        override suspend fun previewCliente(clienteId: String) = Result.success(
            com.aethixdigital.portaljuridico.network.model.dto.PreviewResponse(
                ClienteItem("1", "João Silva", "123.456.789-09", "2h atrás", "Em andamento"), emptyList()
            )
        )
        override suspend fun enviarMensagem(clienteId: String, texto: String) = Result.success("msg-id")
        override suspend fun getPortalSessionUrl() = Result.success("https://billing.stripe.com/test")
    }
    ```

    **CadastroClienteScreenTest.kt** — verifica erro inline de CPF:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.clientes.cadastro
    import androidx.compose.ui.test.*
    import androidx.compose.ui.test.junit4.createComposeRule
    import androidx.test.ext.junit.runners.AndroidJUnit4
    import com.aethixdigital.portaljuridico.data.repository.ClienteRepository
    import com.aethixdigital.portaljuridico.network.model.dto.*
    import com.aethixdigital.portaljuridico.ui.theme.PortalJuridicoTheme
    import dagger.hilt.android.testing.HiltAndroidRule; import dagger.hilt.android.testing.HiltAndroidTest
    import org.junit.*; import org.junit.runner.RunWith

    // Nota: CadastroClienteScreen pode ser testada isoladamente com createComposeRule
    // para evitar dependência de navegação completa
    @RunWith(AndroidJUnit4::class)
    class CadastroClienteScreenTest {
        @get:Rule val composeTestRule = createComposeRule()

        @Test
        fun cpfField_showsErrorForInvalidCpf() {
            val fakeRepo = object : ClienteRepository {
                override suspend fun listarClientes(search: String?) = Result.success(emptyList<ClienteItem>())
                override suspend fun cadastrarCliente(nome: String, cpf: String, email: String, numeroCnj: String) = Result.success("id")
                override suspend fun getClienteById(id: String) = Result.success(ClienteItem("1", "T", "0", null, null))
                override suspend fun previewCliente(clienteId: String) = Result.success(PreviewResponse(ClienteItem("1", "T", "0", null, null), emptyList()))
                override suspend fun enviarMensagem(clienteId: String, texto: String) = Result.success("id")
                override suspend fun getPortalSessionUrl() = Result.success("url")
            }
            val viewModel = CadastroClienteViewModel(fakeRepo)
            composeTestRule.setContent {
                PortalJuridicoTheme {
                    CadastroClienteScreen(onSuccess = {}, onBack = {}, viewModel = viewModel)
                }
            }
            // Digitar CPF inválido e triggar blur via onCpfBlur
            viewModel.onCpfChange("11111111111")
            viewModel.onCpfBlur()
            composeTestRule.waitForIdle()
            composeTestRule.onNodeWithText("CPF inválido").assertExists()
        }
        @Test
        fun cadastrarButton_disabledInitially() {
            val fakeRepo = object : ClienteRepository {
                override suspend fun listarClientes(search: String?) = Result.success(emptyList<ClienteItem>())
                override suspend fun cadastrarCliente(nome: String, cpf: String, email: String, numeroCnj: String) = Result.success("id")
                override suspend fun getClienteById(id: String) = Result.success(ClienteItem("1", "T", "0", null, null))
                override suspend fun previewCliente(clienteId: String) = Result.success(PreviewResponse(ClienteItem("1", "T", "0", null, null), emptyList()))
                override suspend fun enviarMensagem(clienteId: String, texto: String) = Result.success("id")
                override suspend fun getPortalSessionUrl() = Result.success("url")
            }
            composeTestRule.setContent {
                PortalJuridicoTheme {
                    CadastroClienteScreen(onSuccess = {}, onBack = {}, viewModel = CadastroClienteViewModel(fakeRepo))
                }
            }
            composeTestRule.onNodeWithText("Cadastrar").assertIsNotEnabled()
        }
    }
    ```

    **PreviewScreenTest.kt** — verifica disclaimer visível:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.preview
    import androidx.compose.ui.test.*
    import androidx.compose.ui.test.junit4.createComposeRule
    import androidx.lifecycle.SavedStateHandle
    import androidx.test.ext.junit.runners.AndroidJUnit4
    import com.aethixdigital.portaljuridico.data.repository.ClienteRepository
    import com.aethixdigital.portaljuridico.network.model.dto.*
    import com.aethixdigital.portaljuridico.ui.theme.PortalJuridicoTheme
    import org.junit.*; import org.junit.runner.RunWith

    @RunWith(AndroidJUnit4::class)
    class PreviewScreenTest {
        @get:Rule val composeTestRule = createComposeRule()

        @Test
        fun previewScreen_showsDisclaimerInEachCard() {
            val fakeRepo = object : ClienteRepository {
                override suspend fun listarClientes(search: String?) = Result.success(emptyList<ClienteItem>())
                override suspend fun cadastrarCliente(nome: String, cpf: String, email: String, numeroCnj: String) = Result.success("id")
                override suspend fun getClienteById(id: String) = Result.success(ClienteItem("cli-1", "João", "123.456.789-09", "2h atrás", "Em andamento"))
                override suspend fun previewCliente(clienteId: String) = Result.success(PreviewResponse(
                    ClienteItem("cli-1", "João", "123.456.789-09", "2h atrás", "Em andamento"),
                    listOf(MovimentacaoDto("m1", "Aguardando", null, "O juiz analisa", "Aguarde", "Explicação gerada por IA — confirme com seu advogado"))
                ))
                override suspend fun enviarMensagem(clienteId: String, texto: String) = Result.success("id")
                override suspend fun getPortalSessionUrl() = Result.success("url")
            }
            val vm = PreviewViewModel(fakeRepo, SavedStateHandle(mapOf("clienteId" to "cli-1")))
            composeTestRule.setContent {
                PortalJuridicoTheme { PreviewScreen(onBack = {}, viewModel = vm) }
            }
            composeTestRule.waitForIdle()
            composeTestRule.onNodeWithText("Explicação gerada por IA — confirme com seu advogado").assertExists()
        }
        @Test
        fun previewScreen_emptyState_showsMessage() {
            val fakeRepo = object : ClienteRepository {
                override suspend fun listarClientes(search: String?) = Result.success(emptyList<ClienteItem>())
                override suspend fun cadastrarCliente(nome: String, cpf: String, email: String, numeroCnj: String) = Result.success("id")
                override suspend fun getClienteById(id: String) = Result.success(ClienteItem("cli-1", "João", "123.456.789-09", null, null))
                override suspend fun previewCliente(clienteId: String) = Result.success(PreviewResponse(ClienteItem("cli-1", "João", "123.456.789-09", null, null), emptyList()))
                override suspend fun enviarMensagem(clienteId: String, texto: String) = Result.success("id")
                override suspend fun getPortalSessionUrl() = Result.success("url")
            }
            val vm = PreviewViewModel(fakeRepo, SavedStateHandle(mapOf("clienteId" to "cli-1")))
            composeTestRule.setContent {
                PortalJuridicoTheme { PreviewScreen(onBack = {}, viewModel = vm) }
            }
            composeTestRule.waitForIdle()
            composeTestRule.onNodeWithText("Nenhuma movimentação disponível ainda.").assertExists()
        }
    }
    ```

    **MensagemBottomSheetTest.kt** — verifica botão desabilitado com campo vazio:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.clientes.detalhe
    import androidx.compose.ui.test.*
    import androidx.compose.ui.test.junit4.createComposeRule
    import androidx.test.ext.junit.runners.AndroidJUnit4
    import com.aethixdigital.portaljuridico.data.repository.ClienteRepository
    import com.aethixdigital.portaljuridico.network.model.dto.*
    import com.aethixdigital.portaljuridico.ui.theme.PortalJuridicoTheme
    import org.junit.*; import org.junit.runner.RunWith

    @RunWith(AndroidJUnit4::class)
    class MensagemBottomSheetTest {
        @get:Rule val composeTestRule = createComposeRule()

        @Test
        fun enviarButton_disabledWhenTextEmpty() {
            val fakeRepo = object : ClienteRepository {
                override suspend fun listarClientes(search: String?) = Result.success(emptyList<ClienteItem>())
                override suspend fun cadastrarCliente(nome: String, cpf: String, email: String, numeroCnj: String) = Result.success("id")
                override suspend fun getClienteById(id: String) = Result.success(ClienteItem("1", "T", "0", null, null))
                override suspend fun previewCliente(clienteId: String) = Result.success(PreviewResponse(ClienteItem("1", "T", "0", null, null), emptyList()))
                override suspend fun enviarMensagem(clienteId: String, texto: String) = Result.success("id")
                override suspend fun getPortalSessionUrl() = Result.success("url")
            }
            val vm = MensagemViewModel(fakeRepo)
            composeTestRule.setContent {
                PortalJuridicoTheme {
                    MensagemBottomSheet(clienteId = "cli-1", clienteNome = "João", onDismiss = {}, viewModel = vm)
                }
            }
            composeTestRule.onNodeWithText("Enviar").assertIsNotEnabled()
        }
    }
    ```
  </action>
  <verify>
    <automated>./gradlew :app-escritorio:lint :app-escritorio:test :app-escritorio:assembleDebug 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "class HiltTestRunner" app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/HiltTestRunner.kt retorna match
    - grep "HiltTestApplication" app-escritorio/src/androidTest/java/com/aethixdigital/portaljuridico/escritorio/HiltTestRunner.kt retorna match
    - grep "HiltTestRunner" app-escritorio/build.gradle.kts retorna match (testInstrumentationRunner)
    - grep "hilt.android.testing" app-escritorio/build.gradle.kts retorna match (androidTestImplementation)
    - grep "turbine" app-escritorio/build.gradle.kts retorna match
    - ./gradlew :app-escritorio:lint retorna "BUILD SUCCESSFUL" sem lint errors
    - ./gradlew :app-escritorio:test retorna "BUILD SUCCESSFUL" com todos testes unit passando
    - ./gradlew :app-escritorio:assembleDebug retorna "BUILD SUCCESSFUL"
  </acceptance_criteria>
  <done>HiltTestRunner registrado. 5 UI tests instrumentados para as telas principais. Lint + unit + assemble passando. ESCR-11 coberto.</done>
</task>

<task type="auto">
  <name>Task 2: CI workflow atualizado para app-escritorio</name>
  <files>
    .github/workflows/android.yml
  </files>
  <read_first>
    - .github/workflows/android.yml (ler conteúdo atual para não sobrescrever jobs existentes de outros módulos)
    - .planning/phases/00-android-bootstrap-cleanup/00-CONTEXT.md §D-18, D-19, D-20 (CI baseline já configurado em Phase 0)
  </read_first>
  <action>
    Ler o arquivo .github/workflows/android.yml existente.

    Se o arquivo existir e já tiver um job para app-escritorio: verificar se executa lint + test + assembleDebug e atualizar se necessário.

    Se não tiver job para app-escritorio (ou arquivo não existir), adicionar job no YAML existente:
    ```yaml
    app-escritorio-ci:
      name: app-escritorio CI
      runs-on: ubuntu-latest
      steps:
        - uses: actions/checkout@v4
        - uses: actions/setup-java@v4
          with:
            java-version: '17'
            distribution: 'temurin'
        - name: Setup Gradle
          uses: gradle/actions/setup-gradle@v3
        - name: Lint
          run: ./gradlew :app-escritorio:lint
        - name: Unit Tests
          run: ./gradlew :app-escritorio:test :core-common:test :core-network:compileDebugKotlin :core-data:compileDebugKotlin
        - name: Assemble Debug
          run: ./gradlew :app-escritorio:assembleDebug
        - name: Upload lint results
          if: always()
          uses: actions/upload-artifact@v4
          with:
            name: lint-results-app-escritorio
            path: app-escritorio/build/reports/lint-results-debug.html
    ```

    NOTA: Testes instrumentados (connectedDebugAndroidTest) NÃO são incluídos no CI por default — requerem emulador. Phase 0 CI (D-20) não inclui emulator tests. Os UI tests criados na Task 1 rodam manualmente ou em CI com emulator quando necessário.

    Garantir que o workflow tem:
    - `on: push: branches: [master, main]` e `on: pull_request: branches: [master, main]`
    - Não deve remover jobs existentes do workflow
  </action>
  <verify>
    <automated>cat .github/workflows/android.yml | grep -E "app-escritorio|lint|test|assembleDebug" | head -15</automated>
  </verify>
  <acceptance_criteria>
    - grep "app-escritorio" .github/workflows/android.yml retorna ao menos 3 matches (job name + lint + test/assemble commands)
    - grep ":app-escritorio:lint" .github/workflows/android.yml retorna match
    - grep ":app-escritorio:test" .github/workflows/android.yml retorna match
    - grep ":app-escritorio:assembleDebug" .github/workflows/android.yml retorna match
    - yaml é válido (não quebra o workflow existente)
  </acceptance_criteria>
  <done>CI workflow com job app-escritorio executando lint + test + assembleDebug em cada push. ESCR-11 totalmente coberto.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI → Build artifacts | GitHub Actions executa o build em ambiente isolado; APK gerado não é publicado automaticamente |
| androidTest → HiltTestApplication | Testes usam HiltTestApplication (não app real) — isolado do runtime de produção |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-07-01 | Tampering | CI — build com dependências não fixadas | mitigate | Version catalog tem versões fixas (não ranges). Builds são reproduzíveis. Gradle wrapper tem checksum. |
| T-04-07-02 | Information Disclosure | CI logs — tokens de API nos logs de build | mitigate | Testes usam FakeRepository (sem chamadas reais). Nenhum secret/token nos logs de build de debug. |
| T-04-07-03 | Denial of Service | CI — timeout em builds longos | accept | Build típico: ~3-5 min (D-20, Phase 0 target). Sem emulator = sem risco de timeout de CI. |
| T-04-07-04 | Repudiation | UI test com @BindValue injeta fake que passa quando não deveria | mitigate | @BindValue substitui TODA a implementação do repository — fakes devem replicar comportamento real. Código review garante cobertura correta. |
</threat_model>

<verification>
Verificação manual pós-execução:
1. `./gradlew :app-escritorio:lint` — BUILD SUCCESSFUL (zero lint errors)
2. `./gradlew :app-escritorio:test` — BUILD SUCCESSFUL (todos unit tests passando)
3. `./gradlew :app-escritorio:assembleDebug` — BUILD SUCCESSFUL
4. Push no branch master: verificar que GitHub Actions executa o job `app-escritorio-ci`
5. Job deve aparecer verde (não requer emulator — apenas lint + unit + assemble)
</verification>

<success_criteria>
- HiltTestRunner.kt existe com HiltTestApplication como runner
- testInstrumentationRunner no build.gradle.kts aponta para HiltTestRunner
- 5 UI test files criados (LoginScreenTest, ClienteListScreenTest, CadastroClienteScreenTest, PreviewScreenTest, MensagemBottomSheetTest)
- ./gradlew :app-escritorio:lint retorna BUILD SUCCESSFUL
- ./gradlew :app-escritorio:test retorna BUILD SUCCESSFUL (todos unit tests do plano 04-03 ao 04-06)
- ./gradlew :app-escritorio:assembleDebug retorna BUILD SUCCESSFUL
- .github/workflows/android.yml tem job app-escritorio com lint + test + assemble
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-android-app-fluxo-advogado/04-07-SUMMARY.md`
</output>
