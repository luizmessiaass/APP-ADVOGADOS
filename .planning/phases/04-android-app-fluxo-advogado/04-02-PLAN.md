---
phase: 04-android-app-fluxo-advogado
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - core-common/src/main/java/com/aethixdigital/portaljuridico/common/validation/CpfValidator.kt
  - core-common/src/main/java/com/aethixdigital/portaljuridico/common/validation/CnjValidator.kt
  - core-common/src/test/java/com/aethixdigital/portaljuridico/common/validation/CpfValidatorTest.kt
  - core-common/src/test/java/com/aethixdigital/portaljuridico/common/validation/CnjValidatorTest.kt
  - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt
  - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt
autonomous: true
requirements:
  - ESCR-03
  - ESCR-06
  - ESCR-08
  - ESCR-10

must_haves:
  truths:
    - "CPF inválido como 111.111.111-11 (todos dígitos iguais) é rejeitado pela função isValidCpf"
    - "CPF válido como 123.456.789-09 é aceito pela função isValidCpf"
    - "CNJ com ou sem formatação é normalizado e validado corretamente"
    - "MovimentacaoCard exibe o disclaimer 'Explicação gerada por IA' no topo do card (acima do texto)"
    - "ProcessoStatusCard exibe ultima_sincronizacao recebida como parâmetro"
    - "Testes unitários de CpfValidator e CnjValidator cobrem todos os edge cases documentados no RESEARCH.md"
  artifacts:
    - path: "core-common/src/main/java/com/aethixdigital/portaljuridico/common/validation/CpfValidator.kt"
      provides: "Validação CPF mod11 com guard all-same-digits"
      exports: ["isValidCpf", "formatCpf"]
    - path: "core-common/src/main/java/com/aethixdigital/portaljuridico/common/validation/CnjValidator.kt"
      provides: "Validação formato CNJ + normalização"
      exports: ["isValidCnjFormat", "normalizeCnj"]
    - path: "core-common/src/test/java/com/aethixdigital/portaljuridico/common/validation/CpfValidatorTest.kt"
      provides: "Testes unitários CPF — 8 casos"
    - path: "core-common/src/test/java/com/aethixdigital/portaljuridico/common/validation/CnjValidatorTest.kt"
      provides: "Testes unitários CNJ — 6 casos"
    - path: "core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt"
      provides: "Card reutilizável de movimentação traduzida com disclaimer visível no topo"
      exports: ["MovimentacaoCard"]
    - path: "core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt"
      provides: "Card de status do processo com ultima_sincronizacao"
      exports: ["ProcessoStatusCard"]
  key_links:
    - from: "CadastroClienteScreen (Plan 04-04)"
      to: "CpfValidator.isValidCpf"
      via: "chamada direta na validação do formulário em tempo real"
      pattern: "isValidCpf"
    - from: "PreviewScreen (Plan 04-05)"
      to: "MovimentacaoCard"
      via: "LazyColumn items { MovimentacaoCard(...) }"
      pattern: "MovimentacaoCard"
---

<objective>
Implementar os utilitários de validação de CPF e CNJ em :core-common (com testes unitários completos cobrindo todos os edge cases) e criar os componentes Compose reutilizáveis MovimentacaoCard e ProcessoStatusCard em :core-ui que serão usados tanto pelo app-escritorio (Phase 4) quanto pelo app-cliente (Phase 5).

Purpose: Validação CPF/CNJ é o coração do ESCR-03 e está desacoplada da UI (pode ser testada como lógica pura). MovimentacaoCard e ProcessoStatusCard são os componentes visuais centrais do produto — implementados uma vez em :core-ui, reutilizados em ambos os apps.
Output: 2 validators testados + 2 componentes Compose prontos para uso nos planos de Wave 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md
@.planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md
@.planning/phases/04-android-app-fluxo-advogado/04-VALIDATION.md

<interfaces>
<!-- Contratos para o executor. Não explorar codebase adicional. -->

De 04-RESEARCH.md §CPF Validation Algorithm (algoritmo exato, incluindo guards):
```kotlin
// Guard obrigatório — ANTES da aritmética
if (digits.all { it == digits[0] }) return false
// Primeiro check digit (pesos 10..2)
val sum1 = (0..8).sumOf { (digits[it] - '0') * (10 - it) }
val remainder1 = sum1 % 11
val digit1 = if (remainder1 < 2) 0 else 11 - remainder1
// Segundo check digit (pesos 11..2)
val sum2 = (0..9).sumOf { (digits[it] - '0') * (11 - it) }
```

De 04-RESEARCH.md §CNJ Number Format & Validation:
```kotlin
private val CNJ_PATTERN = Regex("""^\d{7}-\d{2}\.\d{4}\.\d\.\d{2}\.\d{4}$""")
// normalizeCnj: se 20 dígitos → formatar como NNNNNNN-DD.AAAA.J.TT.OOOO
```

De 04-CONTEXT.md §Specifics:
- "Validação de CNJ deve aceitar com ou sem formatação... normalizar antes de validar"
- "Disclaimer 'Explicação gerada por IA' deve ser visível sem o usuário ter que rolar — parte superior do card, não rodapé."

De core-network Plan 04-01 (DTOs que MovimentacaoCard usará):
```kotlin
data class MovimentacaoDto(val id: String, val status: String, val proximaData: String?,
    val explicacao: String, val impacto: String, val disclaimer: String)
```

Package do core-common: com.aethixdigital.portaljuridico.common
Package do core-ui: com.aethixdigital.portaljuridico.ui
Theme: PortalJuridicoTheme já existe em core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Validators CPF e CNJ com testes unitários (TDD red→green)</name>
  <files>
    core-common/src/main/java/com/aethixdigital/portaljuridico/common/validation/CpfValidator.kt,
    core-common/src/main/java/com/aethixdigital/portaljuridico/common/validation/CnjValidator.kt,
    core-common/src/test/java/com/aethixdigital/portaljuridico/common/validation/CpfValidatorTest.kt,
    core-common/src/test/java/com/aethixdigital/portaljuridico/common/validation/CnjValidatorTest.kt
  </files>
  <read_first>
    - core-common/build.gradle.kts (confirmar testImplementation(libs.junit) presente)
    - .planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md §CPF Validation Algorithm (algoritmo completo com edge cases)
    - .planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md §CNJ Number Format & Validation (regex e normalizeCnj)
    - .planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md §D-11 (real-time validation, lógica local)
  </read_first>
  <behavior>
    CPF tests (RED first):
    - isValidCpf("000.000.000-00") == false (all-same guard)
    - isValidCpf("111.111.111-11") == false (all-same guard)
    - isValidCpf("999.999.999-99") == false (all-same guard)
    - isValidCpf("123.456.789-09") == true (CPF de teste válido conhecido)
    - isValidCpf("123.456.789-10") == false (dígito verificador errado)
    - isValidCpf("12345678909") == true (sem formatação, mesmo CPF)
    - isValidCpf("abc") == false (não numérico)
    - isValidCpf("") == false (vazio)

    CNJ tests (RED first):
    - isValidCnjFormat("0001234-55.2023.8.26.0100") == true (formatado)
    - isValidCnjFormat("00012345520238260100") == true (sem formatação, 20 dígitos)
    - isValidCnjFormat("0001234-99.2023.8.26.0100") == true (format válido independente dos check digits — apenas formato)
    - isValidCnjFormat("0001234-55.2023.8.260100") == false (OOOO colado ao TT)
    - isValidCnjFormat("") == false (vazio)
    - normalizeCnj("00012345520238260100") == "0001234-55.2023.8.26.0100" (normalização correta)
  </behavior>
  <action>
    Seguir ciclo TDD: criar test files primeiro, verificar falha, criar implementação, verificar sucesso.

    **CpfValidatorTest.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.common.validation
    import org.junit.Assert.*; import org.junit.Test
    class CpfValidatorTest {
        @Test fun allSameDigits_000_isInvalid() = assertFalse(isValidCpf("000.000.000-00"))
        @Test fun allSameDigits_111_isInvalid() = assertFalse(isValidCpf("111.111.111-11"))
        @Test fun allSameDigits_999_isInvalid() = assertFalse(isValidCpf("999.999.999-99"))
        @Test fun validCpf_formatted_isValid() = assertTrue(isValidCpf("123.456.789-09"))
        @Test fun validCpf_unformatted_isValid() = assertTrue(isValidCpf("12345678909"))
        @Test fun wrongCheckDigit_isInvalid() = assertFalse(isValidCpf("123.456.789-10"))
        @Test fun nonNumeric_isInvalid() = assertFalse(isValidCpf("abc.def.ghi-jk"))
        @Test fun empty_isInvalid() = assertFalse(isValidCpf(""))
    }
    ```

    **CnjValidatorTest.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.common.validation
    import org.junit.Assert.*; import org.junit.Test
    class CnjValidatorTest {
        @Test fun formattedCnj_isValid() = assertTrue(isValidCnjFormat("0001234-55.2023.8.26.0100"))
        @Test fun unformattedCnj_20digits_isValid() = assertTrue(isValidCnjFormat("00012345520238260100"))
        @Test fun malformedCnj_isInvalid() = assertFalse(isValidCnjFormat("0001234-55.2023.8.260100"))
        @Test fun empty_isInvalid() = assertFalse(isValidCnjFormat(""))
        @Test fun normalizeCnj_20digits_returnsFormatted() =
            assertEquals("0001234-55.2023.8.26.0100", normalizeCnj("00012345520238260100"))
        @Test fun normalizeCnj_alreadyFormatted_returnsUnchanged() =
            assertEquals("0001234-55.2023.8.26.0100", normalizeCnj("0001234-55.2023.8.26.0100"))
    }
    ```

    **CpfValidator.kt** (arquivo top-level com funções, sem classe wrapper):
    ```kotlin
    package com.aethixdigital.portaljuridico.common.validation

    fun isValidCpf(cpf: String): Boolean {
        val digits = cpf.filter { it.isDigit() }
        if (digits.length != 11) return false
        if (digits.all { it == digits[0] }) return false  // guard all-same-digits ANTES da aritmética
        val sum1 = (0..8).sumOf { (digits[it] - '0') * (10 - it) }
        val remainder1 = sum1 % 11
        val digit1 = if (remainder1 < 2) 0 else 11 - remainder1
        if ((digits[9] - '0') != digit1) return false
        val sum2 = (0..9).sumOf { (digits[it] - '0') * (11 - it) }
        val remainder2 = sum2 % 11
        val digit2 = if (remainder2 < 2) 0 else 11 - remainder2
        return (digits[10] - '0') == digit2
    }

    fun formatCpf(raw: String): String {
        val digits = raw.filter { it.isDigit() }.take(11)
        return buildString {
            digits.forEachIndexed { i, c ->
                if (i == 3 || i == 6) append('.')
                if (i == 9) append('-')
                append(c)
            }
        }
    }
    ```

    **CnjValidator.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.common.validation

    private val CNJ_PATTERN = Regex("""^\d{7}-\d{2}\.\d{4}\.\d\.\d{2}\.\d{4}$""")

    fun normalizeCnj(input: String): String {
        val digits = input.filter { it.isDigit() }
        return if (digits.length == 20) {
            "${digits.substring(0, 7)}-${digits.substring(7, 9)}.${digits.substring(9, 13)}.${digits[13]}.${digits.substring(14, 16)}.${digits.substring(16, 20)}"
        } else input
    }

    fun isValidCnjFormat(input: String): Boolean {
        if (input.isBlank()) return false
        val normalized = normalizeCnj(input.trim())
        return CNJ_PATTERN.matches(normalized)
    }
    ```
  </action>
  <verify>
    <automated>./gradlew :core-common:test --tests "com.aethixdigital.portaljuridico.common.validation.*" 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ./gradlew :core-common:test retorna "BUILD SUCCESSFUL"
    - Output mostra "8 tests completed, 0 failures" para CpfValidatorTest
    - Output mostra "6 tests completed, 0 failures" para CnjValidatorTest
    - grep "if (digits.all { it == digits\[0\] }) return false" core-common/src/main/java/com/aethixdigital/portaljuridico/common/validation/CpfValidator.kt retorna match (guard obrigatório)
    - grep "CNJ_PATTERN" core-common/src/main/java/com/aethixdigital/portaljuridico/common/validation/CnjValidator.kt retorna match
    - grep "normalizeCnj" core-common/src/main/java/com/aethixdigital/portaljuridico/common/validation/CnjValidator.kt retorna match
  </acceptance_criteria>
  <done>CpfValidator e CnjValidator implementados com funções top-level. 14 testes unitários passando: 8 CPF (incluindo 3 casos all-same-digits) + 6 CNJ (incluindo normalização). BUILD SUCCESSFUL.</done>
</task>

<task type="auto">
  <name>Task 2: Componentes Compose — MovimentacaoCard e ProcessoStatusCard em :core-ui</name>
  <files>
    core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt,
    core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt
  </files>
  <read_first>
    - core-ui/build.gradle.kts (confirmar compose = true e material3 presente)
    - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt (PortalJuridicoTheme para Preview)
    - .planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md §D-08, D-09, D-10 (layout decisions)
    - .planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md §Specifics ("disclaimer visível sem rolar — topo do card")
    - .planning/phases/03-claude-ai-translation-core-value-prop/03-CONTEXT.md §D-06..D-10 (schema de output: status, proxima_data, explicacao, impacto)
  </read_first>
  <action>
    Criar os 2 componentes em core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/

    **MovimentacaoCard.kt** — Card para uma movimentação traduzida. Parâmetros individuais (não um DTO — :core-ui não deve depender de :core-network):
    ```kotlin
    package com.aethixdigital.portaljuridico.ui.components

    import androidx.compose.foundation.layout.*
    import androidx.compose.material3.*
    import androidx.compose.runtime.Composable
    import androidx.compose.ui.Modifier
    import androidx.compose.ui.text.font.FontStyle
    import androidx.compose.ui.text.font.FontWeight
    import androidx.compose.ui.tooling.preview.Preview
    import androidx.compose.ui.unit.dp
    import com.aethixdigital.portaljuridico.ui.theme.PortalJuridicoTheme

    /**
     * Card de movimentação traduzida por IA.
     * Reutilizável em app-escritorio (preview) e app-cliente (Phase 5).
     *
     * @param status Status atual do processo (linguagem simples)
     * @param explicacao Descrição da movimentação em linguagem acessível
     * @param impacto Impacto para o cliente (ex: "Aguardar decisão do juiz")
     * @param proximaData Próxima data importante (pode ser null)
     * @param disclaimer Texto de disclaimer — deve ser "Explicação gerada por IA — confirme com seu advogado"
     * @param modifier Modifier padrão Compose
     */
    @Composable
    fun MovimentacaoCard(
        status: String,
        explicacao: String,
        impacto: String,
        proximaData: String?,
        disclaimer: String,
        modifier: Modifier = Modifier
    ) {
        Card(
            modifier = modifier.fillMaxWidth(),
            elevation = CardDefaults.cardElevation(defaultElevation = 2.dp)
        ) {
            Column(modifier = Modifier.padding(16.dp)) {
                // DISCLAIMER NO TOPO — visível sem rolar (per D-10 e CONTEXT specifics)
                Surface(
                    color = MaterialTheme.colorScheme.secondaryContainer,
                    shape = MaterialTheme.shapes.small,
                    modifier = Modifier.fillMaxWidth()
                ) {
                    Text(
                        text = disclaimer,
                        style = MaterialTheme.typography.labelSmall,
                        fontStyle = FontStyle.Italic,
                        color = MaterialTheme.colorScheme.onSecondaryContainer,
                        modifier = Modifier.padding(horizontal = 8.dp, vertical = 4.dp)
                    )
                }
                Spacer(modifier = Modifier.height(12.dp))
                Text(
                    text = status,
                    style = MaterialTheme.typography.titleMedium,
                    fontWeight = FontWeight.Bold
                )
                Spacer(modifier = Modifier.height(8.dp))
                Text(
                    text = explicacao,
                    style = MaterialTheme.typography.bodyMedium
                )
                Spacer(modifier = Modifier.height(8.dp))
                Text(
                    text = "O que isso significa para você: $impacto",
                    style = MaterialTheme.typography.bodySmall,
                    color = MaterialTheme.colorScheme.onSurfaceVariant
                )
                if (proximaData != null) {
                    Spacer(modifier = Modifier.height(8.dp))
                    HorizontalDivider()
                    Spacer(modifier = Modifier.height(8.dp))
                    Text(
                        text = "Próxima data: $proximaData",
                        style = MaterialTheme.typography.labelMedium,
                        fontWeight = FontWeight.SemiBold,
                        color = MaterialTheme.colorScheme.primary
                    )
                }
            }
        }
    }

    @Preview(showBackground = true)
    @Composable
    private fun MovimentacaoCardPreview() {
        PortalJuridicoTheme {
            MovimentacaoCard(
                status = "Aguardando julgamento",
                explicacao = "O juiz recebeu todas as informações e está analisando o caso.",
                impacto = "Você não precisa fazer nada agora. Aguarde a decisão.",
                proximaData = "15/06/2026 — Audiência",
                disclaimer = "Explicação gerada por IA — confirme com seu advogado"
            )
        }
    }
    ```

    **ProcessoStatusCard.kt** — Card de status resumido do processo (usado na lista e no detalhe):
    ```kotlin
    package com.aethixdigital.portaljuridico.ui.components

    import androidx.compose.foundation.layout.*
    import androidx.compose.material3.*
    import androidx.compose.runtime.Composable
    import androidx.compose.ui.Alignment
    import androidx.compose.ui.Modifier
    import androidx.compose.ui.text.font.FontWeight
    import androidx.compose.ui.tooling.preview.Preview
    import androidx.compose.ui.unit.dp
    import com.aethixdigital.portaljuridico.ui.theme.PortalJuridicoTheme

    /**
     * Card de status resumido do processo.
     * Exibe número CNJ, status atual e última sincronização DataJud.
     * Reutilizável em app-escritorio e app-cliente (Phase 5).
     *
     * @param numeroCnj Número CNJ formatado do processo
     * @param statusAtual Status atual em linguagem simples
     * @param ultimaSincronizacao String de timestamp da última sync DataJud (pode ser null = "Nunca sincronizado")
     * @param modifier Modifier padrão Compose
     */
    @Composable
    fun ProcessoStatusCard(
        numeroCnj: String,
        statusAtual: String,
        ultimaSincronizacao: String?,
        modifier: Modifier = Modifier
    ) {
        Card(
            modifier = modifier.fillMaxWidth(),
            elevation = CardDefaults.cardElevation(defaultElevation = 2.dp)
        ) {
            Column(modifier = Modifier.padding(16.dp)) {
                Row(
                    modifier = Modifier.fillMaxWidth(),
                    horizontalArrangement = Arrangement.SpaceBetween,
                    verticalAlignment = Alignment.CenterVertically
                ) {
                    Text(
                        text = numeroCnj,
                        style = MaterialTheme.typography.labelMedium,
                        color = MaterialTheme.colorScheme.onSurfaceVariant
                    )
                    // Status badge
                    Surface(
                        color = MaterialTheme.colorScheme.primaryContainer,
                        shape = MaterialTheme.shapes.small
                    ) {
                        Text(
                            text = statusAtual,
                            style = MaterialTheme.typography.labelSmall,
                            fontWeight = FontWeight.Medium,
                            color = MaterialTheme.colorScheme.onPrimaryContainer,
                            modifier = Modifier.padding(horizontal = 8.dp, vertical = 4.dp)
                        )
                    }
                }
                Spacer(modifier = Modifier.height(8.dp))
                HorizontalDivider()
                Spacer(modifier = Modifier.height(8.dp))
                // ESCR-08: última sincronização DataJud visível
                Text(
                    text = if (ultimaSincronizacao != null)
                        "Última sync DataJud: $ultimaSincronizacao"
                    else
                        "Aguardando primeira sincronização",
                    style = MaterialTheme.typography.labelSmall,
                    color = MaterialTheme.colorScheme.onSurfaceVariant
                )
            }
        }
    }

    @Preview(showBackground = true)
    @Composable
    private fun ProcessoStatusCardPreview() {
        PortalJuridicoTheme {
            ProcessoStatusCard(
                numeroCnj = "0001234-55.2023.8.26.0100",
                statusAtual = "Em andamento",
                ultimaSincronizacao = "há 2 horas"
            )
        }
    }
    ```
  </action>
  <verify>
    <automated>./gradlew :core-ui:compileDebugKotlin 2>&1 | grep -E "^e:|error:" | head -5; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep "fun MovimentacaoCard" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt retorna match
    - grep "disclaimer" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt retorna match em linha ANTES de "Text(text = status" (disclaimer no topo)
    - grep "fun ProcessoStatusCard" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt retorna match
    - grep "ultimaSincronizacao" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt retorna match
    - grep "Última sync DataJud" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt retorna match
    - ./gradlew :core-ui:compileDebugKotlin sem erros "error:"
  </acceptance_criteria>
  <done>MovimentacaoCard e ProcessoStatusCard compilando em :core-ui. Disclaimer no topo do MovimentacaoCard (per D-10). ProcessoStatusCard exibe ultima_sincronizacao (ESCR-08). Ambos têm @Preview funcionais.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Formulário → Validator | Input do usuário (CPF/CNJ digitado) passa pela função de validação antes de qualquer chamada ao backend |
| Backend → MovimentacaoCard | Texto da movimentação vem do backend (Phase 3 AI output); exibido em Card, não interpretado como HTML/código |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | Tampering | CpfValidator — all-same-digits bypass | mitigate | Guard explícito `if (digits.all { it == digits[0] }) return false` ANTES da aritmética — coberto por 3 testes unitários |
| T-04-02-02 | Information Disclosure | MovimentacaoCard exibe disclaimer | mitigate | Disclaimer visível no topo de cada card — usuário não pode ignorar aviso de IA (per D-10, ESCR-06, AI-06) |
| T-04-02-03 | Spoofing | CNJ formatado vs não-formatado | mitigate | normalizeCnj trata ambos antes da validação — não há diferença de comportamento que possa ser explorada |
| T-04-02-04 | Tampering | Texto de movimentação (XSS/injection) | accept | Compose Text() renderiza texto puro — não processa HTML ou Markdown; sem superfície de injection em Text composables |
</threat_model>

<verification>
Verificação manual pós-execução:
1. `./gradlew :core-common:test` — "14 tests completed, 0 failures"
2. `./gradlew :core-ui:assemble` — BUILD SUCCESSFUL
3. Android Studio Preview do MovimentacaoCard deve mostrar o disclaimer no topo (Surface colorida antes do status)
4. Testar manualmente: isValidCpf("111.111.111-11") deve retornar false
</verification>

<success_criteria>
- 14 testes passando: 8 CpfValidator (incluindo 3 all-same) + 6 CnjValidator (incluindo normalizeCnj)
- CpfValidator.kt tem guard all-same-digits ANTES da aritmética mod11
- MovimentacaoCard tem disclaimer como PRIMEIRO elemento visual no Column (acima de status/explicacao)
- ProcessoStatusCard exibe ultimaSincronizacao com fallback "Aguardando primeira sincronização"
- :core-ui compila sem erros
- Componentes não importam :core-network (sem dependência circular — usam parâmetros primitivos/String)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-android-app-fluxo-advogado/04-02-SUMMARY.md`
</output>
