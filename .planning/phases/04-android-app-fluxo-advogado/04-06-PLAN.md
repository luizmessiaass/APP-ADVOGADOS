---
phase: 04-android-app-fluxo-advogado
plan: 06
type: execute
wave: 2
depends_on:
  - "04-05"
files_modified:
  - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/MensagemBottomSheet.kt
  - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/MensagemViewModel.kt
  - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/ClienteDetalheScreen.kt
  - core-data/src/main/java/com/aethixdigital/portaljuridico/data/repository/ClienteRepository.kt
  - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/StripePortalLauncher.kt
  - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/ClienteDetalheViewModel.kt
  - app-escritorio/src/test/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/MensagemViewModelTest.kt
autonomous: true
requirements:
  - ESCR-07
  - ESCR-09
  - ESCR-10

must_haves:
  truths:
    - "Botão 'Enviar mensagem' na ClienteDetalheScreen abre um ModalBottomSheet"
    - "Bottom Sheet contém campo de texto e botão Enviar"
    - "Ao enviar, app chama POST /api/v1/clientes/{id}/mensagens e exibe feedback visual de sucesso/erro"
    - "Bottom Sheet fecha automaticamente após sucesso"
    - "Botão 'Gerenciar assinatura' em ClienteDetalheScreen busca portal session URL do backend e abre Chrome Custom Tabs"
  artifacts:
    - path: "app-escritorio/.../feature/clientes/detalhe/MensagemBottomSheet.kt"
      provides: "ModalBottomSheet com campo de texto e botão Enviar"
      exports: ["MensagemBottomSheet"]
    - path: "app-escritorio/.../feature/clientes/detalhe/MensagemViewModel.kt"
      provides: "MVVM mensagem: POST ao backend, feedback de sucesso/erro"
      exports: ["MensagemViewModel", "MensagemUiState"]
    - path: "app-escritorio/.../feature/clientes/detalhe/StripePortalLauncher.kt"
      provides: "Função que busca URL do portal Stripe e abre Chrome Custom Tabs"
      exports: ["openStripePortal"]
  key_links:
    - from: "ClienteDetalheScreen"
      to: "MensagemBottomSheet"
      via: "showBottomSheet = true → ModalBottomSheet visível"
      pattern: "MensagemBottomSheet"
    - from: "MensagemViewModel"
      to: "ClienteRepository.enviarMensagem"
      via: "viewModelScope.launch { clienteRepository.enviarMensagem(clienteId, texto) }"
      pattern: "enviarMensagem"
    - from: "ClienteDetalheScreen"
      to: "StripePortalLauncher.openStripePortal"
      via: "onClick → viewModel.getPortalUrl() → openStripePortal(url)"
      pattern: "openStripePortal"
---

<objective>
Implementar o envio de mensagem manual via ModalBottomSheet (D-13) e o acesso ao Stripe Customer Portal via Chrome Custom Tabs (ESCR-09) — completando o fluxo funcional do app-escritorio.

Purpose: ESCR-07 e ESCR-09 são os dois últimos features funcionais da Phase 4. O Bottom Sheet de mensagem é o único canal de comunicação direta do advogado com o cliente via app. O Stripe Custom Tab é o gateway de gestão de assinatura.
Output: Bottom Sheet funcional com feedback, Stripe portal abrindo em Chrome Custom Tabs.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md
@.planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md
@.planning/phases/04-android-app-fluxo-advogado/04-05-SUMMARY.md

<interfaces>
<!-- Contratos de planos anteriores. Usar diretamente. -->

De core-network (04-01):
```kotlin
// ClienteApi já definido:
suspend fun enviarMensagem(clienteId: String, request: EnviarMensagemRequest): MensagemResponse
suspend fun getPortalSession(): PortalSessionResponse
// EnviarMensagemRequest: texto: String
// MensagemResponse: id, enviadoEm
// PortalSessionResponse: url: String
```

Adicionar a ClienteRepository:
```kotlin
suspend fun enviarMensagem(clienteId: String, texto: String): Result<String>  // retorna id
suspend fun getPortalSessionUrl(): Result<String>  // retorna url do Stripe portal
```

De 04-RESEARCH.md §Pattern 5 (Chrome Custom Tabs):
```kotlin
fun openStripePortal(context: Context, url: String) {
    val customTabsIntent = CustomTabsIntent.Builder().setShowTitle(true).build()
    customTabsIntent.launchUrl(context, Uri.parse(url))
}
```

De ClienteDetalheScreen (04-05):
- Botão "Enviar mensagem" já chama `onEnviarMensagemClick(clienteId)` — substituir por abertura de BottomSheet local
- Adicionar botão "Gerenciar assinatura" ao mesmo Screen (ou no TopAppBar como ação secundária)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MensagemBottomSheet + MensagemViewModel + Stripe Custom Tabs</name>
  <files>
    core-data/src/main/java/com/aethixdigital/portaljuridico/data/repository/ClienteRepository.kt,
    app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/MensagemViewModel.kt,
    app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/MensagemBottomSheet.kt,
    app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/StripePortalLauncher.kt,
    app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/ClienteDetalheScreen.kt,
    app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/ClienteDetalheViewModel.kt,
    app-escritorio/src/test/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/MensagemViewModelTest.kt
  </files>
  <read_first>
    - core-data/src/main/java/com/aethixdigital/portaljuridico/data/repository/ClienteRepository.kt (adicionar enviarMensagem e getPortalSessionUrl)
    - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/ClienteDetalheScreen.kt (integrar BottomSheet e Stripe button)
    - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/ClienteDetalheViewModel.kt (adicionar getPortalUrl)
    - .planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md §D-13 (fire-and-forget com feedback visual no Bottom Sheet)
    - .planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md §Pattern 5 (Chrome Custom Tabs)
  </read_first>
  <behavior>
    MensagemViewModel tests (RED first):
    - Estado inicial: MensagemUiState.Idle
    - enviar() com texto vazio: estado permanece Idle (não dispara)
    - enviar() com texto válido → Loading → Success("id-da-mensagem")
    - enviar() com texto válido e falha de rede → Error("mensagem de erro")
  </behavior>
  <action>
    **Atualizar ClienteRepository** — adicionar na interface e impl:
    ```kotlin
    // Interface:
    suspend fun enviarMensagem(clienteId: String, texto: String): Result<String>
    suspend fun getPortalSessionUrl(): Result<String>

    // Impl:
    override suspend fun enviarMensagem(clienteId: String, texto: String): Result<String> = runCatching {
        clienteApi.enviarMensagem(clienteId, EnviarMensagemRequest(texto)).id
    }
    override suspend fun getPortalSessionUrl(): Result<String> = runCatching {
        clienteApi.getPortalSession().url
    }
    ```

    **MensagemViewModel.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.clientes.detalhe
    import androidx.lifecycle.ViewModel; import androidx.lifecycle.viewModelScope
    import com.aethixdigital.portaljuridico.data.repository.ClienteRepository
    import dagger.hilt.android.lifecycle.HiltViewModel
    import kotlinx.coroutines.flow.*; import kotlinx.coroutines.launch
    import javax.inject.Inject

    sealed class MensagemUiState {
        object Idle : MensagemUiState()
        object Loading : MensagemUiState()
        data class Success(val mensagemId: String) : MensagemUiState()
        data class Error(val message: String) : MensagemUiState()
    }

    @HiltViewModel
    class MensagemViewModel @Inject constructor(
        private val clienteRepository: ClienteRepository
    ) : ViewModel() {
        private val _uiState = MutableStateFlow<MensagemUiState>(MensagemUiState.Idle)
        val uiState: StateFlow<MensagemUiState> = _uiState.asStateFlow()

        fun enviar(clienteId: String, texto: String) {
            if (texto.isBlank()) return  // guard: não envia mensagem vazia
            viewModelScope.launch {
                _uiState.value = MensagemUiState.Loading
                // D-13: fire-and-forget com feedback visual
                clienteRepository.enviarMensagem(clienteId, texto.trim()).fold(
                    onSuccess = { id -> _uiState.value = MensagemUiState.Success(id) },
                    onFailure = { _uiState.value = MensagemUiState.Error(it.message ?: "Erro ao enviar mensagem") }
                )
            }
        }

        fun resetState() { _uiState.value = MensagemUiState.Idle }
    }
    ```

    **MensagemBottomSheet.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.clientes.detalhe
    import androidx.compose.foundation.layout.*
    import androidx.compose.material3.*
    import androidx.compose.runtime.*
    import androidx.compose.ui.Modifier; import androidx.compose.ui.unit.dp
    import androidx.hilt.navigation.compose.hiltViewModel

    /**
     * ModalBottomSheet para envio de mensagem/aviso manual ao cliente (D-13, ESCR-07).
     * Fire-and-forget com feedback visual de sucesso/erro no próprio Sheet.
     * Fecha automaticamente após sucesso.
     */
    @OptIn(ExperimentalMaterial3Api::class)
    @Composable
    fun MensagemBottomSheet(
        clienteId: String,
        clienteNome: String,
        onDismiss: () -> Unit,
        viewModel: MensagemViewModel = hiltViewModel()
    ) {
        val uiState by viewModel.uiState.collectAsState()
        var texto by remember { mutableStateOf("") }

        // Fechar automaticamente após sucesso
        LaunchedEffect(uiState) {
            if (uiState is MensagemUiState.Success) {
                kotlinx.coroutines.delay(1500)  // mostrar feedback por 1.5s
                viewModel.resetState()
                onDismiss()
            }
        }

        ModalBottomSheet(onDismissRequest = onDismiss) {
            Column(
                modifier = Modifier.fillMaxWidth().padding(horizontal = 24.dp).padding(bottom = 32.dp),
                verticalArrangement = Arrangement.spacedBy(12.dp)
            ) {
                Text("Enviar mensagem para $clienteNome", style = MaterialTheme.typography.titleMedium)
                OutlinedTextField(
                    value = texto,
                    onValueChange = { texto = it },
                    modifier = Modifier.fillMaxWidth().heightIn(min = 100.dp),
                    label = { Text("Mensagem") },
                    placeholder = { Text("Digite o aviso para o cliente...") },
                    enabled = uiState !is MensagemUiState.Loading,
                    maxLines = 6
                )
                when (val state = uiState) {
                    is MensagemUiState.Success -> {
                        Text(
                            "Mensagem enviada com sucesso!",
                            color = MaterialTheme.colorScheme.primary,
                            style = MaterialTheme.typography.bodySmall
                        )
                    }
                    is MensagemUiState.Error -> {
                        Text(
                            state.message,
                            color = MaterialTheme.colorScheme.error,
                            style = MaterialTheme.typography.bodySmall
                        )
                    }
                    else -> {}
                }
                Button(
                    onClick = { viewModel.enviar(clienteId, texto) },
                    modifier = Modifier.fillMaxWidth(),
                    enabled = texto.isNotBlank() && uiState !is MensagemUiState.Loading
                ) {
                    if (uiState is MensagemUiState.Loading) {
                        CircularProgressIndicator(modifier = Modifier.size(20.dp), strokeWidth = 2.dp)
                    } else {
                        Text("Enviar")
                    }
                }
            }
        }
    }
    ```

    **StripePortalLauncher.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.clientes.detalhe
    import android.content.Context; import android.net.Uri
    import androidx.browser.customtabs.CustomTabsIntent

    /**
     * Abre o Stripe Customer Portal em Chrome Custom Tabs.
     * URL é obtida do backend via POST /api/v1/escritorios/portal-session.
     * Session URL tem validade de ~5 minutos (Stripe default) — abrir imediatamente após fetch.
     * ESCR-09: via Chrome Custom Tabs (não browser externo) para manter contexto in-app.
     */
    fun openStripePortal(context: Context, url: String) {
        val customTabsIntent = CustomTabsIntent.Builder()
            .setShowTitle(true)
            .build()
        customTabsIntent.launchUrl(context, Uri.parse(url))
    }
    ```

    **Atualizar ClienteDetalheViewModel.kt** — adicionar getPortalUrl:
    ```kotlin
    // Adicionar ao ClienteDetalheViewModel:
    private val _portalUrl = MutableStateFlow<String?>(null)
    val portalUrl: StateFlow<String?> = _portalUrl.asStateFlow()
    private val _portalError = MutableStateFlow<String?>(null)
    val portalError: StateFlow<String?> = _portalError.asStateFlow()

    fun loadPortalUrl() {
        viewModelScope.launch {
            clienteRepository.getPortalSessionUrl().fold(
                onSuccess = { url -> _portalUrl.value = url },
                onFailure = { _portalError.value = it.message ?: "Erro ao obter link do portal" }
            )
        }
    }
    ```

    **Atualizar ClienteDetalheScreen.kt** — integrar Bottom Sheet e Stripe:
    ```kotlin
    // Adicionar estado e lógica no body do Composable:
    val portalUrl by viewModel.portalUrl.collectAsState()
    val portalError by viewModel.portalError.collectAsState()
    var showMensagemSheet by remember { mutableStateOf(false) }
    val context = androidx.compose.ui.platform.LocalContext.current

    // Abrir CCT quando URL estiver disponível
    LaunchedEffect(portalUrl) {
        portalUrl?.let { url ->
            openStripePortal(context, url)
        }
    }

    // No bloco ClienteDetalheUiState.Success, substituir onEnviarMensagemClick stub:
    // Botão Enviar mensagem → showMensagemSheet = true
    // Adicionar botão Stripe:
    OutlinedButton(
        onClick = { viewModel.loadPortalUrl() },
        modifier = Modifier.fillMaxWidth()
    ) { Text("Gerenciar assinatura") }

    // portalError snackbar (opcional) ou Text:
    portalError?.let {
        Text(it, color = MaterialTheme.colorScheme.error, style = MaterialTheme.typography.bodySmall)
    }

    // ModalBottomSheet condicional:
    if (showMensagemSheet && state is ClienteDetalheUiState.Success) {
        MensagemBottomSheet(
            clienteId = viewModel.getClienteId(),
            clienteNome = (state as ClienteDetalheUiState.Success).cliente.nome,
            onDismiss = { showMensagemSheet = false }
        )
    }
    ```

    **MensagemViewModelTest.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.escritorio.feature.clientes.detalhe
    import com.aethixdigital.portaljuridico.data.repository.ClienteRepository
    import com.aethixdigital.portaljuridico.network.model.dto.*
    import kotlinx.coroutines.Dispatchers; import kotlinx.coroutines.ExperimentalCoroutinesApi
    import kotlinx.coroutines.test.*; import org.junit.*; import org.junit.Assert.*

    @OptIn(ExperimentalCoroutinesApi::class)
    class MensagemViewModelTest {
        private val testDispatcher = UnconfinedTestDispatcher()
        private lateinit var fakeRepo: FakeMensagemClienteRepository
        private lateinit var viewModel: MensagemViewModel

        @Before fun setup() {
            Dispatchers.setMain(testDispatcher)
            fakeRepo = FakeMensagemClienteRepository()
            viewModel = MensagemViewModel(fakeRepo)
        }
        @After fun tearDown() { Dispatchers.resetMain() }

        @Test fun initialState_isIdle() {
            assertTrue(viewModel.uiState.value is MensagemUiState.Idle)
        }
        @Test fun enviar_emptyText_remainsIdle() = runTest {
            viewModel.enviar("cli-1", "")
            assertTrue(viewModel.uiState.value is MensagemUiState.Idle)
        }
        @Test fun enviar_blankText_remainsIdle() = runTest {
            viewModel.enviar("cli-1", "   ")
            assertTrue(viewModel.uiState.value is MensagemUiState.Idle)
        }
        @Test fun enviar_success_emitsSuccess() = runTest {
            fakeRepo.enviarResult = Result.success("msg-uuid-456")
            viewModel.enviar("cli-1", "Olá, temos novidades no seu processo.")
            val state = viewModel.uiState.value
            assertTrue(state is MensagemUiState.Success)
            assertEquals("msg-uuid-456", (state as MensagemUiState.Success).mensagemId)
        }
        @Test fun enviar_failure_emitsError() = runTest {
            fakeRepo.enviarResult = Result.failure(Exception("Timeout de rede"))
            viewModel.enviar("cli-1", "Mensagem de teste")
            assertTrue(viewModel.uiState.value is MensagemUiState.Error)
            assertEquals("Timeout de rede", (viewModel.uiState.value as MensagemUiState.Error).message)
        }
        @Test fun resetState_returnsToIdle() = runTest {
            fakeRepo.enviarResult = Result.success("msg-1")
            viewModel.enviar("cli-1", "Teste")
            viewModel.resetState()
            assertTrue(viewModel.uiState.value is MensagemUiState.Idle)
        }
    }

    class FakeMensagemClienteRepository : ClienteRepository {
        var enviarResult: Result<String> = Result.success("fake-msg-id")
        override suspend fun listarClientes(search: String?) = Result.success(emptyList<ClienteItem>())
        override suspend fun cadastrarCliente(nome: String, cpf: String, email: String, numeroCnj: String) = Result.success("id")
        override suspend fun getClienteById(id: String) = Result.success(ClienteItem("1", "T", "00000000000", null, null))
        override suspend fun previewCliente(clienteId: String) = Result.success(PreviewResponse(ClienteItem("1", "T", "00000000000", null, null), emptyList()))
        override suspend fun enviarMensagem(clienteId: String, texto: String) = enviarResult
        override suspend fun getPortalSessionUrl() = Result.success("https://billing.stripe.com/test")
    }
    ```
  </action>
  <verify>
    <automated>./gradlew :app-escritorio:test --tests "*.MensagemViewModelTest" 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - grep "class MensagemViewModel" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/MensagemViewModel.kt retorna match
    - grep "ModalBottomSheet" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/MensagemBottomSheet.kt retorna match
    - grep "fun openStripePortal" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/StripePortalLauncher.kt retorna match
    - grep "CustomTabsIntent" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/StripePortalLauncher.kt retorna match
    - grep "if (texto.isBlank()) return" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/MensagemViewModel.kt retorna match (guard empty text)
    - ./gradlew :app-escritorio:test --tests "*.MensagemViewModelTest" retorna "5 tests, 0 failures"
    - ./gradlew :app-escritorio:assembleDebug BUILD SUCCESSFUL
  </acceptance_criteria>
  <done>MensagemBottomSheet com fire-and-forget + auto-dismiss após sucesso (D-13, ESCR-07). StripePortalLauncher via CustomTabsIntent (ESCR-09). 5 testes unitários MensagemViewModel passando. ESCR-07 e ESCR-09 cobertos.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| StripePortalLauncher → Chrome Custom Tabs | URL do portal Stripe (HTTPS) passada ao CCT — não ao browser externo |
| MensagemBottomSheet → Backend | Texto livre do advogado enviado como payload JSON — sem sanitização no app |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Spoofing | Stripe portal URL — URL forjada | mitigate | URL obtida do backend autenticado (JWT + RLS). Backend cria a session Stripe e retorna URL. App não gera a URL. |
| T-04-06-02 | Information Disclosure | Stripe portal URL — short-lived | accept | Stripe session URLs expiram em ~5 min por default. App abre imediatamente após receber. |
| T-04-06-03 | Tampering | Texto de mensagem — injection no backend | accept | Backend Fastify valida o payload com TypeBox (Phase 1, D-20). App não precisa sanitizar. Responsabilidade do backend. |
| T-04-06-04 | Denial of Service | Spam de mensagens | accept | v1: single-user app, advogado autenticado. Rate limiting no backend (D-22, Phase 1) cobre se necessário. |
| T-04-06-05 | Information Disclosure | MensagemBottomSheet — texto em memória | accept | Texto é Kotlin String em scope curto, não persistido no device. GC'd após dismiss. |
</threat_model>

<verification>
Verificação manual pós-execução:
1. `./gradlew :app-escritorio:test --tests "*.MensagemViewModelTest"` — 5 tests, 0 failures
2. `./gradlew :app-escritorio:assembleDebug` — BUILD SUCCESSFUL
3. Instalar no emulador: tela de detalhe do cliente
4. Tocar em "Enviar mensagem": Bottom Sheet abre com campo de texto e botão "Enviar"
5. Botão "Enviar" desabilitado quando campo vazio
6. Com texto: botão habilitado, ao tocar mostra loading
7. "Gerenciar assinatura" chama backend (com backend mockado ou real) e abre CCT
</verification>

<success_criteria>
- 5 testes MensagemViewModelTest passando (Idle, empty guard, blank guard, Success, Error, resetState)
- MensagemBottomSheet usa ModalBottomSheet Material3
- Guard `if (texto.isBlank()) return` impede envio de mensagem vazia
- StripePortalLauncher usa CustomTabsIntent.Builder() (não startActivity com ACTION_VIEW)
- assembleDebug BUILD SUCCESSFUL
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-android-app-fluxo-advogado/04-06-SUMMARY.md`
</output>
