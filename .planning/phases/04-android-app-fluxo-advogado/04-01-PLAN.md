---
phase: 04-android-app-fluxo-advogado
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - gradle/libs.versions.toml
  - core-network/build.gradle.kts
  - core-data/build.gradle.kts
  - core-network/src/main/java/com/aethixdigital/portaljuridico/network/di/NetworkModule.kt
  - core-network/src/main/java/com/aethixdigital/portaljuridico/network/interceptor/AuthInterceptor.kt
  - core-network/src/main/java/com/aethixdigital/portaljuridico/network/api/AuthApi.kt
  - core-network/src/main/java/com/aethixdigital/portaljuridico/network/api/ClienteApi.kt
  - core-network/src/main/java/com/aethixdigital/portaljuridico/network/model/dto/AuthDtos.kt
  - core-network/src/main/java/com/aethixdigital/portaljuridico/network/model/dto/ClienteDtos.kt
  - core-data/src/main/java/com/aethixdigital/portaljuridico/data/auth/TokenDataStore.kt
  - core-data/src/main/java/com/aethixdigital/portaljuridico/data/di/DataModule.kt
  - core-data/src/main/java/com/aethixdigital/portaljuridico/data/auth/JwtDecoder.kt
autonomous: true
requirements:
  - ESCR-10

must_haves:
  truths:
    - "O módulo :core-network compila com Kotlin plugin, Retrofit 3, OkHttp 4.12, Moshi 1.15.2"
    - "O AuthInterceptor injeta 'Authorization: Bearer {token}' em toda requisição autenticada"
    - "O TokenDataStore lê e grava JWT como String no DataStore Preferences"
    - "O JwtDecoder extrai app_metadata.role do payload JWT sem round-trip ao backend"
    - "O NetworkModule provê OkHttpClient, Moshi, Retrofit e ClienteApi via Hilt"
    - "O DataModule provê TokenDataStore e AuthRepository via Hilt"
  artifacts:
    - path: "gradle/libs.versions.toml"
      provides: "Entradas de versão para retrofit, okhttp, moshi, navigationCompose, datastore, jwtdecode, browser, turbine, kotlinxSerializationJson"
      contains: "retrofit = \"3.0.0\""
    - path: "core-network/build.gradle.kts"
      provides: "Kotlin plugin + dependências de rede do módulo core-network"
      contains: "alias(libs.plugins.kotlin.android)"
    - path: "core-network/src/main/java/com/aethixdigital/portaljuridico/network/di/NetworkModule.kt"
      provides: "Hilt module: OkHttpClient, Moshi, Retrofit, AuthApi, ClienteApi"
      exports: ["NetworkModule"]
    - path: "core-network/src/main/java/com/aethixdigital/portaljuridico/network/interceptor/AuthInterceptor.kt"
      provides: "Injeção do JWT no header Authorization"
      exports: ["AuthInterceptor"]
    - path: "core-data/src/main/java/com/aethixdigital/portaljuridico/data/auth/TokenDataStore.kt"
      provides: "Leitura/escrita do JWT no DataStore"
      exports: ["TokenDataStore"]
    - path: "core-data/src/main/java/com/aethixdigital/portaljuridico/data/auth/JwtDecoder.kt"
      provides: "Decode local do JWT: role, expiração"
      exports: ["JwtDecoder"]
  key_links:
    - from: "NetworkModule"
      to: "AuthInterceptor"
      via: "OkHttpClient.Builder().addInterceptor(authInterceptor)"
      pattern: "addInterceptor"
    - from: "AuthInterceptor"
      to: "TokenDataStore"
      via: "runBlocking { tokenDataStore.getToken().firstOrNull() }"
      pattern: "runBlocking"
    - from: "NetworkModule"
      to: "ClienteApi"
      via: "retrofit.create(ClienteApi::class.java)"
      pattern: "retrofit.create"
---

<objective>
Estabelecer a fundação de rede e dados para o app-escritorio: adicionar ao version catalog todas as dependências da Phase 4, corrigir o bloqueador do módulo :core-network (falta do Kotlin plugin), implementar a camada de rede Clean Architecture completa (Retrofit 3 + OkHttp 4.12 + Moshi 1.15.2 + Hilt) e a camada de dados (DataStore Preferences + JWT decode local).

Purpose: Sem esta fundação, nenhuma tela pode fazer chamadas autenticadas ao backend Fastify. Este plano cria os contratos de API e o wiring DI que todos os planos de Wave 2 dependem.
Output: :core-network e :core-data com Hilt modules funcionais, ClienteApi e AuthApi definidos, TokenDataStore e JwtDecoder prontos para uso nos ViewModels.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md
@.planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md
@.planning/phases/00-android-bootstrap-cleanup/00-CONTEXT.md

<interfaces>
<!-- Contratos existentes que o executor precisa conhecer. Não explorar codebase adicional. -->

De gradle/libs.versions.toml (catálogo atual — adicionar sem remover entradas existentes):
```toml
[versions]
agp = "9.1.1"
kotlin = "2.2.10"
composeBom = "2026.03.00"
hilt = "2.59.2"
hiltNavigationCompose = "1.2.0"
ksp = "2.2.10-2.0.2"
```

De core-network/build.gradle.kts (estado atual — SEM Kotlin plugin):
```kotlin
plugins {
    alias(libs.plugins.android.library)  // único plugin — bloqueador
}
android { namespace = "com.aethixdigital.portaljuridico.network"; compileSdk = 36; ... }
```

De core-data/build.gradle.kts (estado atual — SEM Kotlin plugin):
```kotlin
plugins {
    alias(libs.plugins.android.library)  // único plugin — bloqueador
}
```

Package base: com.aethixdigital.portaljuridico
JWT claim path (Phase 1, AUTH-03): jwt.getClaim("app_metadata").asObject(Map::class.java)?.get("role")
Backend API prefix (Phase 1, D-19): /api/v1/
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Version catalog + core-network/core-data Kotlin plugin fix</name>
  <files>
    gradle/libs.versions.toml,
    core-network/build.gradle.kts,
    core-data/build.gradle.kts,
    app-escritorio/build.gradle.kts
  </files>
  <read_first>
    - gradle/libs.versions.toml (fonte de verdade de versões — adicionar sem remover entradas)
    - core-network/build.gradle.kts (adicionar kotlin.android + ksp + hilt plugins + deps)
    - core-data/build.gradle.kts (adicionar kotlin.android + hilt + datastore deps)
    - app-escritorio/build.gradle.kts (adicionar navigation-compose, browser, hilt-navigation-compose, kotlinx-serialization-json, buildConfigField)
    - .planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md §Standard Stack (versões exatas verificadas)
  </read_first>
  <behavior>
    - libs.versions.toml recebe as entradas: retrofit="3.0.0", okhttp="4.12.0", moshi="1.15.2", navigationCompose="2.9.7", datastore="1.2.1", jwtdecode="2.0.2", browser="1.10.0", turbine="1.2.0", kotlinxSerializationJson (usar versão compatível com kotlin 2.2.10)
    - Entradas [libraries] correspondentes adicionadas com nomes exatos do RESEARCH.md
    - Entrada [plugins] kotlin-serialization adicionada: { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
    - core-network/build.gradle.kts: adicionar alias(libs.plugins.kotlin.android), alias(libs.plugins.ksp), alias(libs.plugins.hilt.android) — CRITICAL: sem kotlin.android o módulo não compila .kt
    - core-data/build.gradle.kts: adicionar alias(libs.plugins.kotlin.android), alias(libs.plugins.ksp), alias(libs.plugins.hilt.android)
    - app-escritorio/build.gradle.kts: adicionar alias(libs.plugins.kotlin.serialization), implementation(libs.navigation.compose), implementation(libs.hilt.navigation.compose), implementation(libs.browser), implementation(libs.kotlinx.serialization.json). Adicionar buildConfigField("String", "API_BASE_URL", '"http://10.0.2.2:3000"') em buildTypes.debug e '"https://api.portaljuridico.com.br"' em release.
    - kotlinxSerializationJson versão: usar "1.8.1" (compatível com Kotlin 2.2.10)
  </behavior>
  <action>
    1. Abrir gradle/libs.versions.toml e ACRESCENTAR ao bloco [versions]:
       ```
       retrofit = "3.0.0"
       okhttp = "4.12.0"
       moshi = "1.15.2"
       navigationCompose = "2.9.7"
       datastore = "1.2.1"
       jwtdecode = "2.0.2"
       browser = "1.10.0"
       turbine = "1.2.0"
       kotlinxSerializationJson = "1.8.1"
       ```

    2. ACRESCENTAR ao bloco [libraries]:
       ```
       retrofit = { group = "com.squareup.retrofit2", name = "retrofit", version.ref = "retrofit" }
       retrofit-converter-moshi = { group = "com.squareup.retrofit2", name = "converter-moshi", version.ref = "retrofit" }
       okhttp = { group = "com.squareup.okhttp3", name = "okhttp", version.ref = "okhttp" }
       okhttp-logging-interceptor = { group = "com.squareup.okhttp3", name = "logging-interceptor", version.ref = "okhttp" }
       moshi = { group = "com.squareup.moshi", name = "moshi", version.ref = "moshi" }
       moshi-kotlin-codegen = { group = "com.squareup.moshi", name = "moshi-kotlin-codegen", version.ref = "moshi" }
       navigation-compose = { group = "androidx.navigation", name = "navigation-compose", version.ref = "navigationCompose" }
       datastore-preferences = { group = "androidx.datastore", name = "datastore-preferences", version.ref = "datastore" }
       jwt-decode = { group = "com.auth0.android", name = "jwtdecode", version.ref = "jwtdecode" }
       browser = { group = "androidx.browser", name = "browser", version.ref = "browser" }
       turbine = { group = "app.cash.turbine", name = "turbine", version.ref = "turbine" }
       kotlinx-serialization-json = { group = "org.jetbrains.kotlinx", name = "kotlinx-serialization-json", version.ref = "kotlinxSerializationJson" }
       hilt-android-testing = { group = "com.google.dagger", name = "hilt-android-testing", version.ref = "hilt" }
       ```

    3. ACRESCENTAR ao bloco [plugins]:
       ```
       kotlin-serialization = { id = "org.jetbrains.kotlin.plugin.serialization", version.ref = "kotlin" }
       ```

    4. Reescrever core-network/build.gradle.kts com plugins adicionados:
       ```kotlin
       plugins {
           alias(libs.plugins.android.library)
           alias(libs.plugins.kotlin.android)   // BLOQUEADOR: sem este, .kt não compila
           alias(libs.plugins.ksp)
           alias(libs.plugins.hilt.android)
       }
       android {
           namespace = "com.aethixdigital.portaljuridico.network"
           compileSdk = 36
           defaultConfig { minSdk = 27 }
           compileOptions {
               sourceCompatibility = JavaVersion.VERSION_11
               targetCompatibility = JavaVersion.VERSION_11
           }
       }
       dependencies {
           implementation(libs.hilt.android)
           ksp(libs.hilt.compiler)
           implementation(libs.retrofit)
           implementation(libs.retrofit.converter.moshi)
           implementation(libs.okhttp)
           implementation(libs.okhttp.logging.interceptor)
           implementation(libs.moshi)
           ksp(libs.moshi.kotlin.codegen)
           implementation(libs.datastore.preferences)
           implementation(libs.jwt.decode)
           testImplementation(libs.junit)
       }
       ```

    5. Reescrever core-data/build.gradle.kts:
       ```kotlin
       plugins {
           alias(libs.plugins.android.library)
           alias(libs.plugins.kotlin.android)
           alias(libs.plugins.ksp)
           alias(libs.plugins.hilt.android)
       }
       android {
           namespace = "com.aethixdigital.portaljuridico.data"
           compileSdk = 36
           defaultConfig { minSdk = 27 }
           compileOptions {
               sourceCompatibility = JavaVersion.VERSION_11
               targetCompatibility = JavaVersion.VERSION_11
           }
       }
       dependencies {
           implementation(project(":core-network"))
           implementation(libs.hilt.android)
           ksp(libs.hilt.compiler)
           implementation(libs.datastore.preferences)
           implementation(libs.jwt.decode)
           testImplementation(libs.junit)
       }
       ```

    6. No app-escritorio/build.gradle.kts, ADICIONAR ao bloco plugins: `alias(libs.plugins.kotlin.serialization)`.
       No bloco dependencies ADICIONAR:
       ```kotlin
       implementation(project(":core-network"))
       implementation(project(":core-data"))
       implementation(libs.navigation.compose)
       implementation(libs.hilt.navigation.compose)
       implementation(libs.browser)
       implementation(libs.kotlinx.serialization.json)
       ```
       No bloco defaultConfig ADICIONAR: `buildConfigField("String", "API_BASE_URL", "\"http://10.0.2.2:3000\"")`
       No bloco buildTypes.release ADICIONAR: `buildConfigField("String", "API_BASE_URL", "\"https://api.portaljuridico.com.br\"")`
       No bloco android ADICIONAR: `buildFeatures { compose = true; buildConfig = true }`
  </action>
  <verify>
    <automated>./gradlew :core-network:assemble :core-data:assemble :app-escritorio:assemble --dry-run 2>&1 | grep -E "FAILED|error" | head -5; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep "retrofit = \"3.0.0\"" gradle/libs.versions.toml retorna match
    - grep "navigationCompose = \"2.9.7\"" gradle/libs.versions.toml retorna match
    - grep "kotlinxSerializationJson = \"1.8.1\"" gradle/libs.versions.toml retorna match
    - grep "kotlin-serialization" gradle/libs.versions.toml retorna match
    - grep "alias(libs.plugins.kotlin.android)" core-network/build.gradle.kts retorna match
    - grep "alias(libs.plugins.kotlin.android)" core-data/build.gradle.kts retorna match
    - grep "alias(libs.plugins.kotlin.serialization)" app-escritorio/build.gradle.kts retorna match
    - grep "navigation.compose" app-escritorio/build.gradle.kts retorna match
    - grep "API_BASE_URL" app-escritorio/build.gradle.kts retorna match
    - ./gradlew :core-network:assemble não retorna "error:" no output
  </acceptance_criteria>
  <done>Version catalog atualizado com todas as 12 novas dependências; core-network e core-data têm Kotlin plugin; app-escritorio tem navigation + serialization + buildConfig.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Core-network layer — AuthInterceptor, APIs Retrofit, NetworkModule + DataStore e JwtDecoder</name>
  <files>
    core-network/src/main/java/com/aethixdigital/portaljuridico/network/interceptor/AuthInterceptor.kt,
    core-network/src/main/java/com/aethixdigital/portaljuridico/network/api/AuthApi.kt,
    core-network/src/main/java/com/aethixdigital/portaljuridico/network/api/ClienteApi.kt,
    core-network/src/main/java/com/aethixdigital/portaljuridico/network/model/dto/AuthDtos.kt,
    core-network/src/main/java/com/aethixdigital/portaljuridico/network/model/dto/ClienteDtos.kt,
    core-network/src/main/java/com/aethixdigital/portaljuridico/network/di/NetworkModule.kt,
    core-data/src/main/java/com/aethixdigital/portaljuridico/data/auth/TokenDataStore.kt,
    core-data/src/main/java/com/aethixdigital/portaljuridico/data/auth/JwtDecoder.kt,
    core-data/src/main/java/com/aethixdigital/portaljuridico/data/di/DataModule.kt
  </files>
  <read_first>
    - core-network/build.gradle.kts (confirmar que Task 1 adicionou kotlin.android — só criar .kt após isso)
    - .planning/phases/04-android-app-fluxo-advogado/04-RESEARCH.md §Pattern 2 (AuthInterceptor), §Supabase JWT Structure, §Code Examples
    - .planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md §Autenticação e JWT (D-03, D-05, D-06)
  </read_first>
  <behavior>
    - TokenDataStore: classe com @Singleton Hilt inject, expõe getToken(): Flow<String?> e saveToken(token: String), clearToken(). Usa Context via @ApplicationContext. DataStore key: stringPreferencesKey("jwt_token").
    - JwtDecoder: classe pura (sem Hilt no construtor) com fun extractRole(token: String): String? lendo jwt.getClaim("app_metadata").asObject(Map::class.java)?.get("role") as? String. Fun isExpired(token: String): Boolean usa JWT(token).isExpired(0). Captura DecodeException e retorna null/true.
    - AuthInterceptor: @Singleton @Inject constructor(tokenDataStore: TokenDataStore). Implementa Interceptor. Usa runBlocking { tokenDataStore.getToken().firstOrNull() } — seguro em OkHttp thread (background). Se token != null, adiciona header Authorization: Bearer {token}.
    - AuthApi: interface Retrofit. suspend fun login(@Body req: LoginRequest): LoginResponse. LoginRequest: @JsonClass data class email: String, password: String. LoginResponse: @JsonClass data class token: String, role: String.
    - ClienteApi: interface Retrofit com os 5 endpoints conforme RESEARCH.md §Code Examples: cadastrarCliente, listarClientes, previewCliente, enviarMensagem, getPortalSession.
    - NetworkModule: @Module @InstallIn(SingletonComponent). provideOkHttpClient recebe AuthInterceptor + adiciona HttpLoggingInterceptor em DEBUG. provideMoshi(): Moshi = Moshi.Builder().build(). provideRetrofit usa BuildConfig.API_BASE_URL como baseUrl. provideAuthApi e provideClienteApi via retrofit.create.
    - DataModule: @Module @InstallIn(SingletonComponent). Provê TokenDataStore via @Singleton. Expõe JwtDecoder como @Singleton.
    - Todos os DTOs usam @JsonClass(generateAdapter = true) — Moshi KSP gera adapters.
  </behavior>
  <action>
    Criar os arquivos no pacote correto com o conteúdo exato abaixo. Manter ordem: DTOs → API interfaces → interceptor → modules.

    **AuthDtos.kt** (core-network/.../model/dto/):
    ```kotlin
    package com.aethixdigital.portaljuridico.network.model.dto
    import com.squareup.moshi.Json; import com.squareup.moshi.JsonClass
    @JsonClass(generateAdapter = true)
    data class LoginRequest(val email: String, val password: String)
    @JsonClass(generateAdapter = true)
    data class LoginResponse(val token: String, val role: String)
    ```

    **ClienteDtos.kt** (core-network/.../model/dto/):
    ```kotlin
    package com.aethixdigital.portaljuridico.network.model.dto
    import com.squareup.moshi.Json; import com.squareup.moshi.JsonClass
    @JsonClass(generateAdapter = true)
    data class CadastroClienteRequest(val nome: String, val cpf: String, val email: String, @Json(name = "numero_cnj") val numeroCnj: String)
    @JsonClass(generateAdapter = true)
    data class ClienteItem(@Json(name = "id") val id: String, val nome: String, val cpf: String, @Json(name = "ultima_sincronizacao") val ultimaSincronizacao: String?, @Json(name = "status_processo") val statusProcesso: String?)
    @JsonClass(generateAdapter = true)
    data class MovimentacaoDto(val id: String, val status: String, @Json(name = "proxima_data") val proximaData: String?, val explicacao: String, val impacto: String, val disclaimer: String)
    @JsonClass(generateAdapter = true)
    data class PreviewResponse(val cliente: ClienteItem, val movimentacoes: List<MovimentacaoDto>)
    @JsonClass(generateAdapter = true)
    data class EnviarMensagemRequest(val texto: String)
    @JsonClass(generateAdapter = true)
    data class MensagemResponse(val id: String, @Json(name = "enviado_em") val enviadoEm: String)
    @JsonClass(generateAdapter = true)
    data class PortalSessionResponse(val url: String)
    @JsonClass(generateAdapter = true)
    data class CadastroClienteResponse(val id: String, val nome: String, val cpf: String)
    ```

    **AuthApi.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.network.api
    import com.aethixdigital.portaljuridico.network.model.dto.LoginRequest
    import com.aethixdigital.portaljuridico.network.model.dto.LoginResponse
    import retrofit2.http.Body; import retrofit2.http.POST
    interface AuthApi {
        @POST("api/v1/auth/login")
        suspend fun login(@Body request: LoginRequest): LoginResponse
    }
    ```

    **ClienteApi.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.network.api
    import com.aethixdigital.portaljuridico.network.model.dto.*
    import retrofit2.http.*
    interface ClienteApi {
        @POST("api/v1/clientes")
        suspend fun cadastrarCliente(@Body request: CadastroClienteRequest): CadastroClienteResponse
        @GET("api/v1/clientes")
        suspend fun listarClientes(@Query("search") search: String? = null): List<ClienteItem>
        @GET("api/v1/clientes/{id}/preview")
        suspend fun previewCliente(@Path("id") clienteId: String): PreviewResponse
        @POST("api/v1/clientes/{id}/mensagens")
        suspend fun enviarMensagem(@Path("id") clienteId: String, @Body request: EnviarMensagemRequest): MensagemResponse
        @POST("api/v1/escritorios/portal-session")
        suspend fun getPortalSession(): PortalSessionResponse
    }
    ```

    **AuthInterceptor.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.network.interceptor
    import com.aethixdigital.portaljuridico.data.auth.TokenDataStore
    import kotlinx.coroutines.flow.firstOrNull; import kotlinx.coroutines.runBlocking
    import okhttp3.Interceptor; import okhttp3.Response
    import javax.inject.Inject; import javax.inject.Singleton
    @Singleton
    class AuthInterceptor @Inject constructor(
        private val tokenDataStore: TokenDataStore
    ) : Interceptor {
        override fun intercept(chain: Interceptor.Chain): Response {
            val token = runBlocking { tokenDataStore.getToken().firstOrNull() }
            val request = if (token != null) {
                chain.request().newBuilder()
                    .addHeader("Authorization", "Bearer $token")
                    .build()
            } else chain.request()
            return chain.proceed(request)
        }
    }
    ```

    **NetworkModule.kt**:
    ```kotlin
    package com.aethixdigital.portaljuridico.network.di
    import com.aethixdigital.portaljuridico.network.api.AuthApi
    import com.aethixdigital.portaljuridico.network.api.ClienteApi
    import com.aethixdigital.portaljuridico.network.interceptor.AuthInterceptor
    import com.aethixdigital.portaljuridico.escritorio.BuildConfig
    import com.squareup.moshi.Moshi
    import dagger.Module; import dagger.Provides; import dagger.hilt.InstallIn
    import dagger.hilt.components.SingletonComponent
    import okhttp3.OkHttpClient; import okhttp3.logging.HttpLoggingInterceptor
    import retrofit2.Retrofit; import retrofit2.converter.moshi.MoshiConverterFactory
    import javax.inject.Singleton
    @Module @InstallIn(SingletonComponent::class)
    object NetworkModule {
        @Provides @Singleton
        fun provideOkHttpClient(authInterceptor: AuthInterceptor): OkHttpClient =
            OkHttpClient.Builder()
                .addInterceptor(authInterceptor)
                .also { builder ->
                    if (BuildConfig.DEBUG) builder.addInterceptor(
                        HttpLoggingInterceptor().apply { level = HttpLoggingInterceptor.Level.BODY }
                    )
                }.build()
        @Provides @Singleton
        fun provideMoshi(): Moshi = Moshi.Builder().build()
        @Provides @Singleton
        fun provideRetrofit(okHttpClient: OkHttpClient, moshi: Moshi): Retrofit =
            Retrofit.Builder()
                .baseUrl(BuildConfig.API_BASE_URL)
                .client(okHttpClient)
                .addConverterFactory(MoshiConverterFactory.create(moshi))
                .build()
        @Provides @Singleton
        fun provideAuthApi(retrofit: Retrofit): AuthApi = retrofit.create(AuthApi::class.java)
        @Provides @Singleton
        fun provideClienteApi(retrofit: Retrofit): ClienteApi = retrofit.create(ClienteApi::class.java)
    }
    ```

    **TokenDataStore.kt** (core-data):
    ```kotlin
    package com.aethixdigital.portaljuridico.data.auth
    import android.content.Context
    import androidx.datastore.core.DataStore
    import androidx.datastore.preferences.core.Preferences
    import androidx.datastore.preferences.core.edit
    import androidx.datastore.preferences.core.stringPreferencesKey
    import androidx.datastore.preferences.preferencesDataStore
    import dagger.hilt.android.qualifiers.ApplicationContext
    import kotlinx.coroutines.flow.Flow; import kotlinx.coroutines.flow.map
    import javax.inject.Inject; import javax.inject.Singleton
    private val Context.dataStore: DataStore<Preferences> by preferencesDataStore(name = "auth_prefs")
    @Singleton
    class TokenDataStore @Inject constructor(@ApplicationContext private val context: Context) {
        private val jwtKey = stringPreferencesKey("jwt_token")
        fun getToken(): Flow<String?> = context.dataStore.data.map { it[jwtKey] }
        suspend fun saveToken(token: String) { context.dataStore.edit { it[jwtKey] = token } }
        suspend fun clearToken() { context.dataStore.edit { it.remove(jwtKey) } }
    }
    ```

    **JwtDecoder.kt** (core-data):
    ```kotlin
    package com.aethixdigital.portaljuridico.data.auth
    import com.auth0.android.jwt.JWT
    import com.auth0.android.jwt.DecodeException
    import javax.inject.Inject; import javax.inject.Singleton
    @Singleton
    class JwtDecoder @Inject constructor() {
        fun extractRole(token: String): String? = try {
            val jwt = JWT(token)
            @Suppress("UNCHECKED_CAST")
            val appMetadata = jwt.getClaim("app_metadata").asObject(Map::class.java)
            appMetadata?.get("role") as? String
        } catch (e: DecodeException) { null }
        fun isExpired(token: String): Boolean = try {
            JWT(token).isExpired(0)
        } catch (e: DecodeException) { true }
    }
    ```

    **DataModule.kt** (core-data):
    ```kotlin
    package com.aethixdigital.portaljuridico.data.di
    import com.aethixdigital.portaljuridico.data.auth.JwtDecoder
    import com.aethixdigital.portaljuridico.data.auth.TokenDataStore
    import dagger.Module; import dagger.Provides; import dagger.hilt.InstallIn
    import dagger.hilt.components.SingletonComponent
    import javax.inject.Singleton
    @Module @InstallIn(SingletonComponent::class)
    object DataModule {
        @Provides @Singleton
        fun provideJwtDecoder(): JwtDecoder = JwtDecoder()
    }
    // TokenDataStore é injetado diretamente via @Inject constructor — não precisa de @Provides
    ```

    ATENÇÃO com NetworkModule: como ele importa BuildConfig da app-escritorio, mover essa importação para ser injetada como parâmetro String anotado com @Named("baseUrl") para evitar dependência circular entre :core-network e :app-escritorio. Alternativa mais simples: usar uma constante de BuildConfig injetada via @Named no Hilt module em :app-escritorio e criar um NetworkConfig data class em :core-network. Implementar assim:
    - Criar em core-network: `data class NetworkConfig(val baseUrl: String, val isDebug: Boolean)`
    - NetworkModule recebe `NetworkConfig` como parâmetro de `provideOkHttpClient` e `provideRetrofit`
    - Em app-escritorio, criar um AppNetworkModule que provê NetworkConfig com BuildConfig.API_BASE_URL e BuildConfig.DEBUG
  </action>
  <verify>
    <automated>./gradlew :core-network:compileDebugKotlin :core-data:compileDebugKotlin 2>&1 | grep -E "^e:|error:" | head -10; echo "BUILD_EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep "class AuthInterceptor" core-network/src/main/java/com/aethixdigital/portaljuridico/network/interceptor/AuthInterceptor.kt retorna match
    - grep "interface ClienteApi" core-network/src/main/java/com/aethixdigital/portaljuridico/network/api/ClienteApi.kt retorna match
    - grep "suspend fun listarClientes" core-network/src/main/java/com/aethixdigital/portaljuridico/network/api/ClienteApi.kt retorna match
    - grep "suspend fun previewCliente" core-network/src/main/java/com/aethixdigital/portaljuridico/network/api/ClienteApi.kt retorna match
    - grep "class TokenDataStore" core-data/src/main/java/com/aethixdigital/portaljuridico/data/auth/TokenDataStore.kt retorna match
    - grep "fun extractRole" core-data/src/main/java/com/aethixdigital/portaljuridico/data/auth/JwtDecoder.kt retorna match
    - grep "fun isExpired" core-data/src/main/java/com/aethixdigital/portaljuridico/data/auth/JwtDecoder.kt retorna match
    - grep "object NetworkModule" core-network/src/main/java/com/aethixdigital/portaljuridico/network/di/NetworkModule.kt retorna match
    - ./gradlew :core-network:compileDebugKotlin sem erros "error:"
    - ./gradlew :core-data:compileDebugKotlin sem erros "error:"
  </acceptance_criteria>
  <done>Camada de rede completa: AuthInterceptor, ClienteApi, AuthApi, NetworkModule, TokenDataStore, JwtDecoder, DataModule. Compila sem erros. Hilt modules registrados em SingletonComponent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| App → Backend Fastify | JWT enviado em cada request; backend valida e aplica RLS |
| DataStore → Memória | Token lido em claro no processo; sem criptografia extra além de DataStore system encryption API 23+ |
| OkHttp thread → DataStore | runBlocking lê DataStore no background thread — não bloqueia main thread |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | Spoofing | TokenDataStore / JWT | mitigate | DataStore usa encryption em API 23+ by default; token é limpo ao logout (clearToken()) e em 401 |
| T-04-01-02 | Tampering | JWT payload (role) | accept | App apenas lê role para routing — backend RLS é autoritativo. JWT assinado com segredo Supabase; app não verifica assinatura (correto para cliente) |
| T-04-01-03 | Information Disclosure | HttpLoggingInterceptor | mitigate | Interceptor de log habilitado APENAS em BuildConfig.DEBUG — nunca em release builds |
| T-04-01-04 | Denial of Service | runBlocking em AuthInterceptor | accept | App single-user; volume de requests concorrentes desprezível. OkHttp timeout cobre caso extremo |
| T-04-01-05 | Elevation of Privilege | NetworkModule.baseUrl | mitigate | baseUrl injetada via BuildConfig field controlado pelo build system — não modificável em runtime |
</threat_model>

<verification>
Verificação manual pós-execução:
1. `./gradlew :core-network:assemble :core-data:assemble` — BUILD SUCCESSFUL
2. `./gradlew :app-escritorio:assembleDebug` — BUILD SUCCESSFUL (com novas deps)
3. grep na ClienteApi confirma os 5 endpoints definidos
4. grep no TokenDataStore confirma preferencesDataStore com name "auth_prefs"
</verification>

<success_criteria>
- gradle/libs.versions.toml contém as 12 novas entradas de dependências
- :core-network compila Kotlin com o plugin correto
- :core-data compila Kotlin com DataStore e jwtdecode
- :app-escritorio assembleDebug passa com navigation-compose e browser no classpath
- AuthInterceptor injeta Bearer token via runBlocking/DataStore
- ClienteApi define os 5 endpoints: cadastrarCliente, listarClientes, previewCliente, enviarMensagem, getPortalSession
- JwtDecoder lê app_metadata.role do JWT (path correto para Phase 1 hook)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-android-app-fluxo-advogado/04-01-SUMMARY.md`
</output>
