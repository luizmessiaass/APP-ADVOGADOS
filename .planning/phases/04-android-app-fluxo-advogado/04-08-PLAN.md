---
phase: 04-android-app-fluxo-advogado
plan: 08
type: execute
wave: 8
depends_on:
  - "04-07"
files_modified:
  - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt
  - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt
  - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/preview/PreviewScreen.kt
  - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/ClienteDetalheScreen.kt
autonomous: true
gap_closure: true
requirements:
  - ESCR-06
  - ESCR-08
  - ESCR-01
  - ESCR-02
  - ESCR-03
  - ESCR-04
  - ESCR-05
  - ESCR-07
  - ESCR-09
  - ESCR-10
  - ESCR-11

must_haves:
  truths:
    - "MovimentacaoCard exibe o disclaimer 'Explicação gerada por IA' no topo do card (acima do texto) quando parâmetro disclaimer não é nulo"
    - "Cada MovimentacaoCard na PreviewScreen exibe disclaimer no topo — visível sem rolar"
    - "ProcessoStatusCard exibe ultima_sincronizacao via parâmetro dedicado ultimaSincronizacao (não via campo impacto)"
    - "ClienteDetalheScreen passa ultimaSincronizacao = cliente.ultimaSincronizacao ao ProcessoStatusCard (não via campo impacto)"
    - "Callers existentes não precisam de mudanças de assinatura além dos arquivos listados"
  artifacts:
    - path: "core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt"
      provides: "Parâmetro disclaimer: String? = null adicionado. Surface badge renderizado antes de todo o conteúdo quando não-nulo."
      exports: ["MovimentacaoCard"]
    - path: "core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt"
      provides: "Parâmetro ultimaSincronizacao: String? = null adicionado. Linha dedicada renderizada abaixo do status quando não-nulo."
      exports: ["ProcessoStatusCard"]
    - path: "app-escritorio/.../feature/preview/PreviewScreen.kt"
      provides: "MovimentacaoCard passa disclaimer do MovimentacaoDto. ProcessoStatusCard usa ultimaSincronizacao."
    - path: "app-escritorio/.../feature/clientes/detalhe/ClienteDetalheScreen.kt"
      provides: "ProcessoStatusCard usa ultimaSincronizacao = cliente.ultimaSincronizacao (não impacto)."
  key_links:
    - from: "PreviewScreen"
      to: "MovimentacaoCard (core-ui)"
      via: "MovimentacaoCard(movimentacao=..., onExpandToggle=..., isExpanded=..., disclaimer=mov.disclaimer)"
      pattern: "disclaimer"
    - from: "ClienteDetalheScreen"
      to: "ProcessoStatusCard (core-ui)"
      via: "ProcessoStatusCard(ultimaSincronizacao = cliente.ultimaSincronizacao)"
      pattern: "ultimaSincronizacao"
---

<objective>
Fechar os 3 gaps identificados na VERIFICATION.md: restaurar o disclaimer "Explicação gerada por IA" no MovimentacaoCard (Gap 1 + Gap 2) e adicionar parâmetro dedicado ultimaSincronizacao ao ProcessoStatusCard (Gap 3). Intervenção cirúrgica — apenas os 4 arquivos listados.

Purpose: D-10 do CONTEXT.md exige disclaimer visível no topo de cada card de movimentação. O executor anterior removeu o disclaimer sem autorização do product owner ("D-06: removido por decisão de produto" não aparece nas decisões do CONTEXT.md). ESCR-06 e ESCR-08 precisam ser corrigidos para closure de fase.
Output: MovimentacaoCard com parâmetro disclaimer opcional (compatível com código existente) + ProcessoStatusCard com parâmetro ultimaSincronizacao dedicado + callers atualizados.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md
@.planning/phases/04-android-app-fluxo-advogado/04-VERIFICATION.md

<interfaces>
<!-- Estado atual dos arquivos que serão modificados. LER ANTES DE EDITAR. -->

MovimentacaoCard.kt assinatura ATUAL (reimplementada no Plan 07):
```kotlin
@Composable
fun MovimentacaoCard(
    movimentacao: Movimentacao,
    onExpandToggle: () -> Unit,
    isExpanded: Boolean,
    modifier: Modifier = Modifier
)
```
Onde Movimentacao (core-common/model/Movimentacao.kt):
```kotlin
data class Movimentacao(
    val id: String,
    val dataHora: String,
    val descricaoOriginal: String,
    val status: String?,
    val explicacao: String?,
    val proximaData: String?,
    val impacto: String?,
)
```
Nota: Movimentacao NAO tem campo disclaimer — o disclaimer virá como parâmetro separado.

ProcessoStatusCard.kt assinatura ATUAL:
```kotlin
@Composable
fun ProcessoStatusCard(
    status: String?,
    impacto: String?,
    isLoading: Boolean,
    modifier: Modifier = Modifier
)
```

PreviewScreen.kt — trecho de chamada ATUAL:
```kotlin
ProcessoStatusCard(
    status = state.movimentacoes.firstOrNull()?.status,
    impacto = state.ultimaSincronizacao?.let { "Última sync: $it" },  // INCORRETO
    isLoading = false
)
// ...
MovimentacaoCard(
    movimentacao = Movimentacao(
        id = mov.id,
        dataHora = mov.proximaData ?: "",
        descricaoOriginal = mov.explicacao,
        status = mov.status,
        explicacao = mov.explicacao,
        proximaData = mov.proximaData,
        impacto = mov.impacto
    ),
    onExpandToggle = {},
    isExpanded = false
    // disclaimer ausente
)
```

ClienteDetalheScreen.kt — trecho de chamada ATUAL:
```kotlin
ProcessoStatusCard(
    status = cliente.statusProcesso,
    impacto = cliente.ultimaSincronizacao?.let { "Última sync: $it" },  // INCORRETO
    isLoading = cliente.statusProcesso == null
)
```

PreviewUiState.Success (PreviewViewModel.kt):
```kotlin
data class Success(
    val clienteNome: String,
    val movimentacoes: List<MovimentacaoDto>,
    val ultimaSincronizacao: String?
) : PreviewUiState()
```

MovimentacaoDto (já existente em core-network):
```kotlin
data class MovimentacaoDto(
    val id: String, val status: String, val proximaData: String?,
    val explicacao: String, val impacto: String, val disclaimer: String
)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restaurar disclaimer no MovimentacaoCard e adicionar ultimaSincronizacao ao ProcessoStatusCard</name>
  <files>
    core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt,
    core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt
  </files>
  <read_first>
    - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt (arquivo completo — ler antes de editar)
    - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt (arquivo completo — ler antes de editar)
    - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ExpandableText.kt (entender o componente usado internamente)
    - .planning/phases/04-android-app-fluxo-advogado/04-CONTEXT.md §D-10 (disclaimer visível no topo)
  </read_first>
  <action>
    ESTRATEGIA: Adicionar parâmetros opcionais com valor default null para manter compatibilidade com callers existentes.

    **MovimentacaoCard.kt — adicionar parâmetro `disclaimer: String? = null`:**

    Adicionar o parâmetro após `isExpanded: Boolean` e antes de `modifier: Modifier = Modifier`.
    Dentro do Column, ANTES de qualquer outro conteúdo, adicionar renderização condicional do disclaimer:

    ```kotlin
    @Composable
    fun MovimentacaoCard(
        movimentacao: Movimentacao,
        onExpandToggle: () -> Unit,
        isExpanded: Boolean,
        disclaimer: String? = null,    // ADICIONADO — D-10: disclaimer visível no topo (ESCR-06)
        modifier: Modifier = Modifier
    ) {
        Column(modifier = modifier.fillMaxWidth()) {
            Card(
                modifier = Modifier.fillMaxWidth()
            ) {
                Column(modifier = Modifier.padding(16.dp)) {
                    // DISCLAIMER NO TOPO — visível sem rolar (D-10 + CONTEXT specifics + ESCR-06)
                    // Renderizado apenas quando parâmetro não é nulo
                    if (disclaimer != null) {
                        Surface(
                            color = MaterialTheme.colorScheme.secondaryContainer,
                            shape = MaterialTheme.shapes.small,
                            modifier = Modifier.fillMaxWidth()
                        ) {
                            Text(
                                text = disclaimer,
                                style = MaterialTheme.typography.labelSmall,
                                fontStyle = FontStyle.Italic,
                                color = MaterialTheme.colorScheme.onSecondaryContainer,
                                modifier = Modifier.padding(horizontal = 8.dp, vertical = 4.dp)
                            )
                        }
                        Spacer(modifier = Modifier.height(8.dp))
                    }
                    // Conteúdo existente (data, explicacao, fallback) continua igual
                    Row(modifier = Modifier.fillMaxWidth()) {
                        // ... (manter Row existente com data)
                    }
                    // ... (manter lógica existente de explicacaoText)
                }
            }
            HorizontalDivider()
        }
    }
    ```

    Imports a adicionar em MovimentacaoCard.kt:
    - `androidx.compose.material3.Surface`
    - `androidx.compose.ui.text.font.FontStyle`
    - `androidx.compose.foundation.layout.height` (se não presente)

    Também atualizar o KDoc do MovimentacaoCard para remover a afirmação falsa
    "D-06: Sem disclaimer (removido por decisão de produto)" e substituir por:
    "@param disclaimer Texto do disclaimer de IA exibido no topo do card; null = sem disclaimer"

    ---

    **ProcessoStatusCard.kt — adicionar parâmetro `ultimaSincronizacao: String? = null`:**

    Adicionar o parâmetro após `isLoading: Boolean` e antes de `modifier: Modifier = Modifier`.
    Dentro do Column, APÓS o bloco existente de status/impacto (ao final), adicionar linha dedicada:

    ```kotlin
    @Composable
    fun ProcessoStatusCard(
        status: String?,
        impacto: String?,
        isLoading: Boolean,
        ultimaSincronizacao: String? = null,    // ADICIONADO — ESCR-08: campo dedicado para sync DataJud
        modifier: Modifier = Modifier
    ) {
        ElevatedCard(...) {
            Column(modifier = Modifier.padding(16.dp)) {
                // bloco existente de isLoading / status / impacto — manter sem alteração
                if (isLoading || status == null) {
                    // ... manter existente
                } else {
                    // ... manter existente
                }
                // ADICIONADO: linha de sync DataJud separada do campo impacto (ESCR-08)
                if (ultimaSincronizacao != null) {
                    Spacer(modifier = Modifier.height(4.dp))
                    HorizontalDivider(color = MaterialTheme.colorScheme.onPrimaryContainer.copy(alpha = 0.2f))
                    Spacer(modifier = Modifier.height(4.dp))
                    Text(
                        text = "Última sync DataJud: $ultimaSincronizacao",
                        style = MaterialTheme.typography.labelSmall,
                        color = MaterialTheme.colorScheme.onPrimaryContainer.copy(alpha = 0.7f)
                    )
                }
            }
        }
    }
    ```

    Imports a adicionar em ProcessoStatusCard.kt (se não presentes):
    - `androidx.compose.material3.HorizontalDivider`

    Atualizar KDoc do ProcessoStatusCard para documentar o novo parâmetro:
    "@param ultimaSincronizacao Timestamp da última sincronização DataJud (ESCR-08); null = não exibir"

    IMPORTANTE: Manter todos os previews existentes compilando. Adicionar preview para o novo parâmetro:
    ```kotlin
    @Preview(showBackground = true)
    @Composable
    private fun ProcessoStatusCardWithSyncPreview() {
        PortalJuridicoTheme {
            ProcessoStatusCard(
                status = "Aguardando julgamento",
                impacto = "Você não precisa fazer nada agora.",
                isLoading = false,
                ultimaSincronizacao = "há 2 horas"
            )
        }
    }
    ```
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && ./gradlew :core-ui:compileDebugKotlin 2>&1 | grep -E "^e:|error:" | head -10; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep "disclaimer: String? = null" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt retorna match
    - grep "if (disclaimer != null)" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt retorna match
    - grep "Surface" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt retorna match (Surface badge do disclaimer)
    - Linha com "if (disclaimer != null)" aparece ANTES da linha com "Row(" no MovimentacaoCard (disclaimer no topo)
    - grep "ultimaSincronizacao: String? = null" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt retorna match
    - grep "Última sync DataJud" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt retorna match
    - grep "D-06: Sem disclaimer" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt NÃO retorna match (afirmação falsa removida)
    - ./gradlew :core-ui:compileDebugKotlin sem erros "error:"
  </acceptance_criteria>
  <done>MovimentacaoCard com parâmetro disclaimer opcional — Surface badge renderizado antes de todo o conteúdo quando não-nulo. ProcessoStatusCard com parâmetro ultimaSincronizacao opcional — linha dedicada exibida separada do impacto. Ambos compilando. Callers existentes não quebram (parâmetros têm default null).</done>
</task>

<task type="auto">
  <name>Task 2: Atualizar callers — PreviewScreen e ClienteDetalheScreen</name>
  <files>
    app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/preview/PreviewScreen.kt,
    app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/ClienteDetalheScreen.kt
  </files>
  <read_first>
    - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/preview/PreviewScreen.kt (arquivo completo — ler antes de editar)
    - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/ClienteDetalheScreen.kt (arquivo completo — ler antes de editar)
    - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/MovimentacaoCard.kt (verificar assinatura atualizada após Task 1)
    - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/components/ProcessoStatusCard.kt (verificar assinatura atualizada após Task 1)
  </read_first>
  <action>
    ESTRATEGIA: Atualização cirúrgica dos dois callers. Não reescrever o arquivo inteiro — apenas modificar as chamadas incorretas.

    **PreviewScreen.kt — 2 correções:**

    Correção 1: Chamada ao ProcessoStatusCard no item{} header (linha ~110-114):
    ```kotlin
    // ANTES (incorreto — usa impacto para sync timestamp):
    ProcessoStatusCard(
        status = state.movimentacoes.firstOrNull()?.status,
        impacto = state.ultimaSincronizacao?.let { "Última sync: $it" },
        isLoading = false
    )

    // DEPOIS (correto — campo dedicado ultimaSincronizacao):
    ProcessoStatusCard(
        status = state.movimentacoes.firstOrNull()?.status,
        impacto = null,
        isLoading = false,
        ultimaSincronizacao = state.ultimaSincronizacao
    )
    ```

    Correção 2: Chamada ao MovimentacaoCard dentro do items{} (linha ~119-133):
    ```kotlin
    // ANTES (sem disclaimer):
    MovimentacaoCard(
        movimentacao = Movimentacao(
            id = mov.id,
            dataHora = mov.proximaData ?: "",
            descricaoOriginal = mov.explicacao,
            status = mov.status,
            explicacao = mov.explicacao,
            proximaData = mov.proximaData,
            impacto = mov.impacto
        ),
        onExpandToggle = {},
        isExpanded = false
    )

    // DEPOIS (com disclaimer — per D-10 e ESCR-06):
    MovimentacaoCard(
        movimentacao = Movimentacao(
            id = mov.id,
            dataHora = mov.proximaData ?: "",
            descricaoOriginal = mov.explicacao,
            status = mov.status,
            explicacao = mov.explicacao,
            proximaData = mov.proximaData,
            impacto = mov.impacto
        ),
        onExpandToggle = {},
        isExpanded = false,
        disclaimer = mov.disclaimer
    )
    ```

    Nenhum import adicional necessário em PreviewScreen.kt (MovimentacaoCard e ProcessoStatusCard já importados).

    ---

    **ClienteDetalheScreen.kt — 1 correção:**

    Correção: Chamada ao ProcessoStatusCard na linha ~112-116:
    ```kotlin
    // ANTES (incorreto — usa impacto para sync timestamp):
    ProcessoStatusCard(
        status = cliente.statusProcesso,
        impacto = cliente.ultimaSincronizacao?.let { "Última sync: $it" },
        isLoading = cliente.statusProcesso == null
    )

    // DEPOIS (correto — campo dedicado ultimaSincronizacao):
    ProcessoStatusCard(
        status = cliente.statusProcesso,
        impacto = null,
        isLoading = cliente.statusProcesso == null,
        ultimaSincronizacao = cliente.ultimaSincronizacao
    )
    ```

    Nenhum import adicional necessário em ClienteDetalheScreen.kt (ProcessoStatusCard já importado).
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && ./gradlew :app-escritorio:compileDebugKotlin 2>&1 | grep -E "^e:|error:" | head -10; echo "EXIT:$?"</automated>
  </verify>
  <acceptance_criteria>
    - grep "disclaimer = mov.disclaimer" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/preview/PreviewScreen.kt retorna match
    - grep "ultimaSincronizacao = state.ultimaSincronizacao" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/preview/PreviewScreen.kt retorna match
    - grep "ultimaSincronizacao = cliente.ultimaSincronizacao" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/ClienteDetalheScreen.kt retorna match
    - grep "let { \"Última sync:" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/preview/PreviewScreen.kt NÃO retorna match (workaround removido)
    - grep "let { \"Última sync:" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/feature/clientes/detalhe/ClienteDetalheScreen.kt NÃO retorna match (workaround removido)
    - ./gradlew :app-escritorio:compileDebugKotlin sem erros "error:"
    - ./gradlew :app-escritorio:test 2>&1 | grep -E "BUILD|FAILED" — BUILD SUCCESSFUL (testes existentes continuam passando)
  </acceptance_criteria>
  <done>PreviewScreen passa disclaimer = mov.disclaimer ao MovimentacaoCard (D-10 satisfeito). Ambos PreviewScreen e ClienteDetalheScreen usam ultimaSincronizacao em vez de impacto no ProcessoStatusCard (ESCR-08 semanticamente correto). Todos os testes existentes continuam passando. assembleDebug BUILD SUCCESSFUL.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend → MovimentacaoCard (disclaimer) | Texto do disclaimer vem do backend via MovimentacaoDto.disclaimer — exibido como Text() em Compose, sem risco de injection |
| Backend → ProcessoStatusCard (ultimaSincronizacao) | Timestamp de sync vem do backend via ClienteItem.ultimaSincronizacao — exibido como texto puro |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-08-01 | Information Disclosure | MovimentacaoCard — disclaimer ausente | mitigate | Parâmetro disclaimer restaurado. Surface badge renderizado como PRIMEIRO elemento no Column. Verificável por grep na linha de disclaimer antes do Row de data. |
| T-04-08-02 | Tampering | Texto de disclaimer vindo do backend | accept | Compose Text() renderiza texto puro — sem superfície de injection. Disclaimer é string fixa do backend Phase 3, não input de usuário. |
| T-04-08-03 | Information Disclosure | ultimaSincronizacao via campo impacto (confusão semântica) | mitigate | Campo dedicado ultimaSincronizacao adicionado. Campo impacto passa a receber null nos callers corrigidos — separação semântica restaurada. |
</threat_model>

<verification>
Verificação pós-execução:

1. `./gradlew :core-ui:compileDebugKotlin` — sem erros
2. `./gradlew :app-escritorio:compileDebugKotlin` — sem erros
3. `./gradlew :app-escritorio:test` — BUILD SUCCESSFUL (testes existentes passando)
4. grep "disclaimer: String? = null" core-ui/.../MovimentacaoCard.kt — match
5. grep "ultimaSincronizacao: String? = null" core-ui/.../ProcessoStatusCard.kt — match
6. grep "disclaimer = mov.disclaimer" app-escritorio/.../PreviewScreen.kt — match
7. grep "ultimaSincronizacao = state.ultimaSincronizacao" app-escritorio/.../PreviewScreen.kt — match
8. grep "ultimaSincronizacao = cliente.ultimaSincronizacao" app-escritorio/.../ClienteDetalheScreen.kt — match
9. grep "D-06: Sem disclaimer" core-ui/.../MovimentacaoCard.kt — SEM match (afirmação falsa removida)
</verification>

<success_criteria>
- Gap 1 fechado: MovimentacaoCard tem parâmetro disclaimer: String? = null. Quando não-nulo, Surface badge renderizado como primeiro elemento visual (acima do conteúdo de movimentação).
- Gap 2 fechado: PreviewScreen passa disclaimer = mov.disclaimer ao MovimentacaoCard. Cada card na tela de preview exibe disclaimer no topo.
- Gap 3 fechado: ProcessoStatusCard tem parâmetro ultimaSincronizacao: String? = null. ClienteDetalheScreen e PreviewScreen usam o campo dedicado. Campo impacto não é mais usado para dados técnicos de sync.
- Compatibilidade mantida: callers existentes não listados nos files_modified compilam sem alteração (parâmetros têm default null).
- BUILD SUCCESSFUL: :core-ui + :app-escritorio compilam, testes passam.
- ESCR-06 satisfeito: disclaimer visível em cada card de movimentação traduzida.
- ESCR-08 satisfeito semanticamente: ultima_sincronizacao exibida via campo dedicado, não via workaround do campo impacto.
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/04-android-app-fluxo-advogado/04-08-SUMMARY.md`
</output>
