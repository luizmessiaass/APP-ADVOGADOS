---
phase: 01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics
plan: 02
title: Supabase CLI Setup + SQL Migrations + RLS Policies
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/config.toml
  - supabase/migrations/0001_create_escritorios.sql
  - supabase/migrations/0002_create_usuarios.sql
  - supabase/migrations/0003_create_lgpd_consentimentos.sql
  - supabase/migrations/0004_rls_policies.sql
  - supabase/migrations/0005_triggers.sql
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
  - AUTH-05
  - AUTH-07
  - LGPD-01
must_haves:
  truths:
    - "Tabelas escritorios, usuarios, lgpd_consentimentos existem com schema correto (UUID PKs, timestamps, PT-BR naming)"
    - "RLS esta habilitada em todas as tres tabelas"
    - "Policies usam (SELECT auth.jwt()->'app_metadata'->>'tenant_id')::uuid para performance (planner hoisting)"
    - "Indexes existem em tenant_id para todas as tabelas que o possuem"
    - "Trigger update_updated_at_column() atualiza updated_at automaticamente em escritorios e usuarios"
    - "Trigger sync_auth_users() cria registro em public.usuarios ao criar em auth.users"
    - "Status de tenant e` enum restrito: pending, trial, active, suspended"
  artifacts:
    - path: "supabase/config.toml"
      provides: "Configuracao do projeto Supabase local (project_id, porta, etc.)"
    - path: "supabase/migrations/0001_create_escritorios.sql"
      provides: "Tabela escritorios (= tenants) com enum de status e RLS"
    - path: "supabase/migrations/0002_create_usuarios.sql"
      provides: "Tabela usuarios sincronizada com auth.users via FK CASCADE"
    - path: "supabase/migrations/0003_create_lgpd_consentimentos.sql"
      provides: "Tabela lgpd_consentimentos per D-23 com historico completo"
    - path: "supabase/migrations/0004_rls_policies.sql"
      provides: "RLS policies para os tres roles por tabela"
    - path: "supabase/migrations/0005_triggers.sql"
      provides: "Triggers updated_at e sync auth.users -> public.usuarios"
  key_links:
    - from: "supabase/migrations/0002_create_usuarios.sql"
      to: "supabase/migrations/0001_create_escritorios.sql"
      via: "REFERENCES public.escritorios(id) ON DELETE CASCADE"
      pattern: "REFERENCES public.escritorios"
    - from: "supabase/migrations/0004_rls_policies.sql"
      to: "auth.jwt()"
      via: "(SELECT auth.jwt()->'app_metadata'->>'tenant_id')::uuid"
      pattern: "app_metadata.*tenant_id"
---

<objective>
Inicializa o Supabase CLI no monorepo e cria 5 migrations SQL que definem o schema completo da Phase 1: tabelas `escritorios` (tenants), `usuarios` (espelho de auth.users), e `lgpd_consentimentos`; RLS habilitada com policies de isolamento por tenant e por role; indexes de performance em tenant_id; triggers de updated_at e sync de auth.users -> public.usuarios.

Purpose: O schema e as RLS policies sao o alicerce de toda seguranca multi-tenant. Sem eles, nenhum endpoint ou test de cross-tenant pode funcionar.
Output: 5 migrations SQL prontas para `supabase db push`, schema seguro e auditavel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-CONTEXT.md
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-RESEARCH.md
</context>

<interfaces>
<!-- Decisoes de schema do CONTEXT.md (D-14 a D-18) — obrigatorias -->

D-14: Nomenclatura PT-BR sem acentos: escritorios, usuarios, processos, movimentacoes, tenant_id, created_at
D-15: Primary keys UUID v4 via gen_random_uuid()
D-16: Hard delete com cascade (ON DELETE CASCADE) — SEM deleted_at
D-17: public.usuarios sincronizada com auth.users via trigger
D-18: Timestamps created_at + updated_at em todas as tabelas via trigger update_updated_at_column()
D-07: Status de tenant: pending -> trial -> active -> suspended (enum CHECK constraint)
D-08: Roles: admin_escritorio, advogado, cliente (CHECK constraint em role_local)
D-11: Roles no JWT: admin_escritorio, advogado, cliente — policies RLS distintas por role

RLS Pattern OBRIGATORIO (do RESEARCH.md — evita full table scan):
  USING (tenant_id = (SELECT (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid))
  -- O SELECT externo permite que o Postgres hoiste a avaliacao fora do loop por linha

INDEX OBRIGATORIO por convencao:
  CREATE INDEX idx_{tabela}_tenant ON {tabela}(tenant_id); -- toda tabela com tenant_id
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Inicializar Supabase CLI e criar migrations de schema (tabelas + constraints)</name>
  <files>
    supabase/config.toml,
    supabase/migrations/0001_create_escritorios.sql,
    supabase/migrations/0002_create_usuarios.sql,
    supabase/migrations/0003_create_lgpd_consentimentos.sql
  </files>
  <action>
1. Inicializar Supabase CLI no monorepo (se nao existir `supabase/` ainda):
```bash
supabase init
```
Isso cria `supabase/config.toml`. Editar o config.toml para:
- `project_id` = "portaljuridico" (nome local para dev)
- Manter porta padrao 54321 (API), 54322 (DB), 54323 (Studio)
- Adicionar ao .gitignore: `supabase/.branches/` e `supabase/.temp/`

2. Criar `supabase/migrations/0001_create_escritorios.sql`:
```sql
-- Migration: 0001_create_escritorios
-- Tabela raiz de tenants. Cada escritorio = um tenant isolado.
-- D-14: PT-BR naming, D-15: UUID PK, D-07: status enum, D-18: timestamps

CREATE TABLE public.escritorios (
  id          uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  nome        text NOT NULL,
  email       text NOT NULL UNIQUE,
  status      text NOT NULL DEFAULT 'pending'
                CHECK (status IN ('pending', 'trial', 'active', 'suspended')),
  created_at  timestamptz NOT NULL DEFAULT now(),
  updated_at  timestamptz NOT NULL DEFAULT now()
);

COMMENT ON TABLE public.escritorios IS 'Tenants da plataforma. Cada escritorio de advocacia = 1 tenant.';
COMMENT ON COLUMN public.escritorios.status IS 'pending->trial->active->suspended (aprovacao manual pelo admin do produto)';
```

3. Criar `supabase/migrations/0002_create_usuarios.sql`:
```sql
-- Migration: 0002_create_usuarios
-- Espelho de auth.users com campos customizados (tenant_id, cpf, role_local).
-- D-17: sincronizacao via trigger (definido em 0005_triggers.sql)
-- D-16: ON DELETE CASCADE — hard delete, sem soft delete

CREATE TABLE public.usuarios (
  id          uuid PRIMARY KEY REFERENCES auth.users(id) ON DELETE CASCADE,
  tenant_id   uuid NOT NULL REFERENCES public.escritorios(id) ON DELETE CASCADE,
  nome        text NOT NULL,
  cpf         text,                    -- armazenado no banco; NUNCA vai para logs ou prompts (D-26, LGPD-03)
  email       text NOT NULL,
  role_local  text NOT NULL DEFAULT 'cliente'
                CHECK (role_local IN ('admin_escritorio', 'advogado', 'cliente')),
  created_at  timestamptz NOT NULL DEFAULT now(),
  updated_at  timestamptz NOT NULL DEFAULT now()
);

-- INDEX OBRIGATORIO: toda tabela com tenant_id deve ter este index (performance RLS)
CREATE INDEX idx_usuarios_tenant ON public.usuarios(tenant_id);

COMMENT ON TABLE public.usuarios IS 'Espelho de auth.users com campos customizados do dominio.';
COMMENT ON COLUMN public.usuarios.cpf IS 'CPF do usuario. Dado sensivel LGPD. Nunca incluir em logs ou prompts Claude.';
COMMENT ON COLUMN public.usuarios.role_local IS 'Role do usuario neste tenant: admin_escritorio | advogado | cliente';
```

4. Criar `supabase/migrations/0003_create_lgpd_consentimentos.sql`:
```sql
-- Migration: 0003_create_lgpd_consentimentos
-- Registro de consentimento LGPD por usuario, conforme D-23.
-- Suporta historico completo para auditoria ANPD.
-- D-24: versao_termos como ISO date string (ex: "2026-04-14")

CREATE TABLE public.lgpd_consentimentos (
  id              uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  usuario_id      uuid NOT NULL REFERENCES public.usuarios(id) ON DELETE CASCADE,
  versao_termos   text NOT NULL,       -- ISO date string ex: "2026-04-14"
  consentido_em   timestamptz NOT NULL DEFAULT now(),
  ip_origem       inet,                -- inet type para validacao de IP
  user_agent      text,
  revogado_em     timestamptz,         -- NULL = consentimento ativo
  created_at      timestamptz NOT NULL DEFAULT now()
);

-- Index para lookup rapido por usuario (listagem de historico)
CREATE INDEX idx_lgpd_usuario ON public.lgpd_consentimentos(usuario_id);

COMMENT ON TABLE public.lgpd_consentimentos IS 'Registro de consentimento LGPD. Historico imutavel — nunca deletar registros (exceto Art.18 hard delete).';
COMMENT ON COLUMN public.lgpd_consentimentos.versao_termos IS 'ISO date string da versao dos termos aceita. Ex: 2026-04-14';
COMMENT ON COLUMN public.lgpd_consentimentos.revogado_em IS 'NULL = consentimento ativo. Preenchido quando usuario revoga.';
```

RESTRICOES:
- NAO usar `deleted_at` em nenhuma tabela (D-16 — hard delete)
- Todos os nomes de tabelas e colunas em PT-BR sem acentos (D-14)
- PKs sempre UUID via gen_random_uuid() (D-15)
- NUNCA omitir o INDEX em colunas tenant_id
  </action>
  <verify>
    <automated>ls /c/Users/Luiz/AndroidStudioProjects/APPTESTE/supabase/migrations/0001_create_escritorios.sql /c/Users/Luiz/AndroidStudioProjects/APPTESTE/supabase/migrations/0002_create_usuarios.sql /c/Users/Luiz/AndroidStudioProjects/APPTESTE/supabase/migrations/0003_create_lgpd_consentimentos.sql && echo "migrations OK"</automated>
  </verify>
  <done>
    - `supabase/config.toml` existe (Supabase CLI inicializado)
    - As 3 migrations existem com SQL correto (UUID PKs, PT-BR naming, CHECK constraints)
    - Index `idx_usuarios_tenant` criado em 0002
    - `lgpd_consentimentos` tem campo `revogado_em` para rastrear revogacoes
    - Nenhum `deleted_at` em nenhuma tabela
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar RLS policies e triggers de sistema</name>
  <files>
    supabase/migrations/0004_rls_policies.sql,
    supabase/migrations/0005_triggers.sql
  </files>
  <action>
1. Criar `supabase/migrations/0004_rls_policies.sql`:
```sql
-- Migration: 0004_rls_policies
-- RLS policies para isolamento multi-tenant e controle por role.
-- PATTERN CRITICO: usar (SELECT ...) para hoist do subquery — evita re-avaliacao por linha.
-- AUTH-05: RLS ativa em todas as tabelas com dados por tenant
-- AUTH-07: Roles admin_escritorio, advogado, cliente com policies distintas

-- =========================================================
-- TABELA: escritorios
-- =========================================================
ALTER TABLE public.escritorios ENABLE ROW LEVEL SECURITY;

-- Admins do escritorio podem ver e editar APENAS o seu proprio escritorio
CREATE POLICY escritorios_tenant_isolation ON public.escritorios
  FOR ALL
  USING (
    id = (SELECT (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid)
  )
  WITH CHECK (
    id = (SELECT (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid)
  );

-- =========================================================
-- TABELA: usuarios
-- =========================================================
ALTER TABLE public.usuarios ENABLE ROW LEVEL SECURITY;

-- Leitura: admin_escritorio e advogado veem todos do seu tenant;
--          cliente vee apenas a si mesmo
CREATE POLICY usuarios_select ON public.usuarios
  FOR SELECT
  USING (
    tenant_id = (SELECT (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid)
    AND (
      (auth.jwt() -> 'app_metadata' ->> 'role') IN ('admin_escritorio', 'advogado')
      OR id = auth.uid()
    )
  );

-- Insert: apenas admin_escritorio pode criar usuarios no seu tenant
CREATE POLICY usuarios_insert ON public.usuarios
  FOR INSERT
  WITH CHECK (
    tenant_id = (SELECT (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid)
    AND (auth.jwt() -> 'app_metadata' ->> 'role') = 'admin_escritorio'
  );

-- Update: admin_escritorio pode editar qualquer usuario do tenant;
--         usuario pode editar apenas o proprio perfil (exceto role_local)
CREATE POLICY usuarios_update ON public.usuarios
  FOR UPDATE
  USING (
    tenant_id = (SELECT (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid)
    AND (
      (auth.jwt() -> 'app_metadata' ->> 'role') = 'admin_escritorio'
      OR id = auth.uid()
    )
  )
  WITH CHECK (
    tenant_id = (SELECT (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid)
  );

-- Delete: apenas admin_escritorio pode deletar usuarios do tenant
CREATE POLICY usuarios_delete ON public.usuarios
  FOR DELETE
  USING (
    tenant_id = (SELECT (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid)
    AND (auth.jwt() -> 'app_metadata' ->> 'role') = 'admin_escritorio'
  );

-- =========================================================
-- TABELA: lgpd_consentimentos
-- =========================================================
ALTER TABLE public.lgpd_consentimentos ENABLE ROW LEVEL SECURITY;

-- Cada usuario acessa apenas seus proprios consentimentos
-- admin_escritorio pode ver consentimentos de usuarios do seu tenant
CREATE POLICY lgpd_consentimentos_select ON public.lgpd_consentimentos
  FOR SELECT
  USING (
    usuario_id = auth.uid()
    OR (
      (auth.jwt() -> 'app_metadata' ->> 'role') = 'admin_escritorio'
      AND EXISTS (
        SELECT 1 FROM public.usuarios u
        WHERE u.id = lgpd_consentimentos.usuario_id
          AND u.tenant_id = (SELECT (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid)
      )
    )
  );

-- Insert: usuario insere apenas seu proprio consentimento
CREATE POLICY lgpd_consentimentos_insert ON public.lgpd_consentimentos
  FOR INSERT
  WITH CHECK (usuario_id = auth.uid());

-- Update: usuario pode revogar seu proprio consentimento (setar revogado_em)
CREATE POLICY lgpd_consentimentos_update ON public.lgpd_consentimentos
  FOR UPDATE
  USING (usuario_id = auth.uid())
  WITH CHECK (usuario_id = auth.uid());
```

2. Criar `supabase/migrations/0005_triggers.sql`:
```sql
-- Migration: 0005_triggers
-- Triggers de sistema para automacao de timestamps e sync de auth.users
-- D-18: updated_at automatico via trigger em todas as tabelas
-- D-17: sync auth.users -> public.usuarios no signup/invite

-- =========================================================
-- FUNCAO: update_updated_at_column()
-- Atualiza o campo updated_at para now() em qualquer UPDATE
-- =========================================================
CREATE OR REPLACE FUNCTION public.update_updated_at_column()
RETURNS TRIGGER AS $$
BEGIN
  NEW.updated_at = now();
  RETURN NEW;
END;
$$ LANGUAGE plpgsql;

-- Aplicar trigger de updated_at nas tabelas que possuem o campo
CREATE TRIGGER escritorios_updated_at
  BEFORE UPDATE ON public.escritorios
  FOR EACH ROW EXECUTE FUNCTION public.update_updated_at_column();

CREATE TRIGGER usuarios_updated_at
  BEFORE UPDATE ON public.usuarios
  FOR EACH ROW EXECUTE FUNCTION public.update_updated_at_column();

-- =========================================================
-- FUNCAO: sync_auth_user_to_public()
-- Cria registro em public.usuarios quando auth.users recebe INSERT.
-- Triggered por: signup de admin_escritorio E inviteUserByEmail de cliente.
-- D-17: tenant_id extraido de raw_user_meta_data (passado no invite/signup)
-- Pitfall 6 do RESEARCH.md: passar tenant_id no metadata do invite
-- =========================================================
CREATE OR REPLACE FUNCTION public.sync_auth_user_to_public()
RETURNS TRIGGER AS $$
BEGIN
  -- Extrair dados do metadata passado no signup/invite
  -- Para admin_escritorio: tenant_id sera preenchido apos criacao do escritorio
  -- Para cliente: tenant_id passado via inviteUserByEmail metadata
  INSERT INTO public.usuarios (
    id,
    tenant_id,
    nome,
    email,
    role_local,
    cpf
  ) VALUES (
    NEW.id,
    (NEW.raw_user_meta_data ->> 'tenant_id')::uuid,
    COALESCE(NEW.raw_user_meta_data ->> 'nome', split_part(NEW.email, '@', 1)),
    NEW.email,
    COALESCE(NEW.raw_user_meta_data ->> 'role_local', 'cliente'),
    NEW.raw_user_meta_data ->> 'cpf'  -- pode ser NULL para admin_escritorio
  )
  ON CONFLICT (id) DO NOTHING;  -- idempotente: nao recriar se ja existe

  RETURN NEW;
END;
$$ LANGUAGE plpgsql SECURITY DEFINER;

-- Trigger dispara APOS INSERT em auth.users
CREATE TRIGGER on_auth_user_created
  AFTER INSERT ON auth.users
  FOR EACH ROW EXECUTE FUNCTION public.sync_auth_user_to_public();

-- NOTA: O trigger nao cria escritorio automaticamente. O fluxo de signup
-- de admin_escritorio e` tratado no endpoint POST /api/v1/auth/signup/escritorio
-- que: (1) cria o user no Supabase Auth, (2) cria o escritorio, (3) atualiza o usuario.
```

RESTRICOES:
- NUNCA usar policy `USING (true)` — abre a tabela para todos os tenants (anti-pattern critico)
- SEMPRE usar (SELECT ...) no USING para hoist do subquery (performance RLS)
- O trigger sync_auth_user_to_public deve ter SECURITY DEFINER para poder inserir em public.usuarios
- NAO usar user_metadata para claims de seguranca (apenas app_metadata) — pitfall do RESEARCH.md
  </action>
  <verify>
    <automated>ls /c/Users/Luiz/AndroidStudioProjects/APPTESTE/supabase/migrations/0004_rls_policies.sql /c/Users/Luiz/AndroidStudioProjects/APPTESTE/supabase/migrations/0005_triggers.sql && grep -l "ENABLE ROW LEVEL SECURITY" /c/Users/Luiz/AndroidStudioProjects/APPTESTE/supabase/migrations/0004_rls_policies.sql && echo "RLS OK"</automated>
  </verify>
  <done>
    - `0004_rls_policies.sql` habilita RLS nas 3 tabelas com policies distintas por role
    - Todas as policies usam `(SELECT (auth.jwt()->'app_metadata'->>'tenant_id')::uuid)` para performance
    - `0005_triggers.sql` define update_updated_at_column() e sync_auth_user_to_public()
    - Trigger sync tem SECURITY DEFINER para inserir em public.usuarios
    - Nenhuma policy usa USING (true)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| auth.users → public.usuarios | Trigger SECURITY DEFINER — executa com privilegios elevados |
| RLS policy → auth.jwt() | Claims JWT devem vir de app_metadata (server-side only) |
| tenant A → tenant B data | Fronteira primaria de isolamento: RLS policy em todas as tabelas |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02-S | Spoofing | `user_metadata` claims | mitigate | Policies usam apenas `app_metadata` (nao editavel pelo usuario); user_metadata jamais referenciado em policies |
| T-1-02-E | Elevation of Privilege | RLS policies | mitigate | Policy `usuarios_update` impede usuario de alterar `role_local`; somente admin pode alterar |
| T-1-02-I | Info Disclosure | Cross-tenant query | mitigate | `tenant_id = (SELECT ...)::uuid` em TODAS as policies USING; nenhuma policy permissiva como placeholder |
| T-1-02-T | Tampering | Trigger SECURITY DEFINER | mitigate | Trigger apenas insere em public.usuarios; nao modifica auth.users; uso limitado ao escopo minimo |
| T-1-02-D | Denial of Service | Full table scan em RLS | mitigate | INDEX em tenant_id em todas as tabelas (idx_usuarios_tenant, idx_lgpd_usuario); politica de convencao documentada |
</threat_model>

<verification>
- Todas as 5 migrations existem: 0001 a 0005
- `grep "ENABLE ROW LEVEL SECURITY" supabase/migrations/0004_rls_policies.sql` retorna 3 linhas (uma por tabela)
- `grep "USING (true)" supabase/migrations/` retorna vazio (nenhuma policy permissiva)
- `grep "SELECT.*app_metadata.*tenant_id" supabase/migrations/0004_rls_policies.sql` retorna matches em todas as policies de tenant isolation
- `grep "CREATE INDEX idx_" supabase/migrations/0002_create_usuarios.sql` retorna `idx_usuarios_tenant`
</verification>

<success_criteria>
5 migrations SQL criadas e validadas. RLS habilitada em todas as 3 tabelas da Phase 1. Policies usam o padrao de hoist `(SELECT ...)` para performance. Triggers de updated_at e sync auth->public configurados. Schema pronto para `supabase db push`.
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-02-SUMMARY.md`
</output>
