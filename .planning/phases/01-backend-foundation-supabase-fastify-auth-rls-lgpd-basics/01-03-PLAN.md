---
phase: 01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics
plan: 03
title: Custom Access Token Hook (Edge Function) + Supabase Edge Function Deploy
type: execute
wave: 2
depends_on:
  - "01-02"
files_modified:
  - supabase/functions/custom-access-token/index.ts
  - supabase/functions/custom-access-token/deno.json
autonomous: false
requirements:
  - AUTH-03
  - AUTH-08
user_setup:
  - service: supabase-dashboard
    why: "Registrar a Edge Function como Custom Access Token Hook no Supabase Dashboard"
    env_vars: []
    dashboard_config:
      - task: "Registrar Custom Access Token Hook"
        location: "Supabase Dashboard -> Authentication -> Hooks -> Custom Access Token Hook -> Enable -> selecionar funcao 'custom-access-token'"
      - task: "Habilitar Edge Function custom-access-token no Supabase Dashboard"
        location: "Supabase Dashboard -> Edge Functions -> verificar que a funcao esta ativa apos deploy"
      - task: "Configurar SMTP Resend para emails de convite"
        location: "Supabase Dashboard -> Authentication -> SMTP Settings -> habilitar custom SMTP -> inserir credenciais Resend"
must_haves:
  truths:
    - "Todo JWT gerado pelo Supabase Auth contem tenant_id e role em app_metadata"
    - "O hook busca tenant_id e role_local da tabela public.usuarios (nao de user_metadata)"
    - "Clientes convidados via inviteUserByEmail recebem email com magic link para criar senha"
    - "A Edge Function e' deployada no projeto Supabase e registrada como hook"
  artifacts:
    - path: "supabase/functions/custom-access-token/index.ts"
      provides: "Edge Function Deno que injeta tenant_id e role no JWT"
      exports: ["default Deno.serve handler"]
    - path: "supabase/functions/custom-access-token/deno.json"
      provides: "Configuracao da Edge Function Deno"
  key_links:
    - from: "supabase/functions/custom-access-token/index.ts"
      to: "public.usuarios"
      via: "supabase.from('usuarios').select('tenant_id, role_local').eq('id', user_id)"
      pattern: "from.*usuarios.*select.*tenant_id"
    - from: "auth.jwt() in RLS policies"
      to: "custom-access-token/index.ts"
      via: "app_metadata.tenant_id injetado pelo hook"
      pattern: "app_metadata"
---

<objective>
Implementa a Supabase Edge Function `custom-access-token` que injeta `tenant_id` e `role` em `app_metadata` de cada JWT gerado pelo Supabase Auth, e instrui o usuario a registra-la como Custom Access Token Hook no Dashboard. Sem este hook, todas as RLS policies e o middleware Fastify ficam inoperantes.

Purpose: Este e' o ponto central de injecao de contexto de tenant nos tokens. E' o pre-requisito critico para tudo que exige autenticacao multi-tenant.
Output: Edge Function deployada + hook registrado + inviteUserByEmail com tenant_id no metadata.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-CONTEXT.md
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-RESEARCH.md
</context>

<interfaces>
<!-- Contrato do JWT apos o hook (o que o middleware Fastify espera em Plan 04) -->

JWT payload apos Custom Access Token Hook:
```typescript
{
  sub: string,           // user_id (auth.users.id)
  email: string,
  app_metadata: {
    tenant_id: string,   // uuid do escritorio
    role: 'admin_escritorio' | 'advogado' | 'cliente'
  },
  // ... outros claims padrao Supabase
}
```

Tabela public.usuarios (criada em Plan 02):
- id: uuid (= auth.users.id)
- tenant_id: uuid NOT NULL
- role_local: 'admin_escritorio' | 'advogado' | 'cliente'

Pitfall 1 do RESEARCH.md: Hook DEVE estar ativo ANTES do primeiro signup de teste.
Pitfall 6 do RESEARCH.md: inviteUserByEmail DEVE passar tenant_id nos metadados.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar Edge Function custom-access-token</name>
  <files>
    supabase/functions/custom-access-token/index.ts,
    supabase/functions/custom-access-token/deno.json
  </files>
  <action>
1. Criar `supabase/functions/custom-access-token/deno.json`:
```json
{
  "imports": {
    "@supabase/supabase-js": "https://esm.sh/@supabase/supabase-js@2"
  }
}
```

2. Criar `supabase/functions/custom-access-token/index.ts`:
```typescript
// supabase/functions/custom-access-token/index.ts
// Custom Access Token Hook — injeta tenant_id e role em app_metadata de cada JWT.
// Documentacao: https://supabase.com/docs/guides/auth/auth-hooks#custom-access-token-hook
//
// CRITICO: Esta funcao deve estar registrada no Dashboard ANTES do primeiro signup.
// Ver Pitfall 1 no 01-RESEARCH.md.

import { createClient } from 'https://esm.sh/@supabase/supabase-js@2'

interface HookPayload {
  user_id: string
  claims: {
    sub: string
    email?: string
    app_metadata: Record<string, unknown>
    user_metadata: Record<string, unknown>
    [key: string]: unknown
  }
}

Deno.serve(async (req: Request): Promise<Response> => {
  try {
    const payload = await req.json() as HookPayload
    const { user_id, claims } = payload

    const supabase = createClient(
      Deno.env.get('SUPABASE_URL') ?? '',
      Deno.env.get('SUPABASE_SERVICE_ROLE_KEY') ?? '',
      { auth: { autoRefreshToken: false, persistSession: false } }
    )

    // Buscar tenant_id e role do usuario na tabela public.usuarios
    // NUNCA usar user_metadata — usuario pode alterar (D-10, RESEARCH.md anti-pattern)
    const { data: usuario, error } = await supabase
      .from('usuarios')
      .select('tenant_id, role_local')
      .eq('id', user_id)
      .single()

    if (error || !usuario) {
      // Usuario nao encontrado em public.usuarios (possivel estado de signup incompleto)
      // Retornar token sem tenant_id — middleware Fastify rejeitara com 403
      console.error('[custom-access-token] usuario nao encontrado', { user_id, error: error?.message })
      return Response.json({
        claims: {
          ...claims,
          app_metadata: {
            ...claims.app_metadata,
            tenant_id: null,
            role: null,
          }
        }
      })
    }

    // Injetar em app_metadata (NUNCA em user_metadata)
    const enrichedClaims = {
      ...claims,
      app_metadata: {
        ...claims.app_metadata,
        tenant_id: usuario.tenant_id,
        role: usuario.role_local,
      }
    }

    return Response.json({ claims: enrichedClaims })

  } catch (err) {
    console.error('[custom-access-token] erro inesperado', err)
    // Em caso de erro, retornar payload original (Supabase decide como tratar)
    return Response.json({ error: 'hook_error' }, { status: 500 })
  }
})
```

3. Fazer deploy da Edge Function via Supabase CLI:
```bash
supabase functions deploy custom-access-token --project-ref <SEU_PROJECT_REF>
```
(O project_ref e' o ID do projeto Supabase — visivel em Settings > API no Dashboard)

RESTRICOES:
- Injetar APENAS em app_metadata (nunca user_metadata)
- Logar erros com console.error para visibilidade no Supabase Edge Function logs
- Tratar o caso em que usuario nao existe (signup em progresso) graciosamente
- A funcao usa SUPABASE_SERVICE_ROLE_KEY disponivel automaticamente como env var no Edge Function runtime
  </action>
  <verify>
    <automated>ls /c/Users/Luiz/AndroidStudioProjects/APPTESTE/supabase/functions/custom-access-token/index.ts && grep "app_metadata" /c/Users/Luiz/AndroidStudioProjects/APPTESTE/supabase/functions/custom-access-token/index.ts && echo "hook file OK"</automated>
  </verify>
  <done>
    - `supabase/functions/custom-access-token/index.ts` existe e busca tenant_id de public.usuarios
    - Edge Function deployada com `supabase functions deploy`
    - Hook injeta em app_metadata (nao user_metadata)
    - Trata graciosamente o caso de usuario nao encontrado
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Edge Function `custom-access-token` deployada no Supabase e codigo-fonte em `supabase/functions/custom-access-token/index.ts`.
    O hook precisa ser registrado manualmente no Supabase Dashboard para ser ativado.
    Resend SMTP tambem precisa ser configurado para que convites por email funcionem (AUTH-08).
  </what-built>
  <how-to-verify>
    1. Acesse o Supabase Dashboard > Authentication > Hooks
    2. Localize "Custom Access Token Hook"
    3. Clique "Enable" e selecione a Edge Function `custom-access-token`
    4. Salve a configuracao
    5. Acesse Authentication > SMTP Settings
    6. Habilite "Custom SMTP"
    7. Insira as credenciais do Resend (obter em resend.com/api-keys):
       - Host: smtp.resend.com
       - Port: 587 (ou 465 para SSL)
       - Username: resend
       - Password: sua-api-key-do-resend
       - Sender name: Portal Juridico
       - Sender email: noreply@seudominio.com (ou usar resend sandbox para dev)
    8. Salve e envie um email de teste para confirmar funcionamento
    9. CONFIRMAR: Faca um signup de teste via Dashboard > Authentication > Users > Invite
       e verifique que o JWT gerado contem app_metadata.tenant_id (use jwt.io para decodificar)
  </how-to-verify>
  <resume-signal>
    Digite "aprovado" quando o hook estiver registrado e o SMTP configurado.
    Se houver problemas, descreva o erro para ajuste.
  </resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Supabase Auth → Edge Function hook | Hook recebe user_id + claims; tem acesso ao service_role key |
| Edge Function → public.usuarios | Query com service_role — bypass RLS correto aqui (e' operacao de sistema) |
| JWT → Fastify middleware | Claims do JWT sao a fonte de verdade para autorizacao |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-03-S | Spoofing | `user_metadata` claims | mitigate | Hook le de `public.usuarios` (server-side), nao de `user_metadata` (editavel pelo usuario); anti-pattern documentado |
| T-1-03-E | Elevation of Privilege | tenant_id injection | mitigate | tenant_id vem do banco (public.usuarios) — usuario nao pode alterar; hook usa service_role para query |
| T-1-03-T | Tampering | Hook payload | mitigate | Supabase assina o payload do hook; verificar assinatura com HOOK_SECRET (adicionar em config.toml quando disponivel) |
| T-1-03-I | Info Disclosure | Edge Function logs | accept | Logs do hook ficam no Supabase Dashboard (nao drenados para Betterstack); aceitar nivel de risco em dev |
</threat_model>

<verification>
- `supabase/functions/custom-access-token/index.ts` existe e nao referencia user_metadata para claims de seguranca
- Edge Function deployada (verificar em Supabase Dashboard > Edge Functions)
- Custom Access Token Hook registrado (verificar em Dashboard > Authentication > Hooks)
- SMTP Resend configurado (verificar envio de email de teste)
</verification>

<success_criteria>
Edge Function custom-access-token deployada e registrada como hook. Todo JWT subsequente do Supabase contem app_metadata.tenant_id e app_metadata.role. Email de convite para clientes configurado via Resend SMTP.
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-03-SUMMARY.md`
</output>
