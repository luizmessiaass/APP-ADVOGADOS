---
phase: 01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics
plan: 01
title: Monorepo Scaffold + Fastify Project Init + Dev Tooling
type: execute
wave: 1
depends_on: []
files_modified:
  - pnpm-workspace.yaml
  - package.json
  - .gitignore
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/config.ts
  - apps/worker/package.json
  - apps/worker/tsconfig.json
  - .husky/pre-commit
  - .eslintrc.json
  - .prettierrc.json
  - backend/vitest.config.ts
  - apps/api/src/tests/fixtures.ts
autonomous: true
requirements:
  - INFRA-01
  - INFRA-04
must_haves:
  truths:
    - "pnpm install na raiz instala todas as dependencias de todos os workspaces"
    - "cd apps/api && pnpm dev inicia o servidor Fastify com hot-reload via tsx"
    - "As variaveis de ambiente obrigatorias validadas por envalid — processo falha fast com mensagem clara se ausentes"
    - "Pre-commit hook executa ESLint + Prettier apenas nos arquivos staged"
    - "Vitest esta configurado e executa npx vitest run sem erros de configuracao"
  artifacts:
    - path: "pnpm-workspace.yaml"
      provides: "Workspace root — inclui apps/* e supabase/functions/*"
    - path: "apps/api/package.json"
      provides: "Dependencias do servidor API com todas as versoes do standard_stack"
    - path: "apps/api/src/config.ts"
      provides: "Validacao de env vars via envalid — falha fast na inicializacao"
    - path: "apps/worker/package.json"
      provides: "Dependencias do worker BullMQ"
    - path: "backend/vitest.config.ts"
      provides: "Configuracao de testes — Wave 0 para as demais plans"
  key_links:
    - from: "apps/api/src/server.ts"
      to: "apps/api/src/config.ts"
      via: "import { env } from './config.js'"
      pattern: "cleanEnv"
    - from: ".husky/pre-commit"
      to: ".eslintrc.json + .prettierrc.json"
      via: "lint-staged"
      pattern: "lint-staged"
---

<objective>
Cria a estrutura completa do monorepo pnpm com workspaces para `apps/api` (Fastify) e `apps/worker` (BullMQ), instala todas as dependencias do standard_stack com versoes verificadas, configura TypeScript estrito, validacao de env vars com envalid, Husky + lint-staged para pre-commit, e o Vitest config que as plans subsequentes usam como infra de testes.

Purpose: Sem esse scaffold, nenhuma outra plan pode ser executada — e` a fundacao do monorepo Node.js.
Output: Monorepo pronto, `apps/api` inicia com `pnpm dev`, env vars validadas na boot, test runner configurado.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-CONTEXT.md
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-RESEARCH.md
</context>

<interfaces>
<!-- Versoes do standard_stack a usar (extraidas do RESEARCH.md) -->
<!-- Executor deve usar exatamente estas versoes — nao atualizar sem razao -->

Standard stack versions (from 01-RESEARCH.md):
- Node.js: 22 LTS (engines.node: ">=22.0.0")
- TypeScript: 6.0.2
- pnpm: 10.33.0
- Fastify: 5.8.5
- @supabase/supabase-js: 2.103.0
- jose: 6.2.2
- BullMQ: 5.73.5
- ioredis: 5.10.1
- pino: 10.3.1
- @sentry/node: 10.48.0
- envalid: 8.1.1
- vitest: 4.1.4
- @fastify/helmet: 13.0.2
- @fastify/rate-limit: 10.3.0
- @fastify/type-provider-typebox: latest
- @sinclair/typebox: latest
- tsx: 4.21.0
- pino-pretty: 13.1.3 (devDep)
- husky: 9.1.7
- lint-staged: 16.4.0

Env vars required (from 01-CONTEXT.md D-04, D-32):
- NODE_ENV: development | production | test
- PORT: number (default 3000)
- SUPABASE_URL: url
- SUPABASE_ANON_KEY: string
- SUPABASE_SERVICE_ROLE_KEY: string
- REDIS_URL: url
- SENTRY_DSN: string (optional, default "")
- BETTERSTACK_SOURCE_TOKEN: string (optional, default "")
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Criar estrutura de monorepo com pnpm workspaces e instalar dependencias</name>
  <files>
    pnpm-workspace.yaml,
    package.json,
    .gitignore,
    apps/api/package.json,
    apps/api/tsconfig.json,
    apps/worker/package.json,
    apps/worker/tsconfig.json,
    .eslintrc.json,
    .prettierrc.json,
    .husky/pre-commit,
    docker-compose.yml
  </files>
  <action>
Criar o monorepo pnpm na raiz do repositorio (C:\Users\Luiz\AndroidStudioProjects\APPTESTE). O repositorio Android ja existe — o backend Node.js vai em subdiretorio `apps/`.

1. Criar `pnpm-workspace.yaml`:
```yaml
packages:
  - 'apps/*'
  - 'supabase/functions/*'
```

2. Criar/atualizar `package.json` na raiz com scripts de workspace:
```json
{
  "name": "portaljuridico",
  "private": true,
  "engines": { "node": ">=22.0.0", "pnpm": ">=10.0.0" },
  "scripts": {
    "dev:api": "pnpm --filter api dev",
    "dev:worker": "pnpm --filter worker dev",
    "test": "pnpm --filter api test",
    "lint": "pnpm --filter api lint",
    "build:api": "pnpm --filter api build"
  },
  "devDependencies": {
    "husky": "9.1.7",
    "lint-staged": "16.4.0"
  },
  "lint-staged": {
    "apps/**/*.{ts,tsx}": ["eslint --fix", "prettier --write"],
    "supabase/**/*.ts": ["prettier --write"]
  }
}
```

3. Criar `apps/api/package.json`:
```json
{
  "name": "api",
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "dev": "tsx watch src/server.ts",
    "build": "tsc --project tsconfig.json",
    "start": "node dist/server.js",
    "test": "vitest run --reporter=verbose",
    "test:watch": "vitest",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@fastify/helmet": "13.0.2",
    "@fastify/rate-limit": "10.3.0",
    "@fastify/type-provider-typebox": "latest",
    "@sentry/node": "10.48.0",
    "@sinclair/typebox": "latest",
    "@supabase/supabase-js": "2.103.0",
    "bullmq": "5.73.5",
    "envalid": "8.1.1",
    "fastify": "5.8.5",
    "fastify-plugin": "5.0.1",
    "ioredis": "5.10.1",
    "jose": "6.2.2",
    "pino": "10.3.1"
  },
  "devDependencies": {
    "@types/node": "latest",
    "@vitest/coverage-v8": "latest",
    "pino-pretty": "13.1.3",
    "tsx": "4.21.0",
    "typescript": "6.0.2",
    "vitest": "4.1.4"
  }
}
```

4. Criar `apps/api/tsconfig.json` com TypeScript estrito:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "outDir": "dist",
    "rootDir": "src",
    "resolveJsonModule": true,
    "declaration": true,
    "sourceMap": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
```

5. Criar `apps/worker/package.json` (dependencias BullMQ + ioredis + Sentry):
```json
{
  "name": "worker",
  "version": "0.1.0",
  "type": "module",
  "engines": { "node": ">=22.0.0" },
  "scripts": {
    "dev": "tsx watch src/worker.ts",
    "build": "tsc --project tsconfig.json",
    "start": "node dist/worker.js"
  },
  "dependencies": {
    "@sentry/node": "10.48.0",
    "@supabase/supabase-js": "2.103.0",
    "bullmq": "5.73.5",
    "envalid": "8.1.1",
    "ioredis": "5.10.1",
    "pino": "10.3.1"
  },
  "devDependencies": {
    "@types/node": "latest",
    "tsx": "4.21.0",
    "typescript": "6.0.2"
  }
}
```

6. Criar `apps/worker/tsconfig.json` (igual ao da API).

7. Criar `.eslintrc.json` na raiz:
```json
{
  "root": true,
  "parser": "@typescript-eslint/parser",
  "plugins": ["@typescript-eslint"],
  "extends": ["eslint:recommended", "plugin:@typescript-eslint/recommended"],
  "rules": {
    "@typescript-eslint/no-explicit-any": "error",
    "@typescript-eslint/no-unused-vars": "error",
    "no-console": "warn"
  },
  "env": { "node": true, "es2022": true }
}
```
Adicionar ao devDependencies da API: `@typescript-eslint/eslint-plugin`, `@typescript-eslint/parser`, `eslint`.

8. Criar `.prettierrc.json`:
```json
{
  "semi": false,
  "singleQuote": true,
  "trailingComma": "es5",
  "printWidth": 100,
  "tabWidth": 2
}
```

9. Configurar Husky: `pnpm dlx husky init` (cria `.husky/` automaticamente), depois editar `.husky/pre-commit`:
```sh
#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"
npx lint-staged
```

10. Criar `docker-compose.yml` na raiz para Redis local:
```yaml
version: '3.8'
services:
  redis:
    image: redis:alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5
```

11. Atualizar `.gitignore` para incluir:
```
node_modules/
dist/
.env
.env.local
*.env
apps/*/dist/
apps/*/node_modules/
```
(Manter as entradas Android existentes — adicionar as Node.js sem remover o que ja existe.)

12. Executar `pnpm install` na raiz para instalar todas as dependencias.

RESTRICOES (per CONTEXT.md):
- Usar pnpm (D-03), NAO npm ou yarn
- Usar exatamente as versoes do standard_stack (ver interfaces acima)
- NAO tocar arquivos Android (build.gradle.kts, app/, gradle/) — eles sao Phase 0
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && pnpm install && echo "pnpm install OK" && ls apps/api/package.json apps/worker/package.json pnpm-workspace.yaml && echo "files OK"</automated>
  </verify>
  <done>
    - `pnpm install` executa sem erros na raiz do monorepo
    - `apps/api/package.json` e `apps/worker/package.json` existem com dependencias corretas
    - `pnpm-workspace.yaml` configura os workspaces `apps/*`
    - `.husky/pre-commit` existe e e` executavel
    - `docker-compose.yml` define o servico redis:alpine
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar config.ts (validacao de env vars) e vitest.config.ts + test fixtures</name>
  <files>
    apps/api/src/config.ts,
    apps/api/src/env.example,
    apps/worker/src/config.ts,
    backend/vitest.config.ts,
    apps/api/src/tests/fixtures.ts,
    apps/api/src/tests/cross-tenant.test.ts,
    apps/api/src/tests/lgpd.test.ts
  </files>
  <action>
1. Criar `apps/api/src/config.ts` (per D-04, D-26, D-27, D-28, D-29, D-32 do CONTEXT.md):
```typescript
import { cleanEnv, str, url, num } from 'envalid'

export const env = cleanEnv(process.env, {
  NODE_ENV:                  str({ choices: ['development', 'production', 'test'], default: 'development' }),
  PORT:                      num({ default: 3000 }),
  SUPABASE_URL:              url(),
  SUPABASE_ANON_KEY:         str(),
  SUPABASE_SERVICE_ROLE_KEY: str(),
  REDIS_URL:                 url(),
  SENTRY_DSN:                str({ default: '' }),
  BETTERSTACK_SOURCE_TOKEN:  str({ default: '' }),
})
// cleanEnv lanca EnvVarError se variavel obrigatoria ausente — processo termina imediatamente
```

2. Criar `apps/api/.env.example` (nao commitar `.env` real — apenas o example):
```
NODE_ENV=development
PORT=3000
SUPABASE_URL=https://YOUR_PROJECT_REF.supabase.co
SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
REDIS_URL=redis://localhost:6379
SENTRY_DSN=
BETTERSTACK_SOURCE_TOKEN=
```

3. Criar `apps/worker/src/config.ts` (subset das vars — worker nao precisa PORT):
```typescript
import { cleanEnv, str, url } from 'envalid'

export const env = cleanEnv(process.env, {
  NODE_ENV:                  str({ choices: ['development', 'production', 'test'], default: 'development' }),
  SUPABASE_URL:              url(),
  SUPABASE_SERVICE_ROLE_KEY: str(),
  REDIS_URL:                 url(),
  SENTRY_DSN:                str({ default: '' }),
})
```

4. Criar `backend/vitest.config.ts` (Wave 0 — config que TODAS as plans dependem):
```typescript
import { defineConfig } from 'vitest/config'

export default defineConfig({
  test: {
    globals: true,
    environment: 'node',
    include: ['apps/api/src/**/*.test.ts'],
    coverage: {
      provider: 'v8',
      reporter: ['text', 'json-summary'],
    },
    setupFiles: ['apps/api/src/tests/setup.ts'],
    testTimeout: 30000,
  },
  resolve: {
    alias: {
      '@api': '/apps/api/src',
    },
  },
})
```

5. Criar `apps/api/src/tests/setup.ts` (setup global de testes):
```typescript
// Configura variaveis de ambiente para testes antes de qualquer import
process.env.NODE_ENV = 'test'
process.env.SUPABASE_URL = process.env.SUPABASE_URL ?? 'https://test.supabase.co'
process.env.SUPABASE_ANON_KEY = process.env.SUPABASE_ANON_KEY ?? 'test-anon-key'
process.env.SUPABASE_SERVICE_ROLE_KEY = process.env.SUPABASE_SERVICE_ROLE_KEY ?? 'test-service-role-key'
process.env.REDIS_URL = process.env.REDIS_URL ?? 'redis://localhost:6379'
```

6. Criar `apps/api/src/tests/fixtures.ts` (fixtures compartilhadas — tenants ficticios):
```typescript
// Tenants ficticios para testes de integracao
export const TENANT_A = {
  id: '00000000-0000-0000-0000-000000000001',
  nome: 'Escritorio Alpha Advogados',
  email: 'alpha@test.portaljuridico.com.br',
  status: 'active' as const,
}

export const TENANT_B = {
  id: '00000000-0000-0000-0000-000000000002',
  nome: 'Escritorio Beta Advocacia',
  email: 'beta@test.portaljuridico.com.br',
  status: 'active' as const,
}

export const USER_ADMIN_A = {
  id: '00000000-0000-0000-0001-000000000001',
  tenant_id: TENANT_A.id,
  nome: 'Admin Alpha',
  email: 'admin@alpha.test',
  role_local: 'admin_escritorio' as const,
}

export const USER_ADMIN_B = {
  id: '00000000-0000-0000-0001-000000000002',
  tenant_id: TENANT_B.id,
  nome: 'Admin Beta',
  email: 'admin@beta.test',
  role_local: 'admin_escritorio' as const,
}
```

7. Criar `apps/api/src/tests/cross-tenant.test.ts` (stub — sera preenchido em Plan 08):
```typescript
import { describe, it, expect } from 'vitest'

describe('Cross-Tenant Security Gate (AUTH-06)', () => {
  it('STUB: tenant A nao consegue ler dados do tenant B', () => {
    // STUB — implementacao completa em Plan 08 (Wave 3)
    // Este stub garante que o arquivo existe e o Vitest o encontra
    expect(true).toBe(true)
  })
})
```

8. Criar `apps/api/src/tests/lgpd.test.ts` (stub — sera preenchido em Plan 05):
```typescript
import { describe, it, expect } from 'vitest'

describe('LGPD PII Redaction (LGPD-04)', () => {
  it('STUB: CPF nao aparece em logs de request', () => {
    // STUB — implementacao completa em Plan 05
    expect(true).toBe(true)
  })
})
```

RESTRICOES:
- O arquivo `backend/vitest.config.ts` e` o config canonical — todas as plans rodam via este arquivo
- Nao criar `.env` real com secrets — apenas `.env.example`
- Todos os stubs devem ter comentario explicito "STUB — implementacao em Plan XX"
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && npx vitest run --config backend/vitest.config.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `backend/vitest.config.ts` existe e `npx vitest run --config backend/vitest.config.ts` executa sem erros de configuracao
    - `apps/api/src/config.ts` importa e valida env vars via envalid
    - `apps/api/src/tests/fixtures.ts` exporta TENANT_A, TENANT_B, USER_ADMIN_A, USER_ADMIN_B
    - `apps/api/src/tests/cross-tenant.test.ts` e `lgpd.test.ts` existem como stubs passando
    - Vitest reporta "2 passed" (os dois stubs)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev machine → git | Arquivos `.env` nao devem ser commitados |
| CI environment → secrets | Env vars de CI sao secrets do GitHub Actions — nunca em codigo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01-S | Spoofing | `.env` files | mitigate | `.gitignore` inclui `.env*`; apenas `.env.example` e` commitado; validado em Task 1 |
| T-1-01-I | Info Disclosure | `package.json` lock | accept | Versoes publicas de dependencias; sem secrets em package.json |
| T-1-01-T | Tampering | `pnpm-lock.yaml` | mitigate | Lockfile commitado e verificado no CI (`pnpm install --frozen-lockfile`) |
</threat_model>

<verification>
- `pnpm install` na raiz executa sem erros
- `ls apps/api/src/config.ts apps/worker/src/config.ts backend/vitest.config.ts` — todos existem
- `npx vitest run --config backend/vitest.config.ts` reporta "2 passed, 0 failed"
- `.gitignore` inclui `.env` e `node_modules/`
</verification>

<success_criteria>
Monorepo pnpm operacional com workspaces `apps/api` e `apps/worker`. Fastify e BullMQ instalados com versoes corretas. Env vars validadas por envalid na boot. Vitest configurado com 2 stubs passando. Husky + lint-staged configurado para pre-commit.
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-01-SUMMARY.md`
</output>
