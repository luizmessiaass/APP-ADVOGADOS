---
phase: 01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics
plan: 06
title: Health Endpoint + BullMQ Worker Process
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-04"
files_modified:
  - apps/api/src/routes/health.ts
  - apps/api/src/routes/health.test.ts
  - apps/worker/src/worker.ts
  - apps/worker/src/queues/health-check.ts
  - apps/worker/src/config.ts
  - apps/api/src/server.ts
autonomous: true
requirements:
  - INFRA-08
  - INFRA-09
must_haves:
  truths:
    - "GET /health retorna 200 quando Supabase, Redis e DataJud estao acessiveis"
    - "GET /health retorna 503 quando qualquer dependencia esta degradada, com checks individiais no body"
    - "Worker BullMQ inicia como processo separado (apps/worker/src/worker.ts) com job placeholder"
    - "Worker usa cliente Redis com maxRetriesPerRequest: null (obrigatorio para BullMQ)"
    - "Worker loga startup com pino e captura erros com Sentry (tag source=worker)"
  artifacts:
    - path: "apps/api/src/routes/health.ts"
      provides: "GET /health endpoint com checks de Supabase, Redis, DataJud"
      exports: ["healthRoutes"]
    - path: "apps/worker/src/worker.ts"
      provides: "Entry point do worker BullMQ — processo separado do servidor API"
    - path: "apps/worker/src/queues/health-check.ts"
      provides: "Placeholder job BullMQ para validar infraestrutura do worker"
  key_links:
    - from: "apps/worker/src/worker.ts"
      to: "ioredis (REDIS_URL)"
      via: "createBullMQRedisClient() com maxRetriesPerRequest: null"
      pattern: "maxRetriesPerRequest.*null"
    - from: "apps/api/src/routes/health.ts"
      to: "supabaseAdmin.from('escritorios').select('id').limit(1)"
      via: "ping de conectividade ao Supabase"
      pattern: "from.*escritorios.*select"
---

<objective>
Implementa o endpoint `GET /health` que verifica Supabase, Redis e DataJud, e cria o processo worker BullMQ separado com um job placeholder que valida a infraestrutura de filas. O worker sera' expandido em Phase 2 com jobs DataJud.

Purpose: O health endpoint e' requisito para Railway health checks e monitoramento. O worker precisa existir como processo separado desde Phase 1 para que Phase 2 apenas adicione consumers ao worker existente.
Output: /health funcional + worker separado rodando + testes do health passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-CONTEXT.md
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-RESEARCH.md
</context>

<interfaces>
<!-- Tipos exportados de plans anteriores que esta plan consome -->

De apps/api/src/lib/supabase.ts (Plan 04):
  export const supabaseAdmin: SupabaseClient

De apps/api/src/lib/redis.ts (Plan 04):
  export function createRedisClient(): Redis        // maxRetriesPerRequest: 3
  export function createBullMQRedisClient(): Redis  // maxRetriesPerRequest: null (OBRIGATORIO BullMQ)

GET /health response shape:
  200: { status: 'ok', checks: { supabase: 'ok', redis: 'ok', datajud: 'ok' } }
  503: { status: 'degraded', checks: { supabase: 'ok', redis: 'error', datajud: 'ok' } }

DataJud base URL para connectivity check: 'https://api-publica.datajud.cnj.jus.br/'
  AbortSignal.timeout(3000) — nao bloquear health check por mais de 3 segundos
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar GET /health endpoint com checks de dependencias</name>
  <files>
    apps/api/src/routes/health.ts,
    apps/api/src/routes/health.test.ts
  </files>
  <behavior>
    - Test 1: Retorna 200 com status=ok quando todos os checks passam (mocks retornam sucesso)
    - Test 2: Retorna 503 com status=degraded quando Redis falha (mock rejeita ping)
    - Test 3: Retorna 503 com status=degraded quando Supabase falha (mock rejeita query)
    - Test 4: Body inclui checks individuais (supabase, redis, datajud) para diagnostico
    - Test 5: Rota nao exige autenticacao (config skipAuth: true)
  </behavior>
  <action>
1. Criar `apps/api/src/routes/health.ts`:
```typescript
import type { FastifyInstance } from 'fastify'
import { supabaseAdmin } from '../lib/supabase.js'
import { createRedisClient } from '../lib/redis.js'

type CheckStatus = 'ok' | 'error'

interface HealthChecks {
  supabase: CheckStatus
  redis: CheckStatus
  datajud: CheckStatus
}

export async function healthRoutes(app: FastifyInstance): Promise<void> {
  app.get('/health', { config: { skipAuth: true } }, async (_req, reply) => {
    const checks: HealthChecks = {
      supabase: 'error',
      redis: 'error',
      datajud: 'error',
    }

    // Check 1: Supabase — query simples para validar conectividade
    try {
      const { error } = await supabaseAdmin
        .from('escritorios')
        .select('id')
        .limit(1)
      checks.supabase = error ? 'error' : 'ok'
    } catch {
      checks.supabase = 'error'
    }

    // Check 2: Redis — PING
    const redis = createRedisClient()
    try {
      await redis.connect()
      const pong = await redis.ping()
      checks.redis = pong === 'PONG' ? 'ok' : 'error'
    } catch {
      checks.redis = 'error'
    } finally {
      await redis.quit().catch(() => {})
    }

    // Check 3: DataJud — connectivity check com timeout de 3s
    try {
      const res = await fetch('https://api-publica.datajud.cnj.jus.br/', {
        method: 'HEAD',
        signal: AbortSignal.timeout(3000),
      })
      checks.datajud = res.ok || res.status === 405 ? 'ok' : 'error'  // 405 e' esperado para HEAD
    } catch {
      checks.datajud = 'error'
    }

    const allOk = Object.values(checks).every((v) => v === 'ok')

    return reply
      .code(allOk ? 200 : 503)
      .send({
        status: allOk ? 'ok' : 'degraded',
        checks,
        timestamp: new Date().toISOString(),
      })
  })
}
```

2. Registrar healthRoutes no server.ts (adicionar import e register no buildApp()):
```typescript
// Em apps/api/src/server.ts, adicionar:
import { healthRoutes } from './routes/health.js'
// Dentro do buildApp(), antes dos outros registers:
app.register(healthRoutes)
```

3. Criar `apps/api/src/routes/health.test.ts` com mocks dos 5 comportamentos:
- Mockar supabaseAdmin (vi.mock)
- Mockar ioredis Redis (vi.mock)
- Mockar fetch global para DataJud
- Testar que checks individuais aparecem no body mesmo em degraded state

RESTRICOES:
- skipAuth: true — /health e' publico (sem autenticacao)
- Timeout de 3s no DataJud check para nao bloquear o health check
- Criar e fechar o Redis client por request (nao reutilizar conexao global no health check)
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && npx vitest run apps/api/src/routes/health.test.ts --config backend/vitest.config.ts --reporter=verbose 2>&1 | tail -15</automated>
  </verify>
  <done>
    - `health.ts` exporta healthRoutes com GET /health sem autenticacao
    - Todos os 5 testes do health passam
    - Body inclui checks.supabase, checks.redis, checks.datajud individualmente
    - Retorna 200 quando todos ok, 503 quando qualquer um falha
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar BullMQ worker como processo separado com placeholder job</name>
  <files>
    apps/worker/src/worker.ts,
    apps/worker/src/queues/health-check.ts,
    apps/worker/src/config.ts
  </files>
  <action>
1. Criar `apps/worker/src/config.ts` (subset de env vars para o worker):
```typescript
import { cleanEnv, str, url } from 'envalid'

export const env = cleanEnv(process.env, {
  NODE_ENV:                  str({ choices: ['development', 'production', 'test'], default: 'development' }),
  SUPABASE_URL:              url(),
  SUPABASE_SERVICE_ROLE_KEY: str(),
  REDIS_URL:                 url(),
  SENTRY_DSN:                str({ default: '' }),
})
```

2. Criar `apps/worker/src/queues/health-check.ts` (placeholder job — sera expandido em Phase 2):
```typescript
import { Worker, type Job } from 'bullmq'
import type Redis from 'ioredis'

export const HEALTH_CHECK_QUEUE = 'health-check'

export interface HealthCheckJobData {
  triggered_at: string
}

// Cria um Worker BullMQ para o health-check queue
// Phase 1: apenas loga que o job foi processado
// Phase 2+: este arquivo sera substituido por datajud-sync.ts
export function createHealthCheckWorker(redis: Redis): Worker<HealthCheckJobData> {
  return new Worker<HealthCheckJobData>(
    HEALTH_CHECK_QUEUE,
    async (job: Job<HealthCheckJobData>) => {
      console.log(`[health-check] Job ${job.id} processado. triggered_at: ${job.data.triggered_at}`)
      // Placeholder — sem logica de negocio em Phase 1
      return { success: true, processed_at: new Date().toISOString() }
    },
    {
      connection: redis,
      // Concorrencia 1 para o placeholder — DataJud worker usara concorrencia maior
      concurrency: 1,
    }
  )
}
```

3. Criar `apps/worker/src/worker.ts` (entry point do processo separado):
```typescript
// Entry point do BullMQ Worker — roda como processo Railway separado (D-31)
// NAO importar nada do apps/api aqui — processos separados

import * as Sentry from '@sentry/node'
import pino from 'pino'
import { env } from './config.js'
import { createHealthCheckWorker, HEALTH_CHECK_QUEUE } from './queues/health-check.js'
import Redis from 'ioredis'

// Logger do worker
const logger = pino({
  level: env.NODE_ENV === 'production' ? 'info' : 'debug',
  ...(env.NODE_ENV !== 'test' && env.NODE_ENV !== 'production'
    ? { transport: { target: 'pino-pretty', options: { colorize: true } } }
    : {}
  ),
})

// Inicializar Sentry para o worker — tag source=worker (D-29)
if (env.SENTRY_DSN) {
  Sentry.init({
    dsn: env.SENTRY_DSN,
    environment: env.NODE_ENV,
    initialScope: { tags: { source: 'worker' } },
    sendDefaultPii: false,
  })
}

// Cliente Redis para BullMQ — maxRetriesPerRequest: null e' OBRIGATORIO (Pitfall 5, RESEARCH.md)
const redisConnection = new Redis(env.REDIS_URL, {
  maxRetriesPerRequest: null,
  enableReadyCheck: false,
})

// Instanciar workers
const healthCheckWorker = createHealthCheckWorker(redisConnection)

logger.info(
  { queue: HEALTH_CHECK_QUEUE },
  '[worker] BullMQ worker iniciado. Aguardando jobs...'
)

// Graceful shutdown
async function shutdown(signal: string): Promise<void> {
  logger.info(`[worker] Recebeu ${signal}. Encerrando graciosamente...`)
  await healthCheckWorker.close()
  await redisConnection.quit()
  logger.info('[worker] Encerrado.')
  process.exit(0)
}

process.on('SIGTERM', () => shutdown('SIGTERM'))
process.on('SIGINT', () => shutdown('SIGINT'))

// Capturar erros nao tratados
process.on('uncaughtException', (err) => {
  Sentry.captureException(err)
  logger.error({ err }, '[worker] Erro nao capturado — encerrando')
  process.exit(1)
})

process.on('unhandledRejection', (reason) => {
  Sentry.captureException(reason)
  logger.error({ reason }, '[worker] Promise rejection nao tratada — encerrando')
  process.exit(1)
})
```

4. Atualizar `apps/worker/package.json` para adicionar dependencia pino-pretty como devDep:
```json
// Adicionar em devDependencies:
"pino-pretty": "13.1.3"
```

5. Criar `railway.toml` na raiz para configuracao de deploy Railway (D-02):
```toml
[build]
  builder = "NIXPACKS"

[[services]]
  name = "api"
  source = "apps/api"
  
  [services.deploy]
    startCommand = "node dist/server.js"
    healthcheckPath = "/health"
    healthcheckTimeout = 10
    restartPolicyType = "ON_FAILURE"

[[services]]
  name = "worker"
  source = "apps/worker"
  
  [services.deploy]
    startCommand = "node dist/worker.js"
    restartPolicyType = "ON_FAILURE"
```

RESTRICOES:
- Worker e' processo SEPARADO — NAO importar nada de apps/api no worker
- Redis para BullMQ usa maxRetriesPerRequest: null — OBRIGATORIO (BullMQ exige isso)
- Graceful shutdown implementado para SIGTERM e SIGINT (Railway envia SIGTERM ao fazer deploy)
- Sentry no worker usa tag source=worker (diferente da API que usa source=api) — D-29
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && ls apps/worker/src/worker.ts apps/worker/src/queues/health-check.ts && grep "maxRetriesPerRequest: null" apps/worker/src/worker.ts && echo "worker OK"</automated>
  </verify>
  <done>
    - `apps/worker/src/worker.ts` existe como processo separado (sem imports de apps/api)
    - `apps/worker/src/queues/health-check.ts` define placeholder job BullMQ
    - Redis usa maxRetriesPerRequest: null (obrigatorio BullMQ)
    - Graceful shutdown implementado para SIGTERM/SIGINT
    - `railway.toml` define dois servicos separados (api + worker)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → GET /health | Publico — nao expor informacao sensivel na resposta |
| Worker process → Redis | Conexao persistente; erros de Redis nao devem derrubar o worker |
| Worker → Supabase | service_role — usar apenas para operacoes de background |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-06-I | Info Disclosure | GET /health response | mitigate | Health response expoe apenas status (ok/error) das dependencias, nao detalhes de erro, IPs, ou versoes de servico |
| T-1-06-D | Denial of Service | DataJud timeout no health | mitigate | AbortSignal.timeout(3000) — health check nao bloqueia por mais de 3s esperando DataJud |
| T-1-06-D | Denial of Service | Redis connection exhaustion | mitigate | createRedisClient() por request no health check, com quit() no finally; nao reutilizar conexao global |
| T-1-06-T | Tampering | BullMQ job data | accept | Phase 1 usa apenas placeholder job sem dados sensiveis; validacao de job data sera adicionada em Phase 2 |
</threat_model>

<verification>
- `npx vitest run apps/api/src/routes/health.test.ts --config backend/vitest.config.ts` — 5 testes passando
- `grep "skipAuth: true" apps/api/src/routes/health.ts` — rota publica confirmada
- `grep "maxRetriesPerRequest: null" apps/worker/src/worker.ts` — configuracao BullMQ correta
- `grep "source.*worker" apps/worker/src/worker.ts` — tag Sentry source=worker presente
- `ls railway.toml` — arquivo de deploy existe
</verification>

<success_criteria>
GET /health retorna 200/503 com checks individuais de Supabase, Redis e DataJud. Worker BullMQ iniciado como processo separado com placeholder job, graceful shutdown, e Sentry configurado com source=worker. Railway.toml define dois servicos separados.
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-06-SUMMARY.md`
</output>
