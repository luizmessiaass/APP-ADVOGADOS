---
phase: 01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics
plan: 05
title: Auth Routes + LGPD Consent Endpoint
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-04"
files_modified:
  - apps/api/src/routes/auth/index.ts
  - apps/api/src/routes/auth/auth.test.ts
  - apps/api/src/routes/lgpd/index.ts
  - apps/api/src/routes/lgpd/lgpd.test.ts
  - apps/api/src/server.ts
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-09
  - LGPD-01
  - LGPD-06
must_haves:
  truths:
    - "POST /api/v1/auth/signup/escritorio cria escritorio (tenant) + usuario admin no Supabase Auth"
    - "POST /api/v1/auth/login retorna JWT Supabase com tenant_id e role no app_metadata"
    - "POST /api/v1/auth/logout invalida a sessao do usuario (revoga refresh token)"
    - "POST /api/v1/auth/invite cria cliente via inviteUserByEmail passando tenant_id no metadata"
    - "POST /api/v1/lgpd/consentimento registra opt-in com timestamp, versao_termos, ip_origem, user_agent"
    - "Resposta de auth menciona URL da politica de privacidade (LGPD-06)"
    - "Formato de erro padrao: { success: false, error: string, code: string } em todos os endpoints"
  artifacts:
    - path: "apps/api/src/routes/auth/index.ts"
      provides: "Endpoints: signup/escritorio, login, logout, invite"
      exports: ["authRoutes"]
    - path: "apps/api/src/routes/lgpd/index.ts"
      provides: "Endpoint POST /consentimento para registro de consentimento LGPD"
      exports: ["lgpdRoutes"]
  key_links:
    - from: "apps/api/src/routes/auth/index.ts"
      to: "supabase.auth.admin.inviteUserByEmail"
      via: "tenant_id passado no metadata do convite"
      pattern: "inviteUserByEmail.*tenant_id"
    - from: "apps/api/src/routes/lgpd/index.ts"
      to: "public.lgpd_consentimentos"
      via: "supabaseAsUser(jwt).from('lgpd_consentimentos').insert()"
      pattern: "lgpd_consentimentos"
---

<objective>
Cria os endpoints de autenticacao (`/api/v1/auth/*`) e o endpoint de consentimento LGPD (`/api/v1/lgpd/consentimento`). Os endpoints de auth usam o Supabase Auth SDK; o invite passa `tenant_id` no metadata para o trigger `sync_auth_user_to_public()` funcionar corretamente. A politica de privacidade e' referenciada na resposta de auth (LGPD-06).

Purpose: Escritorios e clientes precisam de endpoints funcionais de signup/login/logout para que os testes de integracao nas plans subsequentes possam autenticar.
Output: 4 endpoints auth + 1 endpoint LGPD, todos com TypeBox validation e formato de erro padrao.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-CONTEXT.md
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-RESEARCH.md
</context>

<interfaces>
<!-- Tipos de entrada/saida para TypeBox validation (D-20) -->

POST /api/v1/auth/signup/escritorio body:
  { nome: string, email: string, senha: string }
  Response 201: { success: true, message: string, escritorio_id: string, privacy_policy_url: string }

POST /api/v1/auth/login body:
  { email: string, senha: string }
  Response 200: { success: true, access_token: string, refresh_token: string, user: { id, email, role, tenant_id } }
  Response 400: { success: false, error: string, code: "INVALID_CREDENTIALS" }

POST /api/v1/auth/logout (autenticado)
  Response 200: { success: true, message: string }

POST /api/v1/auth/invite (autenticado, apenas admin_escritorio)
  body: { email: string, nome: string, cpf?: string, role_local: 'advogado' | 'cliente' }
  Response 201: { success: true, message: string }
  CRITICO: passar tenant_id no metadata para evitar Pitfall 6 (RESEARCH.md)

POST /api/v1/lgpd/consentimento (autenticado)
  body: { versao_termos: string }  // ISO date string, ex: "2026-04-14"
  Response 201: { success: true, consentimento_id: string }

Privacy policy URL (D-25): process.env.PRIVACY_POLICY_URL || 'https://notion.so/portaljuridico-privacidade'
  Adicionar PRIVACY_POLICY_URL como env var opcional em config.ts

TenantUser interface (de Plan 04):
  { sub: string, tenant_id: string, role: 'admin_escritorio' | 'advogado' | 'cliente' }
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar auth routes e lgpd routes com TypeBox validation</name>
  <files>
    apps/api/src/routes/auth/index.ts,
    apps/api/src/routes/auth/auth.test.ts,
    apps/api/src/routes/lgpd/index.ts,
    apps/api/src/routes/lgpd/lgpd.test.ts
  </files>
  <behavior>
    - Test 1: POST /api/v1/auth/signup/escritorio com dados validos retorna 201 com escritorio_id e privacy_policy_url
    - Test 2: POST /api/v1/auth/signup/escritorio com email duplicado retorna 400 com code USER_ALREADY_EXISTS
    - Test 3: POST /api/v1/auth/login com credenciais invalidas retorna 400 com code INVALID_CREDENTIALS
    - Test 4: POST /api/v1/auth/login com credenciais validas retorna 200 com access_token (formato nao vazio)
    - Test 5: POST /api/v1/auth/invite sem ser admin_escritorio retorna 403 FORBIDDEN_ROLE
    - Test 6: POST /api/v1/auth/invite por admin_escritorio inclui tenant_id nos metadados do convite
    - Test 7: POST /api/v1/lgpd/consentimento retorna 201 com consentimento_id
    - Test 8: GET /api/v1/lgpd/consentimento retorna historico de consentimentos do usuario autenticado
  </behavior>
  <action>
1. Atualizar `apps/api/src/config.ts` para adicionar PRIVACY_POLICY_URL:
```typescript
// Adicionar ao cleanEnv existente:
PRIVACY_POLICY_URL: str({ default: 'https://notion.so/portaljuridico-privacidade' }),
```

2. Criar `apps/api/src/routes/auth/index.ts`:
```typescript
import type { FastifyInstance } from 'fastify'
import { Type } from '@sinclair/typebox'
import { supabaseAdmin, supabaseAsUser } from '../../lib/supabase.js'
import { env } from '../../config.js'

const SignupEscritorioBody = Type.Object({
  nome:  Type.String({ minLength: 2, maxLength: 200 }),
  email: Type.String({ format: 'email' }),
  senha: Type.String({ minLength: 8, maxLength: 128 }),
})

const LoginBody = Type.Object({
  email: Type.String({ format: 'email' }),
  senha: Type.String({ minLength: 1 }),
})

const InviteBody = Type.Object({
  email:      Type.String({ format: 'email' }),
  nome:       Type.String({ minLength: 2 }),
  role_local: Type.Union([Type.Literal('advogado'), Type.Literal('cliente')]),
  cpf:        Type.Optional(Type.String()),
})

export async function authRoutes(app: FastifyInstance): Promise<void> {
  // POST /api/v1/auth/signup/escritorio
  // Cria escritorio (tenant) + usuario admin. Status inicial: pending (D-07).
  // Sem skipAuth: false porque ainda nao existe autenticacao.
  app.post('/signup/escritorio', {
    config: { skipAuth: true },
    schema: { body: SignupEscritorioBody },
  }, async (req, reply) => {
    const { nome, email, senha } = req.body as typeof SignupEscritorioBody.static

    // 1. Criar usuario no Supabase Auth
    const { data: authData, error: authError } = await supabaseAdmin.auth.admin.createUser({
      email,
      password: senha,
      email_confirm: true,   // confirmar email automaticamente (simplifica dev)
      user_metadata: {},
      app_metadata: {},
    })

    if (authError) {
      if (authError.message.includes('already registered')) {
        return reply.code(400).send({
          success: false,
          error: 'Email ja cadastrado',
          code: 'USER_ALREADY_EXISTS',
        })
      }
      req.log.error({ error: authError.message }, 'Erro ao criar usuario')
      return reply.code(500).send({ success: false, error: 'Erro interno', code: 'SIGNUP_ERROR' })
    }

    // 2. Criar escritorio com status pending (aprovacao manual pelo admin do produto, D-07)
    const { data: escritorio, error: escrError } = await supabaseAdmin
      .from('escritorios')
      .insert({ nome, email, status: 'pending' })
      .select('id')
      .single()

    if (escrError || !escritorio) {
      req.log.error({ error: escrError?.message }, 'Erro ao criar escritorio')
      // Rollback: deletar usuario criado
      await supabaseAdmin.auth.admin.deleteUser(authData.user.id)
      return reply.code(500).send({ success: false, error: 'Erro interno', code: 'ESCRITORIO_ERROR' })
    }

    // 3. Atualizar public.usuarios com tenant_id e role_local (trigger cria o registro, aqui fazemos update)
    await supabaseAdmin
      .from('usuarios')
      .update({ tenant_id: escritorio.id, role_local: 'admin_escritorio' })
      .eq('id', authData.user.id)

    return reply.code(201).send({
      success: true,
      message: 'Escritorio criado. Aguardando aprovacao do administrador do Portal Juridico.',
      escritorio_id: escritorio.id,
      privacy_policy_url: env.PRIVACY_POLICY_URL,  // LGPD-06
    })
  })

  // POST /api/v1/auth/login
  app.post('/login', {
    config: { skipAuth: true },
    schema: { body: LoginBody },
  }, async (req, reply) => {
    const { email, senha } = req.body as typeof LoginBody.static

    // Usar supabaseAdmin para fazer signIn (a partir do SDK server-side)
    const { data, error } = await supabaseAdmin.auth.signInWithPassword({ email, password: senha })

    if (error || !data.session) {
      return reply.code(400).send({
        success: false,
        error: 'Email ou senha incorretos',
        code: 'INVALID_CREDENTIALS',
      })
    }

    return reply.send({
      success: true,
      access_token: data.session.access_token,
      refresh_token: data.session.refresh_token,
      user: {
        id: data.user.id,
        email: data.user.email,
        role: data.user.app_metadata?.role ?? null,
        tenant_id: data.user.app_metadata?.tenant_id ?? null,
      },
    })
  })

  // POST /api/v1/auth/logout (autenticado)
  app.post('/logout', async (req, reply) => {
    const token = req.headers.authorization?.slice(7) ?? ''
    const client = supabaseAsUser(token)
    await client.auth.signOut()  // revoga refresh token

    return reply.send({ success: true, message: 'Logout realizado com sucesso' })
  })

  // POST /api/v1/auth/invite (apenas admin_escritorio)
  app.post('/invite', {
    schema: { body: InviteBody },
  }, async (req, reply) => {
    // Somente admin_escritorio pode convidar usuarios
    if (req.user.role !== 'admin_escritorio') {
      return reply.code(403).send({
        success: false,
        error: 'Apenas administradores podem convidar usuarios',
        code: 'FORBIDDEN_ROLE',
      })
    }

    const { email, nome, role_local, cpf } = req.body as typeof InviteBody.static

    // CRITICO (Pitfall 6, RESEARCH.md): passar tenant_id e role_local nos metadados
    // O trigger sync_auth_user_to_public() le esses dados ao criar o registro em public.usuarios
    const { error } = await supabaseAdmin.auth.admin.inviteUserByEmail(email, {
      data: {
        tenant_id: req.user.tenant_id,
        role_local,
        nome,
        cpf: cpf ?? null,  // CPF e' armazenado; nunca vai para logs (pino redact) nem prompts (LGPD-03)
      },
    })

    if (error) {
      req.tenantLogger.error({ error: error.message }, 'Erro ao convidar usuario')
      return reply.code(500).send({ success: false, error: 'Erro ao enviar convite', code: 'INVITE_ERROR' })
    }

    return reply.code(201).send({
      success: true,
      message: `Convite enviado para ${email}. O usuario recebera um email para criar sua senha.`,
    })
  })
}
```

3. Criar `apps/api/src/routes/lgpd/index.ts`:
```typescript
import type { FastifyInstance } from 'fastify'
import { Type } from '@sinclair/typebox'
import { supabaseAsUser } from '../../lib/supabase.js'
import { env } from '../../config.js'

const ConsentimentoBody = Type.Object({
  versao_termos: Type.String({ pattern: '^\\d{4}-\\d{2}-\\d{2}$' }), // ISO date: 2026-04-14
})

export async function lgpdRoutes(app: FastifyInstance): Promise<void> {
  // POST /api/v1/lgpd/consentimento — registra opt-in
  app.post('/consentimento', {
    schema: { body: ConsentimentoBody },
  }, async (req, reply) => {
    const { versao_termos } = req.body as typeof ConsentimentoBody.static
    const token = req.headers.authorization?.slice(7) ?? ''
    const db = supabaseAsUser(token)  // RLS enforced: usuario so insere o proprio consentimento

    // ip_origem e user_agent para auditoria ANPD (D-23)
    const ip_origem = req.ip
    const user_agent = req.headers['user-agent'] ?? null

    const { data, error } = await db
      .from('lgpd_consentimentos')
      .insert({
        usuario_id: req.user.sub,
        versao_termos,
        ip_origem,
        user_agent,
      })
      .select('id')
      .single()

    if (error) {
      req.tenantLogger.error({ error: error.message }, 'Erro ao registrar consentimento LGPD')
      return reply.code(500).send({ success: false, error: 'Erro ao registrar consentimento', code: 'CONSENT_ERROR' })
    }

    return reply.code(201).send({
      success: true,
      consentimento_id: data.id,
      privacy_policy_url: env.PRIVACY_POLICY_URL,
    })
  })

  // GET /api/v1/lgpd/consentimento — historico do usuario
  app.get('/consentimento', async (req, reply) => {
    const token = req.headers.authorization?.slice(7) ?? ''
    const db = supabaseAsUser(token)

    const { data, error } = await db
      .from('lgpd_consentimentos')
      .select('id, versao_termos, consentido_em, revogado_em')
      .eq('usuario_id', req.user.sub)
      .order('consentido_em', { ascending: false })

    if (error) {
      return reply.code(500).send({ success: false, error: 'Erro ao buscar consentimentos', code: 'FETCH_ERROR' })
    }

    return reply.send({ success: true, consentimentos: data ?? [] })
  })
}
```

4. Criar testes stub em `auth.test.ts` e `lgpd.test.ts` que mock o Supabase e testam os 8 comportamentos declarados. Os testes usam `app.inject()` do Fastify para chamadas HTTP.

5. Atualizar `apps/api/src/server.ts` para registrar as rotas:
```typescript
// Adicionar imports e registros no buildApp():
import { authRoutes } from './routes/auth/index.js'
import { lgpdRoutes } from './routes/lgpd/index.js'

// Dentro do buildApp(), apos registrar plugins:
app.register(authRoutes, { prefix: '/api/v1/auth' })
app.register(lgpdRoutes, { prefix: '/api/v1/lgpd' })
```

RESTRICOES:
- Usar supabaseAsUser(jwt) em handlers autenticados — NUNCA supabaseAdmin diretamente em handlers de usuario
- Formato de erro SEMPRE { success: false, error: string, code: string } (D-21)
- Privacy policy URL em toda resposta de auth onde relevante (LGPD-06)
- invite DEVE passar tenant_id nos metadados (Pitfall 6 RESEARCH.md — conta orca sem tenant)
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && npx vitest run apps/api/src/routes --config backend/vitest.config.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `auth/index.ts` exporta authRoutes com 4 endpoints (signup, login, logout, invite)
    - `lgpd/index.ts` exporta lgpdRoutes com POST e GET /consentimento
    - Testes passam com mocks do Supabase Auth SDK
    - invite passa tenant_id nos metadados do convite (Pitfall 6 prevenido)
    - Formato de erro consistente em todos os endpoints
    - privacy_policy_url presente nas respostas relevantes
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /auth/signup | Dados de signup nao autenticados — validar com TypeBox |
| POST /auth/invite → inviteUserByEmail | Admin pode criar usuarios — restrito a role admin_escritorio |
| POST /lgpd/consentimento → public.lgpd_consentimentos | RLS garante que usuario so insere para si mesmo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-05-S | Spoofing | POST /auth/login | mitigate | Supabase Auth com rate limiting interno + @fastify/rate-limit no Fastify; nao expor detalhes de "usuario nao existe" |
| T-1-05-E | Elevation of Privilege | POST /auth/invite | mitigate | Verificacao explica de role === 'admin_escritorio' antes de chamar inviteUserByEmail; role nao pode ser forjado (vem do JWT verificado) |
| T-1-05-I | Info Disclosure | Resposta de login | mitigate | Resposta usa formato uniforme para credenciais invalidas — nao distingue "usuario nao existe" de "senha errada" (previne user enumeration) |
| T-1-05-T | Tampering | TypeBox validation ausente | mitigate | Todos os endpoints com body usam schema TypeBox; Fastify rejeita automaticamente com 400 se schema invalido |
| T-1-05-I | Info Disclosure | CPF no invite body | mitigate | pino redact inclui `*.cpf`; CPF nao aparece em logs mesmo que passado no body |
</threat_model>

<verification>
- `npx vitest run apps/api/src/routes --config backend/vitest.config.ts` reporta todos os testes passando
- `grep "FORBIDDEN_ROLE" apps/api/src/routes/auth/index.ts` — verificacao de role em invite existe
- `grep "tenant_id.*req.user.tenant_id" apps/api/src/routes/auth/index.ts` — tenant_id passado no invite
- `grep "privacy_policy_url" apps/api/src/routes/auth/index.ts apps/api/src/routes/lgpd/index.ts` — URL referenciada
- `grep "supabaseAdmin.*from\|insert\|update" apps/api/src/routes/auth/index.ts` — supabaseAdmin usado apenas em signup (operacao administrativa valida)
</verification>

<success_criteria>
4 endpoints de auth funcionais (signup, login, logout, invite) e 2 endpoints LGPD (POST e GET /consentimento). TypeBox validation em todos os endpoints com body. Formato de erro padrao D-21 em todos os endpoints. Privacy policy URL presente (LGPD-06). Testes unitarios passando com mocks.
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-05-SUMMARY.md`
</output>
