---
phase: 01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics
plan: 04
title: Fastify Server Core — Logger com PII Redaction, Sentry, Middleware de Tenant
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - apps/api/src/server.ts
  - apps/api/src/lib/supabase.ts
  - apps/api/src/lib/redis.ts
  - apps/api/src/plugins/auth.ts
  - apps/api/src/plugins/sentry.ts
autonomous: true
requirements:
  - INFRA-06
  - INFRA-07
  - AUTH-04
  - LGPD-03
  - LGPD-04
must_haves:
  truths:
    - "Servidor Fastify inicia e loga em JSON estruturado com tenant_id, user_id, request_id em cada request"
    - "CPF, senha e prompt_text nunca aparecem nos logs (pino redact por construcao)"
    - "Sentry captura erros com contexto de tenant e tag source=api"
    - "Requests sem token valido recebem 401 MISSING_TOKEN ou 401 INVALID_TOKEN"
    - "Requests com token valido mas sem tenant_id recebem 403 NO_TENANT_CONTEXT"
    - "req.user e req.tenantLogger estao disponiveis em todos os handlers apos o middleware"
    - "supabaseAdmin usa service_role; supabaseAsUser(jwt) usa anon key com JWT do usuario"
  artifacts:
    - path: "apps/api/src/server.ts"
      provides: "Fastify factory buildApp() com logger, plugins, hooks registrados"
      exports: ["buildApp", "startServer"]
    - path: "apps/api/src/lib/supabase.ts"
      provides: "Dois clientes Supabase: supabaseAdmin (service_role) e supabaseAsUser(jwt)"
      exports: ["supabaseAdmin", "supabaseAsUser"]
    - path: "apps/api/src/lib/redis.ts"
      provides: "ioredis client com maxRetriesPerRequest correto para BullMQ"
      exports: ["createRedisClient", "createBullMQRedisClient"]
    - path: "apps/api/src/plugins/auth.ts"
      provides: "Fastify preHandler: verifica JWT via JWKS, injeta req.user e req.tenantLogger"
      exports: ["authPlugin", "TenantUser interface"]
    - path: "apps/api/src/plugins/sentry.ts"
      provides: "Sentry inicializado com tag source=api e contexto de tenant em cada erro"
      exports: ["sentryPlugin"]
  key_links:
    - from: "apps/api/src/plugins/auth.ts"
      to: "${SUPABASE_URL}/auth/v1/.well-known/jwks.json"
      via: "createRemoteJWKSet (jose)"
      pattern: "createRemoteJWKSet"
    - from: "apps/api/src/server.ts"
      to: "apps/api/src/config.ts"
      via: "import { env } from './config.js'"
      pattern: "cleanEnv"
    - from: "apps/api/src/plugins/auth.ts"
      to: "apps/api/src/lib/supabase.ts"
      via: "supabaseAsUser(token) para RLS enforced"
      pattern: "supabaseAsUser"
---

<objective>
Implementa o nucleo do servidor Fastify: factory `buildApp()` com logger pino configurado (PII redaction + Betterstack transport), plugin Sentry, dois clientes Supabase (admin e user-scoped), cliente Redis, e o plugin de autenticacao que verifica JWTs Supabase via JWKS e injeta contexto de tenant em cada request.

Purpose: Este e' o "sistema nervoso" do backend — sem o middleware de tenant funcionando, nenhum endpoint pode ser autorizado corretamente.
Output: `buildApp()` inicia servidor com autenticacao multi-tenant funcional, logs sem PII, Sentry ativo.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-CONTEXT.md
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-RESEARCH.md
</context>

<interfaces>
<!-- Tipos que este plan exporta e que plans subsequentes (05, 06, 08) consomem -->

De apps/api/src/plugins/auth.ts (criado nesta plan):
```typescript
export interface TenantUser {
  sub: string          // user_id (auth.users.id)
  tenant_id: string    // uuid do escritorio
  role: 'admin_escritorio' | 'advogado' | 'cliente'
}

// Disponivel em qualquer handler como req.user e req.tenantLogger
declare module 'fastify' {
  interface FastifyRequest {
    user: TenantUser
    tenantLogger: FastifyBaseLogger
  }
}
```

De apps/api/src/lib/supabase.ts (criado nesta plan):
```typescript
export const supabaseAdmin: SupabaseClient  // service_role — bypassa RLS
export function supabaseAsUser(jwt: string): SupabaseClient  // RLS enforced
```

De apps/api/src/server.ts (criado nesta plan):
```typescript
export function buildApp(opts?: FastifyServerOptions): FastifyInstance
export async function startServer(): Promise<void>
```

Pino redact paths obrigatorios (D-26):
  - 'req.body.cpf'
  - 'req.body.password'
  - 'req.body.senha'
  - '*.cpf'
  - '*.prompt_text'
  - '*.senha'
  - 'req.headers.authorization'
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Criar lib/supabase.ts, lib/redis.ts e plugins/auth.ts</name>
  <files>
    apps/api/src/lib/supabase.ts,
    apps/api/src/lib/redis.ts,
    apps/api/src/plugins/auth.ts,
    apps/api/src/plugins/auth.test.ts
  </files>
  <behavior>
    - Test 1: Middleware rejeita request sem header Authorization com 401 e code MISSING_TOKEN
    - Test 2: Middleware rejeita token malformado com 401 e code INVALID_TOKEN
    - Test 3: Middleware rejeita JWT valido mas sem app_metadata.tenant_id com 403 e code NO_TENANT_CONTEXT
    - Test 4: Middleware com JWT valido decora req.user com sub, tenant_id, role corretos
    - Test 5: req.tenantLogger e' um child logger com campos tenant_id, user_id, request_id
  </behavior>
  <action>
1. Criar `apps/api/src/lib/supabase.ts` (dois clientes — anti-pattern service_role em handlers, ver RESEARCH.md):
```typescript
import { createClient, type SupabaseClient } from '@supabase/supabase-js'
import { env } from '../config.js'

// Cliente admin: bypassa RLS. NUNCA usar em request handlers.
// Uso legitimo: background jobs, webhooks verificados, admin endpoints com check manual.
export const supabaseAdmin: SupabaseClient = createClient(
  env.SUPABASE_URL,
  env.SUPABASE_SERVICE_ROLE_KEY,
  { auth: { autoRefreshToken: false, persistSession: false } }
)

// Cliente com contexto de usuario: RLS enforced pelo banco.
// Criar NOVO por request — NÃO compartilhar instancia entre requests.
// Usar em todos os route handlers para garantir isolamento de tenant por construcao.
export function supabaseAsUser(jwt: string): SupabaseClient {
  return createClient(
    env.SUPABASE_URL,
    env.SUPABASE_ANON_KEY,
    {
      global: { headers: { Authorization: `Bearer ${jwt}` } },
      auth: { autoRefreshToken: false, persistSession: false }
    }
  )
}
```

2. Criar `apps/api/src/lib/redis.ts`:
```typescript
import Redis from 'ioredis'
import { env } from '../config.js'

// Cliente Redis para uso geral pela API (health check, cache simples)
// maxRetriesPerRequest: 3 — evita bloquear a API em timeouts de Redis
export function createRedisClient(): Redis {
  return new Redis(env.REDIS_URL, {
    maxRetriesPerRequest: 3,
    enableReadyCheck: false,
    lazyConnect: true,
  })
}

// Cliente Redis para BullMQ — maxRetriesPerRequest: null e' OBRIGATORIO para BullMQ
// Ver Pitfall 5 no RESEARCH.md
export function createBullMQRedisClient(): Redis {
  return new Redis(env.REDIS_URL, {
    maxRetriesPerRequest: null,
    enableReadyCheck: false,
  })
}
```

3. Criar `apps/api/src/plugins/auth.ts`:
```typescript
import fp from 'fastify-plugin'
import { createRemoteJWKSet, jwtVerify, type JWTPayload } from 'jose'
import type { FastifyPluginAsync, FastifyBaseLogger } from 'fastify'
import { env } from '../config.js'

export interface TenantUser {
  sub: string
  tenant_id: string
  role: 'admin_escritorio' | 'advogado' | 'cliente'
}

// JWKS cache: createRemoteJWKSet ja faz cache interno — NAO recriar por request
const JWKS = createRemoteJWKSet(
  new URL(`${env.SUPABASE_URL}/auth/v1/.well-known/jwks.json`)
)

declare module 'fastify' {
  interface FastifyRequest {
    user: TenantUser
    tenantLogger: FastifyBaseLogger
  }
}

const authPlugin: FastifyPluginAsync = async (fastify) => {
  fastify.decorateRequest('user', null)
  fastify.decorateRequest('tenantLogger', null)

  fastify.addHook('preHandler', async (request, reply) => {
    // Rotas com skipAuth: true pulam autenticacao (ex: /health)
    if ((request.routeOptions?.config as Record<string, unknown>)?.skipAuth === true) {
      return
    }

    const authHeader = request.headers.authorization
    if (!authHeader?.startsWith('Bearer ')) {
      return reply.code(401).send({
        success: false,
        error: 'Token ausente',
        code: 'MISSING_TOKEN',
      })
    }

    try {
      const token = authHeader.slice(7)
      const { payload } = await jwtVerify(token, JWKS, {
        issuer: `${env.SUPABASE_URL}/auth/v1`,
        audience: 'authenticated',
      })

      const appMeta = (payload as JWTPayload & { app_metadata?: Record<string, string> }).app_metadata

      if (!appMeta?.tenant_id) {
        return reply.code(403).send({
          success: false,
          error: 'tenant_id ausente no token. Hook Custom Access Token pode nao estar ativo.',
          code: 'NO_TENANT_CONTEXT',
        })
      }

      request.user = {
        sub: payload.sub!,
        tenant_id: appMeta.tenant_id,
        role: appMeta.role as TenantUser['role'],
      }

      // Child logger com contexto de tenant — propagado automaticamente em todos os logs do handler
      request.tenantLogger = request.log.child({
        tenant_id: appMeta.tenant_id,
        user_id: payload.sub,
        request_id: request.id,
      })

    } catch {
      return reply.code(401).send({
        success: false,
        error: 'Token invalido ou expirado',
        code: 'INVALID_TOKEN',
      })
    }
  })
}

export default fp(authPlugin, { name: 'auth' })
```

4. Criar `apps/api/src/plugins/auth.test.ts` com os 5 comportamentos declarados:
```typescript
import { describe, it, expect, beforeAll, afterAll } from 'vitest'
import Fastify from 'fastify'
import authPlugin from './auth.js'
import * as jose from 'jose'

// Mock do JWKS — nao chamar endpoint real em testes
vi.mock('jose', async (importOriginal) => {
  const actual = await importOriginal<typeof jose>()
  return {
    ...actual,
    createRemoteJWKSet: vi.fn(() => vi.fn()),
    jwtVerify: vi.fn(),
  }
})

const mockJwtVerify = jose.jwtVerify as ReturnType<typeof vi.fn>

describe('Auth Plugin (AUTH-04)', () => {
  let app: ReturnType<typeof Fastify>

  beforeAll(async () => {
    app = Fastify()
    await app.register(authPlugin)
    app.get('/protected', async (req, reply) => {
      return reply.send({ user: req.user, hasLogger: !!req.tenantLogger })
    })
    app.get('/health', { config: { skipAuth: true } }, async (_req, reply) => {
      return reply.send({ status: 'ok' })
    })
    await app.ready()
  })

  afterAll(() => app.close())

  it('rejeita request sem Authorization header com 401 MISSING_TOKEN', async () => {
    const res = await app.inject({ method: 'GET', url: '/protected' })
    expect(res.statusCode).toBe(401)
    expect(JSON.parse(res.body).code).toBe('MISSING_TOKEN')
  })

  it('rejeita token malformado com 401 INVALID_TOKEN', async () => {
    mockJwtVerify.mockRejectedValueOnce(new Error('invalid token'))
    const res = await app.inject({
      method: 'GET', url: '/protected',
      headers: { authorization: 'Bearer token-invalido' }
    })
    expect(res.statusCode).toBe(401)
    expect(JSON.parse(res.body).code).toBe('INVALID_TOKEN')
  })

  it('rejeita JWT sem app_metadata.tenant_id com 403 NO_TENANT_CONTEXT', async () => {
    mockJwtVerify.mockResolvedValueOnce({
      payload: { sub: 'user-123', app_metadata: {} }
    })
    const res = await app.inject({
      method: 'GET', url: '/protected',
      headers: { authorization: 'Bearer token-sem-tenant' }
    })
    expect(res.statusCode).toBe(403)
    expect(JSON.parse(res.body).code).toBe('NO_TENANT_CONTEXT')
  })

  it('decora req.user com sub, tenant_id, role corretos', async () => {
    mockJwtVerify.mockResolvedValueOnce({
      payload: {
        sub: 'user-abc',
        app_metadata: { tenant_id: 'tenant-xyz', role: 'admin_escritorio' }
      }
    })
    const res = await app.inject({
      method: 'GET', url: '/protected',
      headers: { authorization: 'Bearer token-valido' }
    })
    expect(res.statusCode).toBe(200)
    const body = JSON.parse(res.body)
    expect(body.user.sub).toBe('user-abc')
    expect(body.user.tenant_id).toBe('tenant-xyz')
    expect(body.user.role).toBe('admin_escritorio')
    expect(body.hasLogger).toBe(true)
  })

  it('rota com skipAuth: true nao executa o preHandler de auth', async () => {
    const res = await app.inject({ method: 'GET', url: '/health' })
    expect(res.statusCode).toBe(200)
  })
})
```

RESTRICOES:
- JWKS criado UMA VEZ (singleton) — nao recriar por request (anti-pattern RESEARCH.md)
- Usar jose jwtVerify + JWKS (nao verificar JWT manualmente)
- Erros de JWT nao devem vazar detalhes do erro para o cliente (apenas code generico)
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && npx vitest run apps/api/src/plugins/auth.test.ts --config backend/vitest.config.ts --reporter=verbose 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `auth.test.ts` passa todos os 5 testes
    - `supabase.ts` exporta supabaseAdmin e supabaseAsUser(jwt)
    - `redis.ts` exporta createRedisClient() e createBullMQRedisClient() com maxRetriesPerRequest correto
    - `auth.ts` implementa preHandler que verifica JWT e decora req.user + req.tenantLogger
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar server.ts (buildApp) + plugins/sentry.ts com PII redaction</name>
  <files>
    apps/api/src/server.ts,
    apps/api/src/plugins/sentry.ts
  </files>
  <action>
1. Criar `apps/api/src/plugins/sentry.ts`:
```typescript
import fp from 'fastify-plugin'
import * as Sentry from '@sentry/node'
import type { FastifyPluginAsync } from 'fastify'
import { env } from '../config.js'

const sentryPlugin: FastifyPluginAsync = async (fastify) => {
  if (!env.SENTRY_DSN) {
    fastify.log.info('Sentry DSN nao configurado — erro tracking desabilitado')
    return
  }

  Sentry.init({
    dsn: env.SENTRY_DSN,
    environment: env.NODE_ENV,
    // Tag diferencia erros da API vs Worker no mesmo DSN (D-29)
    initialScope: {
      tags: { source: 'api' }
    },
    // Nao capturar PII automaticamente
    sendDefaultPii: false,
  })

  // Capturar erros nao tratados com contexto de tenant
  fastify.addHook('onError', async (request, _reply, error) => {
    Sentry.withScope((scope) => {
      if (request.user) {
        scope.setUser({ id: request.user.sub })
        scope.setTag('tenant_id', request.user.tenant_id)
        scope.setTag('role', request.user.role)
      }
      scope.setTag('request_id', request.id)
      Sentry.captureException(error)
    })
  })

  fastify.log.info('Sentry inicializado com tag source=api')
}

export default fp(sentryPlugin, { name: 'sentry' })
```

2. Criar `apps/api/src/server.ts` (factory `buildApp`):
```typescript
import Fastify, { type FastifyInstance, type FastifyServerOptions } from 'fastify'
import helmet from '@fastify/helmet'
import rateLimit from '@fastify/rate-limit'
import { env } from './config.js'
import authPlugin from './plugins/auth.js'
import sentryPlugin from './plugins/sentry.js'

export function buildApp(opts: FastifyServerOptions = {}): FastifyInstance {
  const app = Fastify({
    ...opts,
    logger: {
      level: env.NODE_ENV === 'production' ? 'info' : 'debug',
      // D-26: Redacao de PII obrigatoria — por construcao, nao por disciplina
      redact: {
        paths: [
          'req.body.cpf',
          'req.body.password',
          'req.body.senha',
          '*.cpf',
          '*.prompt_text',
          '*.senha',
          'req.headers.authorization',   // nao logar JWTs completos
        ],
        censor: '[REDACTED]',
      },
      // D-28: Betterstack em producao; pino-pretty em dev
      ...(env.NODE_ENV === 'production' && env.BETTERSTACK_SOURCE_TOKEN
        ? {
            transport: {
              target: '@logtail/pino',
              options: { sourceToken: env.BETTERSTACK_SOURCE_TOKEN }
            }
          }
        : env.NODE_ENV !== 'test'
        ? {
            transport: {
              target: 'pino-pretty',
              options: { colorize: true }
            }
          }
        : {}
      ),
    },
    // Gerar request_id automaticamente para rastreabilidade
    genReqId: () => crypto.randomUUID(),
  })

  // Security headers (D-22 rate limiting — limites altos em Phase 1)
  app.register(helmet)
  app.register(rateLimit, {
    max: 1000,
    timeWindow: '1 minute',
    // Em producao, usar tenant_id como key para isolamento de rate limit por tenant
    keyGenerator: (req) => {
      return req.user?.tenant_id ?? req.ip
    },
  })

  // Plugins de infraestrutura
  app.register(sentryPlugin)
  app.register(authPlugin)

  // Rota de health publica (sem autenticacao)
  app.get('/', { config: { skipAuth: true } }, async (_req, reply) => {
    return reply.send({ service: 'portaljuridico-api', version: '0.1.0' })
  })

  return app
}

export async function startServer(): Promise<void> {
  const app = buildApp()

  // Registrar rotas apos build (ver Plans 05 e 06)
  // app.register(healthRoutes)
  // app.register(authRoutes, { prefix: '/api/v1/auth' })
  // app.register(lgpdRoutes, { prefix: '/api/v1/lgpd' })

  try {
    await app.listen({ port: env.PORT, host: '0.0.0.0' })
    app.log.info(`Servidor iniciado na porta ${env.PORT}`)
  } catch (err) {
    app.log.error(err)
    process.exit(1)
  }
}

// Entry point quando executado diretamente
if (process.argv[1] === new URL(import.meta.url).pathname) {
  startServer()
}
```

3. Adicionar dependencia `pino-logtail` ao `apps/api/package.json`:
No campo `dependencies` adicionar: `"@logtail/pino": "latest"`. Executar `pnpm install`.

RESTRICOES:
- O `redact` do pino DEVE estar configurado ANTES do primeiro request (por construcao, D-26)
- Sentry com `sendDefaultPii: false` — NUNCA enviar PII ao Sentry automaticamente
- `genReqId` usa crypto.randomUUID() para request_id unico por request (INFRA-06)
- Rate limiting usa tenant_id como key quando disponivel (isolamento por tenant)
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && node -e "import('./apps/api/src/server.ts').then(m => { const app = m.buildApp(); console.log('buildApp OK'); process.exit(0) }).catch(e => { console.error(e); process.exit(1) })" 2>&1 || npx tsx -e "import('./apps/api/src/server.ts').then(m => console.log('buildApp OK'))" 2>&1 | head -5</automated>
  </verify>
  <done>
    - `server.ts` exporta buildApp() e startServer()
    - `pino` configurado com redact de PII — CPF, password, prompt_text, authorization header
    - Sentry inicializado com sendDefaultPii: false e tag source=api
    - Rate limiting configurado com keyGenerator por tenant_id
    - buildApp() instancia sem erros de importacao
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → Fastify | Requests nao autenticados; headers podem conter payloads maliciosos |
| Fastify preHandler → route handler | Apos middleware: req.user garantido; tenant_id validado |
| Fastify → Sentry | Erros enviados ao Sentry nao devem conter PII |
| Fastify → Betterstack/logs | Logs nao devem conter CPF, senha, tokens JWT |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-04-S | Spoofing | JWT verification | mitigate | jwtVerify com issuer e audience validados; JWKS cache; nao confiar em claims sem verificacao criptografica |
| T-1-04-I | Info Disclosure | pino logger | mitigate | `redact` configurado com caminhos abrangentes `*.cpf`, `*.senha`, `req.headers.authorization`; censor `[REDACTED]` |
| T-1-04-I | Info Disclosure | Sentry error reports | mitigate | `sendDefaultPii: false`; scope.setUser usa apenas user.sub (UUID), nao email/CPF |
| T-1-04-D | Denial of Service | Rate limiting ausente | mitigate | @fastify/rate-limit com 1000 req/min por tenant; ajustar limites em Phase 2-3 |
| T-1-04-T | Tampering | Request headers | mitigate | @fastify/helmet define security headers (CSP, X-Frame-Options, HSTS) |
</threat_model>

<verification>
- `npx vitest run apps/api/src/plugins/auth.test.ts` reporta "5 passed"
- `grep "REDACTED" apps/api/src/server.ts` — redact censor configurado
- `grep "sendDefaultPii: false" apps/api/src/plugins/sentry.ts` — PII nao enviado ao Sentry
- `apps/api/src/lib/supabase.ts` exporta supabaseAdmin e supabaseAsUser
- `apps/api/src/lib/redis.ts` exporta createRedisClient e createBullMQRedisClient com maxRetriesPerRequest distintos
</verification>

<success_criteria>
Server Fastify funcional com buildApp(). Logger pino com PII redaction ativo por construcao. Sentry configurado com contexto de tenant e sendDefaultPii=false. Plugin de autenticacao com 5 testes passando. Dois clientes Supabase (admin/user) e cliente Redis com configuracoes corretas.
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-04-SUMMARY.md`
</output>
