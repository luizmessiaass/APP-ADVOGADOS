---
phase: 01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics
plan: 07
title: "[BLOCKING] Schema Push + CI/CD GitHub Actions"
type: execute
wave: 3
depends_on:
  - "01-02"
  - "01-03"
  - "01-04"
  - "01-05"
  - "01-06"
files_modified:
  - .github/workflows/ci.yml
  - apps/api/.env.ci
autonomous: false
requirements:
  - INFRA-05
must_haves:
  truths:
    - "supabase db push aplicou todas as 5 migrations ao projeto Supabase Cloud (dev)"
    - "GitHub Actions CI executa pnpm install + vitest run em cada push e PR"
    - "CI usa Upstash Redis como REDIS_URL (sem dependencia de Docker em CI)"
    - "CI tem acesso aos secrets de Supabase via GitHub Secrets"
    - "Build de TypeScript (tsc --noEmit) e' verificado no CI antes dos testes"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Pipeline CI: install, typecheck, test, com Redis e Supabase configurados"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "supabase/migrations/*.sql"
      via: "supabase db push --linked antes dos testes de integracao"
      pattern: "supabase db push"
---

<objective>
Aplica as migrations ao Supabase Cloud via `supabase db push` (BLOCKING — sem isso os testes de integracao nao passam), e cria o pipeline CI/CD no GitHub Actions com Vitest, TypeScript check, e secrets configurados para o ambiente de CI.

Purpose: O schema push e' BLOCKING — sem ele o banco esta vazio e os testes de cross-tenant (Plan 08) falham. O CI e' o gate de qualidade da fase.
Output: Banco Supabase com schema aplicado. CI verde no GitHub Actions.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-CONTEXT.md
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-RESEARCH.md
</context>

<interfaces>
<!-- GitHub Secrets necessarios para o CI -->

GitHub Secrets a configurar em Settings > Secrets > Actions:
  SUPABASE_URL             — URL do projeto Supabase dev (https://xxx.supabase.co)
  SUPABASE_ANON_KEY        — Anon key do projeto Supabase dev
  SUPABASE_SERVICE_ROLE_KEY — Service role key do projeto Supabase dev
  SUPABASE_ACCESS_TOKEN    — Token de acesso ao Supabase CLI (supabase.com/dashboard/account/tokens)
  SUPABASE_PROJECT_REF     — ID do projeto Supabase dev (visivel em Settings > API)
  REDIS_URL                — URL do Upstash Redis (https://console.upstash.com)
  SENTRY_DSN               — DSN do Sentry (opcional para CI)

D-32: Upstash Redis para CI (nao Docker — mais rapido e sem overhead de container)
D-37: GitHub Flow — CI em todos os PRs + push para main
</interfaces>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <what-built>
    Migrations SQL criadas em Plan 02 (0001 a 0005), Edge Function deployada em Plan 03, e configuracao do Supabase CLI em supabase/config.toml.
    O banco Supabase Cloud (portaljuridico-dev) ainda nao tem o schema aplicado.
    O CI/CD precisara dos secrets do Supabase e Upstash Redis.
  </what-built>
  <how-to-verify>
    Execute os seguintes passos manualmente:

    1. SCHEMA PUSH [BLOCKING]:
       ```bash
       # Linkar CLI ao projeto Supabase dev
       supabase link --project-ref SEU_PROJECT_REF
       # Aplicar migrations
       supabase db push
       ```
       Confirmar que as 5 migrations foram aplicadas sem erros.
       Verificar no Supabase Dashboard > Table Editor que as tabelas existem:
       - escritorios
       - usuarios
       - lgpd_consentimentos

    2. UPSTASH REDIS para CI:
       Acesse https://console.upstash.com
       Crie um banco Redis gratuito (nome: portaljuridico-ci)
       Copie a URL de conexao: redis://default:xxxxx@xxx.upstash.io:xxxxx
       Esta URL sera usada como REDIS_URL no CI.

    3. GITHUB SECRETS:
       Acesse Settings > Secrets and variables > Actions no repositorio GitHub.
       Adicione os seguintes secrets:
       - SUPABASE_URL: https://SEU_PROJECT_REF.supabase.co
       - SUPABASE_ANON_KEY: (em Settings > API no Supabase Dashboard)
       - SUPABASE_SERVICE_ROLE_KEY: (em Settings > API no Supabase Dashboard)
       - SUPABASE_ACCESS_TOKEN: (em https://supabase.com/dashboard/account/tokens)
       - SUPABASE_PROJECT_REF: SEU_PROJECT_REF
       - REDIS_URL: (URL do Upstash criada acima)
       - SENTRY_DSN: (DSN do Sentry, opcional)

    4. Confirmar: todas as tabelas existem no Dashboard do Supabase.
  </how-to-verify>
  <resume-signal>
    Digite "schema aplicado" quando as migrations estiverem no banco e os GitHub Secrets configurados.
    Descreva qualquer erro de migration para analise.
  </resume-signal>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Criar GitHub Actions CI workflow</name>
  <files>
    .github/workflows/ci.yml
  </files>
  <action>
Criar `.github/workflows/ci.yml`:
```yaml
name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  NODE_ENV: test
  PNPM_VERSION: '10.33.0'

jobs:
  test:
    name: Typecheck + Test
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: TypeScript typecheck (API)
        run: pnpm --filter api exec tsc --noEmit
        
      - name: TypeScript typecheck (Worker)
        run: pnpm --filter worker exec tsc --noEmit

      - name: Run tests
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          REDIS_URL: ${{ secrets.REDIS_URL }}
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
        run: pnpm test

  lint:
    name: Lint
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint
        run: pnpm lint
```

NOTAS:
- `pnpm install --frozen-lockfile` para reproducibilidade (D-37 GitHub Flow)
- tsc --noEmit para typecheck sem gerar arquivos de build
- Secrets passados como env vars apenas no job de tests (nao no lint)
- Timeout de 10 minutos para evitar CI travado infinitamente
- D-32: Upstash Redis via secret REDIS_URL (sem Docker no CI)
  </action>
  <verify>
    <automated>ls /c/Users/Luiz/AndroidStudioProjects/APPTESTE/.github/workflows/ci.yml && grep "pnpm test" /c/Users/Luiz/AndroidStudioProjects/APPTESTE/.github/workflows/ci.yml && echo "CI workflow OK"</automated>
  </verify>
  <done>
    - `.github/workflows/ci.yml` existe com jobs de test e lint
    - Secrets Supabase e Redis referenciados corretamente
    - TypeScript typecheck executado antes dos testes
    - `--frozen-lockfile` para reproducibilidade
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions → Supabase | CI usa service_role key para testes de integracao |
| GitHub Secrets → CI jobs | Secrets nao expostos em logs (GitHub mascara automaticamente) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-07-I | Info Disclosure | GitHub Actions logs | mitigate | Secrets configurados como GitHub Secrets (mascarados automaticamente); nao usar `echo $SECRET` em steps de CI |
| T-1-07-T | Tampering | pnpm lockfile | mitigate | `--frozen-lockfile` garante que CI usa exatamente as versoes do lockfile commitado; evita supply chain drift |
| T-1-07-S | Spoofing | supabase db push com project errado | mitigate | SUPABASE_PROJECT_REF como GitHub Secret — somente projeto dev, nao prod |
</threat_model>

<verification>
- `supabase db push` executado manualmente — 5 migrations aplicadas no Supabase Cloud dev
- Tabelas escritorios, usuarios, lgpd_consentimentos existem no Dashboard
- `.github/workflows/ci.yml` existe com jobs test e lint
- GitHub Secrets configurados (SUPABASE_URL, ANON_KEY, SERVICE_ROLE_KEY, REDIS_URL)
- Push para o repositorio dispara o CI workflow
</verification>

<success_criteria>
Schema Supabase aplicado (BLOCKING). CI/CD configurado com GitHub Actions rodando typecheck + tests + lint. Upstash Redis configurado para CI. GitHub Secrets configurados para o ambiente de teste.
</success_criteria>

<output>
Apos conclusao, criar `.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-07-SUMMARY.md`
</output>
