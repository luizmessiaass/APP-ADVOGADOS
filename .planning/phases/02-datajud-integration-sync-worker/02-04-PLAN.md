---
phase: 02-datajud-integration-sync-worker
plan: 04
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-03"
files_modified:
  - backend/src/datajud/circuit-breaker.ts
  - backend/src/workers/datajud-sync.ts
  - backend/src/workers/scheduler.ts
  - backend/src/queues/datajud-queue.ts
  - backend/src/workers/index.ts
  - backend/src/workers/__tests__/datajud-worker.test.ts
  - backend/src/workers/__tests__/diff.test.ts
  - backend/src/lib/__tests__/circuit-breaker.test.ts
autonomous: true
requirements:
  - DATAJUD-03
  - DATAJUD-04
  - DATAJUD-05
  - DATAJUD-06
  - DATAJUD-07
  - DATAJUD-08

must_haves:
  truths:
    - "Job de sync executa os steps VALIDATE → FETCH → DIFF → PERSIST → CLASSIFY_TIER em sequência"
    - "Após crash e restart, job retoma do step correto sem reprocessar do zero"
    - "Segunda execução do sync para o mesmo processo não cria movimentações duplicadas"
    - "Após 5 falhas consecutivas, circuit breaker abre e chamadas ao DataJud são suspensas"
    - "Estado do circuit breaker (open/closed) é persistido no Redis e restaurado no restart do worker"
    - "Processo hot sincroniza a cada 6h, warm a cada 24h, cold a cada 7 dias via upsertJobScheduler"
    - "Erros de sync são gravados em sync_errors com tipo, processo_id e tenant_id corretos"
    - "Segredo de justiça detectado após 3+ hits=[] em dias diferentes → sync_error tipo segredo_justica"
  artifacts:
    - path: "backend/src/datajud/circuit-breaker.ts"
      provides: "Circuit breaker opossum com estado Redis"
      exports: ["criarCircuitBreaker", "CircuitBreakerError"]
    - path: "backend/src/workers/datajud-sync.ts"
      provides: "Processador step-job BullMQ para sync DataJud"
      exports: ["processarSyncJob"]
    - path: "backend/src/workers/scheduler.ts"
      provides: "Tier management com upsertJobScheduler"
      exports: ["agendarSyncProcesso", "calcularTier", "TIER_INTERVALS_MS"]
    - path: "backend/src/queues/datajud-queue.ts"
      provides: "Queue instance DataJud para BullMQ e Bull Board"
      exports: ["datajudQueue"]
    - path: "backend/src/workers/index.ts"
      provides: "Entry point do worker process com consumer DataJud"
      contains: "datajud-sync"
  key_links:
    - from: "backend/src/workers/datajud-sync.ts"
      to: "backend/src/datajud/adapter.ts"
      via: "DatajudAdapter.buscarProcesso — chamada no step FETCH_DATAJUD"
      pattern: "DatajudAdapter"
    - from: "backend/src/workers/datajud-sync.ts"
      to: "supabase.from('movimentacoes').insert"
      via: "step PERSIST — insert de novas movimentações detectadas pelo diff"
      pattern: "movimentacoes"
    - from: "backend/src/workers/datajud-sync.ts"
      to: "supabase.from('sync_errors').insert"
      via: "catch de erros — gravar contexto em sync_errors (DATAJUD-08)"
      pattern: "sync_errors"
    - from: "backend/src/workers/datajud-sync.ts"
      to: "backend/src/datajud/circuit-breaker.ts"
      via: "breaker.fire() — chamada wrappada pelo circuit breaker"
      pattern: "breaker.fire"
    - from: "backend/src/workers/scheduler.ts"
      to: "backend/src/queues/datajud-queue.ts"
      via: "datajudQueue.upsertJobScheduler — agenda sync por tier"
      pattern: "upsertJobScheduler"
---

<objective>
Implementar o núcleo do worker DataJud: circuit breaker com estado Redis, job step-job com checkpoint, diff idempotente de movimentações, tier scheduler com upsertJobScheduler, e registro de erros em sync_errors. Inclui os test stubs Wave 0 para DATAJUD-03/04/05/06/07/08.

Purpose: Este é o motor da integração DataJud. Sem o worker, processos nunca são sincronizados. O step-job pattern garante que crashes não causam reprocessamento. O circuit breaker protege contra cascata de falhas quando o DataJud está instável.
Output: Worker BullMQ completo com 5 módulos TypeScript + 3 arquivos de teste Wave 0.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-datajud-integration-sync-worker/02-CONTEXT.md
@.planning/phases/02-datajud-integration-sync-worker/02-RESEARCH.md
@.planning/phases/02-datajud-integration-sync-worker/02-01-SUMMARY.md
@.planning/phases/02-datajud-integration-sync-worker/02-03-SUMMARY.md

<interfaces>
<!-- Exportações do Plan 01 que este plan usa -->

From backend/src/datajud/cnj-validator.ts:
```typescript
export function validarNumeroCNJ(numero: string): boolean
export function assertCNJValido(numero: string): void  // lança CNJInvalidoError
export class CNJInvalidoError extends Error { readonly code = 'INVALID_CNJ' }
```

From backend/src/datajud/adapter.ts:
```typescript
export class DatajudAdapter {
  constructor(options?: DatajudAdapterOptions)
  async buscarProcesso(numeroCNJ: string): Promise<DatajudProcesso | null>
}
export class DatajudAdapterError extends Error {
  readonly tipo: 'network' | 'timeout' | 'auth' | 'schema_drift' | 'unknown'
  readonly statusCode?: number
}
```

From backend/src/datajud/types.ts:
```typescript
export type DatajudMovimento = {
  id?: string | null
  data: string
  tipo?: { nacional?: { id: number; nome: string } | null } | null
  descricao?: string | null
}
export type DatajudProcesso = {
  dadosBasicos: { numero: string; nivelSigilo: number; ... }
  movimentos: DatajudMovimento[]
}
```

Schema Supabase (Phase 1 padrão — D-14):
- supabaseAdmin: cliente com service_role key (bypassa RLS)
- supabase: cliente com anon key (respeitando RLS para queries autenticadas)

BullMQ patterns (RESEARCH.md):
- Queue + Worker da library bullmq
- job.updateData({ ...job.data, step: 'NEXT_STEP' }) para checkpoint
- queue.upsertJobScheduler(id, { every: ms }, { name, data }) para tier
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Circuit breaker opossum + Redis + queue DataJud</name>
  <files>
    backend/src/datajud/circuit-breaker.ts
    backend/src/queues/datajud-queue.ts
    backend/src/lib/__tests__/circuit-breaker.test.ts
  </files>
  <read_first>
    - .planning/phases/02-datajud-integration-sync-worker/02-RESEARCH.md §Pattern 4 (Circuit Breaker com Estado no Redis — código exato)
    - .planning/phases/02-datajud-integration-sync-worker/02-CONTEXT.md §D-15 (parâmetros do CB), §D-16 (estado Redis)
    - backend/src/datajud/adapter.ts (importar DatajudAdapter)
    - .planning/phases/02-datajud-integration-sync-worker/02-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <behavior>
    - criarCircuitBreaker(redis) com estado salvo 'open' → instância já começa no estado aberto
    - criarCircuitBreaker(redis) sem estado salvo → instância começa no estado fechado (normal)
    - breaker.fire(fn) quando circuito aberto → rejeita com CircuitBreakerError imediatamente (sem chamar fn)
    - breaker events 'open', 'close', 'halfOpen' → persistem estado no Redis com setex(CB_REDIS_KEY, 3600, ...)
    - Após 5 falhas consecutivas, circuito abre (volumeThreshold: 5, errorThresholdPercentage: 100)
    - datajudQueue é instância de Queue com nome 'datajud-sync'
  </behavior>
  <action>
**Criar `backend/src/datajud/circuit-breaker.ts`:**

```typescript
/**
 * Circuit breaker opossum 9.x com estado persistido no Redis.
 * (D-15/D-16 do CONTEXT.md, DATAJUD-06)
 *
 * Parâmetros (Claude's Discretion):
 * - volumeThreshold: 5 requisições mínimas para avaliar
 * - errorThresholdPercentage: 100 (abre após 100% de falhas no threshold — equivale a 5 consecutivas)
 * - resetTimeout: 60_000ms (1 minuto de half-open)
 * - timeout: 10_000ms (por chamada)
 */
import CircuitBreaker from 'opossum';
import type { Redis } from 'ioredis';

export const CB_REDIS_KEY = 'cb:datajud:global';
export const CB_REDIS_TTL_S = 3600; // 1h — alinhado com os tiers de refresh mínimo (6h hot)

export class CircuitBreakerError extends Error {
  readonly code = 'CIRCUIT_OPEN';
  constructor() {
    super('Circuit breaker DataJud aberto — chamadas suspensas');
    this.name = 'CircuitBreakerError';
  }
}

export interface CircuitBreakerState {
  state?: {
    open?: boolean;
    halfOpen?: boolean;
    closed?: boolean;
  };
}

export async function criarCircuitBreaker(
  redis: Redis,
  // Permite override para testes
  options?: Partial<CircuitBreaker.Options>
): Promise<CircuitBreaker> {
  // Tentar restaurar estado salvo no Redis
  let savedState: CircuitBreakerState | undefined;
  try {
    const raw = await redis.get(CB_REDIS_KEY);
    if (raw) savedState = JSON.parse(raw) as CircuitBreakerState;
  } catch {
    // Redis temporariamente indisponível — iniciar sem estado
  }

  // Função placeholder — o caller substitui via breaker.fire(suaFuncao)
  // O circuit breaker é criado sem função fixa para permitir reuso
  const noop = async () => {};

  const breaker = new CircuitBreaker(noop, {
    timeout: 10_000,
    errorThresholdPercentage: 100, // abre após 100% de erro no volume threshold
    resetTimeout: 60_000,          // 1 minuto até tentar half-open
    volumeThreshold: 5,            // mínimo 5 calls para avaliar
    ...options,
  });

  // Restaurar estado: se estava open, forçar abertura imediata
  if (savedState?.state?.open) {
    breaker.open();
  }

  // Persistir estado a cada mudança
  const persistirEstado = async () => {
    try {
      await redis.setex(CB_REDIS_KEY, CB_REDIS_TTL_S, JSON.stringify(breaker.toJSON()));
    } catch {
      // Não bloquear se Redis estiver indisponível — logar em produção via pino
    }
  };

  breaker.on('open', persistirEstado);
  breaker.on('close', persistirEstado);
  breaker.on('halfOpen', persistirEstado);

  return breaker;
}

/**
 * Executa uma função wrappada pelo circuit breaker.
 * Lança CircuitBreakerError se o circuito estiver aberto.
 * Lança o erro original da função se o circuito estiver fechado e a função falhar.
 */
export async function executarComCircuitBreaker<T>(
  breaker: CircuitBreaker,
  fn: () => Promise<T>
): Promise<T> {
  // Substituir a função do breaker temporariamente não é thread-safe.
  // Usar o padrão de wrapper: criar um breaker wrapper em torno de fn.
  // Como opossum não suporta fire() com função diferente da criada,
  // usamos uma abordagem: verificar estado manualmente.
  if (breaker.opened) {
    throw new CircuitBreakerError();
  }
  // Delegar para o breaker usando fallback manual
  try {
    return await fn();
  } catch (err) {
    // Registrar falha no breaker para atualizar estatísticas
    // opossum não expõe método direto de "registrar falha" fora do .fire()
    // A solução correta: criar o breaker em torno da função específica (ver scheduler.ts)
    throw err;
  }
}
```

NOTA IMPORTANTE PARA O EXECUTOR: O opossum 9.x não suporta bem o padrão de "um breaker compartilhado para múltiplas funções diferentes". A abordagem correta para o worker é: **criar o breaker diretamente em torno da função `DatajudAdapter.buscarProcesso`** no worker (datajud-sync.ts). O arquivo circuit-breaker.ts exporta `criarCircuitBreaker` que recebe a função adaptada no moment da criação do worker. Ajuste datajud-sync.ts para criar o breaker em torno de `adapter.buscarProcesso.bind(adapter)`.

**Criar `backend/src/queues/datajud-queue.ts`:**

```typescript
/**
 * Queue BullMQ para o worker de sincronização DataJud.
 * Instância compartilhada entre worker, scheduler e Bull Board.
 * (DATAJUD-03/04 — D-31 do CONTEXT.md Phase 1)
 */
import { Queue } from 'bullmq';
import type { Redis } from 'ioredis';

export const DATAJUD_QUEUE_NAME = 'datajud-sync';

export interface SyncJobData {
  processoId: string;
  numeroCNJ: string;
  tenantId: string;
  tier: 'hot' | 'warm' | 'cold';
  step: SyncStep;
  // Dados persistidos entre steps pelo checkpoint (job.updateData)
  dadosDatajud?: unknown;
  novasMovimentacoesCount?: number;
  tentativasHitsVazio?: number; // contagem para detecção de segredo de justiça
}

export type SyncStep =
  | 'INITIAL'
  | 'FETCH_DATAJUD'
  | 'DIFF_MOVIMENTACOES'
  | 'PERSIST'
  | 'CLASSIFY_TIER'
  | 'FINISH';

let _datajudQueue: Queue<SyncJobData> | undefined;

export function getDatajudQueue(redis: Redis): Queue<SyncJobData> {
  if (!_datajudQueue) {
    _datajudQueue = new Queue<SyncJobData>(DATAJUD_QUEUE_NAME, {
      connection: redis,
      defaultJobOptions: {
        attempts: 5,
        backoff: {
          type: 'exponential',
          delay: 2000, // 2s inicial; duplica: 2s, 4s, 8s, 16s, 32s
        },
        removeOnComplete: { count: 100 }, // manter os últimos 100 jobs completados
        removeOnFail: { count: 500 },     // manter os últimos 500 falhos para debug
      },
    });
  }
  return _datajudQueue;
}
```

**Criar testes Wave 0 `backend/src/lib/__tests__/circuit-breaker.test.ts`:**

```typescript
import { describe, it, expect, vi, beforeEach } from 'vitest';
import CircuitBreaker from 'opossum';
import { criarCircuitBreaker, CB_REDIS_KEY, CircuitBreakerError } from '../../datajud/circuit-breaker';

// Mock Redis
const mockRedis = {
  get: vi.fn(),
  setex: vi.fn().mockResolvedValue('OK'),
};

describe('criarCircuitBreaker', () => {
  beforeEach(() => {
    vi.clearAllMocks();
  });

  it('deve iniciar fechado quando não há estado salvo no Redis', async () => {
    mockRedis.get.mockResolvedValueOnce(null);
    const breaker = await criarCircuitBreaker(mockRedis as any);
    expect(breaker.closed).toBe(true);
    expect(breaker.opened).toBe(false);
  });

  it('deve iniciar aberto quando estado salvo no Redis é open', async () => {
    const estadoOpen = JSON.stringify({ state: { open: true, closed: false, halfOpen: false } });
    mockRedis.get.mockResolvedValueOnce(estadoOpen);
    const breaker = await criarCircuitBreaker(mockRedis as any);
    expect(breaker.opened).toBe(true);
  });

  it('deve persistir estado no Redis quando o circuito abre', async () => {
    mockRedis.get.mockResolvedValueOnce(null);
    const breaker = await criarCircuitBreaker(mockRedis as any, {
      volumeThreshold: 1,
      errorThresholdPercentage: 100,
    });
    // Forçar abertura manual para testar persistência
    breaker.open();
    // Aguardar evento assíncrono
    await new Promise(resolve => setTimeout(resolve, 10));
    expect(mockRedis.setex).toHaveBeenCalledWith(
      CB_REDIS_KEY,
      expect.any(Number),
      expect.stringContaining('"')
    );
  });

  it('deve ignorar erro de Redis e iniciar sem estado', async () => {
    mockRedis.get.mockRejectedValueOnce(new Error('redis connection refused'));
    const breaker = await criarCircuitBreaker(mockRedis as any);
    // Deve criar sem lançar
    expect(breaker).toBeDefined();
    expect(breaker.closed).toBe(true);
  });
});
```
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && pnpm --filter backend test --run src/lib/__tests__/circuit-breaker</automated>
  </verify>
  <acceptance_criteria>
    - grep "criarCircuitBreaker" backend/src/datajud/circuit-breaker.ts retorna resultado
    - grep "CB_REDIS_KEY" backend/src/datajud/circuit-breaker.ts retorna "cb:datajud:global"
    - grep "breaker.open()" backend/src/datajud/circuit-breaker.ts retorna resultado (restauração de estado)
    - grep "DATAJUD_QUEUE_NAME" backend/src/queues/datajud-queue.ts retorna "datajud-sync"
    - grep "attempts.*5" backend/src/queues/datajud-queue.ts retorna resultado (retry config)
    - Testes Wave 0 passam: circuito inicia fechado sem Redis, inicia aberto com estado salvo
  </acceptance_criteria>
  <done>
    - circuit-breaker.ts exporta criarCircuitBreaker e CircuitBreakerError
    - datajud-queue.ts exporta getDatajudQueue, SyncJobData, SyncStep
    - Testes Wave 0 passam para circuit breaker
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Step-job worker + tier scheduler + diff idempotente</name>
  <files>
    backend/src/workers/datajud-sync.ts
    backend/src/workers/scheduler.ts
    backend/src/workers/index.ts
    backend/src/workers/__tests__/datajud-worker.test.ts
    backend/src/workers/__tests__/diff.test.ts
  </files>
  <read_first>
    - backend/src/datajud/adapter.ts (interface DatajudAdapter — usada no worker)
    - backend/src/queues/datajud-queue.ts (SyncJobData, SyncStep, getDatajudQueue)
    - backend/src/datajud/circuit-breaker.ts (criarCircuitBreaker)
    - .planning/phases/02-datajud-integration-sync-worker/02-RESEARCH.md §Pattern 2 (tier scheduler), §Pattern 3 (step-job checkpoint), §Pattern 5 (diff idempotente)
    - .planning/phases/02-datajud-integration-sync-worker/02-CONTEXT.md §D-01/D-02 (tier reclassificação), §D-03..D-06 (segredo de justiça)
    - .planning/phases/02-datajud-integration-sync-worker/02-VALIDATION.md §Wave 0 Requirements
  </read_first>
  <behavior>
    SCHEDULER:
    - calcularTier(dataHá15Dias) → 'hot'
    - calcularTier(dataHá60Dias) → 'warm'
    - calcularTier(dataHá200Dias) → 'cold'
    - calcularTier(null) → 'cold' (sem movimentação conhecida)
    - agendarSyncProcesso(queue, id, 'hot') → chama upsertJobScheduler com every=6*60*60*1000
    - agendarSyncProcesso(queue, id, 'warm') → every=24*60*60*1000
    - agendarSyncProcesso(queue, id, 'cold') → every=7*24*60*60*1000

    WORKER (step-job):
    - Job no step INITIAL → muda para FETCH_DATAJUD (não chama DataJud ainda)
    - Job no step FETCH_DATAJUD, DataJud retorna processo → muda para DIFF_MOVIMENTACOES
    - Job no step FETCH_DATAJUD, DataJud retorna null (hits=[]) → incrementa tentativasHitsVazio
    - Job com tentativasHitsVazio >= 3 → grava sync_error tipo 'segredo_justica' e vai para FINISH
    - Job no step DIFF_MOVIMENTACOES → filtra movimentos já existentes por datajud_id
    - Job no step PERSIST → insere novas movimentações; não lança em conflito de unique (ON CONFLICT DO NOTHING)
    - Job no step CLASSIFY_TIER → chama agendarSyncProcesso com tier calculado a partir de ultimaMovimentacao
    - Job lança em qualquer step → sync_error é gravado com tipo correspondente

    DIFF:
    - diffMovimentacoes(processoId, [mov1, mov2]) quando mov1 já existe → retorna apenas [mov2]
    - diffMovimentacoes(processoId, []) → retorna []
    - gerarHashMovimento(mov sem id) → string hex de 16 chars
  </behavior>
  <action>
**Criar `backend/src/workers/scheduler.ts`:**

```typescript
/**
 * Tier scheduler para sincronização DataJud.
 * (D-01/D-02 do CONTEXT.md, DATAJUD-04)
 *
 * Limiares (Claude's Discretion):
 * - hot:  < 30 dias desde última movimentação → sync a cada 6h
 * - warm: 30-180 dias → sync a cada 24h
 * - cold: > 180 dias → sync a cada 7 dias
 */
import type { Queue } from 'bullmq';
import type { SyncJobData } from '../queues/datajud-queue.js';

export type Tier = 'hot' | 'warm' | 'cold';

export const TIER_INTERVALS_MS: Record<Tier, number> = {
  hot:  6 * 60 * 60 * 1000,         // 6 horas
  warm: 24 * 60 * 60 * 1000,        // 24 horas
  cold: 7 * 24 * 60 * 60 * 1000,    // 7 dias
};

const TIER_THRESHOLDS_DAYS = {
  HOT_MAX: 30,    // < 30 dias = hot
  WARM_MAX: 180,  // 30-180 dias = warm; > 180 = cold
} as const;

/**
 * Calcula o tier baseado na data da última movimentação detectada.
 * Retorna 'cold' se nunca houve movimentação (processo sem histórico).
 */
export function calcularTier(ultimaMovimentacaoAt: Date | null): Tier {
  if (!ultimaMovimentacaoAt) return 'cold';
  const diasSemMovimentacao =
    (Date.now() - ultimaMovimentacaoAt.getTime()) / (1000 * 60 * 60 * 24);
  if (diasSemMovimentacao < TIER_THRESHOLDS_DAYS.HOT_MAX) return 'hot';
  if (diasSemMovimentacao < TIER_THRESHOLDS_DAYS.WARM_MAX) return 'warm';
  return 'cold';
}

/**
 * Agenda (ou reagenda) o sync de um processo no tier correto.
 * upsertJobScheduler é idempotente: atualiza se o scheduler já existe.
 */
export async function agendarSyncProcesso(
  queue: Queue<SyncJobData>,
  processoId: string,
  numeroCNJ: string,
  tenantId: string,
  tier: Tier
): Promise<void> {
  await queue.upsertJobScheduler(
    `sync-processo-${processoId}`,          // ID único do scheduler por processo
    { every: TIER_INTERVALS_MS[tier] },      // intervalo em ms
    {
      name: 'datajud-sync',
      data: {
        processoId,
        numeroCNJ,
        tenantId,
        tier,
        step: 'INITIAL',
      },
    }
  );
}
```

**Criar `backend/src/workers/datajud-sync.ts`:**

```typescript
/**
 * Step-job BullMQ para sincronização DataJud.
 * Padrão: job.updateData({ step }) persiste checkpoint no Redis.
 * Em crash/restart, job retoma do step correto sem reprocessar do zero.
 * (D-17 do CONTEXT.md, DATAJUD-07)
 *
 * Steps: INITIAL → FETCH_DATAJUD → DIFF_MOVIMENTACOES → PERSIST → CLASSIFY_TIER → FINISH
 */
import { createHash } from 'node:crypto';
import type { Job } from 'bullmq';
import CircuitBreaker from 'opossum';
import type { SupabaseClient } from '@supabase/supabase-js';
import { DatajudAdapter, DatajudAdapterError } from '../datajud/adapter.js';
import type { DatajudMovimento, DatajudProcesso } from '../datajud/types.js';
import type { SyncJobData, SyncStep } from '../queues/datajud-queue.js';
import { calcularTier, agendarSyncProcesso } from './scheduler.js';
import type { Queue } from 'bullmq';

// Limiar para inferência de segredo de justiça (Claude's Discretion: 3 tentativas)
const SEGREDO_JUSTICA_THRESHOLD = 3;

export function gerarHashMovimento(m: DatajudMovimento): string {
  const conteudo = `${m.data}|${m.tipo?.nacional?.id ?? ''}|${m.descricao ?? ''}`;
  return createHash('sha256').update(conteudo).digest('hex').substring(0, 16);
}

/**
 * Diff idempotente: retorna apenas movimentos ainda não persistidos.
 * Usa datajud_id (campo nativo) com fallback para hash_conteudo.
 * (DATAJUD-05, D-18)
 */
export async function diffMovimentacoes(
  supabaseAdmin: SupabaseClient,
  processoId: string,
  movimentosDatajud: DatajudMovimento[]
): Promise<DatajudMovimento[]> {
  if (movimentosDatajud.length === 0) return [];

  const { data: existentes, error } = await supabaseAdmin
    .from('movimentacoes')
    .select('datajud_id, hash_conteudo')
    .eq('processo_id', processoId);

  if (error) throw new Error(`Erro ao buscar movimentações existentes: ${error.message}`);

  const idsExistentes = new Set(
    (existentes ?? []).map(m => m.datajud_id ?? m.hash_conteudo)
  );

  return movimentosDatajud.filter(m => {
    const id = m.id ?? gerarHashMovimento(m);
    return !idsExistentes.has(id);
  });
}

/**
 * Processador principal do job de sync DataJud.
 * Deve ser passado para o BullMQ Worker como handler.
 */
export async function processarSyncJob(
  job: Job<SyncJobData>,
  supabaseAdmin: SupabaseClient,
  datajudQueue: Queue<SyncJobData>,
  breaker: CircuitBreaker
): Promise<void> {
  let step = (job.data.step ?? 'INITIAL') as SyncStep;
  const { processoId, numeroCNJ, tenantId } = job.data;

  try {
    while (step !== 'FINISH') {
      switch (step) {
        case 'INITIAL': {
          await job.updateData({ ...job.data, step: 'FETCH_DATAJUD' });
          step = 'FETCH_DATAJUD';
          break;
        }

        case 'FETCH_DATAJUD': {
          // Executar chamada DataJud via circuit breaker
          let dadosDatajud: DatajudProcesso | null = null;

          try {
            dadosDatajud = await breaker.fire() as DatajudProcesso | null;
          } catch (err) {
            if ((err as Error).name === 'OpenCircuitError') {
              // Circuito aberto — registrar em sync_errors e finalizar
              await gravarSyncError(supabaseAdmin, processoId, tenantId, 'circuit_open',
                'Circuit breaker DataJud aberto — sync suspenso');
              step = 'FINISH';
              await job.updateData({ ...job.data, step });
              break;
            }
            throw err; // re-lançar para BullMQ registrar como falha
          }

          if (dadosDatajud === null) {
            // hits=[] — processo não encontrado ou segredo de justiça
            const tentativas = (job.data.tentativasHitsVazio ?? 0) + 1;
            await job.updateData({ ...job.data, tentativasHitsVazio: tentativas, step: 'FINISH' });

            if (tentativas >= SEGREDO_JUSTICA_THRESHOLD) {
              await gravarSyncError(supabaseAdmin, processoId, tenantId, 'segredo_justica',
                `DataJud retornou hits=[] por ${tentativas} tentativas consecutivas — possível segredo de justiça (D-03/D-05)`);
            }
            step = 'FINISH';
          } else {
            await job.updateData({ ...job.data, dadosDatajud, step: 'DIFF_MOVIMENTACOES' });
            step = 'DIFF_MOVIMENTACOES';
          }
          break;
        }

        case 'DIFF_MOVIMENTACOES': {
          const dados = job.data.dadosDatajud as DatajudProcesso;
          const novas = await diffMovimentacoes(supabaseAdmin, processoId, dados.movimentos ?? []);
          await job.updateData({
            ...job.data,
            novasMovimentacoesCount: novas.length,
            // Salvar apenas IDs/hashes das novas, não o payload completo (economia de memória Redis)
            novasMovimentacoesIds: novas.map(m => m.id ?? gerarHashMovimento(m)),
            step: 'PERSIST',
          });
          step = 'PERSIST';
          break;
        }

        case 'PERSIST': {
          const dados = job.data.dadosDatajud as DatajudProcesso;
          const novasMovimentacoesIds = new Set(
            (job.data as any).novasMovimentacoesIds as string[] ?? []
          );

          const novas = (dados.movimentos ?? []).filter(m => {
            const id = m.id ?? gerarHashMovimento(m);
            return novasMovimentacoesIds.has(id);
          });

          if (novas.length > 0) {
            const rows = novas.map(m => ({
              processo_id: processoId,
              tenant_id: tenantId,
              datajud_id: m.id ?? null,
              hash_conteudo: gerarHashMovimento(m),
              data_hora: m.data,
              tipo_codigo: m.tipo?.nacional?.id ?? null,
              descricao_original: m.descricao ?? '',
            }));

            const { error } = await supabaseAdmin
              .from('movimentacoes')
              .insert(rows)
              // ON CONFLICT DO NOTHING — idempotência extra além do diff
              .select('id');

            if (error && !error.message.includes('duplicate')) {
              throw new Error(`Erro ao inserir movimentações: ${error.message}`);
            }
          }

          // Atualizar ultima_sincronizacao SOMENTE em sync bem-sucedido (D-09)
          const ultimaMovimentacaoAt = novas.length > 0
            ? new Date(novas[novas.length - 1].data)
            : null;

          await supabaseAdmin
            .from('processos')
            .update({
              ultima_sincronizacao: new Date().toISOString(),
              sincronizado: true,
              dados_brutos: dados,
              updated_at: new Date().toISOString(),
            })
            .eq('id', processoId)
            .eq('tenant_id', tenantId);

          await job.updateData({
            ...job.data,
            ultimaMovimentacaoIso: ultimaMovimentacaoAt?.toISOString() ?? null,
            step: 'CLASSIFY_TIER',
          });
          step = 'CLASSIFY_TIER';
          break;
        }

        case 'CLASSIFY_TIER': {
          const ultimaMovAt = (job.data as any).ultimaMovimentacaoIso
            ? new Date((job.data as any).ultimaMovimentacaoIso as string)
            : null;

          const novoTier = calcularTier(ultimaMovAt);

          // Atualizar tier no banco
          await supabaseAdmin
            .from('processos')
            .update({ tier_refresh: novoTier, updated_at: new Date().toISOString() })
            .eq('id', processoId);

          // Reagendar com novo tier (upsertJobScheduler é idempotente — D-01/D-02)
          await agendarSyncProcesso(datajudQueue, processoId, numeroCNJ, tenantId, novoTier);

          await job.updateData({ ...job.data, step: 'FINISH' });
          step = 'FINISH';
          break;
        }
      }
    }
  } catch (err) {
    // Registrar erro em sync_errors com contexto (DATAJUD-08)
    const tipo = err instanceof DatajudAdapterError ? err.tipo : 'unknown';
    await gravarSyncError(
      supabaseAdmin,
      processoId,
      tenantId,
      tipo as any,
      (err as Error).message,
      { jobId: job.id, step, stack: (err as Error).stack?.substring(0, 500) }
    );
    throw err; // re-lançar para BullMQ aplicar retry policy (D-03)
  }
}

async function gravarSyncError(
  supabaseAdmin: SupabaseClient,
  processoId: string,
  tenantId: string,
  tipo: 'rate_limit' | 'timeout' | 'schema_drift' | 'segredo_justica' | 'circuit_open' | 'unknown',
  mensagem: string,
  payload?: Record<string, unknown>
): Promise<void> {
  try {
    await supabaseAdmin.from('sync_errors').insert({
      processo_id: processoId,
      tenant_id: tenantId,
      tipo,
      mensagem,
      payload: payload ?? null,
    });
  } catch {
    // Não bloquear o worker se o registro de erro falhar — logar via pino em produção
  }
}
```

**Criar `backend/src/workers/index.ts`** (atualizar o existente da Phase 1 para adicionar o consumer DataJud):

```typescript
/**
 * Entry point do processo Worker BullMQ.
 * Instancia múltiplos consumers. Roda como processo separado do servidor HTTP.
 * (D-31 do CONTEXT.md Phase 1, INFRA-09)
 *
 * Para adicionar um novo consumer: importar e instanciar abaixo.
 */
import { Worker } from 'bullmq';
import { Redis } from 'ioredis';
import { DatajudAdapter } from '../datajud/adapter.js';
import { criarCircuitBreaker } from '../datajud/circuit-breaker.js';
import { getDatajudQueue, DATAJUD_QUEUE_NAME } from '../queues/datajud-queue.js';
import { processarSyncJob } from './datajud-sync.js';

const redis = new Redis(process.env.REDIS_URL ?? 'redis://localhost:6379', {
  maxRetriesPerRequest: null, // obrigatório para BullMQ
});

const datajudQueue = getDatajudQueue(redis);
const datajudAdapter = new DatajudAdapter(); // lê DATAJUD_API_KEY do env

// Circuit breaker criado em torno do método buscarProcesso
// Inicializado assincronamente antes de processar jobs
let breaker: Awaited<ReturnType<typeof criarCircuitBreaker>>;

async function inicializar(): Promise<void> {
  // Importar Supabase admin client (criado na Phase 1)
  const { supabaseAdmin } = await import('../lib/supabase.js');

  breaker = await criarCircuitBreaker(redis);

  // Sobrescrever a função do breaker com o método real do adapter
  // Nota: opossum requer que a função seja passada no construtor.
  // Aqui usamos a abordagem de criar o breaker diretamente em torno de buscarProcesso:
  const { default: CircuitBreaker } = await import('opossum');
  const CB_REDIS_KEY = 'cb:datajud:global';
  const savedRaw = await redis.get(CB_REDIS_KEY);
  const savedState = savedRaw ? JSON.parse(savedRaw) : undefined;

  breaker = new CircuitBreaker(
    (numeroCNJ: string) => datajudAdapter.buscarProcesso(numeroCNJ),
    {
      timeout: 10_000,
      errorThresholdPercentage: 100,
      resetTimeout: 60_000,
      volumeThreshold: 5,
    }
  );
  if (savedState?.state?.open) breaker.open();

  breaker.on('open', () => redis.setex(CB_REDIS_KEY, 3600, JSON.stringify(breaker.toJSON())));
  breaker.on('close', () => redis.setex(CB_REDIS_KEY, 3600, JSON.stringify(breaker.toJSON())));
  breaker.on('halfOpen', () => redis.setex(CB_REDIS_KEY, 3600, JSON.stringify(breaker.toJSON())));

  // Consumer DataJud
  const datajudWorker = new Worker<any>(
    DATAJUD_QUEUE_NAME,
    async (job) => {
      await processarSyncJob(job, supabaseAdmin, datajudQueue, breaker);
    },
    {
      connection: redis,
      concurrency: 3, // máx 3 jobs paralelos (respeitando rate limit DataJud ~1 req/s)
    }
  );

  datajudWorker.on('failed', (job, err) => {
    console.error(`[datajud-worker] Job ${job?.id} falhou:`, err.message);
  });

  console.log(`[worker] DataJud consumer iniciado — concurrency: 3`);
}

inicializar().catch(err => {
  console.error('[worker] Falha na inicialização:', err);
  process.exit(1);
});
```

**Criar testes Wave 0 — Scheduler `backend/src/workers/__tests__/datajud-worker.test.ts`:**

```typescript
import { describe, it, expect, vi } from 'vitest';
import { calcularTier, TIER_INTERVALS_MS } from '../scheduler';

describe('calcularTier', () => {
  it('deve retornar hot para processo com movimentação há 15 dias', () => {
    const quinzeDiasAtras = new Date(Date.now() - 15 * 24 * 60 * 60 * 1000);
    expect(calcularTier(quinzeDiasAtras)).toBe('hot');
  });

  it('deve retornar warm para processo com movimentação há 60 dias', () => {
    const sessentaDiasAtras = new Date(Date.now() - 60 * 24 * 60 * 60 * 1000);
    expect(calcularTier(sessentaDiasAtras)).toBe('warm');
  });

  it('deve retornar cold para processo com movimentação há 200 dias', () => {
    const duzentosAtras = new Date(Date.now() - 200 * 24 * 60 * 60 * 1000);
    expect(calcularTier(duzentosAtras)).toBe('cold');
  });

  it('deve retornar cold para processo sem movimentação (null)', () => {
    expect(calcularTier(null)).toBe('cold');
  });
});

describe('TIER_INTERVALS_MS', () => {
  it('hot deve ser 6 horas em ms', () => {
    expect(TIER_INTERVALS_MS.hot).toBe(6 * 60 * 60 * 1000);
  });

  it('warm deve ser 24 horas em ms', () => {
    expect(TIER_INTERVALS_MS.warm).toBe(24 * 60 * 60 * 1000);
  });

  it('cold deve ser 7 dias em ms', () => {
    expect(TIER_INTERVALS_MS.cold).toBe(7 * 24 * 60 * 60 * 1000);
  });
});

describe('agendarSyncProcesso', () => {
  it('deve chamar upsertJobScheduler com o intervalo correto para o tier', async () => {
    const { agendarSyncProcesso } = await import('../scheduler');
    const mockQueue = { upsertJobScheduler: vi.fn().mockResolvedValue(undefined) };
    await agendarSyncProcesso(mockQueue as any, 'proc-123', '0000001-47.2024.8.26.0001', 'tenant-456', 'hot');
    expect(mockQueue.upsertJobScheduler).toHaveBeenCalledWith(
      'sync-processo-proc-123',
      { every: TIER_INTERVALS_MS.hot },
      expect.objectContaining({ name: 'datajud-sync' })
    );
  });
});
```

**Criar testes Wave 0 — Diff `backend/src/workers/__tests__/diff.test.ts`:**

```typescript
import { describe, it, expect } from 'vitest';
import { gerarHashMovimento, diffMovimentacoes } from '../datajud-sync';

const MOV_COM_ID = {
  id: 'mov-estavel-abc',
  data: '2024-03-10T14:30:00Z',
  tipo: { nacional: { id: 26, nome: 'Distribuição' } },
  descricao: 'Distribuição por sorteio',
};

const MOV_SEM_ID = {
  data: '2024-04-01T10:00:00Z',
  tipo: { nacional: { id: 848, nome: 'Audiência' } },
  descricao: 'Audiência de instrução realizada',
};

describe('gerarHashMovimento', () => {
  it('deve retornar string de 16 caracteres hex', () => {
    const hash = gerarHashMovimento(MOV_SEM_ID);
    expect(hash).toHaveLength(16);
    expect(/^[a-f0-9]+$/.test(hash)).toBe(true);
  });

  it('deve ser determinístico — mesma entrada, mesmo hash', () => {
    expect(gerarHashMovimento(MOV_SEM_ID)).toBe(gerarHashMovimento(MOV_SEM_ID));
  });

  it('deve gerar hashes diferentes para movimentos diferentes', () => {
    const mov2 = { ...MOV_SEM_ID, descricao: 'Outra audiência' };
    expect(gerarHashMovimento(MOV_SEM_ID)).not.toBe(gerarHashMovimento(mov2));
  });
});

describe('diffMovimentacoes', () => {
  it('deve retornar array vazio quando não há movimentos', async () => {
    const mockSupabase = {
      from: () => ({ select: () => ({ eq: () => ({ data: [], error: null }) }) }),
    };
    const result = await diffMovimentacoes(mockSupabase as any, 'proc-123', []);
    expect(result).toHaveLength(0);
  });

  it('deve filtrar movimentos já existentes por datajud_id', async () => {
    const mockSupabase = {
      from: () => ({
        select: () => ({
          eq: () => ({ data: [{ datajud_id: 'mov-estavel-abc', hash_conteudo: 'h1' }], error: null }),
        }),
      }),
    };
    const result = await diffMovimentacoes(
      mockSupabase as any,
      'proc-123',
      [MOV_COM_ID, MOV_SEM_ID]
    );
    // MOV_COM_ID tem datajud_id='mov-estavel-abc' — já existe → filtrado
    // MOV_SEM_ID não tem datajud_id — hash não está no banco → incluído
    expect(result).toHaveLength(1);
    expect(result[0]).toEqual(MOV_SEM_ID);
  });
});
```
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && pnpm --filter backend test --run src/workers/__tests__</automated>
  </verify>
  <acceptance_criteria>
    - grep "upsertJobScheduler" backend/src/workers/scheduler.ts retorna resultado
    - grep "calcularTier" backend/src/workers/scheduler.ts retorna resultado
    - grep "TIER_INTERVALS_MS" backend/src/workers/scheduler.ts retorna resultado com hot=6h, warm=24h, cold=7d
    - grep "job.updateData" backend/src/workers/datajud-sync.ts retorna múltiplos resultados (um por step)
    - grep "sync_errors" backend/src/workers/datajud-sync.ts retorna resultado (gravar erros — DATAJUD-08)
    - grep "SEGREDO_JUSTICA_THRESHOLD" backend/src/workers/datajud-sync.ts retorna resultado com valor 3
    - grep "ultima_sincronizacao" backend/src/workers/datajud-sync.ts retorna resultado (atualizado apenas no PERSIST)
    - grep "ON CONFLICT\|duplicate" backend/src/workers/datajud-sync.ts retorna resultado (idempotência)
    - Testes Wave 0 passam: calcularTier, TIER_INTERVALS_MS, agendarSyncProcesso, gerarHashMovimento, diffMovimentacoes
    - pnpm --filter backend test --run src/workers/__tests__ retorna 0 falhas
  </acceptance_criteria>
  <done>
    - scheduler.ts exporta calcularTier, agendarSyncProcesso, TIER_INTERVALS_MS, Tier
    - datajud-sync.ts exporta processarSyncJob, diffMovimentacoes, gerarHashMovimento
    - workers/index.ts atualizado com consumer DataJud registrado
    - Todos os 5 steps do job cobertos (INITIAL, FETCH, DIFF, PERSIST, CLASSIFY_TIER)
    - sync_errors gravado em toda falha com tipo e contexto
    - Testes Wave 0 passam
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BullMQ job data (Redis) → worker | Dados de job persistidos no Redis; job.data é confiado como próprio estado interno |
| DataJud response → banco Supabase | Response externo parseado e validado por Zod antes de persistir (herdado do adapter) |
| supabaseAdmin (service_role) → banco | Service role bypassa RLS; worker deve ser o único processo com essa key |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-12 | Denial of Service | datajud-sync.ts (retry storm) | mitigate | BullMQ exponential backoff: 2s, 4s, 8s, 16s, 32s + circuit breaker abre após 5 falhas — previne storm de requests |
| T-02-13 | Tampering | diffMovimentacoes (idempotência) | mitigate | ON CONFLICT DO NOTHING + unique constraint no banco são duas camadas de proteção contra duplicação |
| T-02-14 | Information Disclosure | sync_errors.payload | mitigate | Stack trace truncado a 500 chars; nenhum dado do response DataJud (que pode conter PII) no payload de erro |
| T-02-15 | Denial of Service | segredo de justiça (hits=[]) | mitigate | Após 3 hits=[], job finaliza sem retry infinito; sync_error gravado e tier reclassificado para cold |
| T-02-16 | Elevation of Privilege | workers/index.ts (supabaseAdmin) | mitigate | supabaseAdmin importado de módulo centralizado da Phase 1; SUPABASE_SERVICE_ROLE_KEY nunca exposta ao processo API |
</threat_model>

<verification>
```bash
# Testes Wave 0 completos
pnpm --filter backend test --run src/workers/__tests__
pnpm --filter backend test --run src/lib/__tests__/circuit-breaker

# Verificar exports principais
grep "export" backend/src/workers/scheduler.ts
grep "export" backend/src/workers/datajud-sync.ts
grep "export" backend/src/queues/datajud-queue.ts

# Verificar checkpoint pattern
grep -c "job.updateData" backend/src/workers/datajud-sync.ts
# Esperado: >= 5 (um por step)

# Verificar sync_errors gravação
grep -c "sync_errors" backend/src/workers/datajud-sync.ts
# Esperado: >= 2 (no handler de erro e no segredo de justiça)
```
</verification>

<success_criteria>
- calcularTier retorna hot/warm/cold com limiares 30/180 dias
- agendarSyncProcesso chama upsertJobScheduler com every=6h/24h/7d conforme tier
- processarSyncJob implementa todos os 5 steps com job.updateData em cada transição
- diffMovimentacoes filtra movimentações já existentes por datajud_id com fallback para hash
- Circuit breaker abre após N falhas e restaura estado do Redis no restart
- sync_errors gravado em toda falha com processo_id, tenant_id e tipo corretos
- Todos os testes Wave 0 passam (scheduler, diff, circuit-breaker)
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-datajud-integration-sync-worker/02-04-SUMMARY.md`
</output>
