---
phase: 02-datajud-integration-sync-worker
plan: 03
type: execute
wave: 2
depends_on:
  - "02-02"
files_modified:
  - backend/supabase/migrations/002_datajud_schema.sql
autonomous: false
requirements:
  - DATAJUD-08

must_haves:
  truths:
    - "supabase db push executa sem erros e as tabelas processos, movimentacoes e sync_errors existem no banco"
    - "RLS está ativa e verificável via supabase db remote inspect"
  artifacts:
    - path: "backend/supabase/migrations/002_datajud_schema.sql"
      provides: "Migration já aplicada no banco Supabase"
      contains: "CREATE TABLE processos"
  key_links:
    - from: "supabase db push"
      to: "banco Supabase remoto"
      via: "SUPABASE_ACCESS_TOKEN + project ref"
      pattern: "supabase db push"

user_setup:
  - service: supabase
    why: "Push de migration requer autenticação com o projeto Supabase"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens → Generate new token"
      - name: SUPABASE_PROJECT_ID
        source: "Supabase Dashboard → Project Settings → General → Reference ID"
    dashboard_config: []
---

<objective>
[BLOCKING] Aplicar a migration do schema DataJud no banco Supabase. Este plano é um gate: nenhum plano subsequente (worker, routes) pode ser executado sem o schema no banco.

Purpose: O schema deve existir antes que o worker (Plan 04) possa persistir dados e antes que os endpoints (Plan 05) possam fazer queries. supabase db push aplica a migration de forma idempotente.
Output: Tabelas processos, movimentacoes e sync_errors criadas no banco Supabase com RLS ativa.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-datajud-integration-sync-worker/02-CONTEXT.md
@.planning/phases/02-datajud-integration-sync-worker/02-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: [BLOCKING] supabase db push — aplicar migration 002</name>
  <files>
    backend/supabase/migrations/002_datajud_schema.sql
  </files>
  <read_first>
    - backend/supabase/migrations/002_datajud_schema.sql (verificar que o arquivo existe e está completo — Plan 02 deve ter completado)
    - backend/.env ou backend/.env.local (verificar se SUPABASE_ACCESS_TOKEN e projeto estão configurados)
  </read_first>
  <action>
Executar supabase db push para aplicar a migration no banco Supabase.

**Pré-condição:** Verificar se o arquivo 002_datajud_schema.sql existe:
```bash
ls backend/supabase/migrations/002_*.sql
```

**Executar o push:**
```bash
cd backend
SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase db push --linked
```

Se o projeto não estiver linked, fazer o link primeiro:
```bash
cd backend
npx supabase link --project-ref $SUPABASE_PROJECT_ID
npx supabase db push
```

**Se supabase CLI não estiver instalado:**
```bash
npm install -g supabase
# ou via npx (sem instalar globalmente):
npx supabase@latest db push
```

**Verificar que a migration foi aplicada:**
```bash
npx supabase db remote inspect
# Deve listar: processos, movimentacoes, sync_errors nas tabelas
```

**Alternativa se o push pedir confirmação interativa:**
Definir a variável de ambiente para suprimir prompt:
```bash
SUPABASE_ACCESS_TOKEN=$SUPABASE_ACCESS_TOKEN npx supabase db push --linked --password $DB_PASSWORD
```

Se o comando requerer senha do banco e não puder ser suprimido, este task se torna `checkpoint:human-action`.

**NOTA PARA O EXECUTOR:** Se supabase db push lançar erro sobre a tabela `escritorios` não existir (porque Phase 1 ainda não foi executada), documente no SUMMARY.md com instrução para o usuário executar Phase 1 primeiro. A sequência correta é Phase 1 → Phase 2.
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE/backend && npx supabase db remote inspect 2>&1 | grep -E "processos|movimentacoes|sync_errors"</automated>
  </verify>
  <acceptance_criteria>
    - Comando supabase db push retorna exit code 0 (sem erros)
    - npx supabase db remote inspect lista as tabelas "processos", "movimentacoes", "sync_errors"
    - Nenhum erro "relation already exists" (IF NOT EXISTS previne isso)
    - Se push falhar por dependência de Phase 1 (tabela escritorios não existe): documentar no SUMMARY.md com instrução clara
  </acceptance_criteria>
  <done>
    - Migration 002_datajud_schema.sql aplicada no banco Supabase remoto
    - Tabelas processos, movimentacoes e sync_errors existem no banco
    - RLS ativa nas 3 tabelas
    - Plans 04 e 05 podem prosseguir
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verificar schema no painel Supabase</name>
  <what-built>Migration 002_datajud_schema.sql aplicada no banco via supabase db push</what-built>
  <how-to-verify>
    1. Acessar https://app.supabase.com → seu projeto → Table Editor
    2. Verificar que as tabelas existem: processos, movimentacoes, sync_errors
    3. Clicar em "processos" → verificar colunas: id, tenant_id, numero_cnj, tribunal, tier_refresh, ultima_sincronizacao, sincronizado, dados_brutos, created_at, updated_at
    4. Acessar Authentication → Policies → verificar que cada tabela tem as policies "tenant_isolation" e "service_role_bypass"
    5. Verificar que tier_refresh tem valor padrão 'hot' e CHECK constraint
    6. Se tudo correto, digitar "approved"
    7. Se houver erro, descrever o problema encontrado
  </how-to-verify>
  <resume-signal>Digite "approved" para continuar para o Plan 04, ou descreva o problema para corrigi-lo</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| supabase CLI → banco Supabase | Push autenticado via SUPABASE_ACCESS_TOKEN; token deve ser de uso individual |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-10 | Information Disclosure | SUPABASE_ACCESS_TOKEN | mitigate | Token deve estar em variável de ambiente, nunca no código ou no histórico de comandos do shell; usar export temporário ou .env não commitado |
| T-02-11 | Elevation of Privilege | supabase db push | accept | Push requer acesso admin ao projeto; aceitável pois é operação de deploy controlada pelo proprietário |
</threat_model>

<verification>
```bash
# Após push, verificar tabelas remotas
cd backend && npx supabase db remote inspect | grep -E "processos|movimentacoes|sync_errors"

# Verificar migration registrada
cd backend && npx supabase migration list
# Deve mostrar 002_datajud_schema como "Applied"
```
</verification>

<success_criteria>
- supabase db push executa sem erros (exit code 0)
- As 3 tabelas existem no banco Supabase remoto
- Usuário confirma via painel Supabase que tabelas e RLS policies estão corretas
- npx supabase migration list mostra 002_datajud_schema como "Applied"
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-datajud-integration-sync-worker/02-03-SUMMARY.md`
</output>
