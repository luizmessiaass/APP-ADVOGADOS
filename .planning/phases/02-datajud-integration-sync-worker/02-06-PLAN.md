---
phase: 02-datajud-integration-sync-worker
plan: 06
type: execute
wave: 5
depends_on:
  - "02-05"
files_modified:
  - backend/src/workers/__tests__/datajud-worker.test.ts
  - backend/src/workers/__tests__/diff.test.ts
  - backend/src/lib/__tests__/circuit-breaker.test.ts
  - backend/src/routes/__tests__/processos.test.ts
autonomous: true
requirements:
  - DATAJUD-03
  - DATAJUD-05
  - DATAJUD-06
  - DATAJUD-07
  - DATAJUD-08
  - DATAJUD-09

must_haves:
  truths:
    - "pnpm --filter backend test --run retorna 0 falhas em toda a suite"
    - "Teste de idempotência: segunda execução do sync para o mesmo processo não cria movimentações duplicadas"
    - "Teste de checkpoint: job com step=DIFF_MOVIMENTACOES reiniciado não refaz FETCH_DATAJUD"
    - "Teste de circuit breaker: após N falhas, circuito abre e jobs subsequentes rejeitam sem chamar DataJud"
    - "Teste de staleness: desatualizado=true gerado corretamente para 73h+ e false para 71h-"
    - "Teste de sync_errors: erro de sync grava registro com tipo, processo_id e tenant_id corretos"
  artifacts:
    - path: "backend/src/workers/__tests__/datajud-worker.test.ts"
      provides: "Testes completos do worker (scheduler + step-job + checkpoint)"
      contains: "step.*DIFF_MOVIMENTACOES"
    - path: "backend/src/workers/__tests__/diff.test.ts"
      provides: "Testes completos de diff idempotente"
      contains: "duplicatas"
    - path: "backend/src/lib/__tests__/circuit-breaker.test.ts"
      provides: "Testes completos do circuit breaker"
      contains: "CircuitBreakerError"
    - path: "backend/src/routes/__tests__/processos.test.ts"
      provides: "Testes completos de staleness e validação CNJ"
      contains: "desatualizado"
  key_links:
    - from: "pnpm --filter backend test --run"
      to: "todos os 4 arquivos de teste"
      via: "Vitest test runner coletando src/**/__tests__/*.test.ts"
      pattern: "test --run"
---

<objective>
Expandir os stubs Wave 0 em testes completos que cobrem os cenários críticos da phase 2. Cada teste é automated e finaliza em menos de 30 segundos. DataJud é sempre mockado — sem chamadas reais à API CNJ.

Purpose: Os stubs Wave 0 dos planos anteriores provam que o código importa corretamente, mas não cobrem os fluxos completos. Este plano fecha a cobertura dos 9 requisitos DATAJUD e permite que /gsd-verify-work passe sem gaps.
Output: 4 arquivos de teste com cenários completos, todos passando.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-datajud-integration-sync-worker/02-VALIDATION.md
@.planning/phases/02-datajud-integration-sync-worker/02-05-SUMMARY.md

<interfaces>
<!-- Exportações a testar — extraídas dos planos anteriores -->

From backend/src/workers/scheduler.ts:
```typescript
export function calcularTier(ultimaMovimentacaoAt: Date | null): 'hot' | 'warm' | 'cold'
export async function agendarSyncProcesso(queue, processoId, numeroCNJ, tenantId, tier): Promise<void>
export const TIER_INTERVALS_MS: { hot: number; warm: number; cold: number }
```

From backend/src/workers/datajud-sync.ts:
```typescript
export function gerarHashMovimento(m: DatajudMovimento): string
export async function diffMovimentacoes(supabaseAdmin, processoId, movimentos): Promise<DatajudMovimento[]>
export async function processarSyncJob(job, supabaseAdmin, datajudQueue, breaker): Promise<void>
```

From backend/src/datajud/circuit-breaker.ts:
```typescript
export async function criarCircuitBreaker(redis, options?): Promise<CircuitBreaker>
export class CircuitBreakerError extends Error { readonly code: 'CIRCUIT_OPEN' }
export const CB_REDIS_KEY: string
```

Vitest mock patterns:
- vi.spyOn(globalThis, 'fetch').mockResolvedValueOnce(...)
- vi.fn().mockResolvedValue(...)
- vi.mock('../module') para mock de módulo inteiro
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Testes completos — worker, diff, circuit breaker</name>
  <files>
    backend/src/workers/__tests__/datajud-worker.test.ts
    backend/src/workers/__tests__/diff.test.ts
    backend/src/lib/__tests__/circuit-breaker.test.ts
  </files>
  <read_first>
    - backend/src/workers/__tests__/datajud-worker.test.ts (stubs Wave 0 existentes — expandir, não substituir)
    - backend/src/workers/__tests__/diff.test.ts (stubs Wave 0 existentes)
    - backend/src/lib/__tests__/circuit-breaker.test.ts (stubs Wave 0 existentes)
    - backend/src/workers/datajud-sync.ts (interface de processarSyncJob)
    - backend/src/workers/scheduler.ts (interface de agendarSyncProcesso)
    - backend/src/datajud/circuit-breaker.ts (interface de criarCircuitBreaker)
    - .planning/phases/02-datajud-integration-sync-worker/02-VALIDATION.md §Per-Task Verification Map (cenários exatos por requirement)
  </read_first>
  <behavior>
    WORKER (datajud-worker.test.ts — expandir):
    - processarSyncJob com job no step INITIAL → step muda para FETCH_DATAJUD (job.updateData chamado)
    - processarSyncJob com job no step FETCH_DATAJUD, breaker aberto → gravar sync_error tipo circuit_open, não chamar DataJud
    - processarSyncJob com job no step DIFF_MOVIMENTACOES (já passou pelo FETCH) → não refaz FETCH (checkpoint funciona)
    - processarSyncJob quando DataJud retorna null (hits=[]) 3x → gravar sync_error tipo segredo_justica
    - processarSyncJob completo (INITIAL → FINISH) → ultima_sincronizacao atualizado no banco

    DIFF (diff.test.ts — expandir):
    - diffMovimentacoes com lista de 3 movimentos onde 2 já existem → retorna apenas 1 (o novo)
    - diffMovimentacoes com movimentos sem datajud_id → usa hash_conteudo para comparar
    - Segunda chamada de diffMovimentacoes com os mesmos dados → retorna [] (idempotência)
    - gerarHashMovimento para movimentos idênticos → mesmo hash (determinístico)

    CIRCUIT BREAKER (circuit-breaker.test.ts — expandir):
    - Criar breaker, disparar 5 erros → breaker abre automaticamente
    - Breaker aberto → fire() rejeita com código de circuit open (sem chamar função)
    - Estado open salvo no Redis → novo breaker criado com mesmo Redis → inicia aberto
  </behavior>
  <action>
**Expandir `backend/src/workers/__tests__/datajud-worker.test.ts`** (adicionar ao final dos testes existentes):

```typescript
// =====================================================================
// TESTES COMPLETOS (além dos stubs Wave 0)
// =====================================================================
import { describe, it, expect, vi, beforeEach } from 'vitest';
import { processarSyncJob } from '../datajud-sync';
import type { Job } from 'bullmq';
import type CircuitBreaker from 'opossum';

// Factory de mock de Job BullMQ
function criarMockJob(data: Record<string, unknown>): Partial<Job> {
  const jobData = { ...data };
  return {
    id: 'job-test-123',
    data: jobData as any,
    updateData: vi.fn().mockImplementation(async (newData: any) => {
      Object.assign(jobData, newData);
    }),
  };
}

// Factory de Supabase Admin mock
function criarMockSupabase(processoData?: Record<string, unknown>) {
  return {
    from: vi.fn().mockReturnValue({
      select: vi.fn().mockReturnValue({
        eq: vi.fn().mockReturnValue({
          data: [],
          error: null,
        }),
      }),
      insert: vi.fn().mockReturnValue({ select: vi.fn().mockReturnValue({ data: [], error: null }) }),
      update: vi.fn().mockReturnValue({ eq: vi.fn().mockReturnValue({ eq: vi.fn().mockResolvedValue({ error: null }) }) }),
    }),
  };
}

describe('processarSyncJob — fluxo step-job (DATAJUD-07 checkpoint)', () => {
  it('deve transitar de INITIAL para FETCH_DATAJUD', async () => {
    const job = criarMockJob({
      processoId: 'proc-123',
      numeroCNJ: '0000001-47.2024.8.26.0001',
      tenantId: 'tenant-abc',
      tier: 'hot',
      step: 'INITIAL',
    });

    const mockSupabase = criarMockSupabase();
    const mockQueue = { upsertJobScheduler: vi.fn().mockResolvedValue(undefined) };
    const mockBreaker = {
      opened: false,
      fire: vi.fn().mockResolvedValue(null), // DataJud retorna null (não encontrado)
    } as any;

    await processarSyncJob(job as Job<any>, mockSupabase as any, mockQueue as any, mockBreaker);

    // job.updateData deve ter sido chamado com step FETCH_DATAJUD ao menos uma vez
    const calls = (job.updateData as ReturnType<typeof vi.fn>).mock.calls;
    const fetchStep = calls.find(([data]: any[]) => data.step === 'FETCH_DATAJUD');
    expect(fetchStep).toBeDefined();
  });

  it('deve retomar do step DIFF_MOVIMENTACOES sem refazer FETCH_DATAJUD (checkpoint)', async () => {
    const dadosFake = {
      dadosBasicos: { numero: '0000001-47.2024.8.26.0001', nivelSigilo: 0 },
      movimentos: [],
    };
    const job = criarMockJob({
      processoId: 'proc-123',
      numeroCNJ: '0000001-47.2024.8.26.0001',
      tenantId: 'tenant-abc',
      tier: 'hot',
      step: 'DIFF_MOVIMENTACOES',   // começa do meio — simula restart após crash
      dadosDatajud: dadosFake,
    });

    const mockSupabase = criarMockSupabase();
    const mockQueue = { upsertJobScheduler: vi.fn().mockResolvedValue(undefined) };
    const mockBreaker = { opened: false, fire: vi.fn() } as any;

    await processarSyncJob(job as Job<any>, mockSupabase as any, mockQueue as any, mockBreaker);

    // breaker.fire() NÃO deve ter sido chamado (não refez o FETCH)
    expect(mockBreaker.fire).not.toHaveBeenCalled();
  });

  it('deve gravar sync_error tipo circuit_open quando breaker está aberto', async () => {
    const job = criarMockJob({
      processoId: 'proc-123',
      numeroCNJ: '0000001-47.2024.8.26.0001',
      tenantId: 'tenant-abc',
      tier: 'hot',
      step: 'FETCH_DATAJUD',
    });

    const mockInsert = vi.fn().mockReturnValue({
      select: vi.fn().mockReturnValue({ data: [], error: null }),
    });
    const mockSupabase = {
      from: vi.fn().mockReturnValue({ insert: mockInsert }),
    };
    const mockQueue = { upsertJobScheduler: vi.fn() };

    // Simular breaker aberto: fire() lança OpenCircuitError
    const openError = new Error('Breaker open');
    openError.name = 'OpenCircuitError';
    const mockBreaker = { opened: true, fire: vi.fn().mockRejectedValue(openError) } as any;

    await processarSyncJob(job as Job<any>, mockSupabase as any, mockQueue as any, mockBreaker);

    // Verificar que sync_errors foi chamado com tipo 'circuit_open'
    const insertCall = mockInsert.mock.calls.find(() => true);
    // O from('sync_errors') deve ter sido chamado
    const fromCalls = (mockSupabase.from as ReturnType<typeof vi.fn>).mock.calls;
    const syncErrorCall = fromCalls.find(([table]: string[]) => table === 'sync_errors');
    expect(syncErrorCall).toBeDefined();
  });

  it('deve incrementar tentativasHitsVazio quando DataJud retorna null', async () => {
    const job = criarMockJob({
      processoId: 'proc-123',
      numeroCNJ: '0000001-47.2024.8.26.0001',
      tenantId: 'tenant-abc',
      tier: 'hot',
      step: 'FETCH_DATAJUD',
      tentativasHitsVazio: 0,
    });

    const mockSupabase = criarMockSupabase();
    const mockQueue = { upsertJobScheduler: vi.fn().mockResolvedValue(undefined) };
    const mockBreaker = { opened: false, fire: vi.fn().mockResolvedValue(null) } as any;

    await processarSyncJob(job as Job<any>, mockSupabase as any, mockQueue as any, mockBreaker);

    const calls = (job.updateData as ReturnType<typeof vi.fn>).mock.calls;
    const tentativasCall = calls.find(([data]: any[]) => data.tentativasHitsVazio !== undefined);
    expect(tentativasCall?.[0].tentativasHitsVazio).toBe(1);
  });
});
```

**Expandir `backend/src/workers/__tests__/diff.test.ts`** (adicionar ao final dos testes existentes):

```typescript
// =====================================================================
// TESTES COMPLETOS DE IDEMPOTÊNCIA (DATAJUD-05)
// =====================================================================
import { describe, it, expect, vi } from 'vitest';
import { diffMovimentacoes, gerarHashMovimento } from '../datajud-sync';

const MOV_A = {
  id: 'datajud-id-A',
  data: '2024-01-10T10:00:00Z',
  tipo: { nacional: { id: 26, nome: 'Distribuição' } },
  descricao: 'Distribuição inicial',
};

const MOV_B = {
  id: 'datajud-id-B',
  data: '2024-02-15T14:00:00Z',
  tipo: { nacional: { id: 11009, nome: 'Julgamento' } },
  descricao: 'Julgamento realizado',
};

const MOV_C_SEM_ID = {
  data: '2024-03-20T09:00:00Z',
  tipo: { nacional: { id: 848, nome: 'Audiência' } },
  descricao: 'Audiência de instrução',
};

describe('diffMovimentacoes — idempotência completa (DATAJUD-05)', () => {
  it('deve retornar apenas movimentos novos quando 2 de 3 já existem', async () => {
    const mockSupabase = {
      from: () => ({
        select: () => ({
          eq: () => ({
            data: [
              { datajud_id: 'datajud-id-A', hash_conteudo: 'hash-a' },
              { datajud_id: 'datajud-id-B', hash_conteudo: 'hash-b' },
            ],
            error: null,
          }),
        }),
      }),
    };

    const result = await diffMovimentacoes(mockSupabase as any, 'proc-xyz', [MOV_A, MOV_B, MOV_C_SEM_ID]);
    // A e B existem; C é novo (não tem datajud_id, usa hash)
    expect(result).toHaveLength(1);
    expect(result[0]).toEqual(MOV_C_SEM_ID);
  });

  it('deve retornar [] quando todos os movimentos já existem (segunda execução = zero duplicatas)', async () => {
    const hashC = gerarHashMovimento(MOV_C_SEM_ID);
    const mockSupabase = {
      from: () => ({
        select: () => ({
          eq: () => ({
            data: [
              { datajud_id: 'datajud-id-A', hash_conteudo: 'h1' },
              { datajud_id: 'datajud-id-B', hash_conteudo: 'h2' },
              { datajud_id: null, hash_conteudo: hashC },
            ],
            error: null,
          }),
        }),
      }),
    };

    const result = await diffMovimentacoes(mockSupabase as any, 'proc-xyz', [MOV_A, MOV_B, MOV_C_SEM_ID]);
    expect(result).toHaveLength(0);
  });

  it('deve usar hash_conteudo para movimentos sem datajud_id', async () => {
    const mockSupabase = {
      from: () => ({
        select: () => ({ eq: () => ({ data: [], error: null }) }),
      }),
    };

    // MOV_C_SEM_ID não tem campo 'id' — deve usar hash
    const result = await diffMovimentacoes(mockSupabase as any, 'proc-xyz', [MOV_C_SEM_ID]);
    expect(result).toHaveLength(1);
    expect(result[0]).toEqual(MOV_C_SEM_ID);
  });

  it('gerarHashMovimento deve ser determinístico', () => {
    const hash1 = gerarHashMovimento(MOV_C_SEM_ID);
    const hash2 = gerarHashMovimento(MOV_C_SEM_ID);
    expect(hash1).toBe(hash2);
    expect(hash1).toHaveLength(16);
    expect(/^[a-f0-9]+$/.test(hash1)).toBe(true);
  });

  it('gerarHashMovimento deve gerar hashes diferentes para movimentos diferentes', () => {
    const hashA = gerarHashMovimento({ ...MOV_C_SEM_ID, descricao: 'Audiência 1' });
    const hashB = gerarHashMovimento({ ...MOV_C_SEM_ID, descricao: 'Audiência 2' });
    expect(hashA).not.toBe(hashB);
  });
});
```

**Expandir `backend/src/lib/__tests__/circuit-breaker.test.ts`** (adicionar ao final dos testes existentes):

```typescript
// =====================================================================
// TESTES COMPLETOS DO CIRCUIT BREAKER (DATAJUD-06)
// =====================================================================
import { describe, it, expect, vi, beforeEach } from 'vitest';
import CircuitBreaker from 'opossum';
import { criarCircuitBreaker, CB_REDIS_KEY, CircuitBreakerError } from '../../datajud/circuit-breaker';

const mockRedis = {
  get: vi.fn().mockResolvedValue(null),
  setex: vi.fn().mockResolvedValue('OK'),
};

describe('circuit breaker — comportamento completo (DATAJUD-06)', () => {
  beforeEach(() => {
    vi.clearAllMocks();
    mockRedis.get.mockResolvedValue(null);
  });

  it('deve persistir estado no Redis quando abre por falhas consecutivas', async () => {
    const breaker = await criarCircuitBreaker(mockRedis as any, {
      volumeThreshold: 3,
      errorThresholdPercentage: 100,
      resetTimeout: 5000,
    });

    // Forçar abertura manual para testar
    breaker.open();
    await new Promise(resolve => setTimeout(resolve, 20)); // aguardar evento assíncrono

    expect(mockRedis.setex).toHaveBeenCalledWith(
      CB_REDIS_KEY,
      expect.any(Number),
      expect.any(String)
    );

    // Verificar que o JSON salvo contém alguma informação de estado
    const savedJson = JSON.parse((mockRedis.setex as ReturnType<typeof vi.fn>).mock.calls[0][2]);
    expect(savedJson).toBeDefined();
  });

  it('deve restaurar estado aberto do Redis no restart do worker', async () => {
    // Simular estado salvo no Redis indicando circuito aberto
    const estadoOpen = JSON.stringify({
      state: { open: true, closed: false, halfOpen: false },
    });
    mockRedis.get.mockResolvedValueOnce(estadoOpen);

    const breaker = await criarCircuitBreaker(mockRedis as any);
    expect(breaker.opened).toBe(true);
  });

  it('deve manter estado fechado quando Redis retorna estado fechado', async () => {
    const estadoClosed = JSON.stringify({
      state: { open: false, closed: true, halfOpen: false },
    });
    mockRedis.get.mockResolvedValueOnce(estadoClosed);

    const breaker = await criarCircuitBreaker(mockRedis as any);
    expect(breaker.closed).toBe(true);
    expect(breaker.opened).toBe(false);
  });

  it('deve persistir estado quando circuito fecha (recovery)', async () => {
    const breaker = await criarCircuitBreaker(mockRedis as any);

    // Abrir e depois fechar
    breaker.open();
    breaker.close();
    await new Promise(resolve => setTimeout(resolve, 20));

    // setex deve ter sido chamado em ambas as transições
    expect(mockRedis.setex).toHaveBeenCalledTimes(2);
  });
});
```
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && pnpm --filter backend test --run</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter backend test --run retorna exit code 0 (ZERO falhas)
    - Teste "deve retomar do step DIFF_MOVIMENTACOES sem refazer FETCH_DATAJUD" PASSA (checkpoint funciona)
    - Teste "deve retornar [] quando todos os movimentos já existem" PASSA (idempotência)
    - Teste "deve restaurar estado aberto do Redis no restart" PASSA (circuit breaker Redis)
    - Contagem de testes: pnpm --filter backend test --run mostra >= 20 testes passando
    - Nenhum teste usa fetch real ou Supabase real (todos mockados)
    - Tempo de execução da suite < 30 segundos
  </acceptance_criteria>
  <done>
    - datajud-worker.test.ts tem testes de checkpoint (retomar do step correto), circuit_open, segredo de justiça
    - diff.test.ts tem testes de idempotência (segunda execução retorna [], hash determinístico)
    - circuit-breaker.test.ts tem testes de persistência Redis e restauração de estado
    - Suite completa passa: pnpm --filter backend test --run com 0 falhas
  </done>
</task>

<task type="auto">
  <name>Task 2: Testes completos de staleness e validação CNJ nas rotas</name>
  <files>
    backend/src/routes/__tests__/processos.test.ts
  </files>
  <read_first>
    - backend/src/routes/__tests__/processos.test.ts (stubs Wave 0 existentes — expandir, não substituir)
    - backend/src/routes/processos.ts (lógica de staleness e validação CNJ — reler para alinhar testes)
    - .planning/phases/02-datajud-integration-sync-worker/02-VALIDATION.md §Per-Task Verification Map linha 02-04-01
  </read_first>
  <behavior>
    - calcularDesatualizado(72h + 1ms) → true (exatamente no limite)
    - calcularDesatualizado(72h - 1ms) → false (exatamente antes do limite)
    - calcularDesatualizado(null) → false (nunca sincronizou = não desatualizado)
    - calcularDesatualizado(data futura) → false (edge case: timestamp no futuro)
    - assertCNJValido com CNJ válido → não lança
    - assertCNJValido com string vazia → lança CNJInvalidoError code INVALID_CNJ
    - assertCNJValido com 19 dígitos → lança CNJInvalidoError
  </behavior>
  <action>
**Expandir `backend/src/routes/__tests__/processos.test.ts`** (adicionar ao final dos testes existentes):

```typescript
// =====================================================================
// TESTES COMPLETOS DE STALENESS (DATAJUD-09, D-07/D-08)
// =====================================================================

describe('calcularDesatualizado — limites exatos (DATAJUD-09)', () => {
  const THRESHOLD_MS = 72 * 60 * 60 * 1000;

  it('deve retornar true para ultima_sincronizacao exatamente 72h + 1ms atrás', () => {
    const ts = new Date(Date.now() - THRESHOLD_MS - 1).toISOString();
    expect(calcularDesatualizado(ts)).toBe(true);
  });

  it('deve retornar false para ultima_sincronizacao exatamente 72h - 1ms atrás', () => {
    const ts = new Date(Date.now() - THRESHOLD_MS + 1).toISOString();
    expect(calcularDesatualizado(ts)).toBe(false);
  });

  it('deve retornar false para timestamp no futuro (edge case)', () => {
    const ts = new Date(Date.now() + 60_000).toISOString();
    expect(calcularDesatualizado(ts)).toBe(false);
  });

  it('deve retornar false para processo com ultima_sincronizacao há 1 hora', () => {
    const ts = new Date(Date.now() - 60 * 60 * 1000).toISOString();
    expect(calcularDesatualizado(ts)).toBe(false);
  });

  it('deve retornar true para processo com ultima_sincronizacao há 7 dias', () => {
    const ts = new Date(Date.now() - 7 * 24 * 60 * 60 * 1000).toISOString();
    expect(calcularDesatualizado(ts)).toBe(true);
  });
});

// =====================================================================
// TESTES DE VALIDAÇÃO CNJ NA ROTA (DATAJUD-01 via rota POST /processos)
// =====================================================================

describe('assertCNJValido — casos edge', () => {
  it('deve não lançar para CNJ com formato válido e check-digit correto', async () => {
    const { assertCNJValido } = await import('../../datajud/cnj-validator');
    expect(() => assertCNJValido('0000001-47.2024.8.26.0001')).not.toThrow();
  });

  it('deve lançar CNJInvalidoError para string vazia', async () => {
    const { assertCNJValido, CNJInvalidoError } = await import('../../datajud/cnj-validator');
    expect(() => assertCNJValido('')).toThrow(CNJInvalidoError);
  });

  it('deve lançar CNJInvalidoError para número com 19 dígitos (muito curto)', async () => {
    const { assertCNJValido, CNJInvalidoError } = await import('../../datajud/cnj-validator');
    expect(() => assertCNJValido('000000-47.2024.8.26.0001')).toThrow(CNJInvalidoError);
  });

  it('deve lançar CNJInvalidoError para número com letras', async () => {
    const { assertCNJValido, CNJInvalidoError } = await import('../../datajud/cnj-validator');
    expect(() => assertCNJValido('XXXXXXX-47.2024.8.26.0001')).toThrow(CNJInvalidoError);
  });

  it('erro deve ter code INVALID_CNJ', async () => {
    const { assertCNJValido, CNJInvalidoError } = await import('../../datajud/cnj-validator');
    try {
      assertCNJValido('invalido');
    } catch (e) {
      expect(e).toBeInstanceOf(CNJInvalidoError);
      expect((e as any).code).toBe('INVALID_CNJ');
    }
  });
});
```
  </action>
  <verify>
    <automated>cd /c/Users/Luiz/AndroidStudioProjects/APPTESTE && pnpm --filter backend test --run src/routes/__tests__/processos</automated>
  </verify>
  <acceptance_criteria>
    - pnpm --filter backend test --run src/routes/__tests__/processos retorna 0 falhas
    - Teste "deve retornar true para ultima_sincronizacao exatamente 72h + 1ms" PASSA
    - Teste "deve retornar false para ultima_sincronizacao exatamente 72h - 1ms" PASSA
    - Teste "deve lançar CNJInvalidoError para string vazia" PASSA
    - Contagem de testes em processos.test.ts: >= 10 testes
    - pnpm --filter backend test --run (suite completa) retorna 0 falhas
  </acceptance_criteria>
  <done>
    - processos.test.ts tem testes de limite exato de 72h (72h+1ms = true, 72h-1ms = false)
    - Testes de assertCNJValido com casos edge (string vazia, 19 dígitos, letras)
    - Suite completa (todos os 4 arquivos de teste) passa com 0 falhas
    - pnpm --filter backend test --run --coverage roda sem erros
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Testes → produção | Testes usam mocks; nenhuma chamada real ao DataJud, Supabase ou Redis |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-22 | Tampering | testes com mocks | accept | Mocks são necessários para CI determinístico; testes de integração com DataJud real são manuais (ver VALIDATION.md §Manual-Only) |
| T-02-23 | Denial of Service | suite de testes (tempo) | mitigate | Sem sleep() nos testes; todos os mocks são síncronos ou Promises resolvidas imediatamente; target < 30s total |
</threat_model>

<verification>
```bash
# Suite completa deve passar com 0 falhas
pnpm --filter backend test --run

# Com coverage (executar antes de /gsd-verify-work)
pnpm --filter backend test --run --coverage

# Verificar que nenhum teste usa chamada real
grep -rn "api-publica.datajud.cnj.jus.br" backend/src/**/__tests__/
# Esperado: 0 ocorrências

# Verificar que nenhum teste usa Supabase real
grep -rn "SUPABASE_URL\|createClient" backend/src/**/__tests__/
# Esperado: 0 ocorrências (ou apenas imports de mocks)

# Tempo de execução
time pnpm --filter backend test --run
# Esperado: < 30 segundos
```
</verification>

<success_criteria>
- pnpm --filter backend test --run retorna EXIT CODE 0 com 0 falhas
- >= 20 testes passing no total (Wave 0 stubs + testes completos)
- Teste de checkpoint (retomar do step correto) passa
- Teste de idempotência (segunda execução = zero duplicatas) passa
- Teste de circuit breaker Redis (restaurar estado open) passa
- Testes de staleness nos limites exatos (72h+1ms=true, 72h-1ms=false) passam
- Nenhum teste faz chamada real à API DataJud, Supabase ou Redis
- Tempo de execução da suite < 30 segundos
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-datajud-integration-sync-worker/02-06-SUMMARY.md`
</output>
