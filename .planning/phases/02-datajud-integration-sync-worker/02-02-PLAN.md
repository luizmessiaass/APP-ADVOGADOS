---
phase: 02-datajud-integration-sync-worker
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/supabase/migrations/002_datajud_schema.sql
autonomous: true
requirements:
  - DATAJUD-08
  - DATAJUD-09

must_haves:
  truths:
    - "Tabelas processos, movimentacoes e sync_errors existem no banco com todas as colunas especificadas"
    - "RLS está ativa em todas as três tabelas com policy de isolamento por tenant_id"
    - "UNIQUE constraint em processos(tenant_id, numero_cnj) previne duplicação"
    - "UNIQUE constraint em movimentacoes(processo_id, COALESCE(datajud_id, hash_conteudo)) garante idempotência do diff"
    - "Trigger updated_at atualiza automatically em UPDATE nas tabelas com esse campo"
  artifacts:
    - path: "backend/supabase/migrations/002_datajud_schema.sql"
      provides: "DDL completo das tabelas processos, movimentacoes, sync_errors com RLS"
      contains: "CREATE TABLE processos"
  key_links:
    - from: "backend/supabase/migrations/002_datajud_schema.sql"
      to: "escritorios (tabela da Phase 1)"
      via: "REFERENCES escritorios(id) ON DELETE CASCADE em processos.tenant_id"
      pattern: "REFERENCES escritorios"
    - from: "processos.ultima_sincronizacao"
      to: "lógica de staleness (Plan 05)"
      via: "Fonte de verdade D-09 — atualizada SOMENTE em sync bem-sucedido"
      pattern: "ultima_sincronizacao"
---

<objective>
Criar a migration SQL com o schema completo das tabelas DataJud (processos, movimentacoes, sync_errors) com RLS por tenant, indexes de performance, e constraints de idempotência. Esta migration é o pré-requisito para o push BLOCKING do Plano 03.

Purpose: Sem o schema no banco, o worker (Plan 04) não tem onde persistir dados e o endpoint (Plan 05) não tem o que consultar. O schema correto com RLS garante isolamento de dados processuais por escritório desde o início.
Output: Um arquivo SQL de migration versionado e pronto para supabase db push.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-datajud-integration-sync-worker/02-CONTEXT.md
@.planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-CONTEXT.md

<interfaces>
<!-- Schema esperado (CONTEXT.md D-19, RESEARCH.md planning_guidelines) -->

Tabela processos:
- id UUID PK DEFAULT gen_random_uuid()
- tenant_id UUID NOT NULL REFERENCES escritorios(id)
- numero_cnj TEXT NOT NULL
- tribunal TEXT NOT NULL
- tier_refresh TEXT NOT NULL DEFAULT 'hot' CHECK IN ('hot', 'warm', 'cold')
- ultima_sincronizacao TIMESTAMPTZ (null = nunca sincronizado)
- sincronizado BOOLEAN DEFAULT false
- dados_brutos JSONB (dados brutos do DataJud — campo para Phase 3 usar)
- created_at / updated_at TIMESTAMPTZ

Tabela movimentacoes:
- id UUID PK DEFAULT gen_random_uuid()
- processo_id UUID NOT NULL REFERENCES processos(id) ON DELETE CASCADE
- tenant_id UUID NOT NULL (desnormalizado para RLS simples)
- datajud_id TEXT nullable (movimentos[].id do DataJud — pode ser null)
- hash_conteudo TEXT NOT NULL (fallback para dedup quando datajud_id é null)
- data_hora TIMESTAMPTZ NOT NULL
- tipo_codigo INTEGER nullable
- descricao_original TEXT NOT NULL
- created_at TIMESTAMPTZ

Tabela sync_errors:
- id UUID PK DEFAULT gen_random_uuid()
- processo_id UUID REFERENCES processos(id) ON DELETE CASCADE
- tenant_id UUID NOT NULL
- tipo TEXT NOT NULL CHECK IN ('rate_limit', 'timeout', 'schema_drift', 'segredo_justica', 'circuit_open', 'unknown')
- mensagem TEXT
- payload JSONB
- tentativas INTEGER DEFAULT 1
- created_at TIMESTAMPTZ

RLS pattern da Phase 1:
ALTER TABLE {tabela} ENABLE ROW LEVEL SECURITY;
CREATE POLICY "tenant_isolation" ON {tabela}
  USING (tenant_id = (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid);

Padrões Phase 1 (D-14, D-15, D-18):
- Nomenclatura PT-BR sem acentos
- UUID v4 via gen_random_uuid()
- created_at + updated_at com trigger update_updated_at_column() já criada na Phase 1
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration SQL — schema DataJud completo com RLS</name>
  <files>
    backend/supabase/migrations/002_datajud_schema.sql
  </files>
  <read_first>
    - backend/supabase/migrations/ (verificar número da última migration; usar 002 se Phase 1 usou 001)
    - .planning/phases/01-backend-foundation-supabase-fastify-auth-rls-lgpd-basics/01-CONTEXT.md §D-14 (nomenclatura), §D-15 (UUID), §D-18 (trigger updated_at)
    - .planning/phases/02-datajud-integration-sync-worker/02-CONTEXT.md §D-09 (ultima_sincronizacao como fonte de verdade), §D-19 (nomenclatura tabelas)
  </read_first>
  <action>
Criar `backend/supabase/migrations/002_datajud_schema.sql` com o conteúdo exato abaixo.

IMPORTANTE: Verificar o número da migration em `backend/supabase/migrations/` antes de criar — se a última migration da Phase 1 for `001_*.sql`, usar `002_`. Se já existir `002_*.sql`, usar `003_`. O número deve ser sequencial.

```sql
-- ============================================================
-- Migration: DataJud Schema — Fase 2
-- Tabelas: processos, movimentacoes, sync_errors
-- Requisitos: DATAJUD-01..09 (D-09, D-19, D-20 do CONTEXT.md)
-- ============================================================

-- -------------------------
-- TABELA: processos
-- -------------------------
-- Armazena dados brutos do processo DataJud por tenant.
-- ultima_sincronizacao: atualizada SOMENTE em sync bem-sucedido (D-09).
-- tier_refresh: hot/warm/cold — reclassificado após cada sync (D-01/D-02).
-- dados_brutos: JSONB com response raw do DataJud para Phase 3 (tradução).

CREATE TABLE IF NOT EXISTS processos (
  id                   UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  tenant_id            UUID        NOT NULL REFERENCES escritorios(id) ON DELETE CASCADE,
  numero_cnj           TEXT        NOT NULL,
  tribunal             TEXT        NOT NULL,
  tier_refresh         TEXT        NOT NULL DEFAULT 'hot'
                                   CHECK (tier_refresh IN ('hot', 'warm', 'cold')),
  ultima_sincronizacao TIMESTAMPTZ,                          -- NULL = nunca sincronizado
  sincronizado         BOOLEAN     NOT NULL DEFAULT false,
  dados_brutos         JSONB,                                -- response raw DataJud
  created_at           TIMESTAMPTZ NOT NULL DEFAULT now(),
  updated_at           TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT processos_tenant_cnj_unique UNIQUE (tenant_id, numero_cnj)
);

-- Index para buscas por tenant (acesso mais comum)
CREATE INDEX IF NOT EXISTS idx_processos_tenant_id ON processos (tenant_id);
-- Index para buscas por tier (scheduler queries)
CREATE INDEX IF NOT EXISTS idx_processos_tier_refresh ON processos (tier_refresh);
-- Index para staleness check (ultima_sincronizacao IS NULL ou < now() - interval)
CREATE INDEX IF NOT EXISTS idx_processos_ultima_sincronizacao ON processos (ultima_sincronizacao);

-- Trigger updated_at (função já criada na Phase 1)
CREATE TRIGGER set_processos_updated_at
  BEFORE UPDATE ON processos
  FOR EACH ROW EXECUTE FUNCTION update_updated_at_column();

-- RLS: isolamento total por tenant (D-19, AUTH-05)
ALTER TABLE processos ENABLE ROW LEVEL SECURITY;

CREATE POLICY "processos_tenant_isolation" ON processos
  AS PERMISSIVE FOR ALL TO authenticated
  USING (tenant_id = (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid)
  WITH CHECK (tenant_id = (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid);

-- Service role bypassa RLS (worker BullMQ usa service role key)
CREATE POLICY "processos_service_role_bypass" ON processos
  AS PERMISSIVE FOR ALL TO service_role
  USING (true)
  WITH CHECK (true);

-- -------------------------
-- TABELA: movimentacoes
-- -------------------------
-- Armazena movimentações individuais do processo.
-- datajud_id: campo id nativo do DataJud (movimentos[].id) — pode ser NULL em alguns tribunais.
-- hash_conteudo: fallback determinístico hash(data_hora|tipo_codigo|descricao_original).
-- UNIQUE em (processo_id, COALESCE(datajud_id, hash_conteudo)) garante idempotência do diff (DATAJUD-05).
-- tenant_id desnormalizado para RLS sem JOIN (performance).

CREATE TABLE IF NOT EXISTS movimentacoes (
  id                 UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  processo_id        UUID        NOT NULL REFERENCES processos(id) ON DELETE CASCADE,
  tenant_id          UUID        NOT NULL,
  datajud_id         TEXT,                                   -- movimentos[].id — nullable
  hash_conteudo      TEXT        NOT NULL,                   -- fallback dedup
  data_hora          TIMESTAMPTZ NOT NULL,
  tipo_codigo        INTEGER,                                -- tipo.nacional.id
  descricao_original TEXT        NOT NULL,
  created_at         TIMESTAMPTZ NOT NULL DEFAULT now(),
  CONSTRAINT movimentacoes_dedup_unique
    UNIQUE NULLS NOT DISTINCT (processo_id, datajud_id, hash_conteudo)
);

-- Index para buscas por processo (acesso mais comum)
CREATE INDEX IF NOT EXISTS idx_movimentacoes_processo_id ON movimentacoes (processo_id);
-- Index para buscas por tenant (RLS + queries do app)
CREATE INDEX IF NOT EXISTS idx_movimentacoes_tenant_id ON movimentacoes (tenant_id);
-- Index para ordenação cronológica (timeline do app_cliente)
CREATE INDEX IF NOT EXISTS idx_movimentacoes_data_hora ON movimentacoes (data_hora DESC);

-- RLS: isolamento por tenant
ALTER TABLE movimentacoes ENABLE ROW LEVEL SECURITY;

CREATE POLICY "movimentacoes_tenant_isolation" ON movimentacoes
  AS PERMISSIVE FOR ALL TO authenticated
  USING (tenant_id = (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid)
  WITH CHECK (tenant_id = (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid);

CREATE POLICY "movimentacoes_service_role_bypass" ON movimentacoes
  AS PERMISSIVE FOR ALL TO service_role
  USING (true)
  WITH CHECK (true);

-- -------------------------
-- TABELA: sync_errors
-- -------------------------
-- Registra erros de sincronização com contexto do processo (DATAJUD-08).
-- tipo: enum de tipos de erro — extensível mas controlado via CHECK.
-- segredo_justica: registrado quando DataJud retorna hits=[] após 3+ tentativas (D-05).
-- Retenção: cron de limpeza a definir (D-20 — Claude's Discretion: 90 dias sugerido).

CREATE TABLE IF NOT EXISTS sync_errors (
  id          UUID        PRIMARY KEY DEFAULT gen_random_uuid(),
  processo_id UUID        REFERENCES processos(id) ON DELETE CASCADE,  -- nullable: erro pode ocorrer antes de criar processo
  tenant_id   UUID        NOT NULL,
  tipo        TEXT        NOT NULL
              CHECK (tipo IN ('rate_limit', 'timeout', 'schema_drift', 'segredo_justica', 'circuit_open', 'unknown')),
  mensagem    TEXT,
  payload     JSONB,                                          -- contexto adicional (stack trace, response body, etc.)
  tentativas  INTEGER     NOT NULL DEFAULT 1,
  created_at  TIMESTAMPTZ NOT NULL DEFAULT now()
);

-- Index para buscas por processo (mais comum)
CREATE INDEX IF NOT EXISTS idx_sync_errors_processo_id ON sync_errors (processo_id);
-- Index para buscas por tenant
CREATE INDEX IF NOT EXISTS idx_sync_errors_tenant_id ON sync_errors (tenant_id);
-- Index para monitoramento de tipos de erro
CREATE INDEX IF NOT EXISTS idx_sync_errors_tipo ON sync_errors (tipo);
-- Index para limpeza por data (cron de retenção futuro — D-20)
CREATE INDEX IF NOT EXISTS idx_sync_errors_created_at ON sync_errors (created_at);

-- RLS: isolamento por tenant
ALTER TABLE sync_errors ENABLE ROW LEVEL SECURITY;

CREATE POLICY "sync_errors_tenant_isolation" ON sync_errors
  AS PERMISSIVE FOR ALL TO authenticated
  USING (tenant_id = (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid)
  WITH CHECK (tenant_id = (auth.jwt() -> 'app_metadata' ->> 'tenant_id')::uuid);

CREATE POLICY "sync_errors_service_role_bypass" ON sync_errors
  AS PERMISSIVE FOR ALL TO service_role
  USING (true)
  WITH CHECK (true);

-- ============================================================
-- COMENTÁRIOS DE DECISÃO
-- ============================================================
-- D-09: ultima_sincronizacao atualizada SOMENTE em sync bem-sucedido.
--       Staleness calculado em runtime: now() - ultima_sincronizacao > 72h = desatualizado.
--       O endpoint (Plan 05) adiciona desatualizado: true no response nesse caso.
--
-- D-05/DATAJUD-05: Idempotência do diff garantida pelo UNIQUE em movimentacoes.
--       datajud_id = campo nativo (preferido); hash_conteudo = fallback quando datajud_id é NULL.
--       NULLS NOT DISTINCT no PostgreSQL 15+ trata NULL como igual para fins de unique constraint.
--
-- D-20: Retenção de sync_errors = 90 dias (Claude's Discretion).
--       Cron de limpeza: DELETE FROM sync_errors WHERE created_at < now() - interval '90 days';
--       Implementar como pg_cron job em Phase 8 (hardening) ou manualmente.
-- ============================================================
```

NOTA CRÍTICA sobre UNIQUE constraint em movimentacoes: PostgreSQL antes da versão 15 trata NULLs como distintos em UNIQUE constraints. Se o banco for PostgreSQL 14 ou anterior, substituir por:
```sql
-- Alternativa para PostgreSQL < 15:
CREATE UNIQUE INDEX idx_movimentacoes_dedup_with_null
  ON movimentacoes (processo_id, COALESCE(datajud_id, hash_conteudo));
-- E remover o CONSTRAINT movimentacoes_dedup_unique da definição da tabela.
```
Verificar versão do PostgreSQL no Supabase (geralmente 15+) antes de fazer push.
  </action>
  <verify>
    <automated>grep -c "CREATE TABLE" /c/Users/Luiz/AndroidStudioProjects/APPTESTE/backend/supabase/migrations/002_datajud_schema.sql</automated>
  </verify>
  <acceptance_criteria>
    - grep "CREATE TABLE IF NOT EXISTS processos" backend/supabase/migrations/002_datajud_schema.sql retorna resultado
    - grep "CREATE TABLE IF NOT EXISTS movimentacoes" backend/supabase/migrations/002_datajud_schema.sql retorna resultado
    - grep "CREATE TABLE IF NOT EXISTS sync_errors" backend/supabase/migrations/002_datajud_schema.sql retorna resultado
    - grep "ENABLE ROW LEVEL SECURITY" backend/supabase/migrations/002_datajud_schema.sql retorna 3 ocorrências (uma por tabela)
    - grep "tenant_isolation" backend/supabase/migrations/002_datajud_schema.sql retorna 3 ocorrências
    - grep "service_role_bypass" backend/supabase/migrations/002_datajud_schema.sql retorna 3 ocorrências
    - grep "processos_tenant_cnj_unique" backend/supabase/migrations/002_datajud_schema.sql retorna resultado (UNIQUE por tenant+CNJ)
    - grep "ultima_sincronizacao" backend/supabase/migrations/002_datajud_schema.sql retorna resultado
    - grep "tier_refresh.*CHECK.*hot.*warm.*cold" backend/supabase/migrations/002_datajud_schema.sql retorna resultado
    - grep "'rate_limit'.*'timeout'.*'schema_drift'.*'segredo_justica'" backend/supabase/migrations/002_datajud_schema.sql retorna resultado
    - Arquivo não contém chamadas de função (apenas DDL puro — sem lógica de aplicação)
  </acceptance_criteria>
  <done>
    - backend/supabase/migrations/002_datajud_schema.sql criado com DDL completo
    - 3 tabelas (processos, movimentacoes, sync_errors) com constraints, indexes e RLS
    - Arquivo pronto para supabase db push (Plan 03)
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Worker (service_role) → tabelas | Service role bypassa RLS; worker deve ser o único processo com service role key |
| App Android (authenticated JWT) → tabelas | JWT deve conter tenant_id em app_metadata; RLS filtra por tenant |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-06 | Elevation of Privilege | processos RLS | mitigate | Policy usa auth.jwt() -> 'app_metadata' (não user_metadata) — injetado pelo Custom Access Token Hook da Phase 1; usuário não pode alterar app_metadata |
| T-02-07 | Information Disclosure | movimentacoes (dados processuais) | mitigate | RLS habilitado; tenant_id desnormalizado evita JOIN que poderia bypassar RLS via PostgREST; service_role key nunca exposta ao app Android |
| T-02-08 | Tampering | sync_errors (tipo enum) | mitigate | CHECK constraint restringe valores de tipo; nenhum valor arbitrário pode ser inserido |
| T-02-09 | Information Disclosure | dados_brutos JSONB em processos | accept | Dados brutos do DataJud são protegidos por RLS; campo não é exposto diretamente na API (Plan 05 retorna campos selecionados, não dados_brutos raw) |
</threat_model>

<verification>
```bash
# Contar tabelas na migration
grep -c "CREATE TABLE" backend/supabase/migrations/002_datajud_schema.sql
# Esperado: 3

# Verificar RLS em todas as tabelas
grep -c "ENABLE ROW LEVEL SECURITY" backend/supabase/migrations/002_datajud_schema.sql
# Esperado: 3

# Verificar constraints de idempotência
grep "movimentacoes_dedup_unique\|idx_movimentacoes_dedup" backend/supabase/migrations/002_datajud_schema.sql

# Verificar UNIQUE por tenant+CNJ
grep "processos_tenant_cnj_unique" backend/supabase/migrations/002_datajud_schema.sql

# Verificar tier_refresh enum
grep "tier_refresh.*CHECK" backend/supabase/migrations/002_datajud_schema.sql
```
</verification>

<success_criteria>
- Arquivo 002_datajud_schema.sql criado com 3 CREATE TABLE statements
- RLS habilitado nas 3 tabelas com policy de isolamento por tenant_id
- UNIQUE(tenant_id, numero_cnj) em processos previne duplicação
- UNIQUE idempotente em movimentacoes para diffing (DATAJUD-05)
- CHECK constraint em tier_refresh restringe a 'hot', 'warm', 'cold'
- CHECK constraint em sync_errors.tipo restringe a 6 tipos predefinidos
- Arquivo pronto para supabase db push sem erros de sintaxe SQL
</success_criteria>

<output>
Após conclusão, criar `.planning/phases/02-datajud-integration-sync-worker/02-02-SUMMARY.md`
</output>
