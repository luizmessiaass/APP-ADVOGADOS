---
phase: 00-android-bootstrap-cleanup
plan: 02
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Color.kt
  - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Type.kt
  - core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt
  - app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/MainActivity.kt
  - app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/MainActivity.kt
autonomous: true
requirements:
  - BOOT-01
  - BOOT-04

must_haves:
  truths:
    - "No file in the project contains the string com.example.appteste"
    - "Theme files exist in core-ui under the new package"
    - "Both app modules have a MainActivity under the new package"
    - "Old app/ module directory is removed"
    - "./gradlew :core-ui:compileDebugKotlin succeeds"
  artifacts:
    - path: "core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Color.kt"
      provides: "Color token definitions"
      contains: "package com.aethixdigital.portaljuridico.ui.theme"
    - path: "core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt"
      provides: "PortalJuridicoTheme composable"
      contains: "fun PortalJuridicoTheme"
    - path: "core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Type.kt"
      provides: "Typography definitions"
      contains: "package com.aethixdigital.portaljuridico.ui.theme"
    - path: "app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/MainActivity.kt"
      provides: "Cliente app entry point (stub, Hilt added in Plan 04)"
      contains: "package com.aethixdigital.portaljuridico.cliente"
    - path: "app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/MainActivity.kt"
      provides: "Escritório app stub entry point"
      contains: "package com.aethixdigital.portaljuridico.escritorio"
  key_links:
    - from: "app-cliente/src/main/java/.../MainActivity.kt"
      to: "core-ui Theme.kt"
      via: "import com.aethixdigital.portaljuridico.ui.theme.PortalJuridicoTheme"
      pattern: "import.*portaljuridico\\.ui\\.theme"
    - from: "app-cliente/src/main/AndroidManifest.xml"
      to: "ClienteApp (added in Plan 04)"
      via: "android:name=.ClienteApp"
      pattern: "android:name.*ClienteApp"
---

<objective>
Migrate all Kotlin source code from the old com.example.appteste package to the new com.aethixdigital.portaljuridico package across the correct modules. Theme files move to :core-ui. MainActivity stubs are created in :app-cliente and :app-escritorio. The original app/ directory is deleted. Zero references to com.example.appteste remain in the codebase.

Purpose: Satisfies BOOT-01 (package rename). After this plan, the codebase is free of the legacy package name and all source is in its final module location.

Output: Theme source in core-ui, MainActivity stubs in both app modules, app/ directory removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-android-bootstrap-cleanup/00-CONTEXT.md
@.planning/phases/00-android-bootstrap-cleanup/00-01-SUMMARY.md

<interfaces>
<!-- Source files to migrate. Executor MUST read before writing new versions. -->

Current app/src/main/java/com/example/appteste/ui/theme/Theme.kt:
- Package: com.example.appteste.ui.theme
- Exports: APPTESTETheme (composable)
- References: Purple80, PurpleGrey80, Pink80 from Color.kt; Typography from Type.kt

Current app/src/main/java/com/example/appteste/MainActivity.kt:
- Package: com.example.appteste
- References: APPTESTETheme from ui.theme

New package mappings (per D-01, D-05, D-11, D-12):
- com.example.appteste           → com.aethixdigital.portaljuridico.cliente
- com.example.appteste.ui.theme  → com.aethixdigital.portaljuridico.ui.theme
- APPTESTETheme                  → PortalJuridicoTheme
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create theme source files in :core-ui with new package</name>
  <files>
    core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Color.kt
    core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Type.kt
    core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt
  </files>
  <read_first>
    - app/src/main/java/com/example/appteste/ui/theme/Color.kt (source of truth for color values)
    - app/src/main/java/com/example/appteste/ui/theme/Type.kt (source of truth for typography)
    - app/src/main/java/com/example/appteste/ui/theme/Theme.kt (source of truth for theme)
  </read_first>
  <action>
Create the source directory structure and three theme files in :core-ui. The content is migrated from the existing app/ files with the following changes:
- Package declaration updated: `com.example.appteste.ui.theme` → `com.aethixdigital.portaljuridico.ui.theme`
- Theme function renamed: `APPTESTETheme` → `PortalJuridicoTheme`
- All other code content remains identical to the source files

Create directory: core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/

--- Color.kt ---
```kotlin
package com.aethixdigital.portaljuridico.ui.theme

import androidx.compose.ui.graphics.Color

val Purple80 = Color(0xFFD0BCFF)
val PurpleGrey80 = Color(0xFFCCC2DC)
val Pink80 = Color(0xFFEFB8C8)

val Purple40 = Color(0xFF6650a4)
val PurpleGrey40 = Color(0xFF625b71)
val Pink40 = Color(0xFF7D5260)
```

--- Type.kt ---
```kotlin
package com.aethixdigital.portaljuridico.ui.theme

import androidx.compose.material3.Typography
import androidx.compose.ui.text.TextStyle
import androidx.compose.ui.text.font.FontFamily
import androidx.compose.ui.text.font.FontWeight
import androidx.compose.ui.unit.sp

val Typography = Typography(
    bodyLarge = TextStyle(
        fontFamily = FontFamily.Default,
        fontWeight = FontWeight.Normal,
        fontSize = 16.sp,
        lineHeight = 24.sp,
        letterSpacing = 0.5.sp
    )
)
```

--- Theme.kt ---
```kotlin
package com.aethixdigital.portaljuridico.ui.theme

import android.app.Activity
import android.os.Build
import androidx.compose.foundation.isSystemInDarkTheme
import androidx.compose.material3.MaterialTheme
import androidx.compose.material3.darkColorScheme
import androidx.compose.material3.dynamicDarkColorScheme
import androidx.compose.material3.dynamicLightColorScheme
import androidx.compose.material3.lightColorScheme
import androidx.compose.runtime.Composable
import androidx.compose.ui.platform.LocalContext

private val DarkColorScheme = darkColorScheme(
    primary = Purple80,
    secondary = PurpleGrey80,
    tertiary = Pink80
)

private val LightColorScheme = lightColorScheme(
    primary = Purple40,
    secondary = PurpleGrey40,
    tertiary = Pink40
)

@Composable
fun PortalJuridicoTheme(
    darkTheme: Boolean = isSystemInDarkTheme(),
    // Dynamic color is available on Android 12+
    dynamicColor: Boolean = true,
    content: @Composable () -> Unit
) {
    val colorScheme = when {
        dynamicColor && Build.VERSION.SDK_INT >= Build.VERSION_CODES.S -> {
            val context = LocalContext.current
            if (darkTheme) dynamicDarkColorScheme(context) else dynamicLightColorScheme(context)
        }

        darkTheme -> DarkColorScheme
        else -> LightColorScheme
    }

    MaterialTheme(
        colorScheme = colorScheme,
        typography = Typography,
        content = content
    )
}
```
  </action>
  <verify>
    <automated>
      grep "package com.aethixdigital.portaljuridico.ui.theme" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Color.kt core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Type.kt core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt | wc -l
      # Expect: 3
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Color.kt` exits 0
    - `test -f core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Type.kt` exits 0
    - `test -f core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt` exits 0
    - `grep "package com.aethixdigital.portaljuridico.ui.theme" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt` returns match
    - `grep "fun PortalJuridicoTheme" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt` returns match
    - `grep "APPTESTETheme" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt` returns NO match
    - `grep "com.example" core-ui/src/main/java/com/aethixdigital/portaljuridico/ui/theme/Theme.kt` returns NO match
    - `./gradlew :core-ui:compileDebugKotlin` exits with code 0
  </acceptance_criteria>
  <done>Three theme files exist in core-ui under the new package. PortalJuridicoTheme composable replaces APPTESTETheme. No com.example references remain in core-ui source. :core-ui compiles successfully.</done>
</task>

<task type="auto">
  <name>Task 2: Create MainActivity stubs in both app modules and remove old app/</name>
  <files>
    app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/MainActivity.kt
    app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/MainActivity.kt
  </files>
  <read_first>
    - app/src/main/java/com/example/appteste/MainActivity.kt (current implementation to migrate from)
    - app-cliente/src/main/AndroidManifest.xml (confirm android:name=".MainActivity" is declared)
    - app-escritorio/src/main/AndroidManifest.xml (confirm android:name=".MainActivity" is declared)
  </read_first>
  <action>
Create source directories and MainActivity files for both app modules. Note: @HiltAndroidApp Application classes and @AndroidEntryPoint annotations are added in Plan 04. These are intentionally minimal stubs that compile cleanly now.

Create directory: app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/

--- app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/MainActivity.kt ---
```kotlin
package com.aethixdigital.portaljuridico.cliente

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.activity.enableEdgeToEdge
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.padding
import androidx.compose.material3.Scaffold
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.tooling.preview.Preview
import com.aethixdigital.portaljuridico.ui.theme.PortalJuridicoTheme

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContent {
            PortalJuridicoTheme {
                Scaffold(modifier = Modifier.fillMaxSize()) { innerPadding ->
                    Greeting(
                        name = "Portal Jurídico",
                        modifier = Modifier.padding(innerPadding)
                    )
                }
            }
        }
    }
}

@Composable
fun Greeting(name: String, modifier: Modifier = Modifier) {
    Text(
        text = "Bem-vindo ao $name!",
        modifier = modifier
    )
}

@Preview(showBackground = true)
@Composable
fun GreetingPreview() {
    PortalJuridicoTheme {
        Greeting("Portal Jurídico")
    }
}
```

Create directory: app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/

--- app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/MainActivity.kt ---
```kotlin
package com.aethixdigital.portaljuridico.escritorio

import android.os.Bundle
import androidx.activity.ComponentActivity
import androidx.activity.compose.setContent
import androidx.activity.enableEdgeToEdge
import androidx.compose.foundation.layout.fillMaxSize
import androidx.compose.foundation.layout.padding
import androidx.compose.material3.Scaffold
import androidx.compose.material3.Text
import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import androidx.compose.ui.tooling.preview.Preview
import com.aethixdigital.portaljuridico.ui.theme.PortalJuridicoTheme

class MainActivity : ComponentActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        enableEdgeToEdge()
        setContent {
            PortalJuridicoTheme {
                Scaffold(modifier = Modifier.fillMaxSize()) { innerPadding ->
                    Text(
                        text = "Portal Jurídico — Admin",
                        modifier = Modifier.padding(innerPadding)
                    )
                }
            }
        }
    }
}

@Preview(showBackground = true)
@Composable
fun MainPreview() {
    PortalJuridicoTheme {
        Text("Portal Jurídico — Admin")
    }
}
```

After creating both files, verify they compile with:
`./gradlew :app-cliente:compileDemoDebugKotlin :app-escritorio:compileDebugKotlin`

If build succeeds, delete the old app/ directory:
`rm -rf app/`

WARNING: Only delete app/ AFTER confirming the compile step above succeeds. If compile fails, fix the error first — do not delete app/ while it is still needed for reference.
  </action>
  <verify>
    <automated>
      grep -r "com.example.appteste" . --include="*.kt" --include="*.xml" --include="*.gradle.kts" --include="*.toml" 2>/dev/null | grep -v ".planning/" | wc -l
      # Expect: 0
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -f app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/MainActivity.kt` exits 0
    - `test -f app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/MainActivity.kt` exits 0
    - `grep "package com.aethixdigital.portaljuridico.cliente" app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/MainActivity.kt` returns match
    - `grep "package com.aethixdigital.portaljuridico.escritorio" app-escritorio/src/main/java/com/aethixdigital/portaljuridico/escritorio/MainActivity.kt` returns match
    - `grep "PortalJuridicoTheme" app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/MainActivity.kt` returns match
    - `grep "APPTESTETheme" app-cliente/src/main/java/com/aethixdigital/portaljuridico/cliente/MainActivity.kt` returns NO match
    - `test -d app/` exits 1 (old app/ directory deleted)
    - `grep -r "com.example.appteste" . --include="*.kt" 2>/dev/null | grep -v ".planning/"` returns NO output
    - `./gradlew :app-cliente:compileDemoDebugKotlin` exits with code 0
  </acceptance_criteria>
  <done>Both app module MainActivity files exist under the new package. PortalJuridicoTheme is used. The old app/ directory is deleted. Zero occurrences of com.example.appteste remain in any Kotlin or build file.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Source file migration | Moving source code between directories. No runtime inputs, no user data. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-02-01 | Tampering | Package rename | accept | Source-controlled migration. No runtime exposure. Verified by zero com.example occurrences post-migration. |
| T-00-02-02 | Information Disclosure | app/ directory deletion | accept | Old source files are in git history; deletion from working tree is safe. ASVS does not apply to source migration tasks. |

Note: This plan covers source code migration only. Build infrastructure phase. ASVS L1 controls are not applicable.
</threat_model>

<verification>
After all tasks complete:

1. `grep -r "com.example.appteste" . --include="*.kt" --include="*.xml" --include="*.gradle.kts" | grep -v ".planning/"` returns zero lines
2. `./gradlew :core-ui:compileDebugKotlin` exits 0
3. `./gradlew :app-cliente:compileDemoDebugKotlin` exits 0
4. `test -d app/` exits 1 (directory gone)
5. `find core-ui/src -name "*.kt" | wc -l` outputs 3 (Color, Type, Theme)
</verification>

<success_criteria>
- All theme files migrated to core-ui under com.aethixdigital.portaljuridico.ui.theme
- APPTESTETheme renamed to PortalJuridicoTheme in Theme.kt
- Both app modules have a compiling MainActivity stub
- app/ directory deleted (zero leftover files)
- Zero occurrences of com.example.appteste in any source, build, or manifest file
</success_criteria>

<output>
After completion, create `.planning/phases/00-android-bootstrap-cleanup/00-02-SUMMARY.md` with:
- Files migrated (list all Kotlin files created)
- Rename: APPTESTETheme → PortalJuridicoTheme
- Confirmation: app/ directory deleted
- Compile verification results
- Any issues encountered and resolved
- Status: COMPLETE or BLOCKED (with reason)
</output>
