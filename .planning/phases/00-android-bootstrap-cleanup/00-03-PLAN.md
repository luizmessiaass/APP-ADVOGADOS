---
phase: 00-android-bootstrap-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 00-01
files_modified:
  - gradle/libs.versions.toml
autonomous: true
requirements:
  - BOOT-02
  - BOOT-06

must_haves:
  truths:
    - "Compose BOM version in libs.versions.toml is 2026.03.00"
    - "Hilt version 2.57.1 is declared in libs.versions.toml"
    - "KSP version 2.2.10-1.0.31 is declared in libs.versions.toml"
    - "All required plugin aliases exist in libs.versions.toml [plugins] section"
    - "./gradlew :app-cliente:assembleDemoDebug succeeds after dependency upgrade"
  artifacts:
    - path: "gradle/libs.versions.toml"
      provides: "Single source of truth for all dependency and plugin versions"
      contains: "composeBom = \"2026.03.00\""
  key_links:
    - from: "gradle/libs.versions.toml"
      to: "all module build.gradle.kts files"
      via: "libs.versions.* and libs.plugins.* alias references"
      pattern: "composeBom.*2026\\.03\\.00"
---

<objective>
Upgrade gradle/libs.versions.toml to replace the outdated Compose BOM (2024.09.00 → 2026.03.00) and add all new dependency entries needed for Hilt + KSP across the multi-module project. This is the single source of truth update — all module build.gradle.kts files reference these aliases, so changing them here propagates everywhere.

Purpose: Satisfies BOOT-02 (Compose BOM upgrade) and BOOT-06 (dependencies updated). After this plan, all modules compile against the current stable Compose BOM, and Hilt/KSP aliases are available for Plan 04.

Output: Updated libs.versions.toml. No other files modified (all module build.gradle.kts files already reference the catalog aliases from Plan 01).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/00-android-bootstrap-cleanup/00-CONTEXT.md
@.planning/phases/00-android-bootstrap-cleanup/00-RESEARCH.md
@.planning/phases/00-android-bootstrap-cleanup/00-01-SUMMARY.md

<interfaces>
<!-- Current libs.versions.toml baseline (read before editing): -->

Current [versions]:
  agp = "9.1.1"
  coreKtx = "1.18.0"
  junit = "4.13.2"
  junitVersion = "1.3.0"
  espressoCore = "3.7.0"
  lifecycleRuntimeKtx = "2.10.0"
  activityCompose = "1.13.0"
  kotlin = "2.2.10"
  composeBom = "2024.09.00"   ← MUST upgrade to "2026.03.00"

Current [plugins]:
  android-application = { id = "com.android.application", version.ref = "agp" }
  kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
  ← MISSING: android-library, kotlin-android, hilt-android, ksp

New versions to add (from RESEARCH.md Standard Stack):
  hilt = "2.57.1"
  hiltNavigationCompose = "1.2.0"
  ksp = "2.2.10-1.0.31"

New libraries to add:
  hilt-android = { group = "com.google.dagger", name = "hilt-android", version.ref = "hilt" }
  hilt-compiler = { group = "com.google.dagger", name = "hilt-android-compiler", version.ref = "hilt" }
  hilt-navigation-compose = { group = "androidx.hilt", name = "hilt-navigation-compose", version.ref = "hiltNavigationCompose" }

New plugins to add:
  android-library = { id = "com.android.library", version.ref = "agp" }
  kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
  hilt-android = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }
  ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade libs.versions.toml — BOM, Hilt, KSP, plugin aliases</name>
  <files>
    gradle/libs.versions.toml
  </files>
  <read_first>
    - gradle/libs.versions.toml (MUST read current state before editing — do not overwrite what Plan 01 Task 3 may have already added)
  </read_first>
  <action>
Edit gradle/libs.versions.toml to make the following precise changes. Read the file first to see its current state, then apply only what is missing or needs updating.

CHANGES REQUIRED:

1. In [versions] section:
   - Change: `composeBom = "2024.09.00"` → `composeBom = "2026.03.00"`
   - Add if missing: `hilt = "2.57.1"`
   - Add if missing: `hiltNavigationCompose = "1.2.0"`
   - Add if missing: `ksp = "2.2.10-1.0.31"`
   - Keep all existing entries unchanged (agp, coreKtx, junit, junitVersion, espressoCore, lifecycleRuntimeKtx, activityCompose, kotlin)

2. In [libraries] section, add if missing:
   - `hilt-android = { group = "com.google.dagger", name = "hilt-android", version.ref = "hilt" }`
   - `hilt-compiler = { group = "com.google.dagger", name = "hilt-android-compiler", version.ref = "hilt" }`
   - `hilt-navigation-compose = { group = "androidx.hilt", name = "hilt-navigation-compose", version.ref = "hiltNavigationCompose" }`
   - Keep all existing library entries unchanged

3. In [plugins] section, add if missing:
   - `android-library = { id = "com.android.library", version.ref = "agp" }`
   - `kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }`
   - `hilt-android = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }`
   - `ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }`
   - Keep existing: android-application, kotlin-compose

The final libs.versions.toml MUST contain:

```toml
[versions]
agp = "9.1.1"
coreKtx = "1.18.0"
junit = "4.13.2"
junitVersion = "1.3.0"
espressoCore = "3.7.0"
lifecycleRuntimeKtx = "2.10.0"
activityCompose = "1.13.0"
kotlin = "2.2.10"
composeBom = "2026.03.00"
hilt = "2.57.1"
hiltNavigationCompose = "1.2.0"
ksp = "2.2.10-1.0.31"

[libraries]
androidx-core-ktx = { group = "androidx.core", name = "core-ktx", version.ref = "coreKtx" }
junit = { group = "junit", name = "junit", version.ref = "junit" }
androidx-junit = { group = "androidx.test.ext", name = "junit", version.ref = "junitVersion" }
androidx-espresso-core = { group = "androidx.test.espresso", name = "espresso-core", version.ref = "espressoCore" }
androidx-lifecycle-runtime-ktx = { group = "androidx.lifecycle", name = "lifecycle-runtime-ktx", version.ref = "lifecycleRuntimeKtx" }
androidx-activity-compose = { group = "androidx.activity", name = "activity-compose", version.ref = "activityCompose" }
androidx-compose-bom = { group = "androidx.compose", name = "compose-bom", version.ref = "composeBom" }
androidx-compose-ui = { group = "androidx.compose.ui", name = "ui" }
androidx-compose-ui-graphics = { group = "androidx.compose.ui", name = "ui-graphics" }
androidx-compose-ui-tooling = { group = "androidx.compose.ui", name = "ui-tooling" }
androidx-compose-ui-tooling-preview = { group = "androidx.compose.ui", name = "ui-tooling-preview" }
androidx-compose-ui-test-manifest = { group = "androidx.compose.ui", name = "ui-test-manifest" }
androidx-compose-ui-test-junit4 = { group = "androidx.compose.ui", name = "ui-test-junit4" }
androidx-compose-material3 = { group = "androidx.compose.material3", name = "material3" }
hilt-android = { group = "com.google.dagger", name = "hilt-android", version.ref = "hilt" }
hilt-compiler = { group = "com.google.dagger", name = "hilt-android-compiler", version.ref = "hilt" }
hilt-navigation-compose = { group = "androidx.hilt", name = "hilt-navigation-compose", version.ref = "hiltNavigationCompose" }

[plugins]
android-application = { id = "com.android.application", version.ref = "agp" }
android-library = { id = "com.android.library", version.ref = "agp" }
kotlin-compose = { id = "org.jetbrains.kotlin.plugin.compose", version.ref = "kotlin" }
kotlin-android = { id = "org.jetbrains.kotlin.android", version.ref = "kotlin" }
hilt-android = { id = "com.google.dagger.hilt.android", version.ref = "hilt" }
ksp = { id = "com.google.devtools.ksp", version.ref = "ksp" }
```
  </action>
  <verify>
    <automated>
      grep "composeBom" gradle/libs.versions.toml
      # Expect: composeBom = "2026.03.00"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep 'composeBom = "2026.03.00"' gradle/libs.versions.toml` returns match
    - `grep 'hilt = "2.57.1"' gradle/libs.versions.toml` returns match
    - `grep 'ksp = "2.2.10-1.0.31"' gradle/libs.versions.toml` returns match
    - `grep 'hiltNavigationCompose = "1.2.0"' gradle/libs.versions.toml` returns match
    - `grep "hilt-android.*com.google.dagger.*hilt-android" gradle/libs.versions.toml` returns match (library entry)
    - `grep "hilt-compiler.*hilt-android-compiler" gradle/libs.versions.toml` returns match
    - `grep "hilt-navigation-compose" gradle/libs.versions.toml` returns match
    - `grep 'android-library.*com.android.library' gradle/libs.versions.toml` returns match (plugin entry)
    - `grep 'kotlin-android.*org.jetbrains.kotlin.android' gradle/libs.versions.toml` returns match
    - `grep 'hilt-android.*com.google.dagger.hilt.android' gradle/libs.versions.toml` returns match (plugin entry)
    - `grep 'ksp.*com.google.devtools.ksp' gradle/libs.versions.toml` returns match (plugin entry)
    - `grep '"2024.09.00"' gradle/libs.versions.toml` returns NO match (old BOM version gone)
  </acceptance_criteria>
  <done>libs.versions.toml has Compose BOM at 2026.03.00, Hilt 2.57.1, KSP 2.2.10-1.0.31, and all required library and plugin aliases. The old 2024.09.00 BOM version is gone.</done>
</task>

<task type="auto">
  <name>Task 2: Verify all module build files resolve their catalog aliases</name>
  <files></files>
  <read_first>
    - gradle/libs.versions.toml (post-edit state)
  </read_first>
  <action>
Run Gradle dependency resolution to confirm all catalog alias references in all module build.gradle.kts files resolve to real artifacts.

Run: `./gradlew dependencies --configuration debugRuntimeClasspath -p core-common 2>&1 | head -30`
Run: `./gradlew dependencies --configuration debugRuntimeClasspath -p app-cliente 2>&1 | head -30`

If these fail with "Unresolved reference" or "Could not find" errors, check the failing alias name against libs.versions.toml and fix the toml entry.

Then confirm the full build graph resolves:
`./gradlew :app-cliente:assembleDemoDebug`

Expected: BUILD SUCCESSFUL with APK output at app-cliente/build/outputs/apk/demo/debug/.

If the assembleDemoDebug fails because Plan 02 (source migration) and Plan 04 (Hilt wiring) have not run yet:
- This is expected — source files are added in Plans 02 and 04
- Run instead: `./gradlew :core-ui:compileDebugKotlin` to verify at minimum the core-ui module resolves its BOM-managed Compose dependencies
- Record the partial verification in the summary

Note: Plans 02 and 03 are in Wave 2 and run in parallel. Plan 03 does NOT depend on Plan 02. If running before Plan 02 completes, the app module compilation will fail on missing source — this is expected.
  </action>
  <verify>
    <automated>
      ./gradlew :core-ui:compileDebugKotlin 2>&1 | tail -3
      # Expect: BUILD SUCCESSFUL
    </automated>
  </verify>
  <acceptance_criteria>
    - `./gradlew :core-ui:compileDebugKotlin` exits with code 0 (confirms Compose BOM 2026.03.00 resolves)
    - `./gradlew :core-common:compileDebugKotlin` exits with code 0 (confirms hilt-android and hilt-compiler resolve)
    - Output of both commands contains "BUILD SUCCESSFUL"
    - Neither command outputs "Could not resolve" or "Unresolved reference"
    - `./gradlew dependencies --configuration debugRuntimeClasspath -p core-common 2>&1 | grep "2026.03.00"` returns at least one match (BOM version present in resolved dependency tree)
  </acceptance_criteria>
  <done>All catalog aliases in libs.versions.toml resolve to real Maven artifacts. core-ui compiles with updated Compose BOM. core-common compiles with Hilt 2.57.1. No unresolved dependency errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Dependency catalog | libs.versions.toml specifies artifact coordinates fetched from Maven repositories. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-00-03-01 | Tampering | libs.versions.toml | accept | Version catalog is source-controlled. Dependencies resolved from Google Maven + Maven Central via HTTPS. Gradle's dependency verification can be enabled post-launch if supply-chain integrity becomes a concern. |
| T-00-03-02 | Information Disclosure | Dependency metadata | accept | Version numbers are public. No secrets in toml. ASVS does not apply to build infrastructure. |

Note: Build infrastructure phase. ASVS L1 controls not applicable.
</threat_model>

<verification>
After all tasks complete:

1. `grep 'composeBom = "2026.03.00"' gradle/libs.versions.toml` returns match
2. `grep 'hilt = "2.57.1"' gradle/libs.versions.toml` returns match
3. `grep 'ksp = "2.2.10-1.0.31"' gradle/libs.versions.toml` returns match
4. `./gradlew :core-ui:compileDebugKotlin` exits 0
5. `./gradlew :core-common:compileDebugKotlin` exits 0
6. `grep '"2024.09.00"' gradle/libs.versions.toml` returns no match
</verification>

<success_criteria>
- Compose BOM upgraded from 2024.09.00 to 2026.03.00 in libs.versions.toml
- Hilt 2.57.1 and KSP 2.2.10-1.0.31 added to libs.versions.toml
- All required library and plugin aliases present in libs.versions.toml
- core-ui module compiles successfully against new BOM
- core-common module compiles successfully with Hilt dependencies
</success_criteria>

<output>
After completion, create `.planning/phases/00-android-bootstrap-cleanup/00-03-SUMMARY.md` with:
- Versions changed (before → after for composeBom)
- Versions added (hilt, ksp, hiltNavigationCompose)
- Library aliases added
- Plugin aliases added
- Verification results (which modules compiled successfully)
- Status: COMPLETE or BLOCKED (with reason)
</output>
