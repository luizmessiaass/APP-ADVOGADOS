import { describe, it } from 'vitest';

describe('Prompt cache threshold', () => {
  it.todo('validateCacheThreshold passes when countTokens mock returns 5000 tokens');
  it.todo('validateCacheThreshold throws mentioning 4096 when countTokens mock returns 3000 tokens');
});
